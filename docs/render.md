# awl docs — Render: adaptive column, theme caps, overlay chrome, picker rows

> Read before touching `render/`, `theme/model.rs` (RenderCaps), `overlay/`, `settings.rs` palette rows, or picker/row layout. Moved verbatim out of CLAUDE.md 2026-07-22 (queue item 17); earlier round history: `git log -p CLAUDE.md`.

## Adaptive-column placement (`render/geometry.rs`)

- On a small screen the symmetric centered column cramped the margin outline while the right margin sat empty. `TextPipeline::column_left` is the one owner of an adaptive policy (no config knob): shifts right under pressure to grant the outline a rail (taking the empty right-margin space), back to symmetric when there's room. Every downstream reader (caret/selection/washes, hit-test, drag handle, gutter) composes it for free.
- Pure policy `adaptive_column_left`, one formula `desired_left.min(max_left).max(symmetric_left)`, three regimes (wide = byte-identical passthrough to the old column; narrow = shift, width never touched; narrowest = re-centers → outline auto-hides at its existing floor). Hide threshold + shift threshold read the same `left` (can't drift). Transition is instant (glide banked). **Live-only:** the feel of a real small-screen resize.
- **THE CONTENT CLIP (item 84, RE-OWNED BY ITEM 116b) — `TextPipeline::content_clip` / `clip_rects_to_band` (`render/rects.rs`).** The ONE resolved region every document-content, selection-adjacent quad must paint inside: horizontally the writing column ALWAYS (`column_left()`..`+column_width()`), vertically the whole canvas on an ordinary frame narrowed to whatever region the document layer is actually drawing in (`doc_clip_band`). A drag whose hit-test clamps to the page's own left edge, or a relocated comparison scrolled so a selected/composing/caret row leaves the region, both resolve through this one rect — bounding PAINT only, never the SELECTABLE range. `range_rects` (feeding both `selection_rects` and `search_match_rects`), `preedit_rects`, and the caret's own park gate in `prepare_caret_layer` all route through it; on an ordinary frame this is a no-op (`doc_clip_band` is `None`, and selection/caret x is already inside the column by construction). **Item 84 derived the vertical bound from the diff-as-preview panel's own card rect** — the single case where a document ever drew somewhere other than the whole canvas. Item 116b replaced that composition with a real relocated viewport (`comparison_viewport`, below) and `doc_clip_band` is now its vertical extent; the emitters, the one owner and the by-name sweep are untouched, and the X arm is genuinely exercised for the first time because the region moves horizontally too. Laws: `render/tests/selection_clip_law.rs` (a no-wildcard, by-name check that each emitter's own body calls the shared owner, plus the runtime proof the relocated case actually clips — its straddling canvas is now DERIVED from row geometry, not from the clip's own output, so a reverted clip fails on an escaping rect instead of on a fixture that stopped qualifying).
- **ITEM 84-FIX — the decorative-overhang emitters get their OWN Y-only clip, `clip_decorative_rects_to_band`, never `clip_rects_to_band`.** A follow-up audit found item 84 had ALSO routed `fence_panel_rects` and `code_pill_rects` — pre-existing DECORATIVE quads whose whole point is to overhang the bare glyph column (`FENCE_PANEL_INSET_X`/`CODE_PILL_INSET_X`, "so the panel reads as a distinct surface rather than being clipped exactly to the glyph edges") — through the strict writing-column X-clip meant for selection-adjacent quads. Invisible with page mode ON (the page pad dwarfs both insets); UNIVERSAL with page mode off (`text_pad()` is hard-zeroed then, so `text_left() == column_left()` and there is no margin to absorb the inset): every fenced block's panel clipped flush on both edges every frame, and an inline-code pill lost its left cap whenever the span touched column 0 or the wrap edge. Fix: two distinctly-named owners — `clip_rects_to_band` (X+Y, item 84's strict writing-column bound) stays SELECTION-ADJACENT-only (`range_rects`, `preedit_rects`; the caret gate reads `content_clip` directly); `clip_decorative_rects_to_band` (Y-only, the fn's pre-84 body, restored verbatim) is the DECORATIVE owner — `fence_panel_rects`, `code_pill_rects`, and the column-bound `wash_rects` comment/string/highlight buckets (structurally safe either way, kept on the decorative owner for one consistent story). Laws: `render/tests/washes.rs`'s `fence_panel_overhang_survives_content_clip_with_page_mode_off` / `code_pill_left_inset_survives_content_clip_at_line_start_with_page_mode_off` (each with a page-off zero-slack precondition, page mode off); the four `render/tests/selection_clip_law.rs` tests still hold unmodified — the selection/search/preedit/caret X-clip is untouched by this fix.

## The caret's cell geometry (`render/caret.rs`)

- **THE INK BOX IS THE CARET'S ONE DESCRIPTION OF A LETTER (item 91).** `TextPipeline::caret_anchor_raster_box` is the single swash read on the caret path and returns the FULL placement box (`InkBox { left, top, width, height }`, plus the derived `descent()`); `caret_anchor_ink_box` layers the policy gate on it (no box on a MONO face, on a multi-char LIGATURE cluster, or on a glyphless / zero-mask anchor) and is the ONE funnel every ink-derived caret rule passes through. The horizontal half (the kerned-glyph fix) already rode it; item 91 added the vertical.
- **`caret_cell_vertical` OWNS the CELL caret's top and bottom** — `(center_y, height)` for the resting pose — and every cell-form caret reads it: the Block quad, Morph's fast-travel / ink-caret-world fold onto that quad (both via `caret_geometry`), and the glyphless SPACE BAR. The **ink box grown by `CARET_INK_PAD`** top and bottom on a proportional world, single-glyph anchor; the **row-scaled line cell** (`caret_block_h`) with the descender-aware bottom (`CARET_DESCENDER_PAD`) folded in, on a MONO world only. Descenders need no separate rule — `height - top` is already below the baseline in the same box. The BAR forms (I-beam, Morph's line-start degrade) deliberately stay line-box (`ibeam_bar_dims`): an insertion bar marks a boundary between glyphs, so it has no glyph to hug.
- **The bug it fixed was structural.** The bottom had a glyph-aware rule and the top did not, so the two edges could not agree: `CARET_BLOCK_H` is a fixed 0.8 fraction of the row centred on the line box, giving every letter the same top while the real ink top moves with the letter — measured on Gumtree/Literata at zoom 1, **8–9px of empty accent above `a`/`m`/`g`/`y`**, ~3px above `i`; after, a letter-independent 2–3px on all of them. The descender extension used to be re-derived at the draw site (`layers.rs::prepare_caret_block`) off the already motion-blended rect; folding it into the rest ENDPOINT is algebraically identical at settle 1 and lets `motion_geometry` blend it out mid-glide, so the travelling streak has exactly one thickness rule. Both ink corrections (x-shift and vertical re-centring) are scaled by the settle factor, so a moving caret is untouched.
- `CARET_INK_PAD` (3.0px at zoom 1) sits deliberately ABOVE `CARET_MORPH_DILATE_PX` (2.0): on a `CaretBlockStyle::Filled` world (Cassowary's CRT phosphor cursor) the block KNOCKS the glyph out with the morph silhouette dilated by that constant, so a pad at or below it would let the knockout eat the lit cell.
- **ITEM 105 — THE PROPORTIONAL FALLBACK JOINED THE SAME FORMULA.** Item 91's two arms each proved correct in isolation but never proved the TRANSITION between them: a proportional world's glyphless/ligature fallback centred on `caret.pos.y` (the row-box GEOMETRIC centre), while the ink-box arm one column over centres on the font BASELINE — two unrelated conventions with no reason to agree, and a user's paired release screenshots caught the resulting jump (`aaa`'s Block caret on the final `a` versus one column later at end-of-line — 2–13px at zoom 1 depending on world/glyph class, reproduced on 11/11 proportional worlds, 0/7 mono). The fix: on a PROPORTIONAL world only, the fallback now reads the SAME `baseline - top + height/2` / `height + 2·CARET_INK_PAD` formula as the ink-box arm, fed either a REAL raster box (a multi-char LIGATURE cluster — `caret_anchor_ink_box` gates it out of the ink arm only because one glyph's ink can't be fairly SPLIT across its chars horizontally, not because its vertical ink is untrustworthy) or, for a truly glyphless anchor, a SYNTHETIC typical-letter box (`TextPipeline::caret_synthetic_ink_box`): the row's own real `max_ascent` (from the SAME row lookup the baseline itself uses — `caret_row_metrics`, widened from `caret_baseline_y`) times the anchored family's own measured typical-letter/ascent ratio (`facepitch::typical_letter_ratio` — the MEAN of the face's x-height and cap-height ratios, read via skrifa off the shipped font bytes; the mean, not bare x-height, because a pure x-height reference reproduces item 91's bug in miniature against an ASCENDER neighbour). A MONO world is untouched — item 97's uniform grid never reads any ink box, real or synthetic, so there is no seam to jump across there (0/7 confirmed). `caret_row_metrics` also fixed a latent row-lookup gap it inherited from the original `caret_baseline_y`: the hand-rolled column→row predicate never matched the true END-OF-LINE column on an unwrapped line (`col < end_col` is never true there), silently falling to a crude `font_size * 0.8` ascent guess instead of the row's real `max_ascent` — invisible before item 105 (nothing read baseline/ascent at a glyphless anchor), but a ~30% systematic error once the synthetic box needed it. Reusing the canonical `pick_row_index_aff` (the SAME resolver `visual_row_top_aff`/`col_x_and_advance_aff` already ride) fixed it for free. Full closure is not achievable for every glyph class with one fixed synthetic reference — an ASCENDER, a DESCENDER, a LIGATURE, or a bare PUNCTUATION mark is legitimately taller/shorter than a "typical letter" (the ink-box arm's own letter-to-letter spread already shows a comparable gap with no bug involved, e.g. `l` vs `a` on Literata: 25px vs 19px); those classes are held to a wider, still-authored bound instead, and are proven never WORSE than the pre-105 formula on that same class.
- **THE PROPORTIONAL FALLBACK READS `doc_family()`, NEVER `shaped_font` (found auditing item 105's own fix, before it shipped).** `sync_theme_colors` (the theme-picker's per-arrow preview step) switches the ACTIVE theme and re-tints every baked colour INSTANTLY but deliberately leaves `shaped_font` (the face the document is ACTUALLY shaped in) stale until the separately-DEFERRED `sync_theme_font` reshape catches up — the whole point of the split (docs/fonts.md's "Theme-preview debounce"). Pre-105 this never mattered: a glyphless anchor's fallback was one constant formula regardless of font identity, so nothing color-only-visible read font identity at all. Item 105's font-aware fallback would have read the LAGGING `shaped_font` and left the caret itself showing the SOURCE world's geometry for the whole window between a preview's color retint and its reshape — exactly the surface `theme_preview_retint_regrounds_the_page_surface_on_every_world` (`render/tests/distinguishability.rs`) exists to guard, and it caught this directly (a full-frame pixel diff, Tawny→Magpie, 253px differing) before the fix shipped. The repair: the arm-selection GATE keys on `TextPipeline::doc_family()` (the LIVE effective face the active theme wants for this buffer kind — reads `theme::active()` directly, no lag) instead of `shaped_font` — correct because that gate decides between two structurally DIFFERENT formulas (line-cell vs ink-box), and drawing the wrong KIND of cell for a whole preview window is the coarser, more jarring failure. Law: `caret_transition_item105.rs`'s `caret_fallback_geometry_tracks_the_live_theme_not_the_lagging_shaped_font` — the fast unit-level companion to the pixel law, pinned at the exact seam, non-vacuous against a `shaped_font`-keyed reversion (its one fixture is an EMPTY buffer, where this distinction cannot yet show — see the next bullet).
- **THE REPAIR ROUND (2026-07-26, queue item 105 reopened — item 91's own fix did not fix the live bug).** The FIRST landing (above) synthesized its glyphless-anchor box from a per-font MEAN(x-height, cap-height) ratio — a single fixed reference that closed the reported x-height class (`aaa`→EOL) but was never swept against a CAPITAL letter, which landed WORSE than the pre-105 code on 11/11 proportional worlds (new Δ 2.3–4.4px vs old Δ 0.4–2.9px) — and the WIDE-bound "no worse than before" law meant to catch exactly that regression could not, because its ceiling floored at a flat 7.5px regardless of how small the old delta was. Two structural fixes: (1) `caret_cell_vertical`'s fallback arm now BORROWS the immediately PRECEDING column's own real `InkBox` (`raster_box_at`) when one exists, instead of synthesizing an approximation — a glyphless cell beside a real letter draws with THAT letter's exact ink, closing the adjacent seam to (near-)zero for every glyph class, not merely x-height, because it no longer guesses; the synthetic ratio survives only for a genuinely glyphless anchor with NO real neighbor (an empty line, or column 0). (2) `caret_synthetic_ink_box`'s ratio lookup now keys on `caret_row_metrics`'s own THIRD element — whichever font actually produced the paired `max_ascent` (`shaped_font` for a real shaped row, `doc_family()` only for the theme-independent empty-line approximation) — never an independently-chosen font: multiplying one font's ascent by a DIFFERENT font's ratio (the first landing's bug, invisible on the empty-buffer law since that branch never reads font identity) produced a measurable mixed-font pop on ordinary text during a theme-picker scrub (worst case 5.19px, Tawny→Bilby) that the repair halves (worst case 2.76px) without claiming full closure — the row's on-screen geometry genuinely IS still the source font's until the deferred reshape catches up, so some residual is inherent to the split, not a bug. Laws: `every_glyph_class_closes_exactly_at_the_literal_eol_seam` (every class, literal adjacency, exact closure) and `caret_synthetic_ratio_reads_the_same_font_as_its_paired_ascent` (the mixed-font lag, swept over every mono-source × proportional-destination pair) in `caret_transition_item105.rs`.
- **NEIGHBOR-BORROW, THREE ROUNDS (rounds 2–3, 2026-07-26).** Round 1's single-neighbor borrow (`raster_box_at(col - 1)`, above) closed the literal reported shape but left two shapes open: a glyphless anchor at COLUMN 0 (`" A"`, no `col - 1` to borrow at all) and a RUN of 2+ glyphless columns (`"A  "`, the second column's own `col - 1` is itself glyphless). **Round 2** (`nearest_row_raster_box`) searched OUTWARD from the anchor in both directions, bounded to the caret's own visual row, nearest distance wins (backward preferred on a tie) — closing both shapes, but a hard PICK borrows exactly ONE side's ink. A single glyphless column with a DIFFERENT real letter on EACH side (a table's `"| 1"` — pipe one side, digit the other, tied at distance 1) reads as pure pipe ink: the backward seam (pipe→space) closes to 0.00px while the untouched FORWARD seam (space→digit) inherits the WHOLE pipe-to-digit gap (measured 5–8px, three worlds over the file's absolute ceiling) — flipping the tiebreak to favour forward would only rotate the same whole gap onto the other seam, not fix it. The same hard pick showed a matching defect on a RUN of 4+ glyphless columns flanked by two different letters: the interior column where "nearest" switches sides is a hard, single-column STEP with nothing between to soften it (measured 6.5–7.0px on Bombora). **Round 3** makes the borrow NON-DIRECTIONAL: `nearest_row_raster_box` still searches outward, but when a real box exists on BOTH sides it returns the LINEAR INTERPOLATION of the two, weighted by relative distance (`t = back_dist / (back_dist + fwd_dist)`) — a tied `"| 1"` space now reads as the MIDPOINT of pipe and digit, so EACH seam absorbs roughly half the gap instead of one absorbing all of it (measured worst case 4.0px, both seams near-symmetric), and a flanked run sweeps smoothly with no privileged flip column (every step's magnitude bounded by its neighbours, not one dominating). When only ONE side has real ink (column 0; the tail of a run with nothing real beyond it), the blend degenerates to that single box exactly as round 2 already returned, so the literal-adjacency fixtures stay byte-identical. **THE BAR IS MEASURED, NOT ASSUMED**: round 3 first measures the real GLYPH-TO-GLYPH adjacent-cell delta the product already ships between genuinely different letter classes (x-height/ascender/descender/capital/digit/punctuation/ligature — transitions nobody has filed as a bug) — median ~5.5–6px, worst 14.0px (`digit`→`punctuation` on Bombora/Mopoke/Galah) — and holds every glyphless seam to that SAME empirical ceiling rather than an assumed constant. Laws: `glyphless_seams_stay_within_the_products_own_accepted_glyph_to_glyph_bar` (the bar itself, the `"| 1"` both-seams fixture, the symmetry mechanism claim) and `interior_run_between_two_different_letters_has_no_flip_step` (the 4+-run, no-step-dominates claim) in `caret_transition_item105.rs`; both proven non-vacuous against round 2's hard-pick code.
- **THE WRAP-BOUNDARY TRAP — caret transition claims are measured ROW-RELATIVELY, never absolute (adjudicated 2026-07-26).** A run of spaces long enough to fill an entire wrapped visual row by itself — flanked above and below by real-ink rows of the SAME logical line — forces `nearest_row_raster_box` (bounded to the caret's OWN row by design, an O(row-width)-not-O(doc) tradeoff) to come up empty on both sides, so `caret_cell_vertical` falls to the SYNTHETIC tier-3 box anchored to THAT row's own baseline. Two independent audit passes measured the caret's ABSOLUTE pixel position stepping across that row's wrap boundary and reported a ~30px jump as a HIGH-severity defect; it wasn't one. Crossing a soft wrap moves the caret down exactly one row's `line_height` and back toward the left margin — correct, INTENDED behaviour — and an absolute-position delta cannot distinguish that row-pitch component from a genuine cell-geometry discontinuity, because it compares two rows that are never drawn at the same y in the first place. Isolating the two (subtract each column's own `caret_baseline_y()` before diffing) showed the ~30px figure was ~84% pure row pitch (a flat 32.00px on every proportional world, exactly this fixture's `line_height`); the genuine tier-2/tier-3 boundary residual is 5.85px worst-case (Bilby) — comfortably inside `TRANSITION_BOUND_WIDE_PX` (7.5px) and well under the roster's own empirically-measured 14px glyph-to-glyph worst case. Any future caret-transition claim across a wrap must diff row-relatively (this column's `cy` minus its OWN row's baseline), not the raw absolute `cy`, or it will re-derive this same false positive. Law: `caret_transition_item105.rs`'s `glyphless_row_transition_is_bounded_row_relatively_across_a_wrap` — sweeps every adjacent column pair of the reported fixture over the full proportional roster, asserting BOTH that the absolute delta is genuinely large (proving the fixture reproduces the reported jump) AND that the row-relative residual at the same seam stays bounded.
- Laws: `render/tests/caret_ink_box.rs` — the ink-box hug across ascender / x-height / descender with the pad asserted letter-INDEPENDENT (and the pre-fix gap asserted as a fixture witness), a no-wildcard `CaretMode::ALL` form sweep, the mono uniform-grid law (one top y at every column on Tawny + Mangrove), the moving-streak law, the glyphless space/EOL/line-start fallbacks (now asserting the item-105 baseline-relative formula on a proportional world, the old row-box formula unchanged on mono), the pad-vs-dilation bound, and a grep-law that `render/layers.rs` holds no caret vertical geometry of its own. **`render/tests/caret_transition_item105.rs`** — the ADJACENT-COLUMN transition law item 91 never wrote: outer cell `(center_y, height)` diffed between an on-glyph column and its glyphless/ligature neighbour, over the full proportional-world roster, every representative glyph class (x-height/ascender/descender/punctuation/digit/capital/ligature/space/EOL/empty-line) at LITERAL adjacency, Block and Morph at rest (with Morph's travel proven UNAFFECTED, swept over the same roster), a wrapped-line boundary, two zooms (including a non-1.0 value) and 1x/2x DPI, the theme-preview-lag seam (both the empty-buffer arm-gate law and the repair round's real-row ratio-consistency law), plus the mono complement (must stay exactly zero) — every sweep carries its own non-vacuity proof against the pre-105 formula, and the repair round's own additions are separately proven non-vacuous against the FIRST (flawed) landing, `12c2fb4`.

## Theme capabilities as data (`theme/model.rs::RenderCaps`)

- Render call sites never branch on world identity — they read a `RenderCaps` field: `selection_style`, `caret_block_style`, `backdrop`, `elevation`, `decorative_wash`, `image_reveal`, `highlight_texture`, plus the personality fields `card_anchor`, `chrome_face`, `motion`, `list_style`, `facet_style` (and `TitleStyle::Placard{corner,scale,ink}`). Born as the Wagtail refactor ("default = every other world"); that framing is dead — most worlds now set some field away from its most-common value, and THEMES.md's deviation table is the one place that says WHICH (held to the roster by `theme::tests::themes_md_deviations`, so it cannot drift into the stale enumeration this sentence used to carry). **No theme may need its own code path** — new personality = new caps field + data.
- **ITEM 70's PRINTED-CARD caps** (`card_texture: CardTexture::Flat|HalftoneDots{angle_deg,cell_px,density}`, `card_shape: CardShape::Rectangular|Chamfered{cut_px}`) — Quokka alone assigns `HalftoneDots` (+ `Chamfered`). Read by the ONE `ListBacking::Card` consumer (`render::chrome::TextPipeline::card_shape_texture`, called from `prepare_panel_card_elevation` + `overlay_draw_card`'s spell-popup arm): a plain `sd_round_rect` SDF (`shaders/selection.wgsl`) gets replaced by `sd_card_rect`'s CSG intersection with a diagonal half-plane when `chamfer > 0`, shared by the fill/border/shadow trio so the eight-edge boundary agrees across all three; the texture composites only over the FILL, sampled at the ABSOLUTE canvas pixel (not instance-local) so a Split Pane's two surfaces share one continuous phase across the open gap — Quokka's dot lattice with a pure horizontal rolloff (`halftone_rolloff`) keeping the left content column clear. The texture's own ink is DERIVED (`theme::card_texture_ink`, a `muted`→`base_300` blend) — never a raw/amber literal in the cap. `render::chrome::narrowed_chamfer_px` caps the chamfer at 40% of a card's own smaller dimension so a genuinely tiny surface (the spell popup) never lets the cut steal text room, while an ordinary short query bar stays untouched. Dev-only gallery knob `AWL_CARD_CAPS_FORCE=flat|halftone` (Quokka), mirroring `AWL_CJK_FORCE`'s "total no-op unless set" contract. (**Item 71** briefly extended this with a SECOND card texture, `CardTexture::JaggedWave{tiers,period_x,period_y,amplitude,density}` — Bowerbird's own woven printed-card identity, sharing `dot_color` with `HalftoneDots` via `SelectionPipeline::set_wave` — plus an `AWL_WAVE_FORCE` capture knob. **Item 86's light-worlds taste round retired the whole variant**: Bowerbird's summoned cards returned to plain flat, `JaggedWave` had zero remaining assignees, so it — and `set_wave`, `CardWave`, `CardTextureDraw`, the WGSL `jagged_wave_*` functions, and `AWL_WAVE_FORCE` — were all cut outright rather than left as dead code nothing constructs. `CardTextureDraw` (once a two-variant wrapper enum) collapsed back to a bare `Option<CardHalftone>`.)
- **ITEM 86's ZIGZAG margin ground, TILED INTO A FIELD BY ITEM 89** (`theme::Background::Zigzag{from,to,dir,tint,period_px,amplitude_px,angle,density}`, shader id 7) — a tiled field of repeating chevron ("V") rows over the gradient, in the SAME whisper-mark family as `Dots`/`Pinstripe` (composites through the shared `pattern_coverage` → `fs_main` mix pipeline, unlike `Bands`/`Waves`' own final-color branch). Quokka's old dot grid and Gumtree's old grass-`Bands` field BOTH moved here, each with its OWN authored dials so the two never read as a recolor of one asset: `period_px` is the TOOTH-WAVELENGTH dial (Quokka 100px, tight; Gumtree 170px, broad), `amplitude_px` the PROFILE dial (Quokka 24px; Gumtree 60px — and, through item 89's abutment rule, the field's ROW PITCH and the stroke's own thickness), `angle` the DIRECTION dial (Quokka ~54° steep diagonal; Gumtree ~15° near-horizontal), `density` the CONTRAST dial stacked on the shared `PATTERN_MAX_COVERAGE` ceiling (Quokka 0.60, bold; Gumtree 0.20, quiet). One shader function (`pattern_coverage`'s `shader == 7u` branch) reads all four via the SAME `params` vec4 slots Dots/Stripes/Bands already reuse per-shader (`params.x`=period, `.y`=angle, `.z`=amplitude, `.w`=density) — no new uniform layout, no per-world code path. `Background::Bands` is now DORMANT reusable infrastructure (Gumtree was its one assignee) — the exact "ships with zero worlds until one wants it" shape `edge: true` Dots already held. Laws: `render/tests/backgrounds_item86.rs` (the round's DESIGN brief — roster, four-dial distinctness, real-pixel contrast), `theme::tests`' data-level roster + palette laws. **ITEM 89 — the FIELD fix (correctness, not a new taste round), in TWO parts.** *(a) The FOLD.* The `shader == 7u` branch computed `center` from the ALONG-travel coordinate alone and took `abs(ry - center)`, which is ONE continuous chevron curve embedded in the plane: teeth repeated along travel, nothing repeated the curve ACROSS it, so a margin carried a single wandering stroke with large blank areas (62% of a tall Gumtree margin, the right two thirds of a Quokka one, measured at literally zero ink) and a taller window gained no second row — it only let the same stroke travel further off-canvas. The fix FOLDS the across-travel coordinate through a row pitch (`d = |fract((ry - center)/row_h + 0.5) - 0.5| * row_h`), turning the single curve into the family `center + k*row_h` — a genuinely tiled field. *(b) The ABUTMENT RULE — the coverage repair.* The fold's first cut set `row_h = period_px` (a square lattice). That TILES but does not COVER: row `k`'s ribbon only ever visits `ry` in `[k*row_h - amp - t, k*row_h + amp + t]`, so whenever `2*amp + 2*t < period_px` the field carries a hard BLANK LANE of `period_px - 2*amp - 2*t` that no chevron enters at any point of the plane — ~70px every 250px on the dials that first shipped, and at ordinary-but-shorter windows a narrow margin could sit wholly inside one (verified at the app's real adaptive column geometry: an 80x182px band of a 1600x600 Gumtree right margin, and a 27x234px cell of a 1400x700 left margin, at exactly zero deviation; Quokka's finer field was clean at every size). `row_h` is now `2.0*amp + thickness` (`theme::Background::zigzag_row_pitch_px` is the host mirror), so each row's ribbon sweeps exactly its own lane and its CORE sweeps a lane plus `0.2*t` of overlap: rows ABUT for ANY dials at any angle — coverage stopped being a property of the dial pair and became a property of the shader. (**Item 100** corrected that claim's scope: it read "any viewport size or aspect" and that clause was false — a viewport is a ROTATED RECTANGLE of the plane, so it inherits the guarantee only while it holds a whole tooth of travel; a `period_px: 2000`/`amplitude_px: 150` field on a 1200x800 canvas leaves a measured 78px lane. Bounded by the TOOTH, which `ZIGZAG_MAX_ROW_PITCH_PX` does not bound; no shipping world is within 4x, and that ratio is now the authored guard.) The old amplitude-vs-period collision clamp retired with it (abutting rows cannot smear). `period_px` is now the TOOTH wavelength alone; `amplitude_px` carries the profile, the pitch and the thickness. Dials: Quokka 100px/24px UNCHANGED (its field already covered every margin cell at every swept viewport), Gumtree 170px/60px — the same ~0.35 profile ratio item 86 authored (85/250), the whole field scaled to a pitch a page margin can hold. Recorded cost: Gumtree reads ~6 broad rows down an ordinary window where the first cut read ~3, and that reading is NOT recoverable — a void-free chevron's teeth span its own pitch by construction, so "rows three times the height of their own teeth" and "no blank lane" are mutually exclusive; broadness now lives in the tooth wavelength, the 2.5x pitch ratio against Quokka, and the low density. The draw call is still the LITERAL `pass.draw(0..3, 0..1)` fullscreen triangle (a handful of added fragment ALU ops; zero CPU work, zero new uniforms). Laws: `render/tests/backgrounds_item89.rs` — a per-cell OCCUPANCY grid SWEPT over twelve `(window, measure)` shapes (heights 500..1600, aspect 0.56..2.37, including both verified failures) at the app's OWN `TextPipeline::column_left`/`column_width` owner and in BOTH placement regimes; **item 100 gave that headline law its teeth** — it had SURVIVED the exact regression it names (1.43x its floor against 5.48x healthy) because a 3x3 partition makes cells up to 116x266px, so the cell is now DERIVED from the field's own geometry (`P*|sin a|` by `P*|cos a|`, the smallest axis-aligned rect whose across-travel chord reaches a whole row pitch: Quokka 41x30, Gumtree 33x122) and the ink floor is a twentieth of the field's own structural areal density `2t/pitch` rather than a bare non-zero — 4712 cells graded, tightest occupancy 1.5x the floor, and 1595 cells at literally zero ink once the pitch rule is reverted. Item 100 also PINS the residual that grid deliberately does not claim (`zigzag_sub_pitch_voids_stay_isolated_pockets_never_a_lane`): below the minimal cell, ~30x100px lens voids exist between neighbouring ribbons (67 of 1424 sub-pitch cells on Gumtree, 3 on Quokka) — unavoidable for any sparse row field and closable only by a denser authored dial, so their SHAPE and POPULATION are bounded (no blank cell may touch another; ≤8% blank) instead of failed on, and both bounds go red at the broken pitch rule. Also: a no-blank-LANE law scanned on the ACROSS-TRAVEL axis (a screen-`y` scan cannot see a lane once the angle is nonzero — the trap that hid this); an EVENNESS bound pinning the widest fully-blank horizontal band of any swept margin (174px as first shipped → 60px at real GPU pixels); the abutment THEOREM over 1600 arbitrary dial settings; an authored row-pitch bound (`theme::ZIGZAG_MAX_ROW_PITCH_PX`); a row-rhythm law; a height-scaling law; zero mark ink inside the writing column; byte-determinism at two canvas sizes; a WGSL structural tripwire on the fold AND the abutment rule; and a host mirror kept in lockstep with the GPU, carrying the first cut's pitch rule as a second arm purely so the laws can be proven capable of catching what it did. All pixel claims are DIFFERENTIAL (the world as authored minus the same world with `density: 0.0`, so gradient + dither + quantization cancel exactly and the measurement is the mark alone) — `mark_field` is that oracle's one owner, reused by item 86's contrast law.
- **ITEM 158's DECKLE margin ground** (`theme::Background::Deckle{ground,layer,deckle,weave,period_px,wander_px,density}`, shader id 9) — the handmade-paper MATERIAL field, and the roster's tenth ground. Quasi-random CONTOUR LANES, seeded per lane index and torn by a fixed two-tone sine wander, drawn only in the margins and computed entirely from the fragment position plus the page column. Static: no clock, no texture, no asset. ONE theme-owned dial, `theme::Weave`, picks the profile — `Strata` indexes the lanes on DISTANCE FROM THE PAGE COLUMN (so the contours gather around the writing page and MIRROR across it by construction; Paperbark's) and `Fibres` indexes them on screen `y` as thin strokes plus a diagonal vein family (reusable, DORMANT, the `Bands` / `Dots { edge: true }` shape — item 159 is its intended first assignee, and adopting it is one word in a world literal, not a renderer change). The scalar rides `params.w` through the new `BgDesc::weave` slot, whose INERT default is `0.0` for every ground with no weave (`Background::weave_mode` is the one owner); pitch/wander/density take `params.x/y/z`. `density == 0.0` collapses BOTH profiles to their flat ground EXACTLY — that is deliberate, and it is what gives the family item 86's `mark_field` differential oracle for free. Coverage is a property of the SHADER, not of a dial pair (item 89's abutment lesson): `deckle_rgb` CLAMPS the authored pitch to `DECKLE_MIN_PITCH_PX`, below which the deckled edge — a fraction of a lane — would fall under a pixel and alias. Laws: `render/tests/backgrounds_item158.rs`, whose swept axis is the MARGIN'S OWN WIDTH (the lanes are indexed on page distance, so how many a margin can show is a property of that margin, not the window), times 1x/2x DPI.
- **ITEM 186's GROUND COORDINATE SPACE — every authored ground quantity now declares whether it is COMPOSITION or SAMPLING.** Before it, `scale_px`, `period_px`, Dots' 24px cell and Deckle's pitch were all PHYSICAL pixels by accident of the coordinate a fragment shader runs in, so a 2x display rendered a ground at half its logical size and showed roughly twice as many elements — invisible to every law, because a capture is only ever internally consistent with itself. Item 176's crisp countable grammar is what made it structural rather than a texture difference. **COMPOSITION** (a cell, a pitch, a mark size, a wander, a reach) is now LOGICAL, so matched logical canvases show the same composition at any density; **SAMPLING** (an antialias feather, the banding-kill dither cell) stays PHYSICAL, so a better display resolves that same composition more finely. The distinction is DATA, not a shader convention: `theme::ground_space`'s `GroundSpace` + `Background::authored_quantities` is a wildcard-free per-variant table naming every number, its class and why, and `shaders/background.wgsl` is the consumer — ONE conversion of the fragment position (`to_logical`, fed from `BgDesc`'s new `scale`, which is `TextPipeline::dpi` alone and NOT `metrics.scale`, since a ground has never scaled with text zoom) and ONE owner converting a physical feather back into that space (`sampling_feather`). The page-column punch and `bayer_threshold01` deliberately keep the device pixel. The judgment calls are the deliverable and are recorded in THEMES.md's own section: a floor whose motivation is sampling (`FINDS_MIN_SCALE_PX`, `DECKLE_MIN_PITCH_PX`) is still a floor on a COMPOSITION quantity and stays logical, or the clamp hands the composition back to the display where it binds; a soft ramp that is a visible fraction of its own mark (Zigzag's ribbon edge — which also feeds the row pitch through item 89's abutment rule — Deckle's fibre ramp, a lane's torn boundary) is drawn character, not a sample-grid resolve; item 176's 0.75px `FINDS_EDGE_AA_PX` is the canonical sampling quantity and did not move; `Bands` and `Lava` were already density-independent through viewport-fraction authoring and are named in the table so that absence is on the record. Laws: `render/tests/ground_space_item186.rs` (roster-wide composition identity by cross-correlation of the 1x field against the box-averaged 2x one, the feather's ramp-width invariance, no-wildcard completeness, and a WGSL structural tripwire). The 1x/2x visual sheet is `scripts/capture-ground-space.sh`.
- **ITEMS 132/194/268's WARPED-GRID margin ground** (`theme::Background::WarpedGrid{ground,minor,major,tunnel,spacing_px,density}`, shader id 10) — the roster's ELEVENTH ground and its only TRAVELLING one, and Kite's alone. ONE camera at a FIXED point, ONE circular cross-section: `warp_room_axis(vp_x)` (`shaders/background.wgsl`) returns `vp_x * 0.5` and nothing else — no page term, no margin term, and, the point of item 268, no SIDE term, so there is exactly one vanishing point in the room and it sits BEHIND THE PAGE. Each margin is one flank of that single tube. The projection is closed-form, not an inversion: `w = p - axis`, `u = length(w)` is the projected radius and `atan2(w.y, w.x)` the polar angle, so a RING is a level set of `log2(anchor/u)` (a true circle, not an ellipse — there is no bend to make one) and a RAIL a level set of angle. **`Tunnel::PerMargin` does not exist** (`grep Tunnel:: theme/ground.rs` — the enum's four arms are `Fixed`, `PageScaled`, `MarginPlaced`, `Reversed`); the per-margin axis it would have named, `warp_window_axis`, is gone from the tree entirely, along with `WARP_WINDOW_INSET`, `WARP_BEND_GAIN` and `WARP_SOLVE_STEPS` — a grep-law (`render/tests/warp_tunnel_item194.rs::shader_has_fixed_geometry_and_no_steering_path`, mirrored in `backgrounds_item132.rs`) asserts all four absent from the WGSL by name. That per-margin axis is what the user saw as "two tunnels" and every law of the day passed, because they graded the field's scale, density, travel and aliasing — all of which a second camera preserves exactly — and nothing asserted the two flanks were views of the SAME cylinder. Forward travel is one ADDITION to the ring's own coordinate, `let travel = select(g.warp_travel, -g.warp_travel, reversed); let ring = rpo * log2(anchor / u) + travel;`, so every ring's projected radius GROWS and the lattice sweeps outward past the reader; `Tunnel::Reversed` flips the sign and the world recedes instead. **There is no steering pose.** `crate::warpgrid::route_pose` does not exist (`grep -r route_pose src/` is empty) and `background.rs`'s `Globals` struct carries no `pose` field — `render/tests/warp_tunnel_item194.rs` and `backgrounds_item132.rs` both assert `"g.pose"` absent from the WGSL by name. `src/warpgrid.rs` owns only the ONE thing left to steer: continuous forward travel (`LOOP_SECONDS`, `FORWARD_CELLS_PER_LOOP`, `should_travel`, `forward_cells`, `advance_phase`) — a phase in seconds, not a pose. The SCALE is the ROOM's and never the page's: `WARP_SECTION_ROOM_FRAC` (0.432) sizes the anchor ring's radius against the room's own height alone, and the section is a circle — no affine fit — so the projected cross-section measures aspect 1.00 and one fixed radius at every page width the adaptive column reaches, over the whole `page::MIN_MEASURE..` band (`render/tests/warp_tunnel_item194.rs::fixed_framing_is_room_owned_across_page_width_and_offset`). **Since item 268 the page column is VEILED for this ground rather than punched** — `fs_main` declines the hard punch for `g.shader == 10u` alone — to `WARP_PAGE_VEIL` (0.13), with the minor lattice and the whole rail family retired at the page edge so that only the sparse major RINGS share space with prose; that crossing is what makes "one tunnel" legible (a ring leaving the left flank has to arrive in the right one at the same radius) and is held to a real-composite legibility floor, not taste (`render/tests/warp_one_tunnel_item268.rs::the_under_page_crossing_clears_the_body_ink_legibility_floor`). `theme::Tunnel` carries `Fixed` (the only profile a world authors — Kite's) plus three DATA-ONLY mutation arms that mutation-prove the invariants above by breaking them on purpose: `PageScaled` (round 1's page-derived scale and flattened aspect), `MarginPlaced` (round 2's per-margin window placement, `warp_window_hide`, kept alive to prove the room-owned framing is a real constraint and not a vacuous one), and `Reversed` (the inverted travel sign). The scalar rides `params.w` through `BgDesc::tunnel`, inert `0.0` for every ground with no tunnel; spacing and density take `params.x`/`params.y`. `density == 0.0` collapses the field to its flat `ground` tone EXACTLY (`backgrounds_item132.rs::zero_density_is_an_exact_flat_ground_reference`), which is what gives the family item 86's `mark_field` differential oracle. Laws: `render/tests/backgrounds_item132.rs` (roster, the under-page veil, hierarchy, edge fade + the crossing, no-moire, narrow simplification, the exact wrap, the zero-density reference, the no-steering-path grep), `render/tests/warp_one_tunnel_item268.rs` (the field under the page is a function of radius alone, a ring found at every angle where one tunnel predicts it, the ladder held at two `capture-dpi` scale factors, and the under-page legibility floor) over twelve `(window, measure)` shapes x named travel phases, and `render/tests/warp_tunnel_item194.rs` (room-owned framing invariant under page width/offset, the two page-owned mutation arms breaking it on purpose, forward travel growing the ring ladder at the authored rate, `Reversed` receding, and the no-steering-path grep). **The travel phase advances on the HOT per-frame loop, not the ~10 fps ambient tick every other time-varying ground rides** (`TextPipeline::advance_warp`, gated by `warpgrid::should_travel`, which wraps `lava::lava_should_tick`): those grounds drift, this one travels.
- `is_one_bit()` still exists (pins Wagtail's identity for the monochrome law tests) but the renderer no longer reads it. **Grep-law `theme_caps_law`:** fails if `.is_one_bit(` or a quoted world name appears in real code under `src/render/` — structurally bans a future per-theme special case.
- **Lava frost is an ORGANIC glyph-seeded FIELD (item 32), not rectangular pills.** The lava worlds (Mangrove + Firetail) keep both margins alive and frost the drawn margin ink (outline + gutter). Every visible margin glyph SEEDS a small close halo `[x0, x1, yc, r]`; the shader SUMS the halos into one continuous scalar field and thresholds it (`lava::frost_coverage`), so neighbours MERGE in any direction — nearby words/rows become one organic island, a group-gapped row separates, with NO per-row rule and NO zoom breakpoint (topology falls out of the continuous field). The ROW's radius ceiling is derived from the ZOOMED LABEL row height + the `feather_px` skirt (`render::frost_seed_radius`), and the skirt being a fixed logical px while the row scales with zoom is what lets islands join at small zoom and split at large zoom, naturally. Both worlds share the SAME shape/blur/dim/feather recipe (`crate::lava` consts); only the palette-derived colour differs (the world's own `Background::Lava` ground/blobs, via `render_caps.frost`). Owners: `TextPipeline::outline_frost_seeds` + `gutter_frost_seeds` → the shared `render::push_text_seeds` (per-glyph or the word-run degradation arm, `lava::FROST_SEED_PER_GLYPH`); the seed field is a PROTO-CACHE (`frost_seed_key` / `frost_seeds`, rebuilt only on a viewport/zoom/column/margin-text miss — witnessed by `--bench-frost`). All non-lava worlds stay byte-identical (empty seeds → inert shader path).
- **EACH SEED's own radius is PUNCTUATION-AWARE and BOUNDED, not a blanket row value (item 61).** The row's `frost_seed_radius` is a CEILING, not every run's literal radius: `render::frost_run_radius(r_row, run_ink_w, skirt)` takes the tightest of three — `r_row` itself, the run's OWN measured ink half-width (× `lava::FROST_RUN_INK_RADIUS_FRAC`) plus the skirt, and a BOUNDED END-PAD ceiling (`skirt × lava::FROST_END_RADIUS_SKIRTS`, independent of row height). An isotropic seed's `(1-(d/r)^2)^2` falloff is symmetric, so a near-zero-ink run at the FULL row-height radius reads as a bare circle hung off the glyph (an isolated `&`, a one-letter heading) instead of a close hug — the ink-derived bound ties that run's own radius to ITS OWN advance geometry instead. The end-pad ceiling separately caps how far a LONG single-run label's halo can reach past its own final glyph (a hyphenated compound like "Button-free" has no internal whitespace to break it into shorter runs) — a bound that does NOT grow with a taller row (a deep heading-ladder rung, a big zoom), only with the skirt. An ordinary multi-glyph run's ink half-width and the end-pad ceiling both already sit at or above `r_row` in practice, so `min()` is a no-op there — row/nearby-run merging for ordinary text is unchanged. No glyph or label is identity-matched; the bound is purely geometric. Laws: `render/tests/frost.rs`'s `isolated_punctuation_run_radius_is_bounded_below_a_normal_runs`, `long_run_end_pad_is_bounded_independent_of_row_height`, `nearby_ordinary_runs_still_merge_after_the_bounded_radius_round`.

## Rotated labels (`rotated_label/` + `shaders/rotated_label.wgsl`)

- **awl draws text at ONE angle unless a label asks otherwise.** glyphon 0.11 carries no transform anywhere — `TextArea` exposes `left/top/scale/bounds/default_color/custom_glyphs`, `CustomGlyph` exposes `left/top/width/height`, and there is no rotation, skew or matrix in the crate — so every run the document layer draws is upright and a world wanting a turned or slanted cue has no way to say so. `rotated_label` is that capability and only that: the caller shapes ONE short run into a `GlyphBuffer` (whichever face, size and colour its theme data names), `LabelMask::compose` composes that run's whole coverage into ONE R8 image on the CPU — the same swash cache glyphon rasterises from — and `RotatedLabelPipeline` paints it through ONE quad whose vertices are rotated onto a unit axis. The rotation is the axis rotation `shaders/caret.wgsl` already performs for the caret streak, applied to a glyph mask instead of a rounded rect; `SelectionPipeline::prepare_rotated` was the same step for selection quads.
- **It cannot become a second prose renderer.** The mask reads `layout_runs().next()` and stops, so there is no line breaking, no per-line alignment, no wrapping and no selection — a paragraph is structurally unreachable and the document layer stays the one prose renderer. The pipeline draws one label; a second is a second instance, not a longer buffer.
- **The frame is pure and separately owned** (`rotated_label/geometry.rs`): `label_axis_deg` (0° reads left to right, 90° bottom to top — screen pixels are y-down, so a reader's counter-clockwise turn is a negative one, and the four quadrant axes are returned EXACTLY rather than through the transcendentals, which is what makes a quarter turn a lossless texel transpose), `label_point`/`label_local` (the run's frame on and off the screen), `label_quad`, `label_bounds` (the TIGHT axis-aligned extent, for reserving a gutter or proving non-overlap) and `label_hit` (the rotated box, NOT the bounds, which at a slant over-claims its own corners). `unit_axis` is the one normaliser both the geometry and `prepare` route through, so the CPU's measurements and the GPU's quad cannot disagree; it degenerates to the shared inert `selection::UPRIGHT_AXIS` on a zero/NaN input.
- **Quality is MEASURED.** At 0/90/180/270° the transform is lossless: the composed coverage arrives with ink mass 1.0000, mean absolute error 0.0000 and full stroke/gap contrast, at 1× and 2×. At an arbitrary slant it is one bilinear resample and nothing more — the rendered canvas matches an ideal bilinear rigid rotation computed independently on the CPU to within 0.005 of full coverage, so the softening a slant costs is the unavoidable price of rotating a raster, not ink the pipeline loses. The composed mask carries a one-pixel transparent border because a rotated quad has no anti-aliasing of its own and would otherwise cut a stem square at its boundary. Laws: `rotated_label::tests` (the frame, oracled by polygon winding and measured side lengths) and `render/tests/rotated_label_item235.rs` (the pixels, oracled by the CPU-composed coverage image, which no rotation code touches).
- A per-instance colour PAIR interpolates along the run's own baseline, so a world whose visual language is a gradient line gets one without a second code path; equal values are a flat label. Nothing here reads a theme — which string, which axis, which colours, where, is theme data a caller supplies.
- **Two world expressions share ONE preparation owner** (`render/rotated_location.rs`'s `TextPipeline::prepare_rotated_location_label`, dispatched by `render/chrome/rotated_location.rs` on `theme::LocationStyle`): Cassowary's `RotatedRail` (90°, composed against the ROOM — see the paragraph below) and Magpie's `Raked` (item 224 — the location cue stays in item 220's own inline slot, unmoved, but reads along the diagonal spine's own rake instead of upright, in a gradient between the spine's two authored tones, `muted` at rest and `base_content` selected). Only the flush edge, the bottom anchor, the two-axis fit box, the natural size, the axis angle, the two colours and the overflow policy differ between the calls — one `RotatedLabelPlacement`, decided in the dispatcher; the mask compose, the cache, and the fit solver are shared verbatim. Magpie's angle is DERIVED, never pinned: `render/chrome/diagonal.rs::location_axis_deg` reads the diagonal composition's own `ROW_STEP`/row-height ratio — and specifically the row's own MEASURED step (`DiagonalClusterRail::spine_step`, which a too-narrow card's `TRAVEL_MAX_BAND_FRACTION` yield can shrink below the authored constant), so the cue and the spine beside it cannot drift apart even under that yield. At Magpie's own near-vertical, non-quadrant angle the round trip measures contrast 0.65 at 1× and 0.83 at 2×, with worst-case departure from an ideal bilinear rigid rotation of 0.0046 of full coverage — the softening a slant costs, not ink the pipeline loses; supersampling was deliberately declined (it would buy at 1× what 2× already gives, at the cost of letterforms no longer matching their upright siblings). Laws: `render/tests/raked_location_item224.rs`.

- **`RotatedRail` is composed against the ROOM, not the card** (`render/chrome/rotated_location.rs`'s `rotated_rail_placement`). The cue takes the wordmark placard's own outer MARGIN, sits just ABOVE it, and carries `ROTATED_RAIL_PLACARD_FRACTION` (⅔) of its type size in `theme::placard_ink` — all four quantities read from the placard's own owner (`overlay_shape_placard`) on that frame, so a world redialling its wordmark's loudness moves the cue with it and the pair cannot drift. The placard family is deliberately PHYSICAL (window-short-side scaled, zoom-independent), so the cue inherits that: it does not change size with zoom or DPI, and the ⅔ holds at both tiers by construction rather than by a second multiplication.

  **The size IS the composition, so the cue is exactly ⅔ or absent** — never shrunk (`Overflow::Park`; `Raked` keeps `Overflow::Shrink`, whose budget is a neighbouring row's breathing room). It parks when there is no wordmark on the frame (a card in its fill regime, a title-less kind), when the wordmark hugs the room's ceiling rather than its floor (no shipping world does; a law fails by name if one asks), or when the margin cannot hold the run — past roughly 1.7× zoom on the widest card, where the placard itself is already bleeding behind the card.

  **The responsive bound is measured from real rects, and the card's DRAWN span is wider than its box.** Along the reading axis the run may rise from the placard to the room's own top margin (that margin read off the placard's own inset, never re-stated); across it, it may not reach the card — and the bound there is `overlay_card_drawn_span`, not `card_x`, because under `Bars` the SELECTED row's plate grows OUTWARD past the card box (`grow_span`, mirrored on a right-anchored card) and its scrim pads that again: at 1.8× zoom that is 32 device px of card to the LEFT of `card_x`, and a cue bounded by `card_x` alone cleared the box while sitting 2 px from the plate. Laws: `render/tests/rotated_rail_item297.rs` — the ⅔ relation from both sides at both DPI tiers, presence floors by ink EXTENT and by ink STRENGTH beside the wordmark's own (a non-overlap law gets happier as its subject fades), non-overlap and no-clipping over every lens label in the roster, and the park arm with its non-vacuity neighbour one zoom notch out.

## Overlay personality & chrome composition (render/chrome/ + theme/model.rs)

- A fortnight of rounds, one shape: overlay/chrome variety is data in `RenderCaps`, never a per-world code path. `ListStyle` (Pane default | Bars = per-row plates | Diagonal = a spine and NO plate | Rules = hairlines and NO plate, the quiet one, below; `draws_row_plates()` is the one owner of that distinction, and `list_backing`'s `BarePlates` is a claim about the CARD, not the row — see item 236 below), `FacetStyle` (Text default | Band | Chips), `CardAnchor` (incl. TopRight + `mirrors_growth` for Bars), `TitleStyle::Placard{PlacardCorner, PlacardInk}`.
- **`ListStyle::Rules(RuleSelection)` — the QUIET fourth composition; Paperbark is its one carrier.** It exists because `Pane` carried fifteen of twenty worlds while `Bars` and `Diagonal` between them covered only loud, so the missing grammar was a QUIET one. Its organising idea is ABSENCE: leading and hairline rules do the arranging and nothing is drawn as an object. Structurally that is `ListBacking::BarePlates` with `draws_row_plates()` FALSE — `Diagonal`'s own configuration, so no new seam was needed — plus one thing `Diagonal` did not need: `overlay_prepare_bar_scrims` asks `draws_row_plates()` before padding anything out, because a hairline grown by `BAR_SCRIM_PAD` on every side IS the plate this style refuses (`Diagonal` reached that code with an empty rect list and was scrimless by accident of that emptiness). The rules ride the EXISTING `overlay_bars`/`overlay_rows` quad pipelines — hairlines on the first, the selection mark on the second — so there is no new pass, no new uniform and no new shader. Its lengths are `RULE_HAIRLINE`/`RULE_SELECTED_WEIGHT`/`RULE_ROW_AIR`/`RULES_TEXT_HPAD`/`RULE_MARK_LEN`/`RULE_MARK_GAP` (`render/chrome/overlay_rules.rs`), all `Logical`; the air rides the ONE row-pitch owner `overlay_row_gap` exactly as a `Bars` plate gap does, and the gutter rides `overlay_text_hpad`. **`RuleSelection::Weight` is the shipped treatment** (chosen by the user against both rendered side by side): the two rules bounding the selected row thicken and run out past the text measure to the full band, and the row's interior stays plain ground. `RuleSelection::Gutter` leaves the row and its rules untouched and hangs a short heavy segment in the gutter beside it — and that segment SHORTENS into whatever gutter the band has rather than hanging off the card, because the contextual spell popup insets its text by its own `SPELL_PAD` rather than by the list style's. ⚠️ Neither may fill the row — a filled band is `Pane`'s answer and borrowing it would make this a restyle of that. Both are forceable for capture: `AWL_OVERLAY_LIST_FORCE=rules[:weight|gutter]`.
- **ITEM 294 — A CRISP PICKER FROSTS ITS OWN FOOTPRINT (`render/blur/extent.rs`).** Frost was a property of the PLATE, so on a composition that draws none the theme and caret pickers — the two CRISP exceptions, which stay unfrosted because their whole job is previewing live world colours — drew their rows straight over live prose and the two interleaved glyph-for-glyph. The frost now has TWO EXTENTS carried as one value, `blur::Frost`: `Full` (the historical whole-canvas takeover — a blur-eligible overlay, the held HUD, the lifetime card, the hold-⌘ peek) and `Footprint`, the DRAWN SURFACES' own box (⚠️ **not `overlay_card_rect`'s** — item 318 widens it where it must to seat the card's upright chrome, and item 343 narrows it to what the frame actually drew; see both bullets below) and nothing beyond its own feathered skirt, so the page around the card keeps the live colours the picker exists to show. **`TextPipeline::frost_mode` is the one owner of whether and where**; it asks every full-takeover condition FIRST (so every frame that frosted before still does, byte-identically) and only then the footprint arm. **ENROLMENT IS ASKED OF THE ROSTER, never of a named world:** `blur::footprint_frost_applies` is true exactly when the composition puts nothing opaque between its rows and the document — `list_backing() != Card` AND `!draws_row_plates()` — which is `Diagonal` (Mangrove, Magpie) and `Rules` (Paperbark) today. A `Pane` world is excluded by its panel and a `Bars` world by its plates; both are byte-identical (measured: 0 of 960 000 pixels differ on eight worlds' pickers, and on the full palette). The 1-bit exclusion sits ABOVE both arms (a gaussian of pure black/white smears into forbidden grey at any extent) and is law-tested by FORCING a bare list style onto Wagtail, since its own `Pane` composition makes that ordering otherwise unreachable. **Originally scoped by SCISSOR ALONE, which item 312 replaced with a feathered mask** — the reasoning recorded here ("a rect uniform would produce the same hard edge anyway") is true of a rect uniform and false of a feathered one; the scissor survives as a conservative bound. ⚠️ **The scissor is PASS state, not draw state** — the card, the canvas-anchored wordmark and the whole chrome tail share that pass, so it is RESET to the target before returning; deleting the reset left all 1007 render tests green (the card's rows are inside the box already) and clipped the toast off the frame, which is now its own law. `FOOTPRINT_DIM` is ZERO where the full takeover recedes by `DIM`: a footprint is a hole in a page whose live colours are still the subject a hair outside its edge, so it is a pure defocus, and the frosted patch's mean colour in LINEAR light equals the live page's to within Δa\*/Δb\* 0.5.
- **ITEM 294's other half — THE FROST'S REACH WAS A DEVICE-PIXEL LENGTH.** The Gaussian's reach is a fixed count of quarter-res texels, so a fixed `DOWNSAMPLE` made it constant in PHYSICAL px and therefore HALVED the defocus a reader perceives on a 2x display — invisible to every capture, all of which run at `--capture-dpi 1`. `blur::extent::downsample_for(dpi)` multiplies the authored logical reach by DPI exactly once, and the recreate key is now `(size, downsample)` because a display move between a 1x and a 2x screen changes the working size without changing the surface's. `dpi == 1` returns the authored constant untouched, so every capture and every 1x frame is byte-identical; a 2x FULL-takeover frost genuinely changes (measured: 224 249 of 960 000 pixels on Gumtree's palette at `--capture-dpi 2`), in the direction of the frost the same overlay already shows at 1x.
- **Laws (item 294):** `render/tests/frost_footprint_item294.rs` — the residue oracle (the same picker over a DENSE and an EMPTY document; the card is identical in both, so the per-pixel luma difference is the DOCUMENT'S contribution alone), with the card's own ink masked out by a mask DERIVED from the empty-document frame (its backdrop is a blur of a blank page, smooth by construction, so a strong step there is the card). "No glyph survives as text" is then: zero residue pixels carrying a local luma step ≥ 24/255 inside the footprint, against 2 938–13 693 such pixels in a 24-logical-px collar just outside it — measured peaks 1.0–8.3 inside and 193.6–232.0 outside, a valley wide enough that the threshold is not load-bearing. Beside it a PRESENCE floor (the residue must still reach 12 luma; measured 23.9–218.6), because a smoothness floor alone gets happier as the document vanishes and is satisfied perfectly by an opaque card. Swept over the enrolled worlds × 1x/2x. Plus the hue law (mean colour in linear light, chroma-bounded over the FULLY FROSTED, card-ink-free pixels of the card's box at 1x/2x — a leaning card's off-rake corners are unfrosted, and admitting them pulls the frosted mean toward the live mean it is compared against), the scissor-reset law, the self-backing exclusion sweep, and `render/blur/tests.rs` for the pure arithmetic (outward rounding, the clamp, the off-canvas rejection, the DPI reach, roster enrolment). ⚠️ **The card-ink mask has one owner and one legal reading**, both in `render/tests/frost_card_ink.rs`: `CardInk` is a VETO with no indexing, iterator or count, because "a blur of a blank page" holds only where the frost REACHES — outside it the empty frame shows the world's live ground, so the flagged set is a SUPERSET of the card's drawing whose surplus is a property of the world (measured: it reaches the canvas's first row, 52 logical px above the card's top edge, on every enrolled world, and 6305–412760 px lie outside the card's box). Sound as an exclusion, false as "where the card is", and every consumer therefore owes a presence guard on what survived it. The contract is pinned by a law that measures both directions across the roster × 1x/2x × both menu-bar states.
- **ITEM 312 — THE FOOTPRINT'S EDGE WAS A HARD RECTANGLE, AND UNDER A LEANING LIST IT WANTED TO BE A FEATHERED PARALLELOGRAM (user-reported, with a screenshot).** 294's scissor answers yes or no per pixel, so the frost's boundary was a knife edge that sliced words mid-glyph — hard **by construction**, not by mistuning: `blend: None` on the composite target plus an alpha of `1.0` out of `fs_comp`, with no value anywhere in that path able to soften it. The extent now arrives in the composite's uniform as a SHAPE — box, shear and feather width — and rides in its **alpha**, with `BlendState::ALPHA_BLENDING` on the composite target only. `blur::extent` owns the arithmetic (`footprint_mask` / `footprint_dist_outside`, mirrored in `shaders/blur.wgsl` and marked MUST-match both ways, the same arrangement `lava`'s mask helpers use) and now also owns the uniform `U` itself, since two of its four vectors ARE the extent. **`Frost::Full` needs no second pipeline and is unchanged:** its mask is exactly `1.0` at every pixel, and at `srcA == 1` the blend equation is `src × 1 + dst × 0` — a replace. That is **measured, not argued**, as DESTINATION-INDEPENDENCE (`the_full_frosts_composite_is_destination_independent` draws the same composite over a red and a blue destination through a `LoadOp::Load` pass and requires the two bit-identical; at 0.99 alpha all 120 000 pixels differ). **The feather is 28.0 logical px, AUTHORED here rather than borrowed from `lava::MARGIN_GAP_PX`** (the same number, but that is a ramp across a page margin and shares no subject with a picker's edge); what is borrowed is the SHAPE, `lava_mask_2d`'s gutter carve. The floor under it is law-tested and not taste: a feather narrower than the blur's own ±4-tap × `DOWNSAMPLE` reach (16 logical px) reads hard beside the softness it bounds, which is also why `lava::FROST_FEATHER_PX` = 7.0 cannot serve. It is multiplied by DPI exactly once, so the edge is the same width in the units a reader sees at 1× and 2× — the class every `--capture-dpi 1` capture is blind to. **The ramp runs entirely OUTSIDE the faces**, the same direction `scissor_px` rounds, so the whole card box stays fully frosted and 294's interior laws needed no relaxation; only its confinement collar moved out past the skirt, and it reads `footprint_skirt_px` rather than a second authored inset. **THE LEAN IS READ, NEVER RE-AUTHORED:** `footprint_shear` is the MEASURED rail's `spine_step` over `overlay_lh` — exactly the slope of the two points `DiagonalClusterRail::spine` hands the spine quad — because the rake yields on a cramped card (`TRAVEL_MAX_BAND_FRACTION`) and a second reading of `ROW_STEP` would over-lean precisely there (measured: a card hugging one-glyph rows rakes 3.99 px/row against an authored 7.0). **Enrolment for the shear derives from what the frame DREW**, not a name: no rail, no lean, so `Rules` (Paperbark) takes the feather and keeps its rectangle. ⚠️ **THE SHAPE SHIPPED AS A UNION of the box and the sheared box, and item 318 RETIRED that union — see its own bullet below.** The union was argued as a coverage floor (the card's query line and foot hint were upright and flush to the box's left edge, so a shape following the rake alone left them over sharp document), and the argument was sound while the conclusion was not: the box is one of a union's terms, so the result always CONTAINS the full rectangle and the shear can only add two overhang ears. A parallelogram silhouette was impossible by construction at any shear on any world, which is what the user saw. **`overlay_card_rect` is UNCHANGED and so is the pointer hit region** — the shear and feather are added on the way to `blur::Frost`, so the frost's extent is the frost's alone; a click a hair outside the card still dismisses the picker even where the skirt reaches, because the skirt is a defocus and not a surface.
- **Laws (item 312):** `render/tests/frost_feather_item312.rs` — the edge measured as a RAMP in the PNG (`mean|open − closed|` profiled outward from the face the shape actually has, `rx + shear × (py − cy)` — BOTH faces translate together, and this read `min(0, …)`/`max(0, …)` for as long as the box-union shipped, which item 318 corrected along with the shape — over the card's own rows with card ink vetoed by 294's derived mask; a fixed column would smear the reading across the lean). Measured half-strength crossing 13–14 LOGICAL px out of an authored 28 px feather, holding at both 1× and 2×, swept over the three enrolled worlds; BOTH vertical faces are profiled and which carries the claim is derived, because a face whose skirt lands on a flat margin has no document to defocus and reads honestly zero (Magpie's left face). Beside it a SKIRT PRESENCE floor, because both a knife edge and a frost faded to nothing look identical to a ramp measured as a fraction of its own amplitude. Plus the lean asserted against the DRAWN spine's two endpoints with the rake-yield arm PROVED non-vacuous (the sweep must contain a card whose measured step differs from the authored constant, or the law reports its own vacuity), and the destination-independence law above. `render/blur/tests.rs` carries the pure arithmetic: the mask full inside and monotone outward, the shear leaning the shape (replaced by item 318's parallelogram law, which the retired claim's own mutation would have failed), the scissor bound enclosing every pixel the mask reaches (⚠️ this one failed on its first run and was RIGHT to — the bound must use `|shear| × (h/2 + feather)`, because the skirt above and below the box is sheared too, and a bound derived from `h/2` alone clips the skirt's far corners into a hard edge at the scissor's own boundary), the feather clearing the blur it edges, and the composite uniform carrying the whole extent."
- **`rules_ink` IS THE ONE OWNER of which rules a ruled list draws, and the WORKSPACE RAIL is one of them.** A rail is a list of labels arranged by the boundaries between them, which is exactly what this style says a list is; `prepare_rail_mark` therefore branches on `list_style` with no wildcard, and `Rules` hands the rail to the same function the rows beside it come out of (hairlines on `overlay_facet_ghost`, the selection on `overlay_lens_underline`, the two pipelines the chip skins otherwise use). Its spans mirror the pane's: a hairline runs the LABEL measure — the rail column less the `2 * hpad` its own measurement reserves — and the selection's heavy rule runs the full column, which is the very rect the filled band occupied. **Every other style's rail keeps that band**, and the two regions still differ only in presence (`UNFOCUSED_MARK_ALPHA`), never in decoration. Before this the rail took the world's selected-row band unconditionally, so the carrier world shipped with its content pane ruled and its rail plated — a half-applied world.
- **The LENS STRIP's answer is a rule, not a pill.** A tab pill is a plate, so `overlay_strip_tab_plates` stays empty on `Rules` (as it is on `Pane`) — and the strip is not thereby bare: `FacetStyle::Text` marks the active lens with a hairline under its label, which is the style's own vocabulary. ⚠️ The THEME picker is not where that question lives: its runtime lens strip was RETIRED (`facets.rs`, user decision 2026-07-15), so it carries no strip on any world. The strip surfaces are the file pickers, the Command palette, the History timeline and the Settings menu. **ITEM 289 (fixed):** `FacetStyle::Text`'s underline height and `ChipVariant::Underline`'s (Magpie's chip skin, not Wagtail — Wagtail is a `Text` world like the other thirteen) were raw device-pixel literals in `theme_picker.rs`, so both stayed put while every other term of the same rect doubled on a 2x panel (probed: `[430.0, 153.5, 23.9, 1.5]` at DPI 1, `[860.0, 306.6, 47.8, 1.5]` at DPI 2). Both are now `Logical` constants (`TEXT_MARK_THICKNESS`, `UNDERLINE_CHIP_THICKNESS`) resolved through `Metrics::px` like every other chrome length in the file — dpi-1 output is unchanged (still `1.5`/`3.5`), dpi-2 now doubles (`3.0`/`7.0`). Swept 1x/2x across the live roster (enrollment read from `render_caps.facet_style`, not a named list) in `render/tests/facet_mark_dpi_item289.rs`.
- **ITEM 307 — THE GUTTER'S VISIBILITY GATE IS LOGICAL, CHECKED RATHER THAN ASSUMED.** Item 242's residual lane found `--capture-dpi 2` reporting `gutter.visible: false` at the SAME `--measure` and the SAME 1200x800 *device* canvas where `--capture-dpi 1` drew it — the shape of the items above (289/292/294/242: a chrome length that was quietly a device-pixel literal). Here it isn't one: `gutter_layout`'s `avail_chars` is a ratio of `column_left()` and `label_char_w`, both of which carry `Metrics::scale` (`zoom * dpi`) alike, so the ratio is dpi-invariant by construction, and a fixed *device* canvas genuinely shows LESS logical page at a higher dpi (`--capture-dpi N`'s own contract: `WxH` at dpi N is a `(W/N)x(H/N)` logical window) — the narrower gutter margin at dpi 2 is real, not a bug. Proven by growing the physical canvas in lockstep with dpi (`(logical_w*dpi)x(logical_h*dpi)`, holding the LOGICAL page fixed) and sweeping `--measure` across the hide/show boundary: the boundary lands on the exact same measure at 1x/2x/3x, over two independent logical windows. **A narrower, separate finding, NOT fixed here:** when the margin OUTLINE wants its rail (`outline_wants_rail()` — headings present, outline on, a window tight enough that the rail competes with the symmetric margin), `adaptive_column_left`'s `desired_left`/`min_left` add `crate::render::TEXT_LEFT` (16.0) UNSCALED alongside dpi-scaled terms (`outline_pref_px`, `gap`) — so at matched logical geometry the outline's rail placement (and therefore `column_left()`, which the gutter also reads) measurably drifts across dpi (confirmed: a headed document at logical 1200x800 flips the gutter's own boundary by one `--measure` step between dpi 1 and dpi 2/3). `TEXT_LEFT` plays two roles at that one call site — the intentionally-physical whole-pixel floor the subpixel-shimmer fix (`adaptive_column_left`'s own doc) needs, AND an additive left-inset offset in the rail formula that should track logical geometry — and disentangling them is a `render/geometry.rs` change reaching the outline's rail (and everything that borrows the same `column_left()`: caret, selection, hit-test, the drag handle), not a `chrome/gutter.rs` or `chrome/outline.rs` one. Recorded here as the promotion candidate rather than made inside this item, matching `readout::CANVAS_INSET`'s own precedent. Law: `render/tests/chrome_overlay.rs`'s `gutter_visibility_boundary_is_dpi_invariant_at_matched_logical_geometry` (the logical-quantity claim, non-vacuous — proven red by name against a mutated `label_char_w` that drops the dpi factor).
- **ITEM 314 — THE WRITING COLUMN'S LEFT EDGE IS A LOGICAL QUANTITY (307's promotion candidate, landed).** `TEXT_LEFT` was an untyped `f32` doing duty as a *layout* offset while every scaled term around it carried the display factor, so at a MATCHED LOGICAL window the column's left edge moved with the DPI. Reproduced two ways before touching anything: analytically `desired_left_logical = 228.96 + 16/dpi` (`18 chars × 14.4 × LABEL 0.8 = 207.36`, `gap 21.6`), and empirically at logical 1200×800 — plateaus **244.96 / 236.96 / 234.29** at dpi 1/2/3, a collapsed page pinned at **16 / 8 / 5.33** logical px, and the gutter's own boundary at **75/76** at 1x versus **76/77** at 2x and 3x. **The census found ONE role, not two:** the physical duty 307 attributed to the constant belongs to `adaptive_column_left`'s closing `.floor()` (the subpixel-shimmer snap), which floors whatever the policy returns — `TEXT_LEFT`'s integrality is not load-bearing for it, and scaling the pad leaves the snap exactly as it was. Every reader wants the same logical length, so the fix is enrolment rather than a split: `TEXT_LEFT`, `NONPAGE_INSET` and `PAGE_MIN_MARGIN_PX` are now `Logical`, and the newtype (item 242's family) makes the old bug unrepresentable — the compiler enumerated all 45 call sites. The pads resolve against **`self.dpi` alone, never `Metrics::scale`**, because the page column lives in `page_column_advance`'s zoom-stripped space and its margins are zoom-invariant by design. `adaptive_column_left`'s `left_pad` PARAMETER is gone, replaced by `dpi`: a caller can no longer hand the policy an unscaled pad. `TextPipeline::edge_pad()` is the one pipeline-side resolution, shared by the column, the outline's rail inset and the outline's click band, so the rail is clickable exactly where it draws. **Two things this measured that the fix does not change:** `page_min_margin`'s upper bound is provably INERT in `column_width_for` (0 of 3996 swept cells change if it is removed — whenever it binds the clamp, the measure already fits and the margin never shows), and `column_left_for`'s `.max(pad)` is a second, independently sufficient collapse guard, so each collapse site alone is compensated and a single-site mutation stays green. **Consequence a human should look at:** on a Retina display the page's collapsed side pad and the rail inset now sit at 16 LOGICAL px where they sat at 8 — the intended value, doubled in physical terms, and the same class as `readout::CANVAS_INSET`'s recorded promotion. `TEXT_TOP` (16.0) is the untouched vertical twin: still an untyped `f32` read unscaled by `doc_top`/`visible_lines_z`/`scroll.rs`, and the same defect on the other axis. **Laws:** `render/geometry/tests.rs`'s `adaptive_column_left_is_dpi_invariant_at_matched_logical_geometry` (pure; 4 tiers including fractional 1.5 × 9 logical widths from the true 464 minimum × measures 10–120 × page on/off × rail on/off, asserting the PRE-SNAP position exactly and the snapped one within one physical px, **with a companion PRESENCE floor** because invariance alone is satisfied by deleting the pad — `0 × dpi` is perfectly invariant) and `render/tests/column_left_dpi_item314.rs` (four pipeline claims: column/text-origin invariance, drawn↔hit-test agreement, the rail's click band at one logical x asserted on BOTH sides, and the gutter boundary with a rail present — which 307's own law could not see because its fixture has no headings, so `outline_wants_rail()` is false and the policy is a passthrough). Non-vacuity: the full pre-fix state fails the gutter law with the reported numbers verbatim (`Some(75)` at dpi 1 versus `Some(76)` at dpi 2). The drawn↔hit law survives every pad mutation BY DESIGN — both sides compose one owner — and is red only against a parallel geometry; its doc says so.
- **ITEM 315 — `TEXT_TOP` IS `TEXT_LEFT`'S VERTICAL TWIN, AND THE DECLARATION LAW'S SCOPE IS WHY BOTH SURVIVED.** `TEXT_TOP` was the last untyped `f32` in item 242's chrome-pad family, read unscaled by `doc_top`, `max_scroll_rows`, `visible_lines_z`, `zoom_anchor_target_top` (`render/geometry.rs`) and `viewport_avail_px` / `char_screen_top_scroll` / `hit_test_scroll` (`render/scroll.rs`) — the SAME "unscaled pad mixed with scaled terms" shape 314 fixed one axis over, unconditional on the document's default path (`doc_top`'s `None` arm), so — unlike `TEXT_LEFT`'s rail branch — no heading-bearing fixture was needed to reach it. Fixed the same way: `TEXT_TOP` enrols in the `Logical` family, and the census that followed (45 readers for `TEXT_LEFT`; a comparable spread here across `render.rs`, `render/geometry.rs`, `render/scroll.rs`, `render/text.rs`, `render/chrome/{hud,outline,readout}.rs`, `app/lifecycle.rs`, `app/input/keys.rs`, `capture/sidecar.rs`) is genuinely LOGICAL everywhere — no second physical role. **The repeated idiom `TEXT_TOP + menubar_reserve()` had SIX independent spellings** (`doc_top`, the zoom anchor, both `scroll.rs` fns, the margin Outline, the capture sidecar) before this item — the reason the un-scaled constant survived unnoticed at half of them even after `menubar_reserve` existed. Consolidated into one owner, `TextPipeline::text_origin_top()` (`self.metrics.px(TEXT_TOP) + self.menubar_reserve()`), and every one of the six now calls it instead of re-deriving the sum; `zoom_anchor_target_top`'s `menubar: f32` parameter was renamed to `text_origin_top: f32` and its own `TEXT_TOP` reference deleted, so the pure fn cannot independently forget the scale multiply. `corner_origin`'s `CornerAnchor::TopCenter` arm — the one place a chrome-level fn (not a `TextPipeline` method, so no `Metrics` to multiply by) needed the composed value — now takes it pre-composed from its ONE caller (`prepare_notice`) rather than adding `TEXT_TOP` internally; every other anchor is unaffected.

  ⚠️ **THE STRUCTURAL FINDING IS BIGGER THAN THE CONSTANT: item 242's declaration law (`chrome_pixel_space_item242.rs`'s `every_authored_chrome_constant_declares_its_unit_family`) was scoped to `src/render/chrome/` alone**, which is the literal reason both `TEXT_LEFT` and `TEXT_TOP` lived untyped in `render/geometry.rs` / `render.rs` for their whole lives — outside the one sweep built to catch exactly this. **Widened to also walk `render/geometry.rs`, `render/geometry/**` and `render/scroll.rs`** — the writing column's own pixel-space files, the direct neighbourhood both constants actually lived in. The wider sweep immediately found three MORE unscaled-pad bugs of the identical shape, all fixed the same way: `PAGE_RESIZE_GRAB_PX` (the page-edge drag-hover tolerance, `render/geometry/page.rs`), `IMAGE_RESIZE_GRAB_PX` (the inline-image resize-handle hit tolerance, read from `render/text.rs`) and `MIN_IMAGE_W` (the image-resize floor) — each now `Logical`, each resolved through `Metrics::px` at its one call site. `DEGENERATE_CELL_FRAC` (a genuine fraction) and three test-only helper constants in `render/geometry/tests.rs` (`CW`, `ADAPTIVE_DPI`, `ADAPTIVE_LEFT_PAD` — all pre-existing from item 314, at a permanently-fixed `dpi = 1.0`) joined the DIMENSIONLESS table with their reasons. Seventeen pre-existing `PAGE_MIN_PAD.0` / `RIGHT_MARGIN_BREATH.0` / (new) `PAGE_RESIZE_GRAB_PX.0` / `IMAGE_RESIZE_GRAB_PX.0` / `MIN_IMAGE_W.0` reads in `render/geometry/tests.rs` — all at a fixed dpi of 1.0, so numerically inert — were changed to `.px(1.0)` to satisfy the SIBLING law (`no_migrated_chrome_length_reaches_a_draw_call_through_a_bare_field_access`), which the widened scope also newly swept over that file for the first time.

  **ITEM 322 — `src/render.rs` IS NOW SWEPT, AND THE CENSUS FOUND 37 UNTYPED CONSTANTS WHERE ~30 WERE EXPECTED. Item 315 declined to guess and was right to: the file holds FOUR families, and one of them turned out to be a live defect at four times the count of the constants this item was written to classify.** Each family now has a mechanism rather than a judgement, which is the whole point — a constant mis-declared `Logical` gets multiplied by DPI forever with a law asserting it is right.
  - **Eleven base text/caret metrics are resolved by `Metrics::with_dpi` itself** (`FONT_SIZE`, `LINE_HEIGHT`, `CHAR_WIDTH`, `CARET_W/H/BLOCK_H`, the four `CARET_STREAK_*`, `CARET_TRAIL_TEXT_CENTER_DROP`): the owner multiplies each by `s = zoom * dpi` and stores the result on `Metrics`, which every consumer reads. Declaring one `Logical` and passing it through `Metrics::px` as well would apply DPI TWICE — invisible at the `--capture-dpi 1` every capture defaults to. **The law derives this set by parsing `with_dpi`'s own body** (`metrics_resolved_constants`), so the exclusion expires the day the owner stops multiplying one; mutating `font_size: FONT_SIZE * s` to `font_size: FONT_SIZE` makes `FONT_SIZE` an offender by name.
  - **`PAGE_TEXT_PAD_CHARS` is a `Chars`** — resolved against `metrics.char_width`, already scaled, so it must never also pass `Metrics::px`. The family existed; the constant had simply never joined it.
  - **`CARET_DESCENDER_PAD` and `CARET_INK_PAD` are `Logical`.** Their caret call sites recover the full factor from the metrics themselves — `px = m.caret_h / CARET_H` is exactly `Metrics::scale`, because `with_dpi` built `caret_h` as `CARET_H * s` — and now hand it to `Logical::px`. Same multiply, so every scale is byte-identical.
  - **A new `Millis` newtype carries the three animation durations** (`COPY_PULSE_MS`, `OVERLAY_ENTRANCE_MS`, `OVERLAY_BAND_SLIDE_MS`). A millisecond is not a length and the TYPE says so: `Millis` has no `Metrics::px`, so the compiler — not a table a reader maintains — is what keeps a duration out of a pixel multiply (`self.metrics.px(OVERLAY_ENTRANCE_MS)` fails with *expected `Logical`, found `Millis`*). Its one arithmetic is `progress_per(dt)`. **This is the by-KIND exclusion the ratio family still lacks**: `PAGE_MIN_MARGIN_FRAC`, `COPY_PULSE_LIFT_L`, `COPY_PULSE_LIFT_ALPHA` and `CARET_MORPH_SETTLE_SHOW` are recorded in the law's DIMENSIONLESS table with their reasons, which is a name list guarded only by an exhaustiveness assertion; `Ratio`/`Alpha` newtypes would put them on the same footing as `Millis`.
  ‼ **THE LEFTOVER IS A MEASURED DEFECT, NOT A CLASSIFICATION: FIFTEEN WRITING-COLUMN DECORATION CONSTANTS ARE MULTIPLIED BY `metrics.zoom` ALONE, AND ONE BY NOTHING AT ALL.** `metrics.zoom` is the clamped user zoom; `metrics.scale` is `zoom * dpi`. So a spell squiggle's amplitude, a table's cell pad and column gap, an inline-code pill's overhang, a fence panel's inset, the preedit underline, the two table rule thicknesses, the I-beam's width, the space-bar caret's width and the morph dilation all hold their DEVICE size as the display gets denser — halving against the text beside them on every retina panel, which is exactly the defect item 242 measured in chrome, one neighbourhood over. **Measured, not inferred**, at identical logical geometry (1200x800): the inline-code pill is `92.70`px wide at dpi 1 and `179.39`px at dpi 2, where twice the first is `185.39` — short by exactly `2 x CODE_PILL_INSET_X`; the fence panel is short by exactly `2 x FENCE_PANEL_INSET_X`. They are NOT declared `Logical` here on purpose: resolving them through `Metrics::px` changes what every retina display draws for fifteen separately tuned taste quantities, several of them marked TASTE TUNABLE in their own doc comments, and that is a product decision with a human in it rather than a unit annotation. Declaring them `Physical` would be worse — it would assert the device grid is the right reference for a squiggle's amplitude, which nobody believes. So they sit in `DPI_BLIND_PENDING`, a CLOSED ledger cross-checked against the leftover set from both sides: the law computes every bare `f32` the owner does not resolve and DIMENSIONLESS does not excuse, and requires it to EQUAL the ledger — so a new untyped constant cannot join by being DPI-blind, and a repaired one cannot stay by being forgotten. Each row's recorded read site is graded too, and no product source may resolve a ledger entry through `metrics.scale` or `Metrics::px`.
  **The missing law this audit wrote:** `no_length_is_resolved_against_zoom_alone`. The newtype stops a length reaching a draw call UNMULTIPLIED; it does not stop a caller handing `Logical::px` the wrong factor, since `px` takes any `f32` (the pure policies are called with a bare `dpi`). So the factor is now a law — `Metrics::scale`, or the `dpi` a policy was handed, never `zoom` alone — swept over every product source, because the caret's own pads live outside the declaration files. 46 scale-taking resolutions graded, with a floor asserting the scan found them; `CARET_DESCENDER_PAD.px(m.zoom)` fails it by file and line.
  ⚠️ **Left to a follow-up by the file partition, not by judgement:** four of the DPI-blind fifteen (`CODE_PILL_INSET_X`, `IBEAM_W`, `CARET_SPACE_BAR_W`, `CARET_MORPH_DILATE_PX`) are read from `render/chrome/preview.rs` and `render/chrome/popover.rs`, so typing them requires editing `chrome/**`. And `caret_body.rs`'s own `CARET_VISUAL_BODY_MIN_W/H/AREA` are untyped `f32` outside the swept set — the same scope residual that hid `TEXT_LEFT` and `TEXT_TOP`, one directory further out.

  **Verify:** `render/geometry/tests.rs`'s `visible_lines_z_is_dpi_invariant_at_matched_logical_geometry_with_a_presence_floor` (pure; the row-count arithmetic swept over ~1800 logical heights × 4 tiers including fractional 1.5, asserting invariance per height, a per-tier non-vacuity check that the swept range actually crosses a row-count boundary, and a PRESENCE floor at an exact multiple of the line height — `TEXT_TOP.px(dpi) / (LINE_HEIGHT * dpi)` is the dpi-independent ratio `0.5`, so the row count must drop by exactly one at every tier or the pad is not load-bearing) and `render/tests/text_top_dpi_item315.rs` (four live-pipeline claims: `text_origin_top` invariant at matched logical geometry with the menu bar OFF and a presence floor against the hard-coded `16.0` — proved non-vacuous by mutating `TEXT_TOP` to `Logical(0.0)`, which left invariance green but the presence floor and the byte-identity claim red, exactly as item 314's own doc predicts for this class of law; composition-correctness with the bar ON, checked against `menubar_reserve()`'s own live value rather than a hand-rolled formula so it cannot drift out of step with that constant family's separate, unaudited evolution; a drawn↔hit-test agreement law asserted on BOTH sides of a real row boundary, proved red by reverting `hit_test_scroll` to the un-scaled formula — caught at dpi 1.5; and byte-identity of the default 1x/bar-off origin against the historical `16.0` literal). All four ran green under a temporary local forcing of `MENU_BAR_ON`'s macOS branch to the non-macOS default (`src/menubar.rs`, reverted before commit — CLAUDE.md's "a fix that passes unforced has not been tested").

  ⚠️ **A SIBLING, UNFIXED BUG FOUND BY THE SAME CENSUS: `menubar::bar_height` mixes a SCALED `line_height` argument with the UNSCALED `BAR_PAD_Y` (5.0) constant** (`bar_height(line_height) = line_height + 2.0 * BAR_PAD_Y`), so the drawn/reserved menu-bar height itself does not scale correctly with DPI — the same defect shape, one file over, in `src/menubar.rs` (outside this item's file partition and outside `TEXT_TOP`'s own scope). Not fixed here; flagged for a human / follow-up item. `text_origin_top`'s own composition (`scaled TEXT_TOP + menubar_reserve()`) is correct regardless of this — its own live-pipeline law reads `menubar_reserve()`'s actual value rather than assuming it is DPI-correct, precisely so this sibling bug cannot make `text_origin_top`'s OWN law flaky.
- Laws: `render/tests/rules_composition_item283.rs` — the full no-wildcard `OverlayKind` sweep (both treatments, both DPIs, four canvases), drawn↔hit-test↔sidecar agreement, the two-frame interior-fill oracle (a `Pane` world's band is the positive control), the mark's findability and its distance from the accent, the clip bound, both rail arms (quads uploaded AND real pixels), every `SettingId × SettingKind`, and the pure owner's boundary arithmetic. Plus the `Rules` arm of `render/tests/list_surfaces.rs`'s bare-plate roster sweep.
- **`PaneSplit` (Split default | Unified) — the SPLIT-PANE composition (item 50).** A Pane world's takeover card draws as TWO surfaces: the title/query INPUT on the upper surface, a visible strip of the world's own background between, then ONE lower surface owning facets/section-headers + candidate rows + footer. DATA-gated through the ONE owner `render::effective_pane_split` (probe `AWL_PANE_SPLIT_FORCE=unified|split`); the fill rides `overlay_pane_fills` → `overlay_split_bounds` (the gap is carved from the query beat's own negative space, so NO text moves and no glyph falls in the gap — a pure fill change). Cassowary is the ONE `Unified` exception (dormant under its Bars list). Inert on a Bars world (bare plates) and the spell popup. Two bordered surfaces on a 1-bit world (Wagtail) read by their rims. Laws: `render/tests/split_pane.rs`.
- **Held back, not dead:** Chips is rebuilt-for-real but ships inert pending the user's variant pick; poster facets stay Text. Probe forces for galleries: `AWL_FACET_STYLE_FORCE`, `AWL_OVERLAY_ANCHOR_FORCE` (env, CLI-invisible).
- **`PlacardCorner::Auto` derives complementary to the card anchor via one owner `render::derived_placard_corner`;** `overlay_shape_placard` shrinks-to-fit so placards never clip — the old "every placard BL" pin is retired for an end-to-end no-clip outcome law.
- **One-owner geometry seams (route through these, never re-derive):** `overlay_card_x`, `overlay_row_top`/`_of`/`_index` (+ `header_gap`), `overlay_split_bounds`/`overlay_pane_fills` (the split surfaces), `push_overlay_hint_spans`, `overlay_footer_reclaim`.
- **THE FOOT HINT'S OWN SEPARATOR ROW (item 293).** `OverlayGeom::hint_rows` was always `0` or `1` — the hint's own (shrunk, `overlay_hint_h`) row — and no owner anywhere budgeted a second row for a gap above it, so every geometry family sat the hint flush against the last candidate row (or the empty-state notice, sharing its band) while the KEYBINDINGS footer band already got a blank separator ahead of ITS content (`footer.len() + 1`, `overlay_footer_lines`). Not-drawn AND not-computed together: the shaper (`push_overlay_hint_spans`) had one `"\n"` to work with, and no row budget existed to hold a second one even if it had used it. `overlay_hint_gap_rows` (`chrome/mod.rs`) is now the ONE row-count owner every geometry family (`overlay.rs`'s flat `overlay_geometry`, `theme_picker.rs`'s grouped `theme_overlay_geometry`, `workspace.rs`'s `workspace_geometry`) reserves the gap through, and `push_overlay_hint_spans` draws it with a second, glyph-carrying line (a space, not a bare newline — a glyph-free span needs a real glyph to carry custom metrics, `push_beat_spacer`'s own trick) at its own compact height, `overlay_hint_gap_h` (`OVERLAY_HINT_GAP_ROW`, deliberately smaller than the hint's own `OVERLAY_HINT_ROW` — the separator's job is to stop the hint reading as flush, not to read as a second hint-weight line, `overlay_rhythm_item112.rs`'s compact-chin law). `overlay_footer_reclaim` takes BOTH a hint-row count and a gap-row count, reclaiming each at its own compact height with one trailing `OVERLAY_FOOTER_PAD` kept back regardless of how many compact rows there are (never one pad per row, or the gap's own reclaim would eat into the chin below the hint). The Bars/workspace footer PLATE (`overlay_selection.rs`'s `footer_band`) reads the same two counts so the plate's own bottom edge tracks the hint's real (now shifted) position instead of ending a row short of it. **Starvation degrade:** in the item-184 corner (an extreme zoom / tiny canvas where even the card's fixed chrome, at zero visible items, would overrun the canvas), both `overlay.rs` and `theme_picker.rs` drop the gap row rather than push the card past its canvas — the same shape item 184's own `min_items: 0` floor already takes for a sectioned header's overhead. The workspace family carries no such degrade because its card is CANVAS-sized (`regions.card`), never content-derived, so it cannot overrun by growing content. Laws: `render/tests/hint_gap_item293.rs` (the drawn gap read off the shaped `panel_buffer` by finding the line whose text is the hint string verbatim — never re-derived from row counts — over the whole `OverlayKind` roster, both list styles, both DPIs, full/filtered/scrolled/empty shapes, plus a family-agnostic row-count proof reading `overlay_hint_gap_rows`' effect directly, which is the one law reaching the workspace family's own gap since its fixed card hides the drawn-gap law from that family alone).
- **ITEM 318 — THE FROST UNDER A LEANING LIST NOW READS AS A PARALLELOGRAM (user-reported AND user-decided).** *"the blur was not achieved… you can see how it's kinda like a square right? that's wrong… it should be like a parallelogram."* The cause was structural rather than mistuned and needed no reproduction: `footprint_dist_outside` was `upright.min(leaning)`, a UNION whose upright term was the card's whole box, so the result always contained the full rectangle and the shear could only add two overhang ears. **The lean item 312 built was real and correct; it was invisible because the floor drawn beside it was larger.** The mask now asks the sheared box ALONE, in `extent.rs` and `shaders/blur.wgsl` alike. ✅ **THE COVERAGE DUTY DID NOT GO AWAY — IT STOPPED BEING A SECOND SHAPE.** `blur::extent::footprint_box` widens the footprint's RECT until the parallelogram contains the card's own upright chrome, and widening a parallelogram leaves a parallelogram, so the silhouette pays nothing. The arithmetic is exact rather than padded (the shape's faces sit at `x + shear × (py − cy)` with `cy` a function of `y` and `h` alone, so widening in x cannot move the pivot and the two bounds decouple) and it only ever GROWS — chrome already inside grows it by nothing, which is every upright world's byte-identity. **The upright chrome is the HEAD band and only the head band, measured:** the rows and their accessory column are seated per row by the diagonal rail and item 313's foot band hangs on the same rail's extrapolated line, so all three rake; the head band is the one `TextArea` still at the card's text edge. `TextPipeline::overlay_head_band_ink` (`chrome/diagonal/offband.rs`, formerly `foot.rs`) is its one owner and takes the widest ink of EVERY planned header line, so a third one enrols by existing. **Measured at 1200×900, both DPIs, both menu-bar states:** Mangrove and Paperbark grow by **0.00 logical px** and Magpie's ASCENDING rake grows by **43.15 logical px** on its left face, the deficit exactly. ⚠️ **"grows by 0.00" was read as "the frost rect IS the card's", and item 343 shows that was a coincidence of this step rather than a property of the shape** — the widening is a floor, and the box it floors is now the drawn surfaces' rather than the card's. ⚠️ **THE QUERY FIELD DID NOT MOVE, and that was a deliberate refusal.** 312 left the principled route as "give the query line the same independent `left` 313 gave the foot"; built, it seats the field through the same `ColumnFlow` mirror the rows use, which on the ascending world moves it **+461 logical px** and right-aligns it — a text input whose sigil travels leftward as the user types. That is a product/taste change nobody asked for, so it was backed out and the coverage moved into the shape's width instead. **The remaining question is the user's:** does a mirrored composition's query FIELD mirror like its rows? It is 313's own open terminus question one band over, and nothing is waiting on it — the shape is a parallelogram on all three enrolled worlds either way. **`overlay_card_rect` and the pointer hit region are again UNCHANGED:** the widening happens on the way to `blur::Frost`, so the frost's extent stays the frost's alone.
- **Laws (item 318):** `render/blur/tests.rs` — the silhouette measured as a PARALLELOGRAM over the pure mirror, by the two figures that separate one from a rectangle: every row's frosted span read off the mask has BOTH edges translated by the same `shear × (py − cy)` (the union moved one face at a time, so its span WIDENED away from the centre row — the figure it fails), and the frosted area is strictly smaller than its own bounding box by the two triangular corners, with a PRESENCE floor requiring that area to still be the box's own `w × h` to within 2% so a shape that held its slope while fading cannot pass. Plus `footprint_box` growing enough, growing no more than a px more than it must, and returning the rect BIT-IDENTICALLY when the chrome is already inside. `render/tests/frost_parallelogram_item318.rs` carries the two pixel laws: at most TWO of the CARD's OWN corners FULLY frosted at a real shear, with real SHARP document showing through the rest (⚠️ it asserted *exactly two short of full* until item 343, which held only while the box was bounded below by the card's — a narrowed box leaves a corner short because the frost is narrower there as well as because the rake left it, and a mirrored card measures three. What separates the two shapes at any width is that a union CONTAINS the card's box, so all four of its corners are fully frosted) (1686–14202 wholly unfrosted non-ink px inside the card's box, 195–462 carrying a document edge, against exactly ZERO under the union), and the coverage floor over the head band's declared box — tightest frost coverage **1.0000** across the roster against a 0.9 floor, and zero document edges behind it. ⚠️ **The corner law's FIRST version was green under its own mutation** and is worth recording: it asked about the SHAPE's bounding box, whose two off-rake corners the union's ears reach identically. Only the CARD's box distinguishes them, because a union contains it. ⚠️ **And two derived ink oracles were falsified on their first runs** — 294's `card_ink_mask` is a VETO and does not invert into an inclusion set (its "blur of a blank page" premise holds only where the frost reaches, so it reported card ink 52 rows above the card), and intersecting it with a picker-open-vs-closed difference selects ground structure lying under the card's soft SHADOW (4004 such px on Mangrove, 19584 on Magpie, 8 on Paperbark, which draws no backing). The subject is therefore the box the production owner declares. Swept over the enrolled roster × 1×/2× × both menu-bar states (the bar's 35.6 px reserve moves the head band's own y from 64.0 to 99.6, so the arm is not decorative), with the bar's arm taken from the AMBIENT value and its negation rather than from `cfg!`.
- **ITEM 343 — THE FROST WAS ROUGHLY TWICE AS WIDE AS ANYTHING DRAWN INSIDE IT (user-reported, with a screenshot: *"there's a bit too much blur on the left and right sides"*).** The silhouette was right and the WIDTH was not, and the cause was the card's LAYOUT BOX rather than the feather. `overlay_desired_w(CARD_MAX_W…)` is a fixed desired width clamped to the window with no relation to how wide the shaped rows turned out; on a composition that draws no panel and no plate, nothing occupies the difference, and that width is invisible right up until a frost is scoped to it. Measured at the production seam: a cross-section of `card_w + 2 × feather` = **576 logical px** at any single row over a candidate row carrying **61–110** of ink. ⚠️ **THE FEATHER IS THE WRONG LEVER AND IS FLOORED BY LAW** — spending the whole margin above the Gaussian's own 16 px reach buys back 24 px and reintroduces the knife edge the feather exists to remove. **The frost's box is now the DRAWN SURFACES' union** (`TextPipeline::overlay_drawn_surfaces`, `chrome/overlay_ink.rs`), narrowed in the shape's own un-sheared frame by `blur::narrow::footprint_narrow` and then floored by `footprint_box` exactly as before. It only ever SHRINKS and never past the card, so every claim about the page outside the old footprint holds unchanged; **`overlay_card_rect` and the pointer hit region are again untouched.** ⚠️ **THE BOX COMES FROM THE DRAWN SURFACES, NEVER FROM THE GLYPHS AND NEVER FROM THE LAYOUT BOX**, and that is settled by measurement rather than taste: `overlay_rules` emits every rule at `(band_x, band_w)` and `band_w() == card_w` for every non-workspace picker, so a `Rules` world genuinely draws at the card's full width and a frost narrowed to the glyphs would strand every selected rule over sharp document (measured: dropping the rules term leaves **9700 of Paperbark's 23319** drawn pixels unfrosted). `overlay_line_glyph_box` is blind to that for the same reason it is blind to the selected row's chevron MARK — both are rects, and it reads shaped runs. **The panel buffer's per-band seats moved into one owner the EMITTER itself loops over** (`overlay_panel_bands`), so the seat glyphon is handed and the seat a treatment measures cannot part company; the query caret's box and a ruled list's `RuleSpans` got the same treatment. **Measured, identical in logical px at 1× and 2× and over both menu-bar arms:** Mangrove **520 → 420.4** logical (its left face 49.3 px in, its right 50.3), Magpie **563.2 → 504.3** (bar off) / **518.3** (bar on), Paperbark **520 → 520 and byte-identical** — the shear-0 `Rules` member is the arm that keeps the sweep honest. ⚠️ **The briefed asymmetry did NOT reproduce.** The item predicted 40 logical px of slack on the left against 177 on the right, the larger figure being "dead card width past even the widest LINE"; it is not dead — the selected row's chevron mark stands at the card's outer edge (measured 1036.5–1049.5 physical against a card ending at 1060.0), so the true slack was **49.3 / 50.3, near symmetric**, and the 177 was an artefact of measuring lines on a card whose widest surface is a quad.
- **Laws (item 343):** `render/tests/frost_width_item343.rs` bounds the width from BOTH ends, because neither direction is sufficient alone. **TIGHTNESS** — the box contains the drawn surfaces' union in the un-sheared frame and exceeds it by no more than a stated **2 logical px** (measured excess **0.00** at every one of the twelve cells; the allowance is bounded above by shaper float noise and below by the composition's smallest authored horizontal length, the 7 px marker gap, so a regression that reintroduced any structural slack cannot hide in it). A coverage floor alone is satisfied perfectly by the 520 px box that prompted the item. **COVERAGE** — every pixel the card draws has the shipping mask at or above **0.9** beneath it (roster tightest **0.9963**), which is also the COMPLETENESS proof for the enumeration: a surface nobody remembered is card ink with no frost under it and fails by existing, so every exclusion is earned by measurement rather than by a maintained list. ⚠️ **The card-ink oracle is POSITIVE and is not an inverted `CardInk`** — that veto's "blur of a blank page" premise holds only where the frost reaches, and intersecting it with an open-versus-closed difference selects `blur(ground) − ground`, which is exactly the region a completeness claim must read. So the confound is REMOVED instead: `blur::suppress` (test-only) turns the frost off, and two frames of the same document at the same size on the same world — one with the picker up, one without — share their ground and their document exactly, so the residue between them IS the card's drawing (9.6k–73k px per cell). The window-anchored placard is excluded by the box its own production owner declares. Plus `RuleSpans::x_reach` law-tested to contain every rect `rules_ink` can emit over row counts × selections × both marks × three gutter regimes, and `blur::narrow`'s pure containment/never-grows/actually-narrows trio. Swept over the enrolled roster × 1×/2× × both menu-bar arms. **The final width is TASTE and owed to the user's eye: `gallery/item-343/{before,after}-Mangrove.png`.**
- **ITEM 313 — THE FOOT BAND HANGS ON THE SPINE, NOT ON THE CARD'S LEFT EDGE (user-reported, the same screenshot as item 312).** On the two `Diagonal` worlds the hint held the card's text edge while every row above it raked with the spine. Every line of a card is ONE rich-text run in ONE `panel_buffer`, so a line has no x of its own — but the mechanism that already gives the leaning ROWS independent x's is **neither a second run nor a second buffer**: `overlay_upload_text` emits one `TextArea` per band over that single buffer, each vertically clipped and each seated at its own `left`. The foot band was ALREADY one of those areas (the tail, `clip(band_bottom, canvas)`) and had simply never been asked for a left of its own, so the emitter grows no line: `text_left` became `overlay_foot_left(geom, plan)`. `render/chrome/diagonal/offband.rs` (named `foot.rs` when this landed) is that x's one owner, and the whole band — hint, its separator, and the Keybindings tips beneath it — moves as one block, because it is chrome rather than a list. **Every quantity in it is READ:** the lean from `DiagonalClusterRail`, whose `spine_step` already carries the yield a cramped card takes under `TRAVEL_MAX_BAND_FRACTION` (measured 5.99 logical px/row against an authored 7.0 in the law's cramped cell — a second reading of `ROW_STEP` is green on every roomy card and red only there); the band's y and ink width off the shaped buffer's own runs, with the hint's LINE found by its TEXT rather than by row-count arithmetic (the separator ahead of it is drawn unconditionally while the row it is budgeted as can be dropped in the starvation degrade, so an index would drift); the mirror through `ColumnFlow`, so an ascending world's hint ENDS on the spine as its names do instead of crossing it. ⚠️ **This bullet's sibling figures for item 312's floor were re-measured by item 318 and two were wrong:** the foot clears the leaning term's face by **69.62 logical px** on Mangrove and **84.34** on Magpie (not 66/83), and the query line hangs **43.15** logical px outside it on the ascending world (not 71) while sitting 60.15 px inside it on the descending one (not 12 — that figure was the card's own TOP edge, whose clearance is vertical and says nothing about the rake). The instrument is the leaning term's HORIZONTAL outside distance; a combined figure is dominated by whichever band sits nearest the card's top or bottom edge. `DiagonalClusterRail::spine_at` is now the one owner of the spine's abscissa at a FRACTIONAL step, because the foot's row is not a whole row below the last one (its own row and the separator above it are both compact) — `spine_x` is that owner at a whole step, bit-identically. **A COLUMN CLAMP bounds it:** the emitter clips the band to the text column, so the lean is given up as far as it must be and a band wider than the column keeps the card's edge (the historical placement, bit for bit). On Mangrove's real theme picker the clamp DOES bind — the hint's ink is 359 px in a 496 px column, so it seats 40 px short of the spine's line with its right edge flush to the column — which is also why the terminus question below has no visible stake there. **Inert off the enrolled roster by construction** (no cluster, no placement, `geom.text_left` bit for bit) and inert on a card whose row band is EMPTY, where the empty-state notice shares this band and a lean read off zero rows would indent it with no spine drawn beside it. Measured: 18 upright worlds' PNGs byte-identical at 1x and 2x, and all 20 worlds' sidecars byte-identical (this changes placement, not state). 🔵 **OPEN, THE USER'S CALL:** at the foot's own row the spine has ENDED, so the band either CONTINUES the lean onto the line the spine would have drawn or SITS at the terminal x the last row hangs on. `FOOT_CONTINUES_THE_LEAN` ships `true` and the switch is one word; its laws READ it rather than restating a branch. The two answers are 9 px apart on Magpie and PIXEL-IDENTICAL on Mangrove (the clamp binds there first) — `gallery/item-313/continue-*` vs `terminus-*`. **Laws:** `render/tests/foot_hint_lean_item313.rs` — the foot's displacement from row 0's anchor equals the DRAWN spine's over the same vertical distance (roomy AND cramped, both DPIs, both menu-bar states, every hinted `OverlayKind`, with the sweep required to CONTAIN a cell where the drawn step differs from the constant); the mirror asserted from `ColumnFlow` with both spine directions required present in the enrolled roster; the ink never leaving the text column, with the clamp required to bind somewhere in the sweep; every upright world's seat bit-identical to `text_left`; and a PNG law counting real glyph structure inside the band the owner claims AND requiring the strip the hint vacated to carry none — the drawn-to-owner agreement this non-interactive band leaves no hit-test to provide. A presence floor on the hint's shaped ink runs beside every one of them, because "the hint is offset from the card edge" gets happier as the hint disappears.
- **THE INTERIOR-RAIL PLACEMENT LAW (item 67) — one global resolver, `render::chrome::overlay::overlay_rail_inset(ww)`.** Supersedes the old flush ~28px `TopLeft`/`TopRight` edge-hug: the two side rails are now SYMMETRIC INTERIOR rails whose card CENTERS sit near the viewport's one-third / two-thirds marks when the card holds a comfortable width — generous outer breathing room, still unmistakably left/right. `TopCenter` is untouched (`free * 0.5`, exactly the viewport midpoint, always). The three theme-owned anchor CHOICES and every world's own assignment are unchanged; only the shared placement arithmetic moved. The inset is a PURE function of the window width alone — never the anchor, never the caller's actual `desired_w` — so `overlay_card_box_policy`'s `TopLeft` and `TopRight` arms read the exact same number and the left/right MIRROR law holds by construction, independent of content width (item 51's right-anchor content-hug still shares ONE right edge across a plain and a secondary-bearing card). The SAME `full -> anchored_max -> floor -> free` clamp chain the old fixed constant fed keeps the narrow-window collapse toward `CARD_EDGE_INSET_FLOOR` (then the re-centering fill regime) perfectly CONTINUOUS — no breakpoint jump, no per-anchor branch, no fourth user-facing anchor. `CardAnchor::Inset { x_frac }` keeps its exact `0.0 == TopLeft`, `0.5 == TopCenter`, `1.0 == TopRight` contract untouched (the same clamp expressions, just reading the new inset).
- **The card ANCHOR is frozen at summon but RE-STAMPED on a deliberate theme crossing (item 45 → item 52).** The render path reads the anchor ONLY through the frozen `resolve_overlay_anchor(self.overlay_align)` — never the live world (`render::tests::overlay_align_law`'s alignment-is-data grep-law bans `effective_card_anchor(`/`render_caps.card_anchor` anywhere under `src/render/`). Item 52 makes the theme picker CROSS its anchor like every other theme-owned property: `OverlayState::reanchor` (called by `actions::preview_move` on a keyboard nav / wheel move, and the wheel path in `app/input/mouse.rs`) re-stamps `align` to the highlighted world's anchor AFTER the preview makes it active, so the card SNAPS into the destination rail. Passive `overlay_hover` keeps the BARE `preview_overlay` (no reanchor — no spatial chase). The re-stamp lives in the overlay/actions layer, NOT a render consumer, so the grep-law still holds. Laws: `render::tests::reanchor_crossing_law`.
- **Right-anchored cards shrink to content (item 51):** a `TopRight` (`mirrors_growth`) takeover card no longer seats a wide `CARD_MAX_W` card against the right edge (the 300–400px dead middle between the left-aligned labels and the remote edge). `overlay_desired_w` (the ONE desired-width owner both geometry paths read) returns the MEASURED content width — `measure_overlay_content_w`, cached in `overlay_content_w` at sync via the SAME real shaper the draw runs (widest visible primary + optional secondary column + query/strip/footer + `2·hpad`), clamped to `[CARD_CONTENT_MIN_W, wide cap]` — so the whole group hugs the right window edge as one block, text still LEFT-aligned inside, the secondary right-aligned to the card's own text edge (a content-bounded scanning column, never the remote edge). Left/center keep `overlay_content_w == 0.0` → the fixed wide cap, byte-identical. Laws: `render::tests::overlay_right_hug_law`.
- The theme picker retired its runtime lens strip (user decision 2026-07-15, recorded in src/facets.rs); the axes are a build-time ruler.

## The summoned workspace (`overlay/workspace.rs` + `render/chrome/workspace.rs`) — item 114

- **THE THIRD OVERLAY GEOMETRY FAMILY**, beside the flat pickers (`overlay_geometry`) and the grouped/faceted card (`theme_overlay_geometry`). `overlay_geometry` dispatches to it FIRST, on `overlay_is_workspace()` — a workspace's rail IS its facet strip stood on its end, so the same `overlay_lens` data reaches a different presentation and the faceted branch must not claim it. A workspace's box comes from the CANVAS (`workspace_margin`, a fraction of the smaller window dimension, clamped) rather than from a width cap and an anchor rail: it is not a card looking for a comfortable place to float.
- **WHICH KINDS, AND WHICH SHAPE**: `OverlayKind::workspace_shape` (wildcard-free) returns `Option<WorkspaceShape>` — `Some(shape)` names the arrangement a kind is PRESENTED as, `None` keeps it on its contextual or grouped card. That match is the roster; this doc does not restate its membership. Deliberately NOT `sustained()` — that predicate says a kind has workspace LIFECYCLE (item 173 gave both Settings and History a detail stage and a Back); this one says it is PRESENTED as a relocated workspace, and as which shape.  The renderer never re-tests the kind: `ViewState::overlay_workspace` is the projection, and `actions::tests::workspace_item114::no_second_place_decides_how_settings_is_presented` bans a second test inside `render/chrome/`.
- **ITEM 116a — ONE SHAPE WAS NOT ENOUGH.** `workspace_shell()`'s bare bool put facet LABELS in the primary column and ROWS in the content pane unconditionally — the right composition for Settings, the wrong one for History's "narrow timeline beside a large comparison" (DESIGN.md §5). `WorkspaceShape` names both arrangements (`RailOverRows`, `TimelineOverComparison`), and `WorkspaceShape::rows_are_primary()` is the single fact every consumer reduces to instead of re-deriving which region holds a workspace's own rows — the only match over the enum's variants anywhere in the crate (`render::tests::workspace_shape_item116a`'s grep-law keeps it that way). `workspace_geometry` reads it (via `ViewState::overlay_rows_primary`, a flattened bool mirroring `overlay_workspace` — the render layer still never touches `WorkspaceShape` itself, consistent with `theme_caps_law`'s "render never branches on identity") to decide which of the primary column or the content pane carries the row-window (`text_left`/`text_w`, `visible`/`top_idx`) and which, when rows are primary, becomes a rect this module never draws into (item 116b's future comparison viewport). `rows_are_primary()` is the single fact each consumer reduces to, and it is `true` exactly for `TimelineOverComparison` — so the arm is LIVE, not a structural generalization awaiting an occupant. Which kinds take it is `workspace_shape`'s own match, not a list here.
- **TWO REGIONS, ONE STATE EACH.** The RAIL's selection IS `facet_lens` (no second category model — a rail move and a `←/→` lens step are one state change, and `refilter` already narrows the rows). Which region holds focus IS `OverlayState::detail_focus`, written only by `Journey` — the rail is `Surface::Workspace`, the content is `Surface::WorkspaceDetail`. **ITEM 116d — ONE ESC ALWAYS LEAVES (user decision 2026-08-02, settled once for BOTH members as item 114 asked):** a cancel on the DETAIL stage lands exactly where a cancel on the PRIMARY list lands, at every `Beneath` policy and every width, so `Esc` cannot mean two different things depending on where focus sits; the three `WorkspaceDetail × Cancel` arms are character-for-character their `Workspace` siblings and `journey::tests::one_esc_leaves_from_either_region_of_a_workspace` compares the two ROWS of the table rather than restating the arms. Focus moves between the regions on `Tab`/`Shift-Tab` alone (`Action::InsertTab | Action::Outdent`, one door in `workspace_intercept`). A CHILD AUDITION summoned out of a workspace is a different rung and keeps Esc-returns-to-parent (`Contextual × Beneath::Workspace × Cancel => Resume`, untouched). **The footer pays for it:** Esc was the taught way back, so a workspace now NAMES its Back — Settings' rows line is `↵ edit · ←/→ category · tab back · esc close` and History's comparison line already carried `tab back`; `actions::tests::workspace_esc_item116d` drives the advertised key and asserts the sentence describes what happened, and fails if any stage still calls Esc a back.
- **WIDTH ENTERS ONCE**, in `workspace_is_wide(width)`: room for the rail plus a content pane that can still show a row's NAME and its VALUE (`MIN_PANE_CHARS`, a legibility floor — below it `rowlayout` starts yielding the secondary column, and a settings list that shows what a setting is called but not what it is set to is not a settings list). Wide draws both regions; narrow stages them, and the same focus fact becomes which stage you are on. No lifecycle transition can see width (`landing_of` has no width parameter), so Back means the same thing in both by construction.
- **THE RAIL'S GRID RIDES THE ROW PLAN.** `OverlayGeom::rail` carries the COLUMN only (`[x, w]`); `workspace_rail_box(geom, plan)` seats it on `plan.first_top()`, so a rail entry and the settings row beside it share a line by construction and the shaped labels, the active mark and `workspace_rail_at` all read one grid. `OverlayGeom::band_x`/`band_w` is the one owner of the CONTENT band's horizontal extent — the card for a contextual overlay, the pane for a workspace — so the plan, the selected-row quads, the bar plates and `overlay_row_at` all stop at the rail without knowing it exists.
- **THE FOCUS CUE IS VALUE, NOT DECORATION.** The unfocused region's marker is the same rect at `UNFOCUSED_MARK_ALPHA` of its alpha (`workspace::dimmed`). The rail's active mark rides the existing `overlay_lens_underline` pipeline (it IS the active-lens mark) but bypasses the `FacetStyle` skins on purpose: those describe a horizontal chip run and say nothing about a column, so the rail takes the world's own selected-row band instead. A workspace draws ONE card surface — item 50's split composition is a small-card gesture and would cut the rail in half.
- **ITEM 116d — THE COMPARISON IS COMPOSITED, *ON* THE WORKSPACE SURFACE (user decision 2026-08-02).** 116b relocated the document's GEOMETRY and stopped; the card still drew over it. The comparison now sits ON the card — one opaque surface, "A WORKSPACE IS ONE SURFACE" intact — and the document's CONTENT is submitted AFTER `draw_overlay_card`, into the carved region, **without re-drawing its own ground**. The rejected "window through it" arm would have shown the BACKDROP's ground (the punch is at the page column, not at the region) and, on a blur-eligible world, frosted the frame AROUND the workspace — exactly where the region is not. `draw_document_layers` splits into `draw_document_ground` (background/lava/stars/page-frame — the quiet frame, never relocated) and `draw_document_content` (everything composed off the four relocated owners); the ordinary frame concatenates them in their original order, so every non-comparison frame is byte-identical BY CONSTRUCTION. **The blur path captures the GROUND ALONE while a comparison is up**, and `blur_signature` hashes the comparison flag (Settings' workspace and History's would otherwise sign identically at one scroll and keep the wrong frost). `TextPipeline::clip_text_bounds` is the GLYPH twin of item 84's quad clip — the identity off a comparison, an intersection with the region on one — routed by the document text layer, the ornaments, the table grid and the image placeholders; it matters only now, because a glyph that outruns the region would land on the card it is drawn after. 116b's `the_relocated_document_is_geometrically_placed_but_not_yet_composited` was DELETED as its own message asked. Laws: `render/tests/comparison_composite_item116d.rs` — the surface still covers the whole region in every world; a differential between two documents that differ in every WORD is perceptibly different inside the region, reaches within two line-heights of both its edges, and is BYTE-IDENTICAL everywhere outside it (the byte-identity is what catches the frosted ghost, measured at max-channel-delta 2); swept over the roster and over canvas × zoom × DPI, with the visibility half graded only where the region can hold four lines.
- **ITEM 116b — THE RELOCATED DOCUMENT VIEWPORT (`render/chrome/comparison.rs`).** awl has exactly ONE prose renderer — the document layer — so a workspace whose content region holds prose has to MOVE it rather than grow a second one (the "infrastructure complexity is a smell" CLAUDE.md forbids). `TextPipeline::comparison_viewport` is that move's ONE owner: `Some([x,y,w,h])` (the content pane) exactly when this frame's workspace carries its own rows in the PRIMARY column (`rows_are_primary`) and the content region is on screen, `None` otherwise — which is every frame any kind can reach today, so the change is structural with **zero pixel change** (proven by a 98-capture world × surface fingerprint matrix, PNG and sidecar, not asserted). The FOUR owners every document consumer already routes through — `column_left`, `column_width`, `doc_top`, `doc_clip_band`, ~45 call sites across `rects.rs`/`layers.rs`/`text.rs`/`geometry.rs`/`scroll.rs` — read it, so caret, selection, washes, wrap width, hit-test and item 84's content clip relocate without learning that anything moved. **The BYPASS is private and enumerated:** the PAGE's own column on the canvas (the ground punch, the star margin band, the frost seed key, the sidecar's `page` block) never relocates and is `page_column_left`/`page_column_width`, crate-render-private, its call sites pinned by name — a second "where is the column" is exactly what becomes a parallel geometry. **MARGIN ORIENTATION YIELDS** (`margin_orientation_yields`): the outline, the gutter, the corner readouts (`wordcount_readout_text`/`notice_readout_text`, one owner each), the page frame, the draggable page edges and the lava frost pills all compose off those four owners and have nothing true to say about a read-only comparison; the outline and gutter reach that through item 34's `overlay_active` gate, which subsumes this one and is watched failing rather than trusted. **THE DIFF PANEL IS GONE, not disabled** — `diff_panel`, `diff_panel_rect`, `prepare_diff_panel`, `DIFF_PANEL_TOP`/`BOTTOM` and the three `diffpanel_*` pipelines are removed; its last consumer really was History (`vstate.diff_panel` was lit only by `opts.preview_text`, set only by `history_preview_for`; `scripts/review.sh`'s diff scene sets `opts.diff`, a sidecar block, through `prosediff::env_capture_render` and never `preview_text`). **WHAT IT DOES NOT DO, pinned as a law rather than absorbed:** the relocation moved the document's GEOMETRY, not its place in painter's order — `render` still submits `draw_document_layers` before `draw_overlay_card` and a workspace's fill is its whole card box, so the region is drawn over (`the_relocated_document_is_geometrically_placed_but_not_yet_composited` names what the composition round owes and fails when it lands). Laws: `render/tests/comparison_viewport_item116b.rs` (six), the re-aimed `selection_clip_law.rs`, `capture/tests/panels.rs::history_preview_renders_the_transcript_as_the_document_in_every_world`.
- Laws: `render/tests/workspace_item114.rs` (rail roster, drawn-equals-clickable over canvas × zoom × DPI × stage, wide/narrow staging, viewport-with-a-frame, the focus cue in real differential pixels, and a contextual card's untouched geometry), `actions/tests/workspace_item114.rs` (the keys against `landing_of`, search-moves-into-results, one category state, the Cmd-P deep link, Theme/Caret audition parity), `app/tests/workspace_item114.rs` (TIER 2 — every `SettingId × SettingKind` changed and persisted by real chords; see `docs/harness-reach.md` for why it cannot be a capture).

## Settings in the palette + overlay titles (`overlay/` + `settings.rs`)

- The Cmd-P palette's rows are catalog commands **∪** the surfaced Settings roster (a row like "Keymap" is fuzzy-findable straight from the palette). Still one `OverlayKind::Command`; the union is data (`attach_settings_rows`, typed `RowMeta`). Every command has one task category from `commands::task_category`; every attached setting joins Settings. The visible strip is `All · Files · Navigate · Format · View · Tools · Settings · Recent`; typing searches the whole union from any category, while clearing the query returns to the category left in place. Settings names read plainly because their named browse home replaces the unexplained `§` marker. Dispatch parity remains one owner (`dispatch_settings_row`; `close_on_toggle` is the only difference: the palette closes, Settings stays).
- **Every `OverlayKind` names itself** (`OverlayKind::title`, no-wildcard) — drawn as a muted prefix on the picker's input line (Rename/InsertLink opt out via `draws_title_prefix`, their own prompt orients). Sidecar `overlay.title`.

## Settings RANGE rows — the rail (item 94, `range.rs` + `render/rowlayout`)

- **ONE spec owner: `crate::range::RangeSpec`** (`min`, `max`, `step`, `default`, display `Unit`, `RailMap::Linear|Log`), resolved from a row's identity by the single map `settings::range_spec(SettingId)`. It owns EVERY derivation: `quantize` (the zero-anchored step grid), `step_of`/`value_of_step`, `frac_of`/`value_at_frac` (the rail mapping, both directions), `stepped` (keyboard), `format` (readout + sidecar), `parse` (exact entry), `persist_value` (config RHS). `render::clamp_zoom` is a one-line delegate to `range::ZOOM`, and the old `ZOOM_MIN`/`ZOOM_MAX`/`ZOOM_STEP` consts were DELETED rather than re-pointed — an alias is a drift risk. Zoom: 50 %–300 %, 10-point steps, LINEAR percentage rail. The `Log` map is general (25/50/100/200/400 land on equal intervals — item 90's `scroll_sensitivity`, not built here).
- **The row carries a discrete cell**, `OverlayRow::range: Option<RangeCell{id, step}>` — an integer step, so a row stays `Eq` and the drawn thumb can never sit where the keyboard cannot. Set at build AND at `refresh_settings_overlay`, beside `set_secondaries`, from the SAME gathered `SettingsValues`. `item_range_fracs()` derives the 0..1 column (EMPTY off Settings) → `ViewState::overlay_ranges` → the pipeline.
- **Geometry has ONE owner, `rowlayout::rail_geom`** (track / fill / thumb / hit band / `x0`,`x1`), sized in row-heights so it scales with zoom, seated left of the right-aligned value TEXT (measured from the same shaped buffer the value draws from) and YIELDING entirely when the row lacks room. `chrome::TextPipeline::overlay_rails` resolves the card's rails once; the DRAW path and the POINTER hit-test (`overlay_range_at`) both read it, so the control is clickable exactly where it is drawn. The hit band is padded past both ends and spans the whole row (a generous target around a small thumb).
- **Ink:** track `faint()`, fill + thumb `muted()`, flipped through the shared `theme::selected_row_secondary_ink` when the selected row's band would wash it out — the SAME owner the secondary TEXT uses. It remains secondary to the world's principal interactive ink. Two parked-by-default pipelines (`overlay_range_track` / `overlay_range_thumb`).
- **Input:** Left/Right claim the keys ONLY when the highlighted row carries a rail (checked before the facet-lens cycle, which is otherwise untouched); the step applies IN THE CORE (`Effect::SettingRangeStep` is replay-`Applied`) and the App owns the tail (reflow + the DISCRETE per-step persist). The POINTER: a rail press sets the nearest step and arms `RangeDrag` (press-time track scale snapshotted, the page-drag anchor lesson); every move applies live through `App::range_apply_live`; the RELEASE persists ONCE (`range_persist`). A click on the row's LABEL selects without changing. Enter still opens the exact numeric edit. No Shift/Option variants, no ±buttons, no wheel-to-change.
- **WHEN THE ZOOM PERSISTS HAS ONE OWNER, and one gate.** The write itself is `App::settle_zoom_persist` (persist, disarm the stamp, drop the floating readout, redraw); every door calls it — `about_to_wait`'s ~500 ms quiet window, `range_persist` (the rail's release / a discrete keyboard step), and `setting_value_commit`'s typed entry, which settles at once (a typed commit is its own discrete end) rather than leaving the debounce armed to write the same value again. The quiet window is an INFERENCE of "the gesture ended" for the doors that have no end event (⌘±, ⌘0, ⌘-wheel), so `App::zoom_persist_held` (`range_drag.is_some()`) turns it off while a gesture that DOES have an end is in flight: a live apply arms the in-flight stamp exactly as before (the hold-⌘ peek suppression still reads it), but the WRITE waits for the release. Without that gate, pausing mid-drag to look at the value IS half a second of quiet, and the debounce persisted a value the user never released on. Clock-driven law: `app::tests::files::a_paused_mid_drag_persists_nothing_until_the_release` (VirtualClock + `RecordingScheduler`, the which-key frame-loop pattern).
- **The foot hint is PER-ROW where the row's keys differ from its kind's.** A rail row's footer reads `←/→ adjust`, not the Settings kind's `←/→ lens` — `OverlayKind::range_row_hint` relabels that one cell of `hint_actions()` (so the rest of the line can't drift), selected in `OverlayState::foot_hint` on the SAME `selected_range()` predicate the Left/Right claim itself uses, so advertised keys and live keys are one decision. This is an accessibility requirement, not polish: the footer is awl's only statement of what a key does and there is no accessibility tree behind it (ACCESSIBILITY.md). Outcome law: `actions::tests::overlay_drive::the_foot_hint_names_what_left_right_actually_do_on_every_settings_row` (drives the real `ForwardChar` on EVERY Settings row and asserts the line named what happened); the footer's no-clip sweep measures both variants.
- Laws: `range::tests` (grid/round-trip/log-interval/drag-equivalence/parse), `settings::tests` (the no-wildcard kind-contract sweep + end-to-end range wiring), `render::tests::range_rail` (pure geometry, drawn==clickable, thumb travel, light/dark real-pixel contrast), `app::tests::files` (one-persist-per-drag, discrete keyboard persist), `actions::tests::overlay_drive` (keyboard steps + the lens-cycle gate + parity), `capture::tests::pickers_faceted` (sidecar `overlay.ranges`).

## The scene planner (`render/plan/`) — item 174

- **ONE deterministic, device-free layout stage between the measured inputs and the GPU.** It consumes the `ViewState` projection (the pipeline's `overlay_*` mirror), the already-resolved card box, the overlay row pitch and the grouped display-line sequence, and emits INSPECTABLE primitives plus INTERACTION geometry: one `PlannedRow { display, item, top, height }` per candidate DISPLAY LINE, `row_at(px, py)`, `display_at(py)`, `selected_display()`, `content_rows()`/`footer_top()`. It shapes nothing, measures nothing, reads no clock and touches no wgpu — so presentation decisions are assertable without a device (`render/plan/tests.rs`).
- **THE FORWARD `row -> y` ARITHMETIC AND ITS INVERSE ARE PRIVATE TO `plan/overlay_rows.rs`.** `overlay_row_top`, `overlay_row_of` and `overlay_row_index` are gone from `render/chrome`: a consumer cannot re-derive a row's position, only read it off the plan the pixels came from. Held by `render/tests/overlay_plan_law.rs`'s `only_the_planner_derives_an_overlay_row_position` — sentinels for the retired call names, `header_rows as f32`, AND for stepping off the band origin (`first_top` × a row count), which is the shape that hid `livingband::covered_rows`' own grid and the footer plate's seat for two years.
- **Routed through the one plan** (built once per overlay frame in `prepare_overlay`, threaded down): the pointer hit-test (`overlay_row_at`), the range rails (draw + hit), the visual-selection transaction's band target and coverage grid, the Pane band, the `Bars` plates, the chord plates, the footer plate, the slant text-clip bands, the shaped rows' item mapping, and the sidecar's `overlay.window` report. Item 164's arrangement is preserved: the planner is the one owner of `selected_display()`, `resolve_visual_selection` is still the only render path that reads it (`visual_selection_law`'s no-wildcard source sweep), and `overlay_row_at` deliberately stays outside the transaction.
- **The inverse SCANS the planned slots, it does not invert the formula.** `(py - first_top) / lh` truncated at a row's own first pixel lands on the row ABOVE whenever `k * lh` is not exactly representable (found at `lh = 12`, `header_gap = 63.55`, row 11). Comparing against the same `f32` tops the band was drawn from cannot disagree with the draw. O(visible) over at most a couple of dozen rows.
- **O(visible), never O(doc), and never retained.** One `PlannedRow` per display line the card shows — a 40,000-row Go-to picker plans the twelve on screen (384 bytes for the palette's 106-item catalog). There is no plan cache, so there is no key to go stale and no `buffer.version()` collision can serve a stale band; `the_plan_is_rederived_across_resize_zoom_and_a_buffer_swap` proves the absence is real. The planner counts its own work (`plan::plan_witness`), and `--bench-suite`'s `palette` cell asserts the exact per-frame plan count plus the O(visible) bound.
- **HOW MANY PLANS ONE FRAME IS ENTITLED TO (item 217) — every pass NAMED, never a number.** `prepare_overlay` used to build the row plan TWICE, unconditionally on every world: once up front and once again after `resolve_diagonal_cluster`, because a measured diagonal cluster changes the plan's `dx`/`dw` and the cluster is derived from rows shaped against that very plan. That cycle is now broken the other way — the frame builds ONE plan and COMPLETES it in place (`OverlayRowPlan::complete_row_extent`, whose arithmetic is the same `apply_row_extent` a fresh build runs, so completing lands exactly where rebuilding did). The one remaining second plan belongs to item 51's RIGHT-ANCHORED content-hug measurement, which shapes a PROVISIONAL card inside `set_view` to learn how wide to hug and so cannot be the frame's plan at all. The witness (`benchsuite::FramePasses`, one owner shared by the bench cell and `render/tests/plan_pass_law.rs`) names both and asserts their sum, so a THIRD plan still fails by name; its content-hug oracle reads the measurement's own cached width rather than the anchor predicate the pipeline branches on. **The axis that mattered was the card ANCHOR, not the list style** — the diagonal world Magpie (top-LEFT) plans once and the upright poster world Cassowary (top-RIGHT) plans twice.
- **`content_rows` is the one owner of "how many display lines precede the footer"** — the candidate band PLUS an empty-state notice. It used to be the grouped family's plan length alone, omitting the notice line the card height had already paid for, so a `Bars` world's picker filtered to zero matches drew its footer plate over the "no matches" row. Three-arm law over the whole shipping `Bars` roster (`an_empty_states_notice_row_carries_no_footer_plate_on_any_bare_plate_world`): the emitted quads may not overlap the notice row's planned slot; where an absolute pixel oracle can see the plate at all, the notice band must read as card ground; and the two plateless bare-plate worlds must emit no row surface at all (item 236).
- **THE HEADER BAND (the SECOND family, 2026-08-02).** The two display lines ABOVE the candidate band — the query/title INPUT field every takeover picker draws, and the grouped family's lens STRIP — are planned by the same code, as `PlannedHeader { line, top, height }`. Header line `i` sits at `text_top + i*lh` and is `lh` tall EXCEPT the last, which is `lh + header_gap`: the query BEAT is STRUCTURAL and lives inside the last header line's own box (the shaper inflates that line's real glyph metrics by it — `shape_overlay_names`'s `header_lh`, `shape_theme_spans`'s `strip_lh`), never as a blank line between boxes. So the band tiles from `text_top` down to `plan.first_top()` with no hole and no overlap, by construction. **FOUR `render/chrome` owners were DELETED, not banned by law:** `overlay_secondary_top` → `OverlayRowPlan::secondary_top`, `overlay_split_bounds` → `OverlayRowPlan::split_bounds`, `overlay_strip_band` → `OverlayRowPlan::strip_band`, `overlay_query_center` → `PlannedHeader::center`. The shaper's own `header_lh` READS the planned box rather than re-summing beside it, so the DRAWN box and the planned one are literally one object; `right_bind_lines`' leading-empty count comes from `plan.header_rows()`, paired with the origin it must match. Routed through the one plan: the query caret's centre, `over_overlay_query`'s pointer band, `overlay_lens_at`'s clickable strip band, the active-facet mark's centre, the secondary column's upload origin, and `overlay_pane_fills`' split gap.
- **THE LIVE DEFECT IT FOUND, of the family's own class.** `over_overlay_query` tested the bare row pitch, `text_top .. text_top + lh`. On the FLAT family the beat inflates the QUERY line itself, so the field's box is `lh + header_gap` tall and cosmic-text half-leads its ink LOW inside it. Measured on the shipping default at 1200x800: the field draws `[64.0, 133.2]` with its caret centred at 98.6 and its shaped baseline at 106.0, while the pointer band ended at 91.2 — the I-beam appeared in empty air ABOVE the query text and the text itself took the plain arrow, missing by 7.4px at 1x and 14.8px at 2x. The GROUPED family was right by accident (its beat inflates the lens STRIP instead, leaving the query line a plain `lh`), which is how a parallel calculation survives review: it agrees on the arm somebody looked at. The cursor icon is the ONLY output that changed — 840 PNG probes and 840 sidecars over every world x three canvas/DPI cells x fourteen surfaces are byte-identical (`scripts/item174-header-band-identity.sh`).
- **ITEM 219 — WHERE THE BEAT STANDS, and the blank band the fold opened.** The beat was folded into the LAST header line's box on BOTH families. cosmic-text CENTRES a line's glyph run in its box (`buffer.rs`'s `centering_offset = (line_height - glyph_height) / 2`), so on the FLAT family — whose one header line IS the query field — the field's own glyphs were drawn `header_gap / 2` below the bar's own top pad: measured on the shipping default at 1200x800, the query ink centred at 98.0 where its line box centres at 77.6, a 20.4px blank strip at the top of every takeover picker on every world. Reported against Mopoke/Currawong/Gumtree/Bilby/Bowerbird; present, byte for byte identically, on all twenty. **The rule is now one sentence: THE QUERY FIELD'S BOX IS ALWAYS EXACTLY ONE LINE.** `plan::beat_stands_alone` is the one owner of whether the beat rides the last header line's glyph metrics (a GROUPED card's lens strip, where the half-leading is exactly what seats the strip below the split seam) or closes the band as its own GLYPH-FREE LINE (a flat card, where there is nothing to seat). The shaper emits that line as one space at `OverlayRowPlan::beat_line()`'s height — DERIVED from the planned boxes, so a spacer the band did not plan is unreachable — and `OverlayGeom::shaped_first_row_line` is the one owner of the buffer-line index it shifts (a reader that re-derived `header_rows` measures the spacer as a candidate row). `split_bounds` collapsed from two arms to one: **the seam hangs from the QUERY FIELD's own bottom edge** plus `BREATHE_FRAC`, which is byte-identical arithmetic on the grouped family (the strip's top IS the field's bottom) and is what closes the flat card's bar around its own text. `first_top`, the candidate band, the selected row, keyboard nav and the card box are all untouched. Laws: `render/plan/tests.rs`'s `the_query_field_box_is_one_row_pitch_on_every_family` and `the_split_gap_is_carved_out_of_the_beat_below_the_query_field`, `render/tests/overlay_header_band_law.rs`'s `the_query_bar_closes_below_the_field_and_the_folded_beat_would_overrun_it`, and the real-pixel `render/tests/surfaces_item219_225.rs`'s `a_flat_pickers_query_ink_centres_on_its_own_line_in_every_world` — a DIFFERENTIAL oracle (the same card shot with and without query text, so ground, texture, placard and dither cancel), roster x two canvases x 1x/2x. Item 174's own non-vacuity law `the_pre_plan_query_band_genuinely_missed_the_field` was DELETED rather than shipped vacuous: the formula it reconstructed (`text_top .. text_top + lh`) is now the correct one on both families, so it could no longer fail on anything. Its user-visible outcome — the I-beam appears where the query text is — is still held, by the headline `the_drawn_query_field_the_pointer_band_and_the_caret_are_one_planned_box`.
- **ITEM 225 — A WORKSPACE'S `Bars` FOOTER PLATE ENDS WITH ITS FOOTER.** The plate ran to the card's bottom edge, which is right for a card that HUGS its content (the plate closes the card, and the two are the same line) and wrong for a summoned WORKSPACE, whose card comes from the CANVAS: the same rule painted a slab as tall as whatever vertical space the rows did not use, hanging below the footer's glyphs with nothing in it. On Cassowary, whose plate ink is `base_100` at `#050506`, that is the reported "oversized black sub-settings bar"; the same slab is on the other two `Bars` worlds in a paler ink. The workspace arm now takes the FOOTER'S OWN BAND — the row pitch LESS `overlay_footer_reclaim`, the one owner of how much shorter a footer line is than a row, which the card height already reads — and the contextual arm is untouched and byte-identical. Law: `render/tests/surfaces_item225.rs`'s `a_workspace_footer_plate_ends_with_its_footer_on_every_bare_plate_world`, three arms (the emitted quads: no workspace plate may be taller than one row of its own list; the pixels: the band the retired rule painted must read as workspace ground; and the exclusion, below), the retired rule written out inline for non-vacuity, x 1x/2x.
- **ITEM 236 — THE BARE-PLATE ROSTER IS NOT THE PLATE-DRAWING ROSTER.** `list_backing == BarePlates` is a claim about the CARD — no panel, no shadow, no border, so the rows sit bare on the canvas — and says nothing about what backs a ROW. Two of its five members, Mangrove and Magpie, are `ListStyle::Diagonal`: bare in exactly that sense and drawing no plate anywhere, their selection being a bright spine segment and a connector. Item 225's law and item 174's `content_rows` law both swept `BarePlates` while reasoning about plates, and the test-only `overlay_bar_rects_probe` SYNTHESIZED rects for the two diagonal worlds by calling `overlay_bar_selection` at dials (`6.0/10.0/24.0/HugLabel/All`) that no world authors and no frame draws — so both laws graded an invention on those cells, including the `!plates.is_empty()` non-vacuity guard. `ListStyle::draws_row_plates()` is now the one owner of the question, `overlay_selection_rects` is the one place a list style becomes row surfaces (production and probe both route through it), and **`overlay_bar_rects_probe` REFUSES on a style that draws no plates** — the misuse is a panic at the ask rather than a shape to be found later. Both laws gained a third arm that EARNS the exclusion by measurement: on each plateless bare-plate world the frame must emit no row surface at all (`overlay_row_surfaces_probe`), so a world that starts drawing plates fails there instead of quietly dodging the sweep.
- **ITEM 308 — A `Bars` FOOTER PLATE GETS THE NOTICE CHANNEL'S OWN RIM.** Cassowary's footer plate (`overlay_bar_unselected()` = `base_200`, on a page rooted at `base_100`) measured ΔE 1.91 from its own page — under the ≈2.3 JND, and invisible to the old absolute-luma gate because Firetail aborted the run first (item 306). `footer_plate_rim` (`render.rs`) is a new `SelectionPipeline`, drawn under `overlay_bars` from the SAME rect the footer plate itself uses (`OverlaySelectionRects::footer_plate`), grown one device px on every side — the identical mechanism `notice_rim`/`notice_plate` already use, coloured `theme::overlay_footer_plate_rim()` (`muted`, resolved fresh every frame, no `sync_theme_colors` entry — same reasoning as the notice channel). Re-measured with the rim: Cassowary 54.13/58.47 (1×/2×), Galah 22.33/28.08, Firetail 40.91/44.00 — all clear `render::tests::notice::PLATE_PRESENCE_MIN` (ΔE 15, made `pub(super)` and reused rather than re-derived: both channels earn presence the identical way, a value-stepped fill plus a one-pixel rim). Law: `render/tests/overlay_plan_law.rs`'s `footer_plate_clears_the_notice_channels_presence_floor_on_every_bars_world`, over the plate-drawing roster × both DPI tiers, the page read back from the same rect with the overlay closed (`notice.rs`'s own "what was actually there" technique) rather than a nominal token value.
- **ITEM 316 — A LOCATION ROW THAT PLANS NO GLYPH GETS NO PLATE.** Exposed, not introduced, by item 297: `PlanLine::Location`'s inline text is glyph-free wherever `LocationStyle::draws_inline()` is false (Cassowary's `RotatedRail`, composed off-card into the room's margin), but `overlay_unselected_bar_rects`'s theme-strip loop backed EVERY `item.is_none()` row (header or location) with a plate regardless — a visibly empty chip once the row's own text left. The loop now reads the same `draws_inline()` gate the shaper itself reads before pushing a location row's plate — never a named-world check, so a future non-inline style loses its chip for free and a future inline one keeps it. Law: `render/tests/overlay_location_plate_item316.rs`'s `a_glyph_free_location_row_draws_no_plate_and_an_inline_one_still_does`, over the plate-drawing roster × every faceting `OverlayKind` × both DPI tiers — the one production row-surface probe (`overlay_row_surfaces_probe`) must emit nothing over the location row's own Y-slot on the excluded case and something there on an included one (non-vacuity), plus a real-pixel ground check on the excluded case.
- **ONE MEMBER DID NOT MERGE, and is pinned rather than trusted.** `TextPipeline::workspace_header_beat` (`render/chrome/comparison.rs`) is a fourth copy of the last header line's height. Its consumer `comparison_viewport` is called from ~45 sites a frame through the four relocated document owners (`column_left`/`column_width`/`doc_top`/`doc_clip_band`), so planning inside it would trade one parallel calculation for ~45 plans a frame and break the O(visible) budget; threading the plan down to those owners is its own slice. Until then `the_workspace_beat_still_agrees_with_the_planned_query_box` fails by name if the two drift. Laws: `render/plan/tests.rs`'s pure band/secondary/split/contextual laws (independent accumulating oracles, swept over every header count x four beats x four pitches) and `render/tests/overlay_header_band_law.rs`'s device sweep (the whole `OverlayKind` roster x both list styles x four canvases x both DPIs, plus a full-roster non-vacuity law that writes the retired formula out inline and proves its miss).
- Law probes that report the plan for a law to compare against live in `render/tests/overlay_probe.rs`, not beside the draw code: a probe naming the logical selected row is not a render path, and keeping it out of `render/chrome` keeps item 164's sweep strict rather than growing an exception.
- **THE HEIGHT CLAMP (item 181, extended by item 184), `plan::fit_item_rows`.** The GROUPED family (`theme_overlay_geometry`) already divided its own `avail_px` by the row pitch to bound its item window; the FLAT family (`overlay_geometry`) capped its window only at a per-kind row COUNT (`OverlayKind::window_rows`) that knows nothing about the canvas. A flat picker whose count is its whole corpus — the theme picker, `window_rows() == theme::THEMES.len()`, once its runtime lens strip retired (making it flat, see below) — drew a card taller than the canvas at its own default size (`card_h: 934` against `canvas_h: 800`). `fit_item_rows(avail_px, lh, overhead_rows, min_items)` is now the one owner of "how many item rows fit"; `render/chrome/overlay_clamp.rs`'s `overlay_item_cap`/`overlay_flat_window` route the flat family AND the spell popup through it, `theme_overlay_geometry` routes the grouped family through the same `fit_item_rows`, so no consumer computes its own floor-division. Byte-identical wherever a picker's per-kind cap was already the binding constraint (every shipped flat/grouped picker at ordinary zoom/DPI). **ITEM 184 closed the residual item 181 left open:** a GROUPED picker deliberately cycled onto a sectioned lens carries extra fixed header overhead (the lens strip + real section headers), and at the documented zoom ceiling (3.0) combined with a short canvas that overhead ALONE — before a single item is counted — could still exceed the canvas even with the item-row floor forced to 1 (`card_h: 535.4` against `canvas_h: 460`, the command palette on its File lens; confirmed present on unmodified pre-184 code via `git stash`). `fit_item_rows` gained a `min_items` parameter: the FLAT family and the spell popup keep `min_items: 1` ("a card always attempts to show its own selection", byte-identical to item 181), while the GROUPED family now passes `min_items: 0` — when its own fixed chrome overhead alone cannot fit, the card answers with an EMPTY candidate band rather than a forced row that overruns the canvas. The lens strip, the section headers, AND the footer (`overlay_footer_lines`, the one owner both families now read for `keybindings_tips`, previously grouped-only-omitted and always empty in practice since only the flat Keybindings kind ever populates it) all count against `avail_px`. A no-op everywhere the floor did not already bind (`saturating_sub` alone clears the new floor whenever `fit_lines > overhead_rows`) — proven, not merely asserted, by `already_fitting_grouped_pickers_stay_byte_identical_across_the_floor_fix`, which fingerprints five ordinary grouped scenarios against numbers measured on the unmodified pre-184 code. Laws: `render/plan/tests.rs`'s pure `fit_item_rows_*` tests (the ordinary case, each family's own floor, the zero-pitch guard, non-vacuity against the reported theme-picker shape) and `render/tests/overlay_height_clamp_law.rs`'s device-level sweep (every `OverlayKind`, both list styles, four canvases at 1x/2x DPI with the LOGICAL window always at/above the app's own enforced minimum, plus a zoom sweep 0.5–3.0 against the theme picker, the command palette's "All" home, AND its sectioned lens) — three oracles per cell (the sidecar's `card_h`/`canvas_h`, the planner's own last-row bottom edge from a DIFFERENT function than `card_h`, and real pixel ink at the last row's baseline), a named regression pin reproducing the exact reported numbers for both items, and a non-vacuity floor proving the grouped family's empty-band path is genuinely exercised.

## Picker rows (`render/rowlayout`)

Picker rows go through `render/rowlayout` — never place row text directly. A primary cell (never dropped, elided last-resort) + optional secondary right column (first to yield). `rowlayout::plan` → `fits` → `fit_primary` (the only elision door). The law test enumerates `OverlayKind` with a no-wildcard match. The bottom-left page-mode gutter rides the same owner (`gutter_plan`) — stacked, so neither line yields from width pressure (the filename never wraps; the fix for "DESIGN.md → DESIG/N.md and the project vanishes").

## Picker hover/click stability + movement latency (`app/input/mouse.rs`, `overlay/nav.rs`, `probe.rs`)

- **THE REAL-MOTION HOVER GATE (item 85).** A theme-picker world jump previews the destination world's rail/size/type/material immediately (reanchor, Pane↔Bars, a settling font reshape) — but that re-layout must never itself read as a pointer gesture. `OverlayState::hover_at(px, py, hit)` is the ONE door `app/input/mouse.rs::overlay_hover` calls on every `CursorMoved` (real travel or a platform-synthesized duplicate at the identical coordinates): it compares `(px, py)` against `OverlayState::last_hover_px` and only re-hit-tests/re-selects (`hover_select`) when the pointer's own PHYSICAL position genuinely changed since the last check — a hit-test RESULT changing on its own, with the pixel unmoved, is a no-op. Pure and GPU-free (`hit` is a caller-supplied `Option<usize>`, not a pipeline call), so it's unit-tested directly. Laws: `overlay::tests::hover_at_gates_on_real_pointer_motion_not_a_relayout_hit_test_change` (proves BOTH that the hazard is real — an ungated `hover_select` really does cascade — and that the gate refuses it), `overlay::tests::hover_at_off_a_row_records_position_without_selecting`, `render::tests::overlay_hover_stability_law` (the same hazard against real pipeline geometry: a deliberate Wagtail→Cassowary rail crossing moves what a stationary pixel hits). Keyboard nav is untouched (`move_sel(±1)` still advances exactly one visible row per press, hover's own bookkeeping never leaks into it — `overlay::tests::keyboard_advances_exactly_one_visible_row_per_press_even_after_a_hover`).
- **THE MOVEMENT-SLOP WIDENING + KEYBOARD BASELINE (item 106).** Item 85's gate closed EXACT-pixel duplicates, but left a hazard the exact-equality check couldn't see: a list WINDOW SCROLLING under a genuinely resting pointer (a real keyboard `move_sel` deep enough to shift the candidate window) changes which row a stationary pixel hits, and any real `CursorMoved` past that point — even a single physical pixel of a resting hand's ordinary hardware jitter — used to steal the keyboard's own selection outright. `hover_at` now gates on SQUARED DISTANCE against `HOVER_MOVE_SLOP_PX` (private to `overlay::nav`, one owner; 4.0px, matching `app::DRAG_ARM_SLOP_PX`'s identical "content relocating under a stationary pointer" precedent) rather than bare inequality — real travel PAST the slop still takes over on the very first such event, no added latency, no dead zone; the anchor is STICKY below the slop (stamped forward only on a reported move) so a genuinely slow drag still crosses the threshold from its original anchor rather than re-basing itself every sub-slop increment. The OTHER half: `OverlayState::arm_hover_baseline(px, py)` re-anchors the gate to the pointer's CURRENT resting position, called by the shared keyboard-dispatch seam (`App::apply` live, `ReplaySession::apply_chord` headless) after every keyboard-driven action, AND by `App::overlay_wheel` — the mouse wheel drives `move_sel` exactly like ↑/↓ (a second "deliberate crossing" input class, same as the keyboard) but is dispatched straight from `on_mouse_wheel`, never through `apply`, so it stamps the baseline itself rather than relying on `apply`'s stamp — without either stamp, a session with no prior hover (`last_hover_px` still `None`) would let the pointer's first incidental `CursorMoved`, however small, read as unconditional motion under `hover_at`'s own cold-start rule and hand the selection to wherever a motionless hand happens to rest. `HOVER_MOVE_SLOP_PX` is now a re-export of `app::DRAG_ARM_SLOP_PX` itself (not a second `4.0` literal), so the two gates cannot drift apart under a future retune of either. Laws: `overlay::tests::hover_at_movement_slop_boundary_law` (the threshold in both directions, pure distance), `overlay::tests::keyboard_baseline_stamp_protects_a_pointer_that_was_never_explicitly_hovered` (non-vacuous: proves the unstamped scenario steals first), `overlay::tests::keyboard_nav_survives_a_pointer_parked_over_any_row_relative_to_the_destination` (row above/below/far/landing), `overlay::tests::hover_movement_slop_gate_holds_across_every_overlay_kind_no_wildcard` (the full `OverlayKind` roster via a compile-time-exhaustive match — the sweep still iterates the hand-kept `OverlayKind::ALL` roster, not the match itself; a variant added to the enum and the match but forgotten in `ALL` still compiles clean and silently skips the sweep, a limitation `OverlayKind::ALL`'s own doc discloses), `render::tests::hover_slop_law` (the same hazard against real pipeline geometry, swept across `Pane`/`Bars` and 1×/2× DPI), `app::input::tests::wheel_scroll_from_cold_start_does_not_expose_selection_to_the_next_hover_check` (the wheel path specifically, non-vacuous: proves the unstamped `overlay_wheel` steals first). **The pointer-replay seam:** `TextPipeline::resolve_overlay_hover` is the ONE hit-test-then-`hover_at` seam both `App::overlay_hover` (live) and `ReplaySession::apply_move` (headless, via `capture::OraclePipeline::resolve_overlay_hover` + `sync_overlay`, which pushes the CURRENT overlay's row/window geometry onto the otherwise buffer-only motion oracle) route through — proven end-to-end through the real `--keys` replay engine by `run::tests::item_106_pointer_replay_seam_reproduces_a_keyboard_scroll_stealing_a_stationary_pointer_check`. `apply_move`/`sync_overlay` are NOT yet wired to the `--keys` CLI STRING grammar (no `move X Y` token) — only reachable by constructing `ReplaySession` directly, as that law does — and cover the flat/list picker geometry path only (not the Theme picker's faceted `overlay_lens`/section-header layout).
- **THE ONE CLICK-ACTIVATION RULE.** `App::overlay_click` fires ONLY on `ElementState::Pressed` (never `Released`) and hit-tests `self.cursor_px` at that same instant — so "the row a click activates" is unconditionally the row under the PRESS, never a release position or whatever an earlier hover last computed. No separate release-time re-evaluation exists.
- **MOVEMENT-LATENCY, the live companion to offscreen `--bench-theme-burst` (item 85).** `App::retint_theme_preview` — the ONE owner every input kind (keyboard nav, mouse hover, mouse wheel) funnels a theme-picker world change through — arms `probe::mark_movement_input()` right before the real relayout work; `Gpu::redraw` closes it out against the frame it actually PRESENTS via `probe::note_presented_frame()`, at the exact point the existing `"present"` trace fires. `probe::latency_distribution()` reports count/min/p50/p95/max in ms. Reads through the SAME `probe::recording()` gate (both the automated `--live-script` probe and the user's `AWL_FLIGHT_RECORDER`) every other diagnostic trace point uses — no new shared live/headless driver. Surfaced two ways: the script grammar's `latency` step (prints one `LIVE-PROBE latency …` stdout line, the `shot` line's protocol twin) and, for the flight recorder, one closing `movement-latency distribution: …` trace line at app teardown. Native-only; a no-op on a plain launch (the clock is never even read). Laws: `probe::tests::movement_latency_mark_and_present_produce_a_sample_and_distribution`, `probe::tests::movement_latency_is_a_no_op_outside_recording`, `probe::tests::parse_covers_the_latency_step`, and `probe::tests::movement_latency_burst_of_n_reports_n_not_one` — **one pending mark per step, QUEUED, never a single slot that the next mark evicts.** An overwriting slot collapses an N-step burst to `n=1` whatever N is, and did: it once reported `n=1` for an 8-input burst and put a figure two orders of magnitude low into a design decision. The law sweeps N = 1, 2, 3, 8, 9 under the **zero-gap arrival order** — every mark fires before any present closes one out, which is what a burst produces when input outruns the frame loop, and is the ONLY shape that can observe the collapse; a tidy alternating mark/present loop passes against the bug, because the immediate present drains the slot before the next mark arrives. The distribution's TIMING/feel over real time is live-only by nature (CLAUDE.md) — the user remains the oracle for felt lag; this instruments the same real end-to-end path (dispatch → relayout → encode/submit → compositor present) `--bench-theme-burst` cannot reach offscreen.
