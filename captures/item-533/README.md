# Right-anchored faceted picker stability

`kite-all.png` and `kite-headings.png` are ordinary strict headless captures of
the same seeded workspace and explicit config. The Go-to picker is first shown
at All, then at its empty Headings lens. The fixture configuration lives outside
the captured workspace corpus; no ambient project or config can enter either
frame.

The left edge is measured from the light card boundary in the PNGs, while the
sidecars establish the intended lenses and identical seeded setup.
