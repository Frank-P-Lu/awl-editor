# Reviewed historical exceptions for the two metrics that run in code-health.py.
# Identity is lint + file + enclosing function (`Type::method` where the
# diagnostic sits inside an impl, bare `function` otherwise) + complete
# diagnostic text — deliberately NOT line number. A raw line pin shifts every
# time unrelated code moves above it in the same file (a new `mod` line, an
# earlier lane's own growth), so two branches that each grow the same file
# produce genuinely unresolvable merge conflicts on a line number neither one
# owns. Resolving by content instead removes that shift entirely: code-health.py
# re-derives the anchor from the diagnostic's OWN current line every run
# (`resolve_function_anchor`), so an entry keeps matching across any amount of
# unrelated growth in the same file. Removing, shrinking back under the
# threshold, or renaming a recorded function still makes its entry stale.
# Pure test-file renames keep their frozen long-line baseline under the new
# mechanism name. A merged destination may name both source files; only exact
# line text is grandfathered, so this cannot bless newly written debt.
[baseline_rename]
"src/render/tests/bands_waves.rs" = ["src/render/tests/backgrounds_item69.rs"]
"src/render/tests/caret_transition.rs" = ["src/render/tests/caret_transition_item105.rs"]
"src/render/tests/overlay_rhythm.rs" = ["src/render/tests/overlay_rhythm_item112.rs"]
"src/render/tests/waves_drift.rs" = ["src/render/tests/waves_drift_item87.rs"]
"src/render/tests/zigzag_ground.rs" = [
    "src/render/tests/backgrounds_item86.rs",
    "src/render/tests/backgrounds_item89.rs",
]

#
# The frozen file-size baseline is intentionally old: it is each file's size
# at the pinned BASELINE commit, and a file may never exceed it — that is the
# one hard, non-negotiable ceiling, checked directly against that commit's
# content, not against this table. A mark below is the live high-water line
# code-health.py actually enforces (min(baseline, mark)); it is not itself a
# second ceiling, only bookkeeping for where that line currently sits.
#
# A mark must be lowered with every legitimate shrink. It may also rise —
# strictly below the frozen baseline, never past it — when growth genuinely
# cannot be avoided at a single-owner seam: an exhaustive construction site, a
# one-owner accessor sequence, a dispatch call site the design (CLAUDE.md's
# architecture) requires to stay singular, where decomposing instead would
# break the exhaustiveness or ownership property the seam exists for. Such a
# raise is only legitimate with a `reason` field naming the seam and the size
# delta; it is never a substitute for extraction the file can actually afford.
# A raise recorded relative to `main` with no `reason` fails the gate
# (code-health.py: check_mark_raises). That comparison only has force where
# HEAD differs from `main` — a worker's worktree branch, checked before
# landing. HEAD-is-`main` (CI's push-to-`main` job, and the merge train's
# post-merge candidate) is a vacuous comparison-to-itself and is reported as
# SKIPPED, not as a clean audit (code-health.py: previous_marks). Do not read
# "the ledger enforces raise reasons" as something CI verifies generally.

[[file_size_mark]]
file = "src/actions.rs"
lines = 754
reason = "Reveal in File Manager / Copy File Path: the `CopyFilePath` dispatch arm in `apply_transition`, plus both actions' membership in the exhaustive `ActionFamily` classifier (`CopyFilePath` as Buffer, `RevealInFileManager` as Deferred) (+3). +1: Save a Copy enters the one exhaustive transition dispatcher beside the other file actions. +1: `Action::OpenScratch` joins the exhaustive `ActionFamily::Deferred` classifier, mirroring `Action::NewDocument`. Insert Table's InsertTable action joins the exhaustive dispatch and ActionFamily classification this file already owns for every formatting command. +39 (709->748): item 542's auto-align-on-row-leave trigger — `ActionSnapshot` gains `row_before` (the cheap rope line lookup `capture` already owns the seam for) and `finish_action` gains the row-leave hook, dispatched through the one exhaustive post-transition seam every action already passes through. +4: the four sentence-motion Action arms in apply_transition (ForwardSentence/BackwardSentence/DeleteSentenceForward/DeleteSentenceBackward), dispatched through the one exhaustive transition seam every action already passes through. +2: the two MoveLineUp/MoveLineDown dispatch arms in apply_buffer_action, mirroring the sentence-motion arms directly above them."

[[file_size_mark]]
file = "src/actions/format.rs"
lines = 590
reason = "The mod tests block (~470 lines) was split out to src/actions/format/tests.rs, restoring real headroom below the frozen baseline rather than raising it. The already-fenced judgment (is_fence, format.rs's own duplicate of the fence-line prefix scan) now routes through markdown::is_fence_line, the one shared owner spell.rs's fence-skip toggle also reads."

[[file_size_mark]]
file = "src/actions/overlay_nav.rs"
lines = 1076
reason = "The workspace keyboard and deep link route through `actions/workspace_nav.rs`; this file retains the intercept-chain entry and dispatcher arm. Preview opening is gated by the declared kind before its match, so an undeclared arm is inert. Project navigation shares `is_folder_destination`, direct children switch on Enter, non-path-bound projects cannot ascend above the configured workspace, and lifecycle wrappers rebuild through one owner. `descend_target` documents the one path-name rule shared by both row shapes. +11: the Keymap picker's accept-by-corpus-index arm, mirroring Date rather than label-parsing. +13: Go to Line's own accept path, the Headings branch's numeric sibling — both resolve through the same shared jump owner (`Effect::JumpToLine`) rather than repeating caret/fold/scroll work. +42: the mid-query gate — one owner deciding whether `ForwardChar`/`BackwardChar`/`LineStart`/`LineEnd` read as the list-nav overloads or the query field's own char motion / Home-End, checked once ahead of every arm below it. +40: `accept_move_dest`, Move's own bespoke accept — its contextual action rows (`Move here` commits, `New folder…` creates-and-moves, a folder row descends like `→`) made the shared `dest_value`/\"highlighted folder, else the level\" grammar ambiguous, so it left that family for one owner of its own three-way dispatch; `dest_value`'s doc narrowed to the two siblings that still share it. Insert Table's dimension-picker key ownership (arrow sculpt, typed-digit parse, Enter-commits, Esc-cancels) joins the other per-overlay-kind intercepts this file already owns."

[[file_size_mark]]
file = "src/app.rs"
lines = 1383
reason = "The clobber-notice predicate MOVED OUT to `app/files/external.rs` beside the constants it matches (-5); what remains is the disk-baseline construction at the one exhaustive `App::new` site now taking a `crate::external::Seen` rather than a bare stat (+4), the one-line relaunch-recovery call after the startup buffer decisions settle (+2), and a test-gated re-export of the notice constant so prose laws read the running app's own string instead of a retyped copy (+3). Net +3. +32: the theme-font CHEAP-RESHAPE threshold, its compile-time guard and the measured provenance that places it, beside the debounce constant this file already owns as the one scheduling-constant seam; plus the reshape-completion stamp on `ThemeSettleInFlight` that lets the settled-present fold measure the scheduling phase. +6: the launch buffer's project root is enrolled into the new working set inside `App::new`, after the scratch-stash and session-restore decisions settle, so every capture door gets it for free. +100: the clipboard bridge's `ClipboardBackend` trait (native-only, implemented for `arboard::Clipboard` and a test-only `FakeClipboard`) so a buffer-switch/clipboard law can substitute a hermetic stand-in, plus the shared `clipboard_disabled` helper both construction arms call. +4: `App::new`'s `linux_emacs_meta` gate threaded to the live keymap constructor alongside the existing config/keep plumbing. +10: the drag-scroll re-arm's one scheduling-constant seam (`DRAG_SCROLL_TICK`), beside the other debounce/tick constants this file already owns. +1: `_menu_bar`'s doc comment now names its third live reader, `refresh_accelerators`."

[[file_size_mark]]
file = "src/app/apply.rs"
lines = 917
reason = "The conflict workspace's summon reaches the live effect interpreter's one `PersistenceEffect` match and the one `apply_overlay_accept` roster, both exhaustive by design (+6). +34: `sync_theme_font_measured`, the ONE reshape door every live theme-font path runs through. The split-reach preview path shapes the presentable prefix immediately; the quiet-settle owner finishes only the latest highlighted world's tail. +11: `refresh_kill_from_clipboard`'s duplicate-read skip now also requires the active buffer's own kill ring to already equal the OS text, not just the App-global last-written stamp, so a buffer switch cannot leave a Paste reading a kill ring that stamp never actually hydrated. +11: `App::apply_keymap_flavor` — sets the config field, persists it, rebuilds the live keymap, refreshes an open Settings menu, and emits the resulting-flavor notice, replacing the old always-toggling handler. +1: `Previous`/`CloseActive` need two dispatch arms where there was one, now that Finish file's last leg is its own effect. +12: Reveal in File Manager's `Effect::RevealInFileManager` arm reuses the export write's own `reveal_path` gate rather than a second implementation, with a wasm counterpart to keep the match exhaustive on the browser target too. +2: Go to Line's `goto_line_count` field threads through the two `OverlayState` construction sites that gather it from the live buffer. +1: Save a Copy's named destination reaches the one exhaustive live effect interpreter. +1: the typed resolved-paste continuation reaches the one exhaustive live effect interpreter. +12: the expanded working-set panel's own Esc-collapse arm, alongside the shared core's ordinary Cancel rather than instead of it — the panel is not part of `workspace_state`'s summoned-overlay precedence ladder (it draws in the margin, not as an overlay), so it needs its own arm here. +8: a held row drag aborts BEFORE that same Esc-collapse runs, so a stray Escape mid-drag can never replay a click or reorder against a row index the collapse just invalidated. +1: the exhaustive `apply_buffer_effect` match gains one arm, `BufferEffect::OpenScratch => self.open_scratch()`. Insert Table's palette dispatch reaches the one exhaustive live effect interpreter beside the other formatting commands. Item 527/528's palette dispatch reaches the one exhaustive live effect interpreter beside the other formatting commands, threading the new keymap flavor field."

[[file_size_mark]]
file = "src/app/files/settings.rs"
lines = 350
reason = "`App::setting_toggle` and the replay interpreter both route through `crate::settings::flip_toggle_global` in `src/settings/toggle_core.rs`, preventing separate read/negate/set matches from drifting. +2: the live Keymap-picker apply path also threads `linux_emacs_meta` through the same setter chain the keys/keep fields already use. +1: `reload_config` now calls `refresh_native_menu_accelerators` after the config mirror settles, so a config-buffer reload's `[keys]` changes reach the native macOS menu the same way a rebind-menu commit does."

[[file_size_mark]]
file = "src/app/gpu.rs"
lines = 634

[[file_size_mark]]
file = "src/app/input/keys.rs"
lines = 508
reason = "The event-to-present flight recorder needs its winit-receipt and keymap-resolve points at this one dispatch seam; there is no second place a press is received or resolved. Item 527/528's which-key panel now falls back to seeded chords when a command has no config override, reading the same active_seed_tables selection point dispatch uses."

[[file_size_mark]]
file = "src/app/input/mouse.rs"
lines = 1189
reason = "The wheel-region predicate at the one pointer entry point stopped naming a single overlay kind and asks the shape-neutral question instead (+7, all of it the comment recording why the kind check was wrong). +3: the working-set stack's left-click-to-switch route, enrolled in this file's own input-substate privacy law. +11: `sync_cursor_icon` gains the stack-row hand-cursor arm, hit-tested through the same `gutter_stack_hit` the click path already uses. +21: cursor movement and leave route one shared gutter-stack hit through the hover state, so the close mark follows exactly the row under the pointer and clears when the pointer exits. +7: the find/replace panel's hit-test is computed once and shared by the case-toggle, field-cell, and dead-chrome cursor arms instead of only the case toggle. The pointer-derived state resync (fold/stack hover, cursor icon) is pulled out into pointer_sync.rs behind one owner, netting this file down even after the panel/case-toggle growth. +84: scroll-on-drag's App-level owner — `drag_scroll_primed` (the one dragging+armed+overshoot-active predicate both `step_drag_scroll` and `App::schedule_drag_scroll` gate on, so a stationary unarmed press can never be mistaken for a held edge-drag) and `step_drag_scroll` itself, the ONE seam both `on_drag` (per-`CursorMoved`) and the idle-timer re-arm drive, so a held drag past the edge keeps scrolling even once the pointer stops generating move events. +8: `on_mouse_wheel` gains the swallow-not-scroll guard over a summoned modal card, ahead of every existing branch. +44: the query field's own click-to-place — `overlay_click` resolves a query-line press to a char index and arms `on_query_drag` for the rest of the gesture, released by `on_mouse_input`'s own `query_drag` arm. +58: the six-item merge round's own reconciliation — the wheel guard, query-drag release, drag-scroll tick reset, and panel hit-test share the one pointer-button/wheel dispatch after independently-authored diffs to the same functions landed together. +36: a wheel gesture landing inside the working-set stack's own bounds routes to the expanded panel's scroll instead of the document's, sharing the existing wheel-region predicate's early-exit shape. +10: a press-armed row drag owns the pointer outright once armed (no row hover/switch fires under the travelling pointer), and aborts on cursor-left rather than replaying a stale click or reorder on the next press. DOWN 1 (net): `on_mouse_wheel` decomposes into a short dispatcher plus five private phase helpers (`try_working_set_panel_scroll`, `try_horizontal_table_pan`, `try_overlay_wheel_route`, `try_zoom_wheel`, `scroll_document_wheel`), mirroring this file's own `on_press`/`on_drag` shape; the decomposition's structural cost (new signatures/braces/doc lines) is offset by two real simplifications surfaced while extracting it — `try_overlay_wheel_route`'s add/sub branch collapses to `saturating_add_signed`, and `scroll_document_wheel`'s two match arms share one tail instead of repeating it. Insert Table's dimension-picker click-to-pick-and-commit joins the other overlay-kind pointer hit-tests this file already owns. +38: Insert Table's dims-grid gains pointer-Moved hover, routed through the same table_dims_pick write the click and keyboard arrows already use, armed against a stationary duplicate CursorMoved reverting a keyboard sculpt."

[[file_size_mark]]
file = "src/app/session.rs"
lines = 526
reason = "Session restore enrolls every surviving background file into the working set, in session-file order, before the idempotent active-file enrolment lands — closing a real gap where a multi-file session silently collapsed to one margin row on restart. session_buffers() now reads the working set's own order rather than the registry's MRU order, the one honest round trip a drag-reordered stack can pass through a restart."

[[file_size_mark]]
file = "src/app/stats.rs"
lines = 453

[[file_size_mark]]
file = "src/app/viewstate.rs"
lines = 601
# 596 -> 598: Fresh identity keys the per-document debounce ledger at this one
# exhaustive live ViewState projection site.
reason = "`sync_view` is the sole exhaustive `ViewState` construction site, so every render datum must be spelled out here. It carries the fold chevron's real fold set, the summoned picker's secondary location, the persistent changed-elsewhere affordance, photographable/findable notices, and world-authored diagonal-mark weight without creating a second non-exhaustive construction path. +11: the working-set stack's row count and its own fold-independent geometry, the one new field a margin widening to more than one row needs. +7: mirrors config_keys/config_linux_keep, the menu chord column's config-awareness plumbing. +13: the bottom identity's `gutter_project` reads the OPEN file's own remembered root through `working_set().active_root()` rather than the ambient switched-to project, falling back to the old ambient read only when the working set has no active file. +2: the gated pointer-derived resync's one call site — `sync_view` is where scroll/zoom/the active document are settled for the frame, so it is the seam every non-`CursorMoved` door (wheel scroll, wheel zoom's deferred reflow, keyboard-driven scroll) reaches without a per-door special case. +10: `sync_view` auto-collapses the expanded working-set panel whenever an overlay opens, so the two summoned surfaces can never be up at once. +1: mirrors the query field's own seeded selection range from `OverlayInfo`/live query into `overlay_query_selection`, beside the other query-caret mirrors. Insert Table's overlay_table_dims field joins the exhaustive ViewState construction site. Item 527/528's config_keymap_flavor field mirrors config_linux_keep's existing trail, so the menu bar and palette read the same flavor dispatch resolves against. +3: mirrors the Asset Cleaner's selected-orphan path into `overlay_asset_preview` through the same `selected_asset_path()` accessor other summoned-surface mirrors already read."

[[file_size_mark]]
file = "src/app/window.rs"
lines = 624
reason = "The external-change check at focus return belongs at the winit focus callback. `fold_settled_theme_switch` owns the settled-present transaction and its scheduling/acquire phases. Preview redraw deliberately leaves superseded off-screen shaping debt to the latest-wins quiet settle. +4: redraw samples one monotonic instant before advancing and preparing the living theme-selection band. +7: focus loss clears pointer-derived hover state through the same owner cursor-left uses, so a fold chevron or a working-set close mark can never survive behind another app."

[[file_size_mark]]
file = "src/background.rs"
lines = 280
reason = "The grouped per-frame AmbientUpload keeps the prepare call below the argument ceiling; Globals remains the uniform's single Rust mirror, including Kite's forward-travel scalar. The one construction site each pipeline family has now sits inside gpu_cache::render_pipeline's build closure — the seam that makes a GPU program once-per-process instead of once-per-instance, which is what the hosted-macOS gate wedged on. The growth is that closure's own indent (+1). Organic's field-translation drift is replaced by a companion breathe phase, one new Globals/AmbientUpload scalar each (+5)."

[[file_size_mark]]
file = "src/buffer.rs"
lines = 764
reason = "DOWN 67: save, save_owned, and save_into_folder moved to buffer/save.rs beside persistence ownership; Buffer keeps the data/editing core. +1: the URL plausibility owner is enrolled as a private buffer submodule. +3: declares the sentence submodule and re-exports its boundary functions, beside the buffer's other private submodule owners. +6: declares the reorder submodule (Buffer::move_line_up/move_line_down's owner) beside the buffer's other private submodule owners."

[[file_size_mark]]
file = "src/buffer/edit.rs"
lines = 526

[[file_size_mark]]
file = "src/buffers.rs"
lines = 286

[[file_size_mark]]
file = "src/capture.rs"
lines = 105
reason = "Each schema addition includes its `/N` ledger row and moves with `SCHEMA_VERSION`, enforced by this file's law. Planned overlay geometry serializes in `plan_sidecar` rather than expanding the single writer, and `redact` runs over finished artifact text at that writer so a future path-bearing field cannot opt out. +5: the working-set stack's `/204` ledger row (SCHEMA_VERSION 203 -> 204, buffers.files[]/active_index). +3: `/205` records the held HUD's optional selection figures. +2: `/208` records honest document absence and its start actions. +1: `/209` records the query field's own char-index caret. +7: the /210 ledger row for the windowed picker's positional count cue (overlay.window.cue_above/cue_below). +6: the `/211` ledger row for `overlay.asset_preview`, the Asset Cleaner's live preview panel's own Verify seam."

[[file_size_mark]]
file = "src/capture/modes.rs"
lines = 550
reason = "The headless snapshot rebuilds secondary picker location through `facets::strip_location` rather than serializing a duplicate fact, and `gutter.changed` sits at the sole `base_viewstate` consumer. It also carries photographable/findable notices, world-authored diagonal-mark weight, and caret-kind resolution through `from_mode`. +6: `overlay_query_caret` now threads the real caret through from a live-replay `OverlayInfo`, falling back to the query's own end only for a hand-built synthetic override. +1: mirrors `opts.overlay.query_selection` into `overlay_query_selection` for the headless capture path. Insert Table's overlay_table_dims mirrors the same field into the headless capture path's own separate ViewState construction chain. +3: the headless replay path mirrors `opts.overlay.asset_preview` into `overlay_asset_preview` the same way `sync_view` does, so an ordinary `--keys` capture reaches the preview panel."

[[file_size_mark]]
file = "src/capture/sidecar.rs"
lines = 879
reason = "`gutter.changed` and `overlay.preview_view` belong in the sole serializer, whose no-overlay literal remains in lockstep with its populated arm. `readout_json` and `hud_json` share the count/unit owner, and the serializer also carries photographable/findable notices and world-authored diagonal-mark weight. Shrank 871 -> 857: `buffers_json` moved out to sit beside its own type, `BuffersInfo`, in `capture/opts.rs`, rather than growing this file for the working-set stack's files[]/active_index. +11: `hud_json` serializes the optional selection word/grapheme figures from the same HUD report the renderer consumes. +3: `overlay.query_caret` in both the populated and no-overlay literal arms. +8: the sole serializer gains `overlay.asset_preview` (via `asset_preview_report()`), in lockstep in both the populated and no-overlay literal arms."

[[file_size_mark]]
file = "src/caret.rs"
lines = 285
reason = "Declaration docs name the entitled scale factor for spring/damping constants, px/s velocities, and streak radius; the classification evidence belongs beside the constants the unit scan deliberately excludes. `morph_anchor_col` and `morph_row_start` take the caret's visual-row start because a wrapped row's first column is a row start like column 0; logical-line-relative anchoring draws the caret a visual row from its insertion point."

[[file_size_mark]]
file = "src/caret_glyph.rs"
lines = 539
reason = "+2: the glyph pipeline owns the Filled block's named zero-dilation knockout constant and distinguishes true-weight legibility restoration from Morph's authored accent dilation."

[[file_size_mark]]
file = "src/commands.rs"
lines = 841
reason = "The review row joins the two resolutions in `row_hidden`, the one place a runtime palette gate is written down (+4). +13: Command gains a `description` field, and the added lines are its doc comment explaining the mechanism that makes the omission unrepresentable — Option<&'static str> with no Default impl on the struct, so all 96 literals must write the arm and a 95th command cannot be added without one. +56: native_label_effective and menu_native_label, the one owner both the palette's join_slots_truthful and the drawn menu's chord column now share for a config-aware native label, plus their doc comments naming the shared-owner rule. +12: the new Keymap picker catalog command and row_hidden's macOS-inert arm, mirroring Command palette's own hide-on-macOS shape. +10: Reveal in File Manager / Copy File Path need a real on-disk location, so `PaletteGates` grows a `named_file` field and `row_hidden` gates both rows off it — an unnamed scratch buffer hides both rather than silently no-opping. +34: native_accelerator_chord, the Mac-only reduction of the live-keymap chord the native macOS menu's accelerator table now reads, dropping keep/convention/platform because the real AppKit menu is always Mac + Native and native_label_effective's own doc already proves keep is inert there — so the native table and the drawn bar's menu_native_label agree on the same chord by construction rather than a second hand-typed resolution; `target_os = \"macos\"`-gated since its one reader is itself Mac-only. Item 527/528's label-truth mechanism: join_slots_truthful and menu_native_label both fall back to a seeded chord via keymap::seeded_chords_for when no ordinary chord survives, plus the new flavor-parametric GUIDE generator sibling."

[[file_size_mark]]
file = "src/config/model.rs"
lines = 352
reason = "+15: `Config::effective_linux_keep` filters the emacs preset's own `C-c`/`C-v` contribution out before composing the keep-list, leaving an explicit `[keys] linux_keep_emacs` reclaim untouched — the one composition owner named in CLAUDE.md's Linux-emacs tripwire."

[[file_size_mark]]
file = "src/crashlog.rs"
lines = 546

[[file_size_mark]]
file = "src/cursor_shape.rs"
lines = 223
reason = "The item-562 merge (over_table_dims_cell) pushed this file to 1305 lines, past its own frozen baseline (1208) -- a hard ceiling no reason can waive. mod tests carved out; the split test file itself then tripped the flat 500-line ceiling with zero baseline grace (it never existed at freeze), so it further split into src/cursor_shape/tests/{helpers,basic,priority}.rs (each under 500 on its own), included/declared from one mod tests block here. Production code alone (this file) is now 223 lines, well under both the 500 limit and the baseline."

[[file_size_mark]]
file = "src/daemon.rs"
lines = 588

[[file_size_mark]]
file = "src/debug.rs"
lines = 475

[[file_size_mark]]
file = "src/durable.rs"
lines = 162
reason = "+2: the atomic create-if-absent writer is exported only on native targets, matching the Save a Copy filesystem boundary while ordinary durable writes remain shared."

[[file_size_mark]]
file = "src/export/docx.rs"
lines = 690
reason = "Word export keeps its package writer and semantic block emitters together. Footnotes add numbered reference runs, definition paragraphs, relationships, and a mechanically wrapped byte-identical styles roster."

[[file_size_mark]]
file = "src/export/model.rs"
lines = 701
reason = "The one semantic export model now resolves footnote labels, first-reference numbering, repeated references, definitions, and missing-label fallbacks beside the other Markdown block/inline normalization. Item 530's smart-punctuation wiring in push_text: apply_smart_punct runs before the highlight scan, sharing the same mapping owner the live WYSIWYG conceal paints."

[[file_size_mark]]
file = "src/frontmatter.rs"
lines = 524

[[file_size_mark]]
file = "src/fs.rs"
lines = 334
reason = "+3: the no-replace filesystem operation and its race-test fixture exports are native-only, keeping browser builds exhaustive without pretending to own a native destination."

[[file_size_mark]]
file = "src/history/store.rs"
lines = 591

[[file_size_mark]]
file = "src/index.rs"
lines = 890

[[file_size_mark]]
file = "src/keymap.rs"
lines = 24
reason = "+3: `KeymapState::seed_defaults` gains the `linux_emacs_meta` gate deciding whether the Linux-only classic Meta layer seeds, at the same construction site the keep-list already threads through. Item 527/528's KeymapFlavor re-export, read by every label surface alongside Convention."

[[file_size_mark]]
file = "src/keyspec.rs"
lines = 864
reason = "`undo_chord_label`: Undo has no palette command to resolve a label from, and the restore notice has to name a key that fires on the reader's own convention (+17), and its law resolves each spec through the real keymap on its own convention rather than trusting a comment (+33)."

[[file_size_mark]]
file = "src/lava.rs"
lines = 924
reason = "The one construction site each pipeline family has now sits inside gpu_cache::render_pipeline's build closure — the seam that makes a GPU program once-per-process instead of once-per-instance, which is what the hosted-macOS gate wedged on. The growth is that closure's own indent (+1)."

[[file_size_mark]]
file = "src/main/args.rs"
lines = 54
reason = "DOWN 920: `parse_args`'s own body (the argument-token loop, the hidden bench/soak short-circuit, CLI validation, hermetic scenario filesystem setup, config load, sticky-preference resolution, capture-opts assembly, final `Mode` construction) moved to `args/parse.rs` and its own `loop_flags.rs`/`validate.rs`/`finish.rs` phase files, the same submodule shape this file already used for `flags.rs`/`modes.rs`/`parsers.rs`. What remains here is the module-declaration shell plus `resolve_default_folder`, the one function other modules reach through `crate::args::`."

[[file_size_mark]]
file = "src/main/run.rs"
lines = 838
reason = "Item 527/528's config_keymap_flavor threads through the headless replay's own ViewState construction, mirroring the live App."

[[file_size_mark]]
file = "src/markdown/spans.rs"
lines = 25
reason = "The `Tag::Heading` Start arm's else branch now promotes a qualifying SETEXT dash underline to a real `MdKind::Rule` span over exactly its own bytes (inline, no second single-call-site helper — the file was already sitting at its frozen ceiling), `is_thematic_break`'s doc comment is re-authored now that the setext case is no longer a false positive under the decided rule, and several pre-existing doc comments in the same neighborhood are tightened to make room without losing their content — net -2 lines even after the new logic. +2: a `#[cfg(test)]` re-export of `bare_url_ranges`/`bare_url_split` for the bare-URL detector's own unit tests, mirroring `push_highlight_spans`'s existing test-export pattern. Item 530's re-export of SmartPunctKind/smart_punct_ranges/smart_punct_runs/apply_smart_punct from spans/detect.rs, beside the module's other public re-exports."

[[file_size_mark]]
file = "src/mas.rs"
lines = 661

[[file_size_mark]]
file = "src/menu.rs"
lines = 1133
reason = "The three Export rows' File-menu label now diverges from their catalog command (dropping a stale ellipsis with no surface behind it — the shorthand `r()`/`ri()` constructors can't express that, so each grew into a full `Routed` literal), plus the label divergence law's `EXPLICIT_MENU_LABELS` entries and doc comment, and a two-line `#[cfg(test)] mod ellipsis_law;` declaration for the new law itself (+29). +25: the NATIVE_PANEL_IDS table and its one predicate, declared as DATA beside the roster it indexes so ellipsis_law can sweep the AppKit-panel door from any host — a literal buried in the macOS handler is unreadable to the law that must hold both doors to the same promise, which is exactly how `Browse files…` passed for the wrong reason. +10: `mod chord_truth;` and its own roster x flavor law proving the drawn menu's chord column agrees with real dispatch under both keymap flavors. +4: the real Command palette menu item, `awl.command_palette`, replacing the uncatalogued hand-written resolver arm. +7: Save a Copy joins the one File-menu roster, native-panel roster, and exact command-resolution laws. +6: `resolve`/`item_chord_for_id` now share one `routed_command_for_id` lookup instead of duplicating the id-to-command scan, so the native accelerator builder has a third caller of the same owner instead of a fourth hand-typed one. Item 527/528's drawn menu chord column falls back to a seeded chord via the same active_seed_tables query the palette and GUIDE generator read."

[[file_size_mark]]
file = "src/menubar.rs"
lines = 391
reason = "+163: the menu bar's own pixel-space declarations. Six pads move off bare `f32` — four to `Logical`, two to `Physical` — and each carries at its declaration the usage evidence that decided which, following `CANVAS_INSET`'s precedent: a classification whose reason lives in a test is the one that gets re-guessed, and getting one backwards multiplies it by DPI forever with a law asserting it is right. `EDGE_BLEED_PX` and `FLUSH_EPS` are the two Physical ones, and both reasons name the device-fixed quantity they answer to (the fill shader's ~1px `smoothstep` feather and a `CORNER_RADIUS` uploaded once, unmultiplied). Plus `AWL_MENU_BAR_FORCE`'s memoized reader, its pure classifier, and `platform_default(is_macos)` — the platform fork extracted as a function taking its own argument, which is what makes both arms gradable from one host instead of only the one that compiled the test."

[[file_size_mark]]
file = "src/overlay/capture.rs"
lines = 595

[[file_size_mark]]
file = "src/overlay/kind.rs"
lines = 472
reason = "A new `OverlayKind` variant crosses seven exhaustive per-kind matches in this one file — name, accept disposition, row-meta roster, hints, empty copy, title, row split — each of which exists so a new kind cannot inherit an untested path (+20). +45: `keeps_backdrop_crisp` becomes the ONE owner of which pickers keep the document sharp behind them, as a wildcard-free 21-arm match -- the eighth exhaustive per-kind match this file carries. It replaced two independent deciders, one of which keyed on the mode's own SPELLING (`o.mode == \"theme\" || o.mode == \"caret\"`) in the capture door, so the live App and a capture could disagree about the same kind. A comment naming a stale set was the symptom; two deciders were the cause. +49: `previews_live_document` is the ninth wildcard-free per-kind match this file carries. It is the subject of the equivalence law that replaced a subset law: the frost exemption and the live audition are ONE decision, so a kind repainting the page behind its card must not frost it, and a kind repainting nothing has no exemption to claim. +9: the detail stage's Back cell is appended by one owner rather than authored per kind, so a kind cannot name a key its own intercept does not honour. +22: `project_flat_hint` filters Project's own kind-level cells by GLYPH against `workspace::ERASE_GLYPH` rather than re-listing them, so a hint cell added elsewhere cannot be silently dropped here. +78: OverlayKind::ProjectBrowse crosses the file's exhaustive per-kind matches by design — that is the mechanism stopping a new kind inheriting an untested path — plus its hint arm, routed through the existing footer owner rather than a second answer. +10: OverlayKind::Keymap crosses the same exhaustive per-kind matches, grouped with Dictionary/CjkLang/Date (no live audition, unlike Caret/Theme). +14: OverlayKind::Credits crosses the same matches, reusing TimelineOverComparison's shape with a degenerate one-row primary column. +9: `row_meta_roster` gives MoveDest its own arm (`Plain`/`MoveHere`/`NewFolder`) split out of the shared single-`Plain` arm the other destination navigators keep, and `is_folder_destination`'s doc now says which members still share `↵`'s commit-the-highlighted-folder grammar and which (MoveDest) does not. A new OverlayKind::TableDims variant crosses the same seven exhaustive per-kind matches this file already owns."

[[file_size_mark]]
file = "src/overlay/nav.rs"
lines = 501
reason = "`item_sections` gives a workspace one category name in its rail and lets `refilter` own bucketing. The remembered dotfile exemption ensures a chosen project remains in Recent even when directory listings hide dotfiles. +16: Go to Line's `selected_is_line_jump` accept-path gate, the numeric sibling of `selected_is_heading` — true only when the highlighted row is the synthesized line-jump row — plus its `RowMeta::GotoLine` arm in the shared line-target and label matches. +43: the query field's own caret door — `query_at_rest`, `query_char_left`/`query_char_right`, `query_home`/`query_end`, `query_set_caret` — the one seat both the mid-query gate and a pointer click/drag read. +8: `query_set_caret` routes a click through `rename_edit_set_caret` first when a rename edit is active, so a click can't be overwritten by the next keystroke's mirror. +14: `selected_asset_path()`, the Asset Cleaner preview's one input — the highlighted row's own resolved absolute path, gated on `RowMeta::Asset` rather than a kind check."

[[file_size_mark]]
file = "src/overlay/state.rs"
lines = 930
reason = "The conflict card's payload field on the one `OverlayState` struct and its one exhaustive `new_marked` initializer (+5). +10: `BackKey` is the one owner of what goes back from a detail stage — erase when free, focus while a live query owns the erase key, nothing during a value edit — so the footer and the intercept cannot disagree. +23: `foot_hint_scoped` takes the `Bind` the card was entered under, because whether Backspace ascends is a property of the JOURNEY and not of the kind — the two switch-project surfaces are the same `OverlayKind`. The zero-argument form remains, delegating with `None`, so roughly thirty bare-card call sites keep the correct default: a card with no journey is flat. +68: RowMeta::ProjectDoor and RowMeta::terminal, which merges the add-to-dictionary row's always-last exemption with the door's — both act on something other than the query, so a fuzzy match on their own label must not float them above the answers. +39: `OverlayState::new_keymap` — native/emacs rows with plain-language primary labels and the concrete chord difference in the secondary column, current flavor pre-selected, sourced from the caller-gathered Config-owned flavor rather than a process-global. +33: Go to Line's one fact (`goto_line_count`, the destination buffer's total line count) and `attach_line_jump`, which appends the line-jump row as a single fixed slot refreshed live by `refilter`'s `sync_goto_line_row` step, mirroring `attach_headings`/`attach_folders`'s own opt-in shape. +29: Save a Copy's payload, truthful shared-card title/footer owner, and exhaustive initializer values keep its folder-then-filename journey on the existing overlay mechanism. +47: `OverlayState::new_move_dest`, the Move navigator's own level constructor — folders-only rows plus its two contextual action rows (`Move here` pinned first at rest, `New folder…` synced live from the query), pure over caller-gathered data so a test can build a level without touching disk, mirroring `new_project`'s own shape one level down in this same file. +15: `move_filename`, the file Move is finding a destination for — a per-summon fact the directory-rebuilt LEVEL cannot know, so `title()` reads it to name the errand instead of the generic kind title, and it survives every descend/ascend through the same `carry_level_payload_from` seam `export_format`/`save_copy` already use. +58: `OverlayState::title()` folds the current root-relative destination into the Move/Export/Save-a-Copy card title once descended (`browse_dir` was already tracked, never rendered), reusing the one existing title-building owner rather than a second breadcrumb widget. TableDimsEdit joins the RenameEdit/LinkEdit/KeepEdit/ValueEdit sub-state family as a fifth sibling, with its own clamp/parse/pick verbs."

[[file_size_mark]]
file = "src/peek.rs"
lines = 550
reason = "The STARTER SIX table's Command palette row comment updated to reflect it is now a real catalog command rather than a synthetic keytoken."

[[file_size_mark]]
file = "src/probe.rs"
lines = 667
reason = "Two test-only doors (arm/disarm) so a law can assert over the recorder's own lines rather than a reimplementation of them; the recorder is the one owner of every trace print. The inline `mod tests` is carved out to the sibling `src/probe/tests.rs`, which `production()` exempts by design — its own comment notes that counting a sibling `tests.rs` as production would defeat the point of carving one out — so this mark now measures production code alone and ratchets 825 -> 628. +39: the flight recorder publishes prepared phase and input-to-settled theme-band timing through its single trace owner."

[[file_size_mark]]
file = "src/prosediff.rs"
lines = 1248

[[file_size_mark]]
file = "src/range.rs"
lines = 559

[[file_size_mark]]
file = "src/render.rs"
lines = 2921
# 2876 -> 2881: item 540's table-dims picker gained a caret-anchor field
# (overlay_table_dims_hint_w, the shaped hint width measured once per
# set_view so the geometry function can size the card without a
# FontSystem in hand).
# 2867 -> 2876: the declared GPU-state floor now owns the empty-cell wash
# pipeline and the immutable faceted-hug roster/memo state.
reason = "The footer plate uses the notice channel's one-pixel rim so it remains visible. `Millis` and `progress_per` keep durations out of pixel multiplication by type, while declaration docs name the entitled scale factor for page/caret pads, page-frame strokes, and spell gaps. `HudDefaults`, `WhichKeyDefaults`, and `DebugDefaults` give inert field groups exhaustive defaults beside their sole `TextPipeline` owner. Shape reach rejects unshaped-line sentinels, and `Metrics` carries `dpi` beside zoom scale so a logical floor does not vary by display density. `CARET_INK_PAD_W` gained a doc comment naming its rationale and the near-zero vertical headroom finding that kept the tuning to width alone. +7: the working-set stack plate's own row-height and inset constants, named beside the other margin-row geometry this file already owns. +10: the menu chord column's config-aware label plumbing (ViewState mirrors of config_keys/config_linux_keep). +4: inert hover defaults and mirrored row state keep the production close affordance explicit in the exhaustive pipeline state. +1: the shared overlay-material module is declared beside the other data-driven chrome owners. +14: the streaks card's drawn rect and text origin join the other per-frame HUD state, named beside `HudDefaults`. +30: the format popover's hover-ring pipeline field and the inline-image resize-handle's hover-grip pipeline field, declared beside their sibling `TextPipeline` fields with doc comments naming what each draws and why — a hairline stroke and a small filled mark that can never disagree with the hit-test each mirrors. +11: the row-drag insertion hairline's own field pair (the drop-slot index and its drawn plate), mirroring gutter_stack_hover's own pointer-only, ViewState-free shape. +11: the fold-chevron mark grew from a two-quad pair to a per-mark rotated-glyph pipeline (RotatedLabelPipeline slots + a content-addressed mask cache), replacing the retired quad geometry. +13: the Rename minibuffer's seeded-stem selection wash -- its own `SelectionPipeline` field (`panel_query_selection`) and the `overlay_query_selection` range it reads, doc-commented beside the other per-frame query state. Insert Table's table_dims_cells SelectionPipeline field joins the other per-frame overlay pipelines this file already owns. Item 527/528's config_keymap_flavor field joins the other per-frame config mirrors this file already owns. +3: `ORNAMENT_WEIGHT` is the one shared authored 500 request used by section-break and About ornament shapers. +5: `gutter_close_hover_plate`, the close-zone hover plate's own `SelectionPipeline` field, declared beside its sibling `gutter_stack_plate` with a doc comment naming the shared rect it draws (the exact zone the hit-test accepts, never a second geometry). +19: the Asset Cleaner's live preview panel — three dedicated pipelines (panel background, image quad, text) that never share the float-panel trio the search/caret-preview panels claim in the same frame — plus the `overlay_asset_preview` mirror field. +17: `frame_clock::Activity::TableDimsHover`'s eased fractional (rows_f, cols_f) target state, mirroring `overlay_band_drawn`'s own juice/Reduce-Motion discipline."

[[file_size_mark]]
file = "src/render/benchsuite/corpus.rs"
lines = 560

[[file_size_mark]]
file = "src/render/benchsuite/report.rs"
lines = 598

[[file_size_mark]]
file = "src/render/benchsuite/scenarios.rs"
lines = 582
reason = "Go to Line's `goto_line_count` field on the bench fixture's `OverlayState` construction, defaulted to 0 (no buffer known) like every other bare-constructed bench overlay. Item 527/528's config_keymap_flavor joins the other ViewState fields this bench fixture already threads."

[[file_size_mark]]
file = "src/render/caret.rs"
lines = 932
reason = "+31: `effective_caret_look` and `caret_row_start_col` — the one owner of the ink-caret Morph→Block fold, and the caret's own visual-row start. Both exist because the fold and the row-relative Morph rules had been spelled at their call sites: the fold in the paint path only (so geometry anchored a Morph caret a column back on a world that paints a Block) and the degrade against the logical column only (so a wrapped row's first column reached the row above). Owners, not helpers — the bypass is what the bug was. +35: caret_cell_vertical's Block-only branch to the ink-envelope box (Morph and the mono/glyphless paths are untouched, bypassed before this code runs)."

[[file_size_mark]]
file = "src/render/chrome/hud.rs"
lines = 480
reason = "+2 net: prepare_hud and prepare_streaks_card consume the shared plan_float_card and plan_streaks_card geometry records instead of owning card arithmetic. +1: the HUD re-exports its selection-figure drawing owner. +27: `streaks_card_rect`/`streaks_hint_row_rect` publish the drawn card's rect and its first line's rect (the ←/→ paging hint) — the one owner of both `prepare_hud`'s and `prepare_streaks_card`'s set/clear of that state, read by the click hit-test and by render-tier laws."

[[file_size_mark]]
file = "src/render/chrome/menubar.rs"
lines = 545
reason = "+6: the dropdown plan takes the frame's scale so its minimum width resolves at the pixel-space boundary, and the two dial constants gain the doc lines that record their unit family. +2: the dropdown plan now also takes the config keys/keep slices it threads into the chord column. Item 527/528's drawn menu reads config_keymap_flavor to resolve a seeded chord fallback, beside the config_linux_keep it already reads."

[[file_size_mark]]
file = "src/render/chrome/mod.rs"
lines = 1259
reason = "+1: re-export PANEL_MARGIN and PANEL_PAD so panel shaping and rectangle layout resolve one physical panel inset owner. +6: re-exports the working-set stack's gutter submodules so the chrome pipeline resolves them through the same module surface as every other gutter row. +1: re-exports the new gutter_hit pointer module beside them. +1: the shared chrome plan carries the hovered working-set row to the one draw owner. +1: selection HUD figures are re-exported through the shared chrome surface. +31: card_shape_texture becomes the single corner-mask owner every console layer (panel/scanline/placard) resolves its chamfer through, replacing overlay_material.rs's independent hardcoded zero. +13: the float-panel border's outset moves off an inline literal onto a named, documented `FLOAT_BORDER_RING_PX` constant, so the docked-tab seam merge can size its own overlap against the ring's real width instead of guessing it. +1: declares the gutter's own lava frost seeds as a sibling module, split out to keep gutter.rs under its production ceiling. +1: declares the new overlay_query_field module beside its sibling overlay_draw/overlay_rows, split out to keep overlay_draw.rs under its own file-size mark. +151: re-exports the count-cue arithmetic (window_edge_counts, resolve_window_and_cue, OverlayGeom's cue_reserved/cue_above/cue_below split) through the shared chrome surface, plus the new fit_rows submodule split out of overlay_rows.rs to keep it under its own file-size mark. Insert Table's dimension-picker geometry module is declared beside the other per-overlay-kind chrome owners. +1: declares the new `asset_preview` submodule beside the other per-overlay-kind chrome owners."

[[file_size_mark]]
file = "src/render/chrome/outline.rs"
lines = 925
reason = "The persistent outline publishes its interactive geometry to the shared toast-obstacle roster so authored anchors cannot overlap active outline chrome."

[[file_size_mark]]
file = "src/render/chrome/overlay.rs"
lines = 939
reason = "+1: the Range rail's read hands the accessory column's ANCHOR and its flow to `rail_geom`, so a mirrored cluster seats its rail on the other side of its value through the same owner. +34: eight bare geometry literals inside the two geometry owners become named constants of a declared unit family (the Pane text inset, the card's pad / margin / top drop / anchor drop, the spell popup's pad / window breath / word gap), each with the doc line that records why, plus the resolved locals the owners read them through and `overlay_leading`, the third term of the row pitch. DOWN 27: the roster-width measurements moved to `chrome/roster.rs`. The overlay owns the one shared row plan and pointer geometry; the measured diagonal span and Range rail read remain at that seam alongside the workspace dispatch and row-offset inputs. The lens strip's Y band left for the scene planner. The `Rules` PROTOTYPE (the quiet fourth list composition, organised by absence) takes two of this file's three list-style match arms: the row AIR between rules rides the one row-pitch owner `overlay_row_gap` exactly as a `Bars` plate gap does, and the GUTTER a selection mark hangs in rides `overlay_text_hpad`. Both are the seams that already exist for exactly this question, so the growth is the two arms and the comments saying why a rule's air is the same quantity as a plate's gap (+9). +13: the rail inset and card-box policy take `dpi`, since the inset resolves the cap that seats the card. +50: `overlay_query_char_at`, the click-to-place pointer inverse of `overlay_query_caret_box` — same containment box as `over_overlay_query`'s I-beam gate, so a click can only land where the I-beam already promised a caret. +42: `overlay_header_gap` splits into a card arm and a `overlay_header_gap_workspace` arm sharing one private resolver, so a unified-pane taste dial (`OVERLAY_QUERY_BEAT_UNIFIED_PANE`) reaches only the card family; `overlay_row_plan` derives `billed_header_rows` from the active facet style once and threads it into the row-plan input, since a docked facet strip's own header box charges no row budget of its own. +42: the six-item merge round's own reconciliation of the query-drag and billed-header-rows diffs, both touching `overlay_row_plan`'s input construction and its callers. +125: the positional count cue's flat-family geometry -- window_edge_counts, the scroll-invariant cue_reserved reservation (resolve_window_and_cue), and the starvation-degrade arm (flat_window_without_cue/flat_card_h_without_header_gap) that drops the cue before overrunning the canvas, plus placard_vetoes closing the frost-hue law's card-growth interaction. Insert Table's overlay_geometry arm joins the spell/workspace/theme dedicated-kind arms this file already owns."

[[file_size_mark]]
file = "src/render/chrome/overlay_draw.rs"
lines = 525
reason = "The footer plate earns the notice channel's own one-pixel rim, which is what lifts it from delta-E 1.91 to 54 on Cassowary -- a plate nobody can see is not a plate. +2: parking the new rim alongside every other overlay pipeline, so a frame that does not draw it cannot leave last frame's quad on screen. +2: the docked facet strip contributes its single dedicated text area through the shared overlay upload owner. +1: a query-caret comment split to its own line under the column limit. Insert Table's dimension-picker grid draw joins the other overlay pipelines this shared upload owner already serves. +8: `prepare_asset_preview`/`park_asset_preview` join the shared overlay prepare/park pass, drawn after the card so painter's order never hides the preview."

[[file_size_mark]]
file = "src/render/chrome/overlay_rows.rs"
lines = 231
reason = "+15: every range rail now computes its OWN thumb ink and uploads through `prepare_multicolor`, instead of one frame-wide `thumb_ink` painted onto every rail by a shared `set_color` -- a rail that was not the selected row wore the selected row's ink. No new pipeline: `prepare_multicolor` already carries per-instance colour for the writing-streaks heatmap, so this reaches for the existing mechanism rather than a second one. The track stays uniform."

[[file_size_mark]]
file = "src/render/chrome/overlay_shape.rs"
lines = 909
reason = "The chord column's glyph alignment is the accessory column's own flow rather than Right outright, so a mirrored cluster's chords hang on the end they are seated at. The placard family is chrome's annotated PHYSICAL exception and each of its four constants carries the reason it is one — the wordmark is frame, not text, and `placard_size_is_window_scaled_not_zoom_scaled` fails by name if that changes. `push_beat_spacer` emits the query beat as its OWN glyph-free line rather than as line height on a line that has glyphs, because cosmic-text centres a glyph run in its box and a folded beat otherwise draws the query field's text half a beat below its own bar; it is a free function so the flat shaper itself stays under the 100-line bar. Faceted diagonal rows must shape against their measured side territory and re-fit real glyph widths before the accessory column is granted, so the row plan is threaded to the two shapers: the inflated header line's height IS the query field's planned box (read, not re-summed) and the secondary column's leading-empty count comes from the same object as its upload origin. The content-hug measurement and the two roster widths it reads live in `chrome/roster.rs`, not here. +10: `right_bind_lines` leads the first secondary label with exactly `header_rows` empty lines instead of `header_rows.max(1)`, plus the doc comment stating the buffer contract it restores (`secondary_top() + (header_rows + r) * lh == row_top(r)`). The `.max(1)` was invisible everywhere `header_rows` is 1 and dragged every secondary label -- and the ink resolved for it -- one display row down on the ONE kind that has no header line. The comment is there because the expression looks like a harmless guard against zero. +6: `overlay_row_secondary_px` takes the row plan's own `billed_header_rows` instead of reading `geom.header_rows` off the geometry, plus the doc comment naming why the box count desyncs the secondary column by one row under a docked facet strip; both `right_bind_lines` call sites in this file already led with the plan's billed count, so the reader had quietly drifted from the writer. +65: the count cue's shaping-side plumbing -- the beat spacer auto-inflating to carry the cue's text, and shaped_first_row_line's line-budget accounting for it."

[[file_size_mark]]
file = "src/render/chrome/popover.rs"
lines = 604
reason = "+5: the format popover threads a CardChamfer pair through card_shape_texture instead of a bare chamfer f32, matching every other non-owner console-layer call site. +49: the format popover's hover state (`popover_hover`, `resolve_popover_hover`, `clear_popover_hover`) and its paint — a hairline ring drawn over the hovered button's full hit region (padding plus half the inter-button gap either side), sharing one owner with the actual click-routing hit-test so the drawn acknowledgement and the clickable region can never disagree."

[[file_size_mark]]
file = "src/render/chrome/readout.rs"
lines = 577
reason = "Toast placement and collision planning live in `readout/toast.rs`, leaving `readout.rs` as the draw/report owner."

[[file_size_mark]]
file = "src/render/chrome/theme_picker.rs"
lines = 760
reason = "`theme_overlay_geometry` is an exhaustive `OverlayGeom` construction site, so every workspace field is spelled out here, and the strip band reads the row plan threaded through the two shaping owners. The grouped card's pad, margin and top drop read the one owner rather than repeating the flat family's literals, and the lens strip's chip pads, corner ticks and underline drop resolve at the boundary. Its display plan carries a THIRD line kind: a section whose label is the card's own location is not a section, so it is planned and shaped as the second level of the heading hierarchy — its own scale const, its own metrics, its own span arm in the chrome face at `muted`, and the plan arm that chooses between the two. The location-scale constant itself lives in `render::rotated_location`, shared with the rotated-rail treatment, and the inline span arm carries a `RotatedRail`-world guard so that world's own vertical cue is not drawn twice; the guard reads the exhaustive `LocationStyle::draws_inline()` rather than a direct `!= RotatedRail` comparison, so a third style (`Raked`) is a conscious decision there too. The `Rules` PROTOTYPE draws no plates anywhere, so it is deliberately absent from the lens-strip TAB PILL gate — the comment records that absence as a decision rather than an oversight, and notes that `Diagonal` sits on the yes side here and the no side of `draws_row_plates`. Grew by 22 lines (610 -> 632): the active-lens mark's `Text` and `Chips(Underline)` stroke weights moved off bare device-pixel literals onto two documented `Logical` constants resolved through `Metrics::px`, and the two draw-call array literals each grew from one line to a multi-line `rustfmt` layout once their fourth element became a method call instead of a bare float. Grew by 23 lines (542 -> 565): the DockedTab active-facet plate now overlaps the card's own top border by a named, dpi-scaled `DOCKED_TAB_SEAM_OVERLAP` so the tab's mouth reads continuous with the card ground instead of showing a border stroke through it, plus a `#[cfg(test)]` probe exposing that resolved overlap to the geometry law that asserts against it. +18: `theme_plan` gates `PlanLine::Location` on `LocationStyle::needs_plan_row` so a `RotatedRail` world's off-card cue is charged no row at all, and `theme_overlay_geometry` derives `billed_header_rows` from whether the active facet style docks its strip outside the card, so a docked strip's own header box stops billing a row of `lh` it never draws. +18: the six-item merge round's own reconciliation of the DockedTab seam overlap and billed-header-rows diffs, both touching the same `overlay_shape_theme`/`theme_overlay_geometry` arms. +40: `theme_reseat_marks` is the one owner both the shape pass and the hit-test route through for the active-lens mark's seat, called after the diagonal cluster resolves (shape time is too early to see it) -- every mark rect is now recorded buffer-local at shape time and reseated once, in one place, instead of each site computing its own upright-only `text_left` reading. +121: the count cue's grouped/theme-family geometry (theme_window_without_cue/theme_card_h_without_header_gap starvation-degrade arms) and the workspace-family exclusion this item deliberately disclosed (Settings' RailOverRows shares its row origin with a separate navigation rail, so wiring the cue in would move the rail on every content clip). +16: item 519's floating_strip_band relocation is threaded through this shaper alongside docked_facet_band, both reached via the merged relocated_strip_seat accessor, so the strip's active-mark seat stays consistent across shaping, drawing, and hit-test."

[[file_size_mark]]
file = "src/render/framebench.rs"
lines = 1026
reason = "`--bench-theme-burst` uses shared `assert_reshape_witness` checks: every `BURST_WORLDS` hop and eager theme-preview arrow step must reshape. The witness prevents a timing result from passing when no reshape occurred. Its tests live in sibling `framebench/tests.rs` because this file is at its frozen baseline and further growth requires a carve-out."

[[file_size_mark]]
file = "src/render/geometry.rs"
lines = 1320
reason = "+29: `image_handle_mark_rect`, a pure function centering the drawn resize-handle grip exactly on the point the image resize hit-test itself arms — one owner for both, so the visible grip and the clickable zone can never drift apart. +7: corrected `assemble_glyph_xs`'s doc, which wrongly claimed Monaspace's tilde ligatures were AAT/`morx`-driven and unsuppressable; the font has no `morx` table at all (item 552). +22: `xray_x_span`, `row_x_span`'s counterpart reading a table x-ray row's real `glyph_xs` instead of the concealed doc row, so a selection-touched row's band can be rebuilt against what is actually on screen."

[[file_size_mark]]
file = "src/render/layers.rs"
lines = 1356
reason = "The per-frame GPU preparation hub remains an explicit renderer exception; missing-image placeholder shaping lives in its own submodule rather than raising this ceiling. page_frame_vertical_bounds now takes an explicit stroke_px parameter so its top edge derives from the menu-bar reserve rather than the document text inset, with a doc comment naming the two quantities it must not conflate. +8: the resize-handle hover grip's own draw call, gated to at most one rect and cleared whenever no image border is hovered. +10: the preview's own decoded path is folded into `prepare_images`' per-frame keep set, so the cache doesn't evict and redecode it every frame merely because no open document references it."

[[file_size_mark]]
file = "src/render/livingband.rs"
lines = 568

[[file_size_mark]]
file = "src/render/pipeline_draw.rs"
lines = 571
# 553 -> 554: item 540's table-dims card gained one field init
# (overlay_table_dims_hint_w) in the exhaustive TextPipeline::new literal.
# 546 -> 553: the exhaustive initializer constructs the empty-cell wash
# pipeline and seeds the faceted-hug roster/memo fields inertly.
reason = "The footer plate earns the notice channel's own one-pixel rim, which is what lifts it from delta-E 1.91 to 54 on Cassowary -- a plate nobody can see is not a plate. +5: the rim's construction and its draw slot immediately before `overlay_bars`, matching the order `notice_rim`/`notice_plate` already keep. +1: the `shape_tail_owed` field initialiser in TextPipeline::new's literal, its sole construction site — the flag that records a presented frame still owes its tail. +7: the working-set stack plate's draw call, slotted beside the other gutter-row draws it shares a pipeline with. +2: threads config_keys/config_linux_keep into the menu dropdown plan's construction. +1: initializes the inert hovered-row field beside the working-set stack's other exhaustive state. +1: initializes the dedicated docked-facet strip buffer beside the overlay text buffers it replaces. +2: initializes the streaks card's rect/text-origin fields to `None` beside the other per-frame HUD state. +13: the format popover's hover-ring pipeline and the inline-image resize-handle's hover-grip pipeline, initialized beside their sibling `SelectionPipeline` construction sites. +6: the row-drag insertion hairline's own seeded pipeline, beside gutter_stack_plate's construction. +3: seeds the per-mark fold-chevron rotated-label pipeline slots beside the other per-mark construction sites. +4: `panel_query_selection`'s construction and its `overlay_query_selection: None` initializer, beside the other per-frame query-caret state. Insert Table's table_dims_cells construction joins the other per-frame SelectionPipeline construction sites. Item 527/528's config_keymap_flavor construction joins the other per-frame config field initializers. +2: `gutter_close_hover_plate`'s construction, seeded beside `gutter_stack_plate` and threaded into the exhaustive field list. +13: constructs the asset preview's dedicated pipeline trio, seeded like the other per-frame `SelectionPipeline`/`ImageQuadPipeline`/`TextRenderer` fields. +3: seeds the table-dims hover ease's initial settled state beside the pipeline's other per-frame Activity-driven fields."

[[file_size_mark]]
file = "src/render/pipeline_geometry.rs"
lines = 561
# 548 -> 561: item 540's table-dims picker measures its hint text's shaped
# width once per set_view (overlay_table_dims_hint_w), mirroring how
# overlay_spell_w is already measured there, so the card-width geometry
# function can size itself without a FontSystem in hand.
# 544 -> 548: the exhaustive theme/view sync colors the empty-cell wash and
# mirrors the immutable faceted-hug roster beside the visible overlay rows.
reason = "`set_view` is the sole ViewState-to-pipeline mirror: it measures workspace rails, mirrors row ownership, invalidates view-derived diagonal geometry, and publishes every projection. It carries fold-chevron retinting, fold headings/tails, secondary location, comparison/source mirrors, selection-ui overlay rows, photographable/findable notices, and world-authored diagonal-mark weight. +1: mirrors the working-set stack's row count alongside the other row-ownership mirrors. +6: mirrors config_keys/config_linux_keep onto TextPipeline alongside every other synced field, the plumbing the menu chord column's config-awareness rides. +14: the format popover's hover ring and the inline-image resize-handle's hover grip both take the same `muted` ink the Chips ghost pills and fold chevron already use, synced beside the pipeline's other per-theme colors. +1: retints the fold-chevron mark's mask-based pipelines instead of the retired quad ink field. +3: colors `panel_query_selection` via `theme::selection_document()` and mirrors `overlay_query_selection` from the view, beside the pipeline's other per-frame syncs. Insert Table's table_dims_cells is colored via the same theme sync every other per-frame pipeline field already rides. Item 527/528's config_keymap_flavor mirrors into the pipeline beside config_linux_keep, the plumbing the menu chord column's config-awareness already rides. +1: mirrors `overlay_asset_preview` from the view beside the pipeline's other per-frame syncs."

[[file_size_mark]]
file = "src/render/pipeline_overlay.rs"
lines = 449
reason = "Overlay composition and ambient resolvers live here because they read the pipeline's own settled state; Kite shares that ambient ownership seam. The one `advance` OR-fold is the single per-frame stepper, so the diagonal marker's turn and the fold chevron's join it there beside the caret spring, the caret preview and the copy pulse. +12: the band reads the stamped input epoch and exposes its prepared phase through this same per-frame owner."

[[file_size_mark]]
file = "src/render/rects.rs"
lines = 1755
reason = "+4: panel_layout reads shared physical PANEL_PAD and PANEL_MARGIN and clamps the content-sized card to narrow-canvas room, matching panel_shape_text fitted rows. +76: `OrnamentCache::bare_url_tails`'s field, its `ensure_ornament_lists` collection arm, and `bare_url_marks()`, beside the other per-frame ornament collections. +7: `MdKind::BareUrlText` joins `MdKind::LinkText` in the bucket-derivation match that feeds `Bucket::LinkUnderline`, so a tamed bare URL draws the same quiet underline a named link already does, through the one shared band/proto pipeline. Item 530's smart-punct ornament collection (smart_punct_marks) and its OrnamentCache::smart_punct_spans field, beside the other per-frame ornament collections this file already owns. +45: the table x-ray selection-band branch in `range_rects`, rebuilding the selection quad against the revealed row's real advances instead of the concealed geometry that collapsed it to a sliver."

[[file_size_mark]]
file = "src/render/rowlayout.rs"
lines = 765
reason = "+69: `ColumnFlow` — the one signed answer to which way a row's column grows from the edge it hangs on, with the origin/span/mirror/align derivations every consumer reads instead of its own sign, plus `rail_geom` taking that flow so a mirrored accessory seats its rail inward of its value at either orientation. It lands here because a row's name and its accessory are one layout question and this file already owns it; the diagonal cluster then MIRRORS by asking this enum once. The hand-kept `ALL_KINDS` array — which had silently omitted a shipping kind — is now derived from `OverlayKind::ALL` (-10). +12: `rows_for`'s Keymap arm. +1: `rows_for`'s Credits arm. Insert Table's dimension-picker cell geometry (table_dims_cell_rect) shares this file's one-geometry-serves-paint-and-hit-test discipline. Item 527/528's which-key row generation reaches the seeded-chord fallback the panel now advertises."

[[file_size_mark]]
file = "src/render/spans.rs"
lines = 43
reason = "A new `line_has_rule_span` helper (matching the existing `line_has_image_span`/`line_has_code_span` family) plus a `confirmed_rule` parameter threaded through `md_line_scale` and its doc comment — the corroborating check that stops a bare-scan false positive (e.g. `---` inside a fenced code block) from growing a row for an ornament that never draws. `line_has_rule_span`'s and `md_line_scale`'s doc comments are re-authored (+4 net) now that a qualifying SETEXT dash underline is no longer an example of the gap — it gets a real Rule span and agrees with the bare scan. Item 530's re-export of the smart-punct conceal span builder from spans/conceal/smart_punct.rs, beside the module's other conceal re-exports."

[[file_size_mark]]
file = "src/render/text.rs"
lines = 1482
reason = "The text shaping and incremental-restyle hub remains an explicit renderer exception; image span discovery and cursor-only image forcing live in focused submodules rather than raising this ceiling. +42: the resize handle under the live pointer is mirrored into render state by re-deriving its rect fresh from `image_hit_rects()` each frame — never a stashed pixel rect, so it cannot go stale across a document edit — through the SAME `image_handle_at` hit-test the cursor-icon path already reads, so the drawn grip can never disagree with a clickable target. +53: `font_features` disables `rlig` per-face for Monaspace Xenon only (its GSUB routes the tilde family and ~40 other operator ligatures through that DEFAULT feature, verified directly against the font — no other bundled face registers it), and the doc's prior claim that this ligation was AAT/`morx`-driven and unsuppressable is corrected (item 552)."

[[file_size_mark]]
file = "src/replay.rs"
lines = 386
reason = "The conflict kind's arm in `accept_class`, the exhaustive per-kind accept classifier, plus the correction to that arm's own premise — it is the one member there that really can reach the generic accept fallthrough (+7). +4: the door's kind in the replay classifier, so a capture and the live App agree about what it is. +12: the Keymap overlay kind's own arm in the same two exhaustive classifiers. +5: the Credits overlay kind's own arm in the same two exhaustive classifiers. +6: Reveal in File Manager's `Effect::RevealInFileManager` arm is recorded (intercepted, never performed) in `--keys` replay, the same external-handoff shape as `Export`/`FollowLink`. +4: Save a Copy's emitted payload shapes are exhaustively classified and recorded without performing filesystem writes in replay. Insert Table's overlay accept classifies as Unsupported here, mirroring Rename/InsertLink -- the commit mutates the buffer directly rather than reaching the generic accept path."

[[file_size_mark]]
file = "src/search/keys.rs"
lines = 368

[[file_size_mark]]
file = "src/search/mod.rs"
lines = 545

[[file_size_mark]]
file = "src/selection.rs"
lines = 694
reason = "DOWN 86: baked blend/flavor/vertex-layout/render-pipeline construction moved to selection/pipeline.rs; SelectionPipeline::build retains shared bind-group/buffer/uniform assembly and is now below the lint ceiling. +32: chamfer splits into a top/bottom pair (Globals gains a field, _pad2 shrinks to keep std140 size fixed, a compile-time size assert, set_chamfer takes two args, a test-only chamfer() accessor, and the Globals-literal duplication across three prepare paths)."

[[file_size_mark]]
file = "src/session.rs"
lines = 514

[[file_size_mark]]
file = "src/settings.rs"
lines = 746
reason = "+2: a test-gated `pub(crate) use toggle_core::toggle_default`, the read-only sibling of `toggle_core_now` (\"what is this when nothing has set it\" rather than \"what is it now\"). The function itself lives in `settings/toggle_core.rs` beside the map it mirrors, so this file carries only the door the generated reference reads it through. +14: the Keymap row's kind changed Toggle -> Picker, plus COVERED_BY and row_available_on's macOS-inert gate."

[[file_size_mark]]
file = "src/soak_gpu/mod.rs"
lines = 618

[[file_size_mark]]
file = "src/spell.rs"
lines = 962
reason = "+8: `SPELLCHECK_DEFAULT` and `DictVariant::DEFAULT` — the two fresh-install values this file owns, each now a const its own static is initialised from (`SPELLCHECK_ON`, `ACTIVE_VARIANT`) rather than a bare literal, so the generated reference reads the owner instead of restating `true`/`en_us`."

[[file_size_mark]]
file = "src/stars.rs"
lines = 724

[[file_size_mark]]
file = "src/stats.rs"
lines = 713

[[file_size_mark]]
file = "src/storyboard.rs"
lines = 618

[[file_size_mark]]
file = "src/streaks.rs"
lines = 833

[[file_size_mark]]
file = "src/syntax/mod.rs"
lines = 754
reason = "+2: `SynKind` joins `enum_with_all!`, deriving its `ALL` roster from its own variant list. The reference's markdown-span law sweeps every syntax role through it, so a new role cannot ship with an undocumented span tag; the macro's wrapper is the entire cost."

[[file_size_mark]]
file = "src/syntax/ruby.rs"
lines = 644

[[file_size_mark]]
file = "src/syntax/toml.rs"
lines = 515

[[file_size_mark]]
file = "src/theme/derive.rs"
lines = 343
reason = "The footer plate earns the notice channel's own one-pixel rim, which is what lifts it from delta-E 1.91 to 54 on Cassowary -- a plate nobody can see is not a plate. +14: `overlay_footer_plate_rim()` resolved fresh every frame with no `sync_theme_colors` entry -- the notice channel's own reasoning, so a theme swap cannot serve last world's rim. +5: the fold-mark glyph accessor derives from a world's existing ornament_face field, beside fold_afford_chevron_ink/fold_afford_tail_ink."

[[file_size_mark]]
file = "src/theme/model.rs"
lines = 778
reason = "The exhaustive theme model authors rotated-location treatment and toast anchor as data; shared renderers own both geometries without world-specific branches. +9: CardShape::Chamfered splits cut_px into a top_cut_px/bottom_cut_px pair, so a world can cut only the free edge and keep a docked seam square. +18: `LocationStyle::needs_plan_row` is the one exhaustive answer to whether the row planner reserves a display line for a world's location cue -- `RotatedRail` composes off-card against the room's own wordmark margin and needs no row, `Raked` still anchors its rotated run to that row's own geometry and does, so a future style is a conscious decision here rather than a silently vacated or silently extra row. +13: `Theme::fold_mark()` derives the fold-chevron glyph from `ornament_face` via `theme::ornament::fold_mark_for` — no per-world literal, a new world inherits from its existing ornament classification alone. +3: `bullet_face` preserves each world's approved list bullets while the Nishiki section-break cabinet changes independently."

[[file_size_mark]]
file = "src/theme/worlds.rs"
lines = 1178
reason = "Every world explicitly authors its toast anchor, while Cassowary and Magpie author their shared rotated-location treatments in the same roster. Kite authors the ruled list style (one line). +3: Cassowary and Quokka each author their own top_cut_px/bottom_cut_px pair instead of one shared cut_px. +18: the in-file worlds explicitly retain their existing list-bullet face while section-break ornaments move to Nishiki."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/apply.rs"
function = "App::run_action_core"
message = "this function has too many lines (101/100)"
reason = "Item 527/528: gathering config_keymap_flavor into BuildCtx alongside the existing config_keys/config_linux_keep pair, one call-site line."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/overlay/tests/row_meta_laws.rs"
function = "representative_overlay"
message = "this function has too many lines (103/100)"
reason = "Item 527/528: visible_effective_bindings gained a third KeymapFlavor argument, wrapping this fixture's one call site across four lines."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/menubar.rs"
function = "TextPipeline::prepare_menu_dropdown"
message = "this function has too many lines (101/100)"
reason = "Item 527/528: the drawn menu's dropdown plan now threads self.config_keymap_flavor into the same call join_slots_truthful already makes for the palette."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/viewstate.rs"
function = "App::sync_view"
message = "this function has too many lines (271/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. `sync_view` is the one exhaustive `ViewState` projection site, so each new render datum is spelled out here: the fold chevron's `folded_headings` (its direction needs the real fold set, not merely `fold_tails`'s own key set, so `fold::apply_to_view` takes the buffer's folds at its one call site here) and the summoned picker's secondary location. The document-session extraction replaced direct buffer and cache fields with explicit read projections while preserving that one exhaustive site. +5: the bottom identity's folder label reads the open file's own remembered root instead of the ambient project. +1: the gated pointer-derived resync's one call site, reached after scroll/zoom/the active document settle for the frame. +3: auto-collapses the expanded working-set panel whenever an overlay opens. +1: mirrors the query field's own seeded selection range into `overlay_query_selection`, beside the other query-caret mirrors. +3: mirrors `overlay_asset_preview` from `selected_asset_path()`, beside the other summoned-surface mirrors this function already owns."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app.rs"
function = "App::new"
message = "this function has too many lines (101/100)"
reason = "Existing constructor owner and exhaustive App construction site. DocumentSession moved the active slot, registry, previous target, spell checker, and buffer extras behind one initializer, reducing the constructor by twenty counted lines. Shifted +32 lines, unchanged, by the theme-font cheap-reshape constant and its compile-time guard. UsageLedger folded nine loose odometer/streaks initializers into one, taking a further sixteen lines off. +9: the working-set enrolment call and its doc comment naming why it runs last; the clipboard construction arms grew by a shared helper call each. +4: the `linux_emacs_meta` gate threaded to the live keymap constructor. -16: `scratch_buffer_from_stash` extracted into `app/startup.rs` so App::new and the launch-time restore path share one owner instead of duplicating the stash-read logic."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/lifecycle.rs"
function = "App::resumed"
message = "this function has too many lines (118/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. +1: the accessibility tree must be parked BEFORE the adapter is constructed and the adapter before the window is shown, so the seed call has exactly one place it can stand in `resumed`."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app.rs"
function = "run"
message = "this function has too many lines (103/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. DocumentSession removed root fields and shifted this unchanged function upward. Shifted +32 lines, unchanged, by the theme-font cheap-reshape constant and its compile-time guard."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/args/parse/loop_flags.rs"
function = "parse_flag_loop"
message = "this function has too many lines (255/100)"
reason = "The one argument-token loop, decomposed out of the former `parse_args` monolith (644 lines) into named phase functions threaded through `Ctx` (see `args/parse.rs`'s module doc). What remains here is the no-wildcard match on `FlagId` the flag roster's own doc requires — a new roster row fails to compile until an arm reads it — so splitting the match itself would break the exhaustiveness property the design exists for, the same shape `main/run.rs`'s own `run` exception documents for its exhaustive `Mode` match."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/args/parse/finish.rs"
function = "build_mode"
message = "this function has too many lines (142/100)"
reason = "The final phase of the decomposed `parse_args` (see the `parse_flag_loop` exception above for the decomposition): the exhaustive `Mode`-construction match, whose arm order mirrors `resolve_capture_kind`'s own precedence (held > timeline > screenshot-app > screenshot-frames > motion > screenshot > windowed). Splitting the match would give that precedence two owners instead of one."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/run.rs"
function = "run"
message = "this function has too many lines (296/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. `run` is one exhaustive `Mode` match whose arms are already one-line delegates into the files the modes themselves live in (`run/live_app.rs`, `run/location.rs`); what is left is the match."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/modes.rs"
function = "settled_viewstate"
message = "this function has too many lines (236/100)"
reason = "The snapshot rebuilds secondary picker location from its existing strip. Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. The capture path derives a summoned workspace's shape and whether its comparison region carries prose, because a sidecar records mode rather than shape. +1: mirrors `opts.overlay.query_selection` into `overlay_query_selection` for the headless capture path. +1: mirrors `opts.overlay.asset_preview` into `overlay_asset_preview` for the headless replay path."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/sidecar.rs"
function = "overlay_json"
message = "this function has too many lines (154/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. `overlay_json` is one format string over the whole overlay record, so each serialized field — the workspace, the `driver` argument — is a line in it. +8: serializes `overlay.asset_preview` via `asset_preview_report()`, in lockstep in both literal arms."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/caret_glyph.rs"
function = "CaretGlyphPipeline::new"
message = "this function has too many lines (214/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. The construction now happens inside gpu_cache::render_pipeline's build closure, so the program is compiled once per process rather than once per pipeline instance; the added lines are that closure's own."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/export/pdf/writer.rs"
function = "emit"
message = "this function has too many lines (123/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/story.rs"
function = "run_storyboard"
message = "this function has too many lines (116/100)"
reason = "The one storyboard orchestrator owns ordered replay, expectation aborts, per-step capture, trace completion, and film encoding; its phase helpers remain separate below."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/image_pipeline.rs"
function = "ImageQuadPipeline::new"
message = "this function has too many lines (134/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. The construction now happens inside gpu_cache::render_pipeline's build closure, so the program is compiled once per process rather than once per pipeline instance; the added lines are that closure's own."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/rects.rs"
function = "TextPipeline::ensure_ornament_lists"
message = "this function has too many lines (106/100)"
reason = "Item 530's smart-punct ornament collection joins the other per-frame ornament lists this function already builds (bare-URL tails, rule lines, bullet marks) -- one shared collection walk, not a second owner."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/markdown/spans/parse.rs"
function = "spans"
message = "this function has too many lines (171/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. The `Tag::Heading` Start arm grew with an `else if` arm that promotes a qualifying SETEXT dash underline to a real `MdKind::Rule` span, inlined here (rather than a second single-call-site helper) to stay under the file's own frozen line ceiling — DECIDED: `---` always draws as the rule, whatever precedes it. +3: the guarded `push_bare_url_spans` call in the `Event::Text` arm. +3: item 530's guarded push_smart_punct_spans call in the Event::Text arm, mirroring push_bare_url_spans's own code_block guard."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/panel.rs"
function = "TextPipeline::panel_shape_text"
message = "this function has too many lines (143/100)"
reason = "One shaper owns responsive field width, semantic hint wrapping, and row/caret spans; splitting these would create a second sizing owner."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/overlay_draw.rs"
function = "TextPipeline::overlay_upload_text"
message = "this function has too many lines (204/100)"
reason = "The mirrored row cluster seats names from shaped ink and chords from the accessory-column flow; the diagonal chord column uses the shaped buffer width for right-aligned ink. Existing chrome draw owner; extraction needs a deliberate rendering-state seam. The workspace rail has its own clipped text area, and rotated-location shaping/geometry lives in dedicated `chrome::rotated_location` rather than growing this function."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/menubar.rs"
function = "TextPipeline::prepare_menubar"
message = "this function has too many lines (131/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/outline.rs"
function = "TextPipeline::outline_layout"
message = "this function has too many lines (104/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/outline.rs"
function = "TextPipeline::prepare_outline"
message = "this function has too many lines (124/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/theme_picker.rs"
function = "TextPipeline::overlay_shape_theme"
message = "this function has too many lines (207/100)"
reason = "The row plan reaches this shaper so the strip's active mark rides its planned box. Existing chrome draw owner; extraction needs a deliberate rendering-state seam. `LOCATION_SCALE` lives in `render::rotated_location`; grouped-card pad, margin, and top drop share one owner with the flat family; and text/underline mark thicknesses use named `Logical` constants through `Metrics::px`."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/hud.rs"
function = "TextPipeline::prepare_hud"
message = "this function has too many lines (162/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam. +2: clears the streaks card's rect/text-origin state in the `!streaks` arm, beside the sibling quad-clear it already performs there."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/hud.rs"
function = "TextPipeline::prepare_streaks_card"
message = "this function has too many lines (153/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam. +2: stores the card's plan rect/text origin for the click hit-test and render-tier laws to read, beside the plan it is derived from."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/popover.rs"
function = "TextPipeline::popover_layout"
message = "this function has too many lines (114/100)"
reason = "Measured button, anchor, card, and hit geometry moved to render::plan; this function remains the single shaping and measurement seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/preview.rs"
function = "TextPipeline::prepare_caret_preview_panel"
message = "this function has too many lines (110/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/preview.rs"
function = "TextPipeline::emit_preview_caret"
message = "this function has too many lines (135/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/whichkey.rs"
function = "TextPipeline::prepare_whichkey"
message = "this function has too many lines (144/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/blur.rs"
function = "BlurBackdrop::new"
message = "this function has too many lines (109/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions. The construction now happens inside gpu_cache::render_pipeline's build closure, so the program is compiled once per process rather than once per pipeline instance; the added lines are that closure's own."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/text.rs"
function = "TextPipeline::compute_image_layout"
message = "this function has too many lines (116/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/text.rs"
function = "TextPipeline::refresh_rule_conceal"
message = "this function has too many lines (105/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/rects.rs"
function = "TextPipeline::ensure_wash_protos"
message = "this function has too many lines (151/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/asset_preview.rs"
function = "TextPipeline::prepare_asset_preview"
message = "this function has too many lines (104/100)"
reason = "One frame-prep function owns the full seam: resolve the selected orphan, decode it through the shared image_cache (never a second decoder), contain-fit it, and prepare all three dedicated pipelines (panel/image/text) including the honest can't-decode text state. Splitting it would scatter one small feature's state across call sites for no real decoupling."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/layers.rs"
function = "TextPipeline::prepare_images"
message = "this function has too many lines (124/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions. +3: the resize-handle hover grip's own draw call, gated to at most one rect. +3: folds the asset preview's own decoded path into the per-frame keep set, so it isn't evicted and redecoded every frame."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/perfbench.rs"
function = "run_async"
message = "this function has too many lines (135/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/benchsuite/mod.rs"
function = "run_async"
message = "this function has too many lines (109/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/caretbench.rs"
function = "run_async"
message = "this function has too many lines (111/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/pipeline_draw.rs"
function = "TextPipeline::new"
message = "this function has too many lines (508/100)"
reason = "The one exhaustive TextPipeline initializer keeps adjacent GPU state transitions together. Four added lines initialize arbitrary two-colour ground/ink endpoints for independent selection and caret pipelines; their shared baked construction lives in SelectionPipeline::new_two_colour. Five added lines initialize the living band's input epoch beside its existing selection-motion state. +2: initializes the streaks card's rect/text-origin fields to `None`, beside the other per-frame HUD state. +8: the format popover's hover-ring pipeline and the inline-image resize-handle's hover-grip pipeline, initialized beside their sibling SelectionPipeline construction sites. +4: the row-drag insertion hairline's own seeded pipeline, beside gutter_stack_plate's construction. +3: seeds the per-mark fold-chevron rotated-label pipeline slots beside the other per-mark construction sites. +4: `panel_query_selection`'s construction and its `overlay_query_selection: None` initializer, beside the other per-frame query-caret state. +1 (492->493): item 540's table-dims card gains one exhaustive field init, overlay_table_dims_hint_w, beside the other per-frame overlay measurements this initializer already owns. +3 (493->496): `gutter_close_hover_plate`'s two-line construction (`SelectionPipeline::new`) plus its field in the exhaustive literal, mirroring `gutter_stack_plate`'s own seeding immediately above it. +9: constructs the asset preview's dedicated pipeline trio (panel/image/text). +3: seeds the table-dims hover ease's initial settled state."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render.rs"
function = "build_font_system"
message = "this function has too many lines (105/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions. Its source position moves when neighboring module declarations change, while its size is unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/gutter.rs"
function = "TextPipeline::prepare_gutter"
message = "this function has too many lines (127/100)"
reason = "The one shared owner every gutter block's shape and hit-test read; widening the block to a stable multi-row stack when the active project holds more than one open file is a single additional branch rather than a second construction path. The stable close lane is shaped in that same branch so hover changes ink only and cannot shift a filename. +9 net: the active-row plate and the row-drag insertion hairline now resolve together through gutter_stack::plates_and_drag_indicator (extracted to keep both this function and the file under their ceilings), so this function only sets each pipeline's color/corner and calls prepare — the geometry itself moved out, not just relabelled here. +2 (123->125): item 539's leading-side close mark widens the shaped box and shifts its render origin left by the mark's own reserved width, plus a Wrap::None backstop so an overrun clips at the canvas edge instead of reflowing — a handful of real lines at the same single-owner seam; the lane tried several inline consolidations and none reduced clippy's counted total (one made it worse, 127). +2 (125->127): the close-zone hover plate's own prepare call, threaded through prepare_close_hover_plate (extracted to gutter_stack.rs to keep this function and the file under their ceilings) — the two call sites (stack rows, single-file identity line) are the actual line cost, the geometry itself lives in the extracted function."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/spellunderline.rs"
function = "SpellUnderlinePipeline::new"
message = "this function has too many lines (133/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. The construction now happens inside gpu_cache::render_pipeline's build closure, so the program is compiled once per process rather than once per pipeline instance; the added lines are that closure's own."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/bash.rs"
function = "spans"
message = "this function has too many lines (127/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/html.rs"
function = "spans"
message = "this function has too many lines (123/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/ruby.rs"
function = "spans"
message = "this function has too many lines (137/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/toml.rs"
function = "spans"
message = "this function has too many lines (118/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/tests/files.rs"
function = "a_paused_mid_drag_persists_nothing_until_the_release"
message = "this function has too many lines (109/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Explicit DocumentSession test transitions shifted this unchanged scenario downward."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app_icon/tests.rs"
# `roster_growth_does_not_dilute_a_known_erosion` — same function and size;
# only its position moved during the surrounding test reorganization.
function = "roster_growth_does_not_dilute_a_known_erosion"
message = "this function has too many lines (117/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/folds.rs"
function = "fold_tail_never_bleeds_past_the_text_column_edge_on_a_wrapped_heading"
message = "this function has too many lines (126/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. The scenario uses `testscratch::ScratchDir` for its real-filesystem fixture."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
function = "streaks_card_absent_by_default_and_summoned_shows_the_placeholder_year"
message = "this function has too many lines (107/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. It uses `testscratch::ScratchDir`; neighboring HUD readout unit assertions affect its source position, not its body."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
function = "popover_card_hugs_the_button_row"
message = "this function has too many lines (141/100)"
reason = "Existing end-to-end scenario with no coherent setup/assertion split. Removing the identity fixture wrapper makes the direct default construction two lines longer without changing this test's behaviour."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
function = "popover_labels_demonstrate_their_own_effects"
message = "this function has too many lines (113/100)"
reason = "Existing end-to-end scenario with no coherent setup/assertion split. Removing the identity fixture wrapper makes the direct default construction two lines longer without changing this test's behaviour."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
function = "history_comparison_is_relocated_by_the_capture_path_in_every_world"
message = "this function has too many lines (139/100)"
reason = "Existing end-to-end scenario with no coherent setup/assertion split. Its two identity-wrapper call sites became explicit default constructions, adding four source lines without changing behaviour. +1: the `OverlayInfo` literal gained its own `query_caret` field. +1: the `OverlayInfo` literal gained its own `query_selection` field. +1: asserts the new `overlay.asset_preview` field stays absent/null for this unrelated overlay kind."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/pickers_faceted.rs"
function = "command_and_history_pickers_faceted_lens_render_and_report"
message = "this function has too many lines (171/100)"
reason = "Existing end-to-end scenario with no coherent setup/assertion split. Removing the identity fixture wrapper makes the direct default construction two lines longer without changing this test's behaviour. +1: the `OverlayInfo` literal gained its own `query_caret` field. +1: the `OverlayInfo` literal gained its own `query_selection` field. +1: asserts `overlay.asset_preview` stays absent for these faceted, non-Assets pickers."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/pickers_faceted.rs"
function = "a_settings_range_row_steps_and_reports_its_rail_through_the_sidecar"
message = "this function has too many lines (118/100)"
reason = "Existing end-to-end scenario with no coherent setup/assertion split. Removing the identity fixture wrapper makes the direct default construction two lines longer without changing this test's behaviour."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/export/pdf/tests.rs"
function = "pdf_has_exact_classic_xref_object_plan_pages_and_a4_geometry"
message = "the function has a cognitive complexity of (31/25)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/keymap/tests.rs"
function = "every_catalog_default_slot_dispatches_through_real_keymap_under_both_conventions_and_flavors"
message = "this function has too many lines (103/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/rowlayout.rs"
function = "rows_for"
message = "this function has too many lines (129/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions. +12: the Keymap kind's row-shape arm. +1: the Credits kind's row-shape arm."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/tests/credits_capture.rs"
function = "credits_text_is_present_and_legible_across_a_world_sample"
message = "this function has too many lines (111/100)"
reason = "Real `capture_screenshot` sweep over a three-world sample (Tawny, Mopoke, Wagtail — the same sample panels.rs uses for this shape of check), asserting both a presence floor (pixel-diff count) and a WCAG AA legibility floor (ink-vs-plate contrast) per world; the coverage is the point, not a setup/assertion split."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/chrome_overlay.rs"
function = "overlay_row_elements_agree_in_y_flat_and_faceted_every_world"
message = "this function has too many lines (125/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."


[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/chrome_panels.rs"
function = "faceted_palette_shapes_the_chord_column_aligned_to_its_rows"
message = "this function has too many lines (154/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/distinguishability.rs"
function = "check_color_math"
message = "this function has too many lines (104/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/distinguishability.rs"
function = "check_real_pixels"
message = "this function has too many lines (125/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."



[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/images.rs"
function = "tall_image_survives_the_viewport_top_boundary_then_culls_and_restores_on_scroll_up"
message = "this function has too many lines (103/100)"
reason = "One end-to-end viewport-boundary scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
function = "spell_popup_floats_bare_on_bars_keeps_the_card_on_pane"
message = "this function has too many lines (180/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged. The `Rules` PROTOTYPE joins this roster sweep with its own arm (+32): the style's whole claim is structural and NEGATIVE — no float pane, no scrim, and no emitted quad within half a row's height — and those assertions have to be made at the same fixture the other three styles are graded at, not at one of their own that could drift from it."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
function = "poster_bars_centered_lists_preserve_page_and_distinguish_plates"
message = "this function has too many lines (152/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged. Shifted by one more line: the new Keymap kind joins the roster this test sweeps. Shifted by one more line: the new Credits kind joins the roster this test sweeps."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
function = "bars_float_bounded_plates_for_every_overlay_kind"
message = "this function has too many lines (118/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged. Shifted by one more line: the new Keymap kind joins the roster this test sweeps. Shifted by one more line: the new Credits kind joins the roster this test sweeps."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
function = "faceted_lens_strip_tabs_sit_on_plates_on_every_bars_world"
message = "this function has too many lines (118/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by the command-facet roster law above it. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/one_bit.rs"
function = "wagtail_living_band_ink_rides_the_band_on_the_faceted_palette"
message = "this function has too many lines (109/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/oracle.rs"
function = "assert_vertical_sweep_clean"
message = "this function has too many lines (107/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/outline.rs"
function = "lava_frost_pills_follow_outline_visibility"
message = "the function has a cognitive complexity of (27/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/outline.rs"
function = "lava_frost_pills_follow_outline_visibility"
message = "this function has too many lines (126/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged. Crossed by one more line: the `testlock::misc::TogglesRestore` capture guard, added so this test restores the outline flag it forces off, not just leaves it off for whatever runs next."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/overlay_personality.rs"
function = "every_shipping_placard_world_paints_visible_wordmark_ink_pixels"
message = "this function has too many lines (121/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Shifted by the `testlock::serial()` guard that every test reaching the shared wgpu device now takes; the line count moves by one and the body is otherwise unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/query_field.rs"
function = "a_flat_pickers_query_ink_centres_on_its_own_line_in_every_world"
message = "this function has too many lines (109/100)"
reason = "One roster sweep with a differential two-shot oracle: the second render and its readback cannot be split from the comparison they exist for."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/split_pane.rs"
function = "every_world_splits_by_the_shared_default_never_by_identity"
message = "this function has too many lines (103/100)"
reason = "One-render-per-world roster and override sweep; the per-world capability arm disappeared when split became renderer-owned."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/workspace_footer_plate.rs"
function = "a_workspace_footer_plate_ends_with_its_footer_on_every_bare_plate_world"
message = "this function has too many lines (149/100)"
reason = "One roster sweep with a geometry arm, a pixel arm and an exclusion arm over the same rendered frame; splitting them would render each world twice, and the exclusion arm exists precisely to be measured at the same fixture the other two are graded at. +5: the exclusion arm's premise generalised. `Rules` is bare-plate and plateless and yet DOES emit quads (rules), so the arm now measures what it always meant — nothing drawn can be a slab that outlives the footer — instead of the narrower 'emits nothing' proxy it could get away with while `Diagonal` was the only plateless style."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/overlay_plan_law.rs"
function = "an_empty_states_notice_row_carries_no_footer_plate_on_any_bare_plate_world"
message = "this function has too many lines (123/100)"
reason = "One roster sweep with a geometry arm, a pixel arm and an exclusion arm over the same rendered frame; splitting them would render each world twice, and the exclusion arm exists precisely to be measured at the same fixture the other two are graded at."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/footer_gap_magnitude.rs"
function = "no_candidate_row_pixel_changes_when_the_pickers_own_placard_draws"
message = "this function has too many lines (107/100)"
reason = "One roster sweep over every placard world, grading each candidate row's pixels against its own best-overlap ink-backed region; splitting the per-row masking check from the roster loop that drives it would require re-rendering the same frame twice."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/page_frame.rs"
function = "wagtail_page_frame_draws_pure_ladder_white_in_bounds_and_none_worlds_draw_none"
message = "this function has too many lines (205/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Gained a row-0-exact top-band assertion and a drawn-menu-bar discriminating pixel case for the page-frame-meets-canvas-top fix."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/page_frame.rs"
function = "page_frame_top_meets_canvas_or_bar_across_line_worlds_and_configs"
message = "this function has too many lines (125/100)"
reason = "Roster-derived sweep over every PageFrame::Line world x page width x viewport height x scroll x DPI x menu-bar state (64 cells); the coverage is the point, not a setup/assertion split."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/syntax_ligatures.rs"
function = "font_features_owner_is_the_three_way_ligature_split"
message = "the function has a cognitive complexity of (29/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/syntax_roles.rs"
function = "role_style_laws_hold_for_every_world"
message = "the function has a cognitive complexity of (36/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/syntax_roles.rs"
function = "role_style_laws_hold_for_every_world"
message = "this function has too many lines (192/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests/ambient.rs"
function = "ambient_stars_laws_hold_for_every_world"
message = "this function has too many lines (130/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. The function lives in the per-subject theme test module and is otherwise untouched."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests/personality.rs"
function = "personality_assignments_are_exactly_the_decided_table"
message = "this function has too many lines (172/100)"
reason = "The exhaustive per-world capabilities table includes rotated-location treatment assignments; its no-wildcard shape makes every world's face, scale, ink, tracking, and locator grammar an explicit decision. +3: the table now asserts Cassowary and Quokka's own top/bottom chamfer pair instead of one shared cut."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/distinguishability.rs"
function = "theme_preview_retint_regrounds_the_page_surface_on_every_world"
message = "this function has too many lines (102/100)"
reason = "Crossed the threshold by exactly one line, and that line is the `testlock::serial()` guard every test reaching the shared wgpu device now takes — the shared device is process-global state and an unguarded caller races every other test on it. The body is unchanged and this is not a decomposition signal; splitting a per-world sweep to buy back one counted line would be metric-only churn. Crossed by one more line since: the `testlock::misc::TogglesRestore` capture guard, added so this test restores the process-global caret-mode override it forces, not only the concrete mode it happened to set."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/dither.rs"
function = "dither_mode_paints_only_pure_values_at_roughly_the_configured_density"
message = "this function has too many lines (101/100)"
reason = "Crossed the threshold by exactly one line, and that line is the `testlock::serial()` guard every test reaching the shared wgpu device now takes — the shared device is process-global state and an unguarded caller races every other test on it. The body is unchanged and this is not a decomposition signal; splitting a real-pixel dither law to buy back one counted line would be metric-only churn."

# Add structural exceptions only for intentional URL/unbreakable/rustfmt-skip/generated/architectural cases.
# Each entry must pin the exact line text so removal or movement is stale.
# [[structural_exception]]
# target = "src/example.rs"
# kind = "unbreakable"
# line = 12
# text = "..."
# reason = "Why this exact source line cannot follow the ordinary rule."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 8
text = "            \"assets/keymap-defaults.toml names {slug_in_file:?}, which is not a commands::COMMAND_SEED slug\""
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/main/tests/caret_mode.rs"
kind = "architectural"
line = 86
text = "                \"skipping caret_render_is_a_pure_function_of_mode_and_world_across_a_wagtail_detour: no wgpu adapter\""
reason = "Pure relocation from the inline `src/main/run.rs` test module into the per-subject `src/main/tests/caret_mode.rs` file; the line itself is unchanged."

[[structural_exception]]
target = "src/main/tests/caret_mode.rs"
kind = "architectural"
line = 126
text = "            \"mode {mode:?}: caret pixels must be byte-identical whether or not Wagtail was visited in between\""
reason = "Pure relocation from the inline `src/main/run.rs` test module into the per-subject `src/main/tests/caret_mode.rs` file; the line itself is unchanged."

[[structural_exception]]
target = "src/main/tests/caret_mode.rs"
kind = "architectural"
line = 160
text = "            \"skipping pointer_replay_seam_reproduces_a_keyboard_scroll_stealing_a_stationary_pointer_check: \\"
reason = "The GPU-unavailable diagnostic is a continuation line in a test moved from `src/main/run.rs` into its per-subject test module."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 182
text = "        \"narrow: outline granted its full preferred rail (within the whole-pixel snap), avail={avail} pref={pref}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 311
text = "        \"boundary resolves to WIDE (no shift) at the exact threshold: left={left} symmetric={symmetric}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 332
text = "            \"shifted column must still fit the window (win={win} measure={measure}): left={left} width={width}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 353
text = "                \"width {w}px: column_left jumped {step}px in a single pixel of resize ({p} -> {left}) — the jitter bug\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 401
text = "                    \"width {w}: symmetric-regime left must step exactly 0 or 1 whole px per width px, got {step}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/settings/tests.rs"
kind = "architectural"
line = 729
text = "            \"{kind:?} must have exactly one palette door (commands={command_doors}, rows={row_doors})\""
reason = "Pure relocation from the inline src/settings.rs test module; this exact line was grandfathered there."

# Newly added Rust comment citations are checked against the branch fork.
# Index-named tests and capture schema rows are structural exceptions. A
# measured threshold or product constant may use an explicit entry below;
# narration and changelog prose may not.
#
[[structural_exception]]
target = "src/theme/tests/ambient.rs"
kind = "architectural"
line = 79
text = "                    \"{}: the visibility band must be ordered (0 < floor {floor} < peak {peak} <= 1)\","
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/theme/tests/ambient.rs"
kind = "architectural"
line = 165
text = "                            \"{}: the band must brighten from floor to peak (floor ΔY {floor_dev:.3} \\"
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/theme/tests/firetail.rs"
kind = "architectural"
line = 32
text = "        \"Firetail ground {fire_ground:.1}° must read far redder/warmer than Bombora {bombora_violet:.1}°\""
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/theme/tests/firetail.rs"
kind = "architectural"
line = 36
text = "        \"Firetail ground {fire_ground:.1}° must stay substantially clear of Potoroo's orange-rust {potoroo_rust:.1}°\""
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/theme/tests/firetail.rs"
kind = "architectural"
line = 42
text = "        \"Firetail base_100 must stay deep oxblood-charcoal, got h={base_h:.1}° s={base_s:.2} l={base_l:.2}\""
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/theme/tests/heatmap.rs"
kind = "architectural"
line = 100
text = "                \"{}: heatmap level {i} (sat {:.2}) manufactures chroma beyond the ink ladder (max {:.2})\","
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/theme/tests/placard.rs"
kind = "architectural"
line = 185
text = "            \"{}: placard presence ordering inverted (Faint ΔY {dy_faint:.3} < Ghost ΔY {dy_ghost:.3})\","
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[structural_exception]]
target = "src/overlay/tests/spell.rs"
kind = "architectural"
line = 76
text = "            \"n={n}: the picker's whole corpus always fits the window — it can never be forced to scroll\""
reason = "This already-long line lives in a per-subject test file. A one-file-to-many split has no prior per-path baseline for `git diff -M` to compare, so the 100-column ratchet cannot grandfather it; the line itself is unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/frost_feather.rs"
function = "the_footprints_edge_is_a_ramp_of_the_authored_width_in_logical_px_at_every_dpi"
message = "this function has too many lines (137/100)"
reason = "A pixel-profiling law, not a routine assertion: it builds a real frame open and closed, profiles outward from BOTH vertical faces along each face's own sheared x, and derives which face carries the claim from whether a skirt has any document to defocus. The length is the sweep (3 worlds x 2 DPI tiers x 2 faces) plus the veto that keeps card ink out of the first samples. Splitting it would separate the profile from the non-vacuity clause that gives its numbers meaning, which is how a measurement law goes quietly vacuous. The largest of the three and the first candidate if this file is ever decomposed. Tightened from 147 when the sheared face got a single owner (`footprint_face_x`) -- this law had been profiling the retired union's face rather than the drawn one, so the ten lines went away as a by-product of fixing what it measured."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/frost_feather.rs"
function = "the_footprints_lean_is_read_from_the_spine_the_frame_actually_drew"
message = "this function has too many lines (114/100)"
reason = "The length is the geometry hunt this law needs to be non-vacuous. The measured per-row step and the authored ROW_STEP agree on every ordinary card, so asserting equality with the drawn spine tests nothing unless the sweep contains a YIELDING geometry — three plausible ones, including a deliberately cramped card, all resolved 7.000 = 7.000. The extra lines carry the geometry that does yield (rows hugging one glyph, 3.993 against 7.000) and the clause that makes the law report its own vacuity if such a case ever stops being present."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/frost_feather.rs"
function = "the_full_frosts_composite_is_destination_independent"
message = "this function has too many lines (123/100)"
reason = "Proves Frost::Full is still a replace under ALPHA_BLENDING by DRAWING it twice, over a red destination and a blue one through a LoadOp::Load pass, and requiring bit-identity — the operational meaning of `alpha 1.0 is a replace`, rather than an assertion about the constant. The length is two full GPU passes plus the non-vacuity step that first proves the composite differs from the bare clear; at alpha 0.99 all 120000 pixels differ."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/geometry/tests.rs"
function = "adaptive_column_left_is_dpi_invariant_at_matched_logical_geometry"
message = "this function has too many lines (119/100)"
reason = "A 3996-cell sweep, and the cells are the point: 4 DPI tiers including a fractional 1.5, 9 logical widths from the true 464 minimum, measures 10-120, page on/off and rail on/off. The length is that product plus four non-vacuity gates and the PRESENCE clause that catches a pad deleted rather than scaled, which invariance alone cannot: 0 x dpi is perfectly invariant."
[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests/frost.rs"
function = "gutter_frost_pill_keeps_ink_contrast_on_every_lava_world"
message = "this function has too many lines (102/100)"
reason = "One exhaustive lava-world phase-by-in-pill grid plus phase-free and locality controls; splitting would duplicate the enrolled world and shared non-vacuity state for metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/hover_grammar.rs"
function = "engaging_new_hover_leaves_the_outline_and_menu_bar_untouched_on_every_world"
message = "this function has too many lines (119/100)"
reason = "Engages both new hover mechanisms in one fixture and proves three things at once (the file's own module doc names all three): the mechanisms are genuinely live in this fixture (non-vacuous), the margin outline column stays byte-identical, and the drawn menu bar's own band stays byte-identical — splitting the three assertions apart would need three separate live-mechanism fixtures instead of one shared one."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/symbol_atlas_gallery.rs"
function = "render_html"
message = "this function has too many lines (129/100)"
reason = "The one HTML-emission owner for the survey page: page shell/CSS, the per-face @font-face block, and the per-block glyph grid all write through this single writer so the generated markup and its styling cannot drift into two owners."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/pipeline_geometry.rs"
function = "TextPipeline::sync_theme_colors"
message = "this function has too many lines (105/100)"
reason = "The one exhaustive per-theme color sync keeps every pipeline's recolor beside its siblings so a new theme constant cannot be wired to only some of them. +2: colors `panel_query_selection` via `theme::selection_document()` and mirrors `overlay_query_selection` from the view."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/pipeline_geometry.rs"
function = "TextPipeline::sync_view_fields"
message = "this function has too many lines (104/100)"
reason = "The one ViewState-to-pipeline mirror (see the file_size_mark reason above for the full roster it already carries): item 540's table-dims picker adds a single measurement, overlay_table_dims_hint_w, via measure_workspace_hint_text_px -- the same shaper the spell popup's own overlay_spell_w measurement already uses at this seam -- so the card-width geometry function can size itself without a FontSystem in hand. First diagnostic to cross the 100-line clippy floor at this seam (103/100); the mirror stays one function so a new theme/view field is never wired to only some pipelines. +1: mirrors `overlay_asset_preview` from the view beside the pipeline's other per-frame syncs."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/facet_strip_seat_law.rs"
function = "active_lens_mark_sits_under_the_active_labels_own_glyphs"
message = "this function has too many lines (107/100)"
reason = "One roster sweep over every world, classifying upright vs. banded via the same gate the emitter itself uses and reading the active label's glyph span independently off the panel buffer -- splitting the sweep from its own independent oracle would let the two drift apart, which is the exact class of bug this law exists to catch."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/hover_slop_law.rs"
function = "a_keyboard_scroll_moves_what_a_stationary_pixel_hits_and_the_gate_refuses_it"
message = "this function has too many lines (103/100)"
reason = "One real-pipeline scenario proving the item's own named live hazard end to end, swept over list style x DPI in one loop rather than four near-duplicate functions -- the menu-bar pin/restore this session added (capture ambient, force off, restore) is the same three lines chrome_panels.rs's own convention costs, and belongs beside the scenario it protects, not a second helper."
