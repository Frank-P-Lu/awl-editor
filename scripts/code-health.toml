# Reviewed historical exceptions for the two metrics that run in code-health.py.
# Identity is exact: lint + file + primary line + complete diagnostic text.
# Removing, moving, or increasing a recorded function makes this entry stale.
#
# The frozen file-size baseline is intentionally old: it is each file's size
# at the pinned BASELINE commit, and a file may never exceed it — that is the
# one hard, non-negotiable ceiling, checked directly against that commit's
# content, not against this table. A mark below is the live high-water line
# code-health.py actually enforces (min(baseline, mark)); it is not itself a
# second ceiling, only bookkeeping for where that line currently sits.
#
# A mark must be lowered with every legitimate shrink. It may also rise —
# strictly below the frozen baseline, never past it — when growth genuinely
# cannot be avoided at a single-owner seam: an exhaustive construction site, a
# one-owner accessor sequence, a dispatch call site the design (CLAUDE.md's
# architecture) requires to stay singular, where decomposing instead would
# break the exhaustiveness or ownership property the seam exists for. Such a
# raise is only legitimate with a `reason` field naming the seam and the size
# delta; it is never a substitute for extraction the file can actually afford.
# A raise recorded relative to `main` with no `reason` fails the gate
# (code-health.py: check_mark_raises). That comparison only has force where
# HEAD differs from `main` — a worker's worktree branch, checked before
# landing. HEAD-is-`main` (CI's push-to-`main` job, and the merge train's
# post-merge candidate) is a vacuous comparison-to-itself and is reported as
# SKIPPED, not as a clean audit (code-health.py: previous_marks). Do not read
# "the ledger enforces raise reasons" as something CI verifies generally.

[[file_size_mark]]
file = "src/actions.rs"
lines = 817
reason = "item 114: +1 line — the `workspace_nav` module declaration, at the one module list."

[[file_size_mark]]
file = "src/actions/format.rs"
lines = 908

[[file_size_mark]]
file = "src/actions/overlay_nav.rs"
lines = 791
reason = "item 114: +8 lines net. The workspace's own two-region keyboard and its deep link were carved out to `actions/workspace_nav.rs`; what remains here is the intercept-chain entry and the dispatcher arm that calls them."

[[file_size_mark]]
file = "src/app.rs"
lines = 1805
reason = "item 188: +7 lines — the `capture_state` and `press` module declarations at the one module list, each with the `#[cfg]` naming where it exists (`capture_state` is native-only like the `--screenshot-app` mode it serves; `press` is native-or-test). `capture_state.rs` carries the live-`App` capture constructor and the `CaptureSubject` impl (it needs `App`'s private fields); `press.rs` is item 183's headless press door, moved out of `app/tests/` because the `--screenshot-app` mode is now a production caller. Both bodies live in their own files precisely to keep them off this one."

[[file_size_mark]]
file = "src/app/apply.rs"
lines = 1037

[[file_size_mark]]
file = "src/app/files/settings.rs"
lines = 382

[[file_size_mark]]
file = "src/app/gpu.rs"
lines = 645

[[file_size_mark]]
file = "src/app/input/keys.rs"
lines = 536

[[file_size_mark]]
file = "src/app/input/mouse.rs"
lines = 1106
reason = "item 114: +23 lines in `overlay_click`, the ONE pointer-dispatch owner — a click in the navigation rail resolves through the picker's own lens setter and the lifecycle's named focus transition, so the pointer and the keyboard reach one state."

[[file_size_mark]]
file = "src/app/session.rs"
lines = 526

[[file_size_mark]]
file = "src/app/stats.rs"
lines = 500

[[file_size_mark]]
file = "src/app/viewstate.rs"
lines = 534
reason = "item 114: +1 line — the `overlay_workspace` projection at the one exhaustive `ViewState` construction site."

[[file_size_mark]]
file = "src/app/window.rs"
lines = 690

[[file_size_mark]]
file = "src/background.rs"
lines = 286
reason = "items 132/194: +23 lines — the grouped per-frame AmbientUpload (the shared ambient clock's fourth consumer pushed prepare past the argument ceiling, and a struct is the honest answer rather than an allow), the inert BgDesc::tunnel slot, and the Globals.pose row the host-resolved steering pose rides. Unavoidable at this seam: Globals IS the uniform's one Rust mirror, and a second file describing half of it is exactly the drift the byte-match law exists to stop."

[[file_size_mark]]
file = "src/buffer.rs"
lines = 936

[[file_size_mark]]
file = "src/buffer/edit.rs"
lines = 548

[[file_size_mark]]
file = "src/buffers.rs"
lines = 567

[[file_size_mark]]
file = "src/capture.rs"
lines = 57
reason = "items 188/194: +2 lines — the schema /194 history row, which the ledger law requires to move with the const, plus item 188's own /193 row and the rustfmt wrap of pub use opts::{..}. Not extractable: the ledger law's whole point is that the const and its row live together."

[[file_size_mark]]
file = "src/capture/modes.rs"
lines = 552
reason = "item 114: +4 lines net — the workspace/detail-focus projection, minus the hand-copied per-kind window-row table this item replaced with the one `OverlayKind::window_rows` owner."

[[file_size_mark]]
file = "src/capture/sidecar.rs"
lines = 844
reason = "items 188/194: +1 line — the warped-grid sidecar arm is dispatched from the ONE background_json call site, which gained the resolved route pose as a third argument. A second format site is exactly what this file exists to prevent."

[[file_size_mark]]
file = "src/caret.rs"
lines = 259

[[file_size_mark]]
file = "src/caret_glyph.rs"
lines = 539

[[file_size_mark]]
file = "src/commands.rs"
lines = 828

[[file_size_mark]]
file = "src/config/model.rs"
lines = 369

[[file_size_mark]]
file = "src/crashlog.rs"
lines = 546

[[file_size_mark]]
file = "src/cursor_shape.rs"
lines = 1028

[[file_size_mark]]
file = "src/daemon.rs"
lines = 588

[[file_size_mark]]
file = "src/debug.rs"
lines = 475

[[file_size_mark]]
file = "src/durable.rs"
lines = 527
reason = "item 189: +3 lines, the main/tests.rs bare-write accounted-for count moved 19->21 for two real-fixture marker-file writes the Switch-project-then-Goto capture law seeds."

[[file_size_mark]]
file = "src/export/docx.rs"
lines = 692

[[file_size_mark]]
file = "src/export/model.rs"
lines = 701

[[file_size_mark]]
file = "src/frontmatter.rs"
lines = 524

[[file_size_mark]]
file = "src/fs.rs"
lines = 1207

[[file_size_mark]]
file = "src/history/store.rs"
lines = 591

[[file_size_mark]]
file = "src/index.rs"
lines = 841

[[file_size_mark]]
file = "src/keymap.rs"
lines = 1133

[[file_size_mark]]
file = "src/keyspec.rs"
lines = 814

[[file_size_mark]]
file = "src/lava.rs"
lines = 946

[[file_size_mark]]
file = "src/main/args.rs"
lines = 1437
reason = "item 188: +31 lines net for one new capture mode in the one CLI parser — the `--screenshot-app` flag arm, the `Mode::ScreenshotApp` variant, its `live_app` flag and its Mode-selection arm, minus 5 lines from MERGING the strict-replay and storyboard hermetic-fs installs into the single call `scenario.rs`'s module doc already claimed was there. The mode's own body lives in `main/run/live_app.rs`; nothing here is extractable without splitting the single-owner arg parser."

[[file_size_mark]]
file = "src/main/run.rs"
lines = 1104
reason = "item 188: +17 lines — the `#[path] mod live_app;` declaration (+4), the `Mode::ScreenshotApp` dispatch arm (+9, a struct destructure plus a one-line delegate), and the `CaptureSubject` re-export split onto its own native-only line (+4) so the wasm build gains no unused-import warning. The mode itself lives in the new run/live_app.rs, kept out of this file for the same reason item 190 kept settings_effects.rs out."

[[file_size_mark]]
file = "src/markdown/spans.rs"
lines = 1057

[[file_size_mark]]
file = "src/mas.rs"
lines = 661

[[file_size_mark]]
file = "src/menu.rs"
lines = 1081

[[file_size_mark]]
file = "src/menubar.rs"
lines = 368

[[file_size_mark]]
file = "src/overlay/capture.rs"
lines = 761

[[file_size_mark]]
file = "src/overlay/kind.rs"
lines = 314
reason = "item 114: +12 lines at the per-kind statement site — the Settings foot-hint cells that now name a category and a back, and the workspace's canvas-bounded `window_rows` arm. Both are single no-wildcard matches over `OverlayKind`; splitting one would be splitting the exhaustiveness."

[[file_size_mark]]
file = "src/overlay/nav.rs"
lines = 571
reason = "item 114: +12 lines. `item_sections` gained the workspace's empty-sections arm and its argument — a workspace names its category once, in its rail, and `refilter`'s bucketing is the one owner that must not answer twice."

[[file_size_mark]]
file = "src/overlay/state.rs"
lines = 652
reason = "item 114: +13 lines in `foot_hint`, the ONE owner of what a summoned surface advertises; a workspace has two stages and each must say its own keys (ACCESSIBILITY.md — the footer is awl's only such statement)."

[[file_size_mark]]
file = "src/peek.rs"
lines = 549

[[file_size_mark]]
file = "src/probe.rs"
lines = 806

[[file_size_mark]]
file = "src/prosediff.rs"
lines = 1248

[[file_size_mark]]
file = "src/range.rs"
lines = 559

[[file_size_mark]]
file = "src/render.rs"
lines = 2455
reason = "item 194: +5 lines — the warp route's phase accumulator joins the three ambient resolvers already here (waves, lava, stars). This file's GPU floor is declared; the addition is one more member of a family that must stay beside its siblings to share the single sparse tick."

[[file_size_mark]]
file = "src/render/benchsuite/corpus.rs"
lines = 560

[[file_size_mark]]
file = "src/render/benchsuite/report.rs"
lines = 598

[[file_size_mark]]
file = "src/render/benchsuite/scenarios.rs"
lines = 580

[[file_size_mark]]
file = "src/render/caret.rs"
lines = 1025

[[file_size_mark]]
file = "src/render/chrome/hud.rs"
lines = 539

[[file_size_mark]]
file = "src/render/chrome/menubar.rs"
lines = 612

[[file_size_mark]]
file = "src/render/chrome/mod.rs"
lines = 1106
reason = "item 114: +23 lines. `OverlayGeom` gained the five workspace fields; its band/probe accessors moved to `chrome/workspace.rs`. The struct is the one resolved-geometry record every chrome consumer reads, so the fields cannot live elsewhere."

[[file_size_mark]]
file = "src/render/chrome/outline.rs"
lines = 894

[[file_size_mark]]
file = "src/render/chrome/overlay.rs"
lines = 758
reason = "item 114: +11 lines. `overlay_geometry` gained the workspace family's dispatch arm — the one place a card's presentation family is chosen — and `overlay_row_plan` now feeds the plan the CONTENT band rather than the card."

[[file_size_mark]]
file = "src/render/chrome/overlay_draw.rs"
lines = 485
reason = "item 114: +22 lines. `prepare_overlay` shapes the rail and `overlay_upload_text` pushes its own clipped area; the placement and clip themselves moved to `chrome/workspace.rs`, leaving only the batch entry the one upload owner must hold."

[[file_size_mark]]
file = "src/render/chrome/overlay_rows.rs"
lines = 309
reason = "item 114: +14 lines. `overlay_prepare_facet_marks` gained the workspace's own arm — two lines of dispatch plus its argument; the arm's body lives in `chrome/workspace.rs`."

[[file_size_mark]]
file = "src/render/chrome/overlay_shape.rs"
lines = 829

[[file_size_mark]]
file = "src/render/chrome/popover.rs"
lines = 630

[[file_size_mark]]
file = "src/render/chrome/readout.rs"
lines = 634

[[file_size_mark]]
file = "src/render/chrome/theme_picker.rs"
lines = 568
reason = "item 114: +8 lines. `theme_overlay_geometry` is an exhaustive `OverlayGeom` construction site (item 184 removed its `..base()`), so the five new workspace fields must be spelled out here."

[[file_size_mark]]
file = "src/render/framebench.rs"
lines = 1121

[[file_size_mark]]
file = "src/render/geometry.rs"
lines = 1502

[[file_size_mark]]
file = "src/render/layers.rs"
lines = 1330
reason = "items 132/194: +10 lines — prepare_background_layer now hands the background pass the grouped AmbientUpload rather than a bare drift scalar, and the warp pose is resolved at the same one seam the drift is. Unavoidable: this function is the single owner of what the background uniform receives each frame."

[[file_size_mark]]
file = "src/render/livingband.rs"
lines = 579

[[file_size_mark]]
file = "src/render/pipeline_draw.rs"
lines = 563
reason = "items 114/194: +9 lines — the workspace rail's glyph buffer and its four mirror fields, plus the warp phase accumulator, at the ONE TextPipeline initializer."

[[file_size_mark]]
file = "src/render/pipeline_geometry.rs"
lines = 603
reason = "item 114: +11 lines in `set_view`, the one `ViewState`->pipeline mirror, plus the rail-width measurement that must happen in the same `&mut FontSystem` window item 51's content-hug measurement already uses."

[[file_size_mark]]
file = "src/render/pipeline_overlay.rs"
lines = 423
reason = "items 132/194: +23 lines — the warped grid's two pipeline seams (the route-phase resolver beside its three siblings, and the pose the shader receives) plus the two lines that make advance_lava/freeze_lava the one door for every accumulator the shared tick owns. Extracting would put one member of a four-resolver family in its own file."

[[file_size_mark]]
file = "src/render/rects.rs"
lines = 1449

[[file_size_mark]]
file = "src/render/rowlayout.rs"
lines = 678

[[file_size_mark]]
file = "src/render/spans.rs"
lines = 1102

[[file_size_mark]]
file = "src/render/text.rs"
lines = 1440

[[file_size_mark]]
file = "src/replay.rs"
lines = 364
reason = "item 190: +4 lines net — classify_for's three settings-trio arms now call the new `typed::classify_settings` (mirroring how `classify_persistence` already keeps this match's own line budget), passing the effect-specific Unsupported reason and, for SettingToggle, the one excluded key (`\"keymap\"`, which needs a live keymap rebuild no capability grant can supply)."

[[file_size_mark]]
file = "src/search/keys.rs"
lines = 380

[[file_size_mark]]
file = "src/search/mod.rs"
lines = 855

[[file_size_mark]]
file = "src/selection.rs"
lines = 716

[[file_size_mark]]
file = "src/session.rs"
lines = 536

[[file_size_mark]]
file = "src/settings.rs"
lines = 730

[[file_size_mark]]
file = "src/soak_gpu/mod.rs"
lines = 618

[[file_size_mark]]
file = "src/spell.rs"
lines = 1629

[[file_size_mark]]
file = "src/stars.rs"
lines = 724

[[file_size_mark]]
file = "src/stats.rs"
lines = 713

[[file_size_mark]]
file = "src/storyboard.rs"
lines = 618

[[file_size_mark]]
file = "src/streaks.rs"
lines = 833

[[file_size_mark]]
file = "src/syntax/mod.rs"
lines = 752

[[file_size_mark]]
file = "src/syntax/ruby.rs"
lines = 644

[[file_size_mark]]
file = "src/syntax/toml.rs"
lines = 515

[[file_size_mark]]
file = "src/theme/derive.rs"
lines = 315

[[file_size_mark]]
file = "src/theme/model.rs"
lines = 537
reason = "items 132/194: +2 lines — has_ambient_motion gained the warped-grid arm, so the new ground inherits the page-mode force and the move-stream present hold rather than re-implementing them. The match is no-wildcard by design; the arm cannot live anywhere else."

[[file_size_mark]]
file = "src/theme/worlds.rs"
# item 121: one `icon_ground: IconGround::Base100,` field per shipped world
# literal (the same per-world-explicit shape `icon_cursor` already has) —
# irreducible without inventing a second, implicit default path the design
# deliberately rejects (item 121's mechanism note: represent the capability
# as theme-owned data, never a silent default a world could skip choosing).
# Combined with item 158's Paperbark addition + Ornaments::of compaction
# (1110 -> 1099) on top of the rebased tree. Item 132 added Kite and moved the
# never-enrolled gallery audition out to theme/worlds_gallery.rs, so this file
# holds only the shipped roster (1124 -> 1123).
lines = 1123

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "tests/caret_mono_grid_pixels.rs"
line = 205
message = "this function has too many lines (141/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line/count shifted by queue item 168's testscratch::ScratchDir conversion."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/input/mouse.rs"
line = 866
message = "this function has too many lines (107/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 172 replaced two hand-written ladder conjunctions with one call. Item 183 narrowed the &ActiveEventLoop parameter to app::Exit, collapsing the wrapped signatures (-5). Line shifted by queue item 114's summoned Settings workspace (the navigation rail's own click resolution)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/viewstate.rs"
line = 5
message = "this function has too many lines (241/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 172 bound the summoned picker once for the whole ViewState projection (-46). Item 114 added the `overlay_workspace` projection to the one exhaustive construction site (+1)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/window.rs"
line = 485
message = "this function has too many lines (132/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 172 routed the autosave clock through PersistenceRuntime (-1)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app.rs"
line = 841
message = "this function has too many lines (217/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 172 moved 6 lines of state out of the constructor into two owners. Line shifted by item 188's two module declarations (`capture_state`, `press`) landing earlier in the file (+7); count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app.rs"
line = 1343
message = "this function has too many lines (106/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Line shifted by item 172's field removals; count unchanged. Line shifted by item 188's two module declarations (+7); count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/lifecycle.rs"
line = 22
message = "this function has too many lines (118/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app.rs"
line = 1602
message = "this function has too many lines (102/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Line shifted by item 172's field removals; count unchanged. Line shifted by item 188's two module declarations (+7); count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/args.rs"
line = 251
message = "this function has too many lines (749/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 121 added one minimal --ground-audition arm (+13 lines). Item 188 added one CLI flag arm, one Mode variant selection arm and its `live_app` flag to the one parser (+20), and MERGED the two hermetic-fs installs into one unconditional call (-1, after routing its one branch through the new `scenario::seed_document` owner) — the net is the smallest a new capture mode can cost in a single-owner arg parser."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/run.rs"
line = 297
message = "this function has too many lines (152/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Line shifted by item 190's `mod settings_effects;` declaration landing earlier in the file (+2); count unchanged. Line shifted by item 188's `mod live_app;` declaration and the native-only `CaptureSubject` re-export landing earlier in the file (+8); count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/run.rs"
line = 591
message = "this function has too many lines (112/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Line shifted by item 190's `mod settings_effects;` declaration landing earlier in the file (+2); count unchanged. Line shifted by item 188's `mod live_app;` declaration and the native-only `CaptureSubject` re-export (+8); count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/run.rs"
line = 760
message = "this function has too many lines (330/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Line shifted by item 190's `mod settings_effects;` declaration landing earlier in the file (+2); count unchanged. Item 188 added the `Mode::ScreenshotApp` dispatch arm — a struct destructure plus a one-line delegate into `run/live_app.rs`, where the mode itself lives (+9)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/modes.rs"
line = 201
message = "this function has too many lines (213/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/sidecar.rs"
line = 53
message = "this function has too many lines (105/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 188 added the one `driver` argument to the one sidecar format call (+1)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/sidecar.rs"
line = 244
message = "this function has too many lines (140/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 114 added the `overlay.workspace` field to the one format string (+1). Line/size moved by queue item 114's summoned Settings workspace. Line shifted by item 188's `driver` argument + its comment (+2); count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/caret_glyph.rs"
line = 163
message = "this function has too many lines (213/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/config/model.rs"
line = 149
message = "this function has too many lines (111/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/export/pdf/writer.rs"
line = 12
message = "this function has too many lines (123/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/main/story.rs"
line = 42
message = "this function has too many lines (122/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn. Item 189 stopped pre-resolving `effective_workspace` (ReplaySession now resolves the raw flag itself) (-1)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/image_pipeline.rs"
line = 75
message = "this function has too many lines (133/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/markdown/spans.rs"
line = 453
message = "this function has too many lines (159/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/panel.rs"
line = 62
message = "this function has too many lines (119/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/overlay_draw.rs"
line = 199
message = "this function has too many lines (182/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam. Item 114 added the workspace rail's own clipped text area (+17). Line/size moved by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/menubar.rs"
line = 49
message = "this function has too many lines (131/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/menubar.rs"
line = 233
message = "this function has too many lines (158/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/outline.rs"
line = 185
message = "this function has too many lines (104/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/outline.rs"
line = 472
message = "this function has too many lines (124/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/theme_picker.rs"
line = 179
message = "this function has too many lines (167/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam. Line/size moved by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/theme_picker.rs"
line = 388
message = "this function has too many lines (124/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam. Line/size moved by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/hud.rs"
line = 114
message = "this function has too many lines (220/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/hud.rs"
line = 355
message = "this function has too many lines (169/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/popover.rs"
line = 236
message = "this function has too many lines (135/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/preview.rs"
line = 56
message = "this function has too many lines (110/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/preview.rs"
line = 243
message = "this function has too many lines (128/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/whichkey.rs"
line = 33
message = "this function has too many lines (142/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/blur.rs"
line = 119
message = "this function has too many lines (104/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/text.rs"
line = 414
message = "this function has too many lines (134/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/text.rs"
line = 985
message = "this function has too many lines (140/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/rects.rs"
line = 761
message = "this function has too many lines (151/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/layers/table_grid.rs"
line = 40
message = "this function has too many lines (310/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/layers.rs"
line = 950
message = "this function has too many lines (170/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/perfbench.rs"
line = 68
message = "this function has too many lines (135/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/framebench.rs"
line = 435
message = "this function has too many lines (112/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/benchsuite/mod.rs"
line = 59
message = "this function has too many lines (110/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/caretbench.rs"
line = 94
message = "this function has too many lines (111/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/pipeline_draw.rs"
line = 7
message = "this function has too many lines (497/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions. Item 114 added the workspace rail's buffer and its four mirror fields (+6)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render.rs"
line = 970
message = "this function has too many lines (105/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/selection.rs"
line = 198
message = "this function has too many lines (106/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/spellunderline.rs"
line = 66
message = "this function has too many lines (132/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/bash.rs"
line = 39
message = "this function has too many lines (127/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/html.rs"
line = 40
message = "this function has too many lines (123/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/ruby.rs"
line = 46
message = "this function has too many lines (137/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/syntax/toml.rs"
line = 51
message = "this function has too many lines (118/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/actions/tests/picker_misc_smoke.rs"
line = 705
message = "this function has too many lines (122/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/actions/tests/mod.rs"
line = 528
message = "this function has too many lines (109/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line/size moved by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/actions/tests/mod.rs"
line = 734
message = "this function has too many lines (109/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/actions/tests/mod.rs"
line = 733
message = "this function has too many lines (220/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line/size moved by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/actions/tests/mod.rs"
line = 985
message = "this function has too many lines (109/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app/tests/files.rs"
line = 1627
message = "this function has too many lines (106/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by item 172's summoned-layer accessor migration; count unchanged."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/app_icon/tests.rs"
# `roster_growth_does_not_dilute_a_known_erosion` — same function, same
# size, position fixed up after the item-121/158/161/168 rebase.
line = 1909
message = "this function has too many lines (117/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/folds.rs"
line = 363
message = "this function has too many lines (126/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line/count shifted by queue item 168's testscratch::ScratchDir conversion."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
line = 742
message = "this function has too many lines (107/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
line = 1183
message = "this function has too many lines (138/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
line = 1486
message = "this function has too many lines (111/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/panels.rs"
line = 1640
message = "this function has too many lines (126/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace (+1 for the `workspace` field)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/pickers_faceted.rs"
line = 218
message = "this function has too many lines (113/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace (+1 for the `workspace` field)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/pickers_faceted.rs"
line = 352
message = "this function has too many lines (128/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace (+1 for the `workspace` field)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/pickers_faceted.rs"
line = 511
message = "this function has too many lines (157/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace (+1 for the `workspace` field)."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/capture/tests/pickers_faceted.rs"
line = 896
message = "this function has too many lines (114/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam. Line shifted by queue item 168's testscratch::ScratchDir conversion. Line shifted by queue item 114's summoned Settings workspace (+1 for the `workspace` field)."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/export/pdf/tests.rs"
line = 25
message = "the function has a cognitive complexity of (31/25)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/keymap/tests.rs"
line = 1513
message = "this function has too many lines (103/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/chrome/readout.rs"
line = 490
message = "this function has too many lines (112/100)"
reason = "Existing chrome draw owner; extraction needs a deliberate rendering-state seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/rowlayout.rs"
line = 227
message = "this function has too many lines (101/100)"
reason = "Existing renderer owner; extraction must preserve adjacent GPU state transitions."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/caret_transition_item105.rs"
line = 1257
message = "this function has too many lines (115/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/caret_transition_item105.rs"
line = 1593
message = "the function has a cognitive complexity of (31/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/caret_transition_item105.rs"
line = 1593
message = "this function has too many lines (182/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/chrome_overlay.rs"
line = 205
message = "this function has too many lines (128/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."


[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/chrome_panels.rs"
line = 1315
message = "this function has too many lines (124/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/distinguishability.rs"
line = 155
message = "this function has too many lines (104/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/distinguishability.rs"
line = 642
message = "this function has too many lines (125/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."



[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/images.rs"
line = 1739
message = "this function has too many lines (105/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
line = 693
message = "this function has too many lines (123/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
line = 1046
message = "this function has too many lines (145/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
line = 1311
message = "this function has too many lines (109/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/list_surfaces.rs"
line = 2251
message = "this function has too many lines (117/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/markdown.rs"
line = 260
message = "this function has too many lines (132/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/one_bit.rs"
line = 501
message = "this function has too many lines (108/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/oracle.rs"
line = 303
message = "the function has a cognitive complexity of (28/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/oracle.rs"
line = 303
message = "this function has too many lines (122/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/outline.rs"
line = 1150
message = "the function has a cognitive complexity of (27/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/outline.rs"
line = 1150
message = "this function has too many lines (125/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/overlay_personality.rs"
line = 1282
message = "this function has too many lines (120/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/overlay_rhythm_item112.rs"
line = 57
message = "the function has a cognitive complexity of (27/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/overlay_rhythm_item112.rs"
line = 57
message = "this function has too many lines (159/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/page_frame.rs"
line = 89
message = "this function has too many lines (183/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/syntax_ligatures.rs"
line = 80
message = "the function has a cognitive complexity of (29/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/render/tests/syntax_roles.rs"
line = 116
message = "the function has a cognitive complexity of (36/25)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/render/tests/syntax_roles.rs"
line = 116
message = "this function has too many lines (192/100)"
reason = "Existing end-to-end test scenario; split only with a coherent setup/assertion seam."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/syntax/tests.rs"
line = 123
message = "the function has a cognitive complexity of (29/25)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/theme/tests.rs"
line = 214
message = "the function has a cognitive complexity of (30/25)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests.rs"
line = 214
message = "this function has too many lines (155/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests.rs"
line = 965
message = "this function has too many lines (101/100)"
reason = "Item 132: a per-world roster sweep whose length IS the roster's, over a no-wildcard `Background` match that gained one arm for the warped grid. Its twin one function above (the outline rail) carries the same shape. The honest fix is merging the two frost-rail sweeps into one owner, a focused follow-up rather than metric-only churn here."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests.rs"
line = 625
message = "this function has too many lines (130/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::cognitive_complexity"
file = "src/theme/tests.rs"
line = 1870
message = "the function has a cognitive complexity of (27/25)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

[[clippy_exception]]
lint = "clippy::too_many_lines"
file = "src/theme/tests.rs"
line = 2986
message = "this function has too many lines (161/100)"
reason = "Existing named owner; decomposition needs a focused follow-up rather than metric-only churn."

# Add structural exceptions only for intentional URL/unbreakable/rustfmt-skip/generated/architectural cases.
# Each entry must pin the exact line text so removal or movement is stale.
# [[structural_exception]]
# target = "src/example.rs"
# kind = "unbreakable"
# line = 12
# text = "..."
# reason = "Why this exact source line cannot follow the ordinary rule."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 8
text = "            \"assets/keymap-defaults.toml names {slug_in_file:?}, which is not a commands::COMMAND_SEED slug\""
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 752
text = "                        \"{}: {:?}'s resolved native chord {resolved:?} must parse via parse_binding\","
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 759
text = "                        \"{}: {:?}'s resolved native chord {resolved:?} must resolve to the catalog action\","
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 1584
text = "                        \"{}: reserved native chord {native_resolved:?} still shown verbatim ({convention:?}/{platform:?})\","
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 1602
text = "                            \"{}: no alternate defined, label should be blank ({convention:?}/{platform:?})\","
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/commands/tests.rs"
kind = "architectural"
line = 1613
text = "                        \"{}: displaced emacs default {:?} still shown ({convention:?}/{platform:?}) — label was {label:?}\","
reason = "Pure relocation from the inline src/commands.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/main/tests.rs"
kind = "architectural"
line = 3709
text = "                \"skipping caret_render_is_a_pure_function_of_mode_and_world_across_a_wagtail_detour: no wgpu adapter\""
reason = "Pure relocation from the inline src/main/run.rs test module; this exact line was grandfathered there. Line number shifted by queue item 168's testscratch::ScratchDir conversion, item 171's replay tests, item 189's Switch-project-then-Goto law + edge-case tests, and item 190's settings-trio hermetic tests."

[[structural_exception]]
target = "src/main/tests.rs"
kind = "architectural"
line = 3749
text = "            \"mode {mode:?}: caret pixels must be byte-identical whether or not Wagtail was visited in between\""
reason = "Pure relocation from the inline src/main/run.rs test module; this exact line was grandfathered there. Line number shifted by queue item 168's testscratch::ScratchDir conversion, item 171's replay tests, item 189's Switch-project-then-Goto law + edge-case tests, and item 190's settings-trio hermetic tests."

[[structural_exception]]
target = "src/main/tests.rs"
kind = "architectural"
line = 3783
text = "            \"skipping item_106_pointer_replay_seam_reproduces_a_keyboard_scroll_stealing_a_stationary_pointer_check: \\"
reason = "Pure relocation from the inline src/main/run.rs test module; this exact line was grandfathered there. Line number shifted by queue item 168's testscratch::ScratchDir conversion, item 171's replay tests, item 189's Switch-project-then-Goto law + edge-case tests, and item 190's settings-trio hermetic tests."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 182
text = "        \"narrow: outline granted its full preferred rail (within the whole-pixel snap), avail={avail} pref={pref}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 321
text = "        \"boundary resolves to WIDE (no shift) at the exact threshold: left={left} symmetric={symmetric}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 342
text = "            \"shifted column must still fit the window (win={win} measure={measure}): left={left} width={width}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 373
text = "                \"width {w}px: column_left jumped {step}px in a single pixel of resize ({p} -> {left}) — the jitter bug\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/render/geometry/tests.rs"
kind = "architectural"
line = 430
text = "                    \"width {w}: symmetric-regime left must step exactly 0 or 1 whole px per width px, got {step}\""
reason = "Pure relocation from the inline src/render/geometry.rs test module; this exact line was grandfathered there."

[[structural_exception]]
target = "src/settings/tests.rs"
kind = "architectural"
line = 705
text = "            \"{kind:?} must have exactly one palette door (commands={command_doors}, rows={row_doors})\""
reason = "Pure relocation from the inline src/settings.rs test module; this exact line was grandfathered there."
