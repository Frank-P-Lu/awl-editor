# awl — live build queue

> Live execution state only. Completed and superseded work is in git history
> (`git log -p .orchestrator/queue.md`). Protocol, claiming, worktrees, and
> execution hygiene live in `.orchestrator/README.md`.

## Ready — harness correctness

98. **`i18n_fixtures::sidecar_reports_doc_lang_and_per_script_font_resolution` flakes under full-suite parallelism.** **Build:** Make the test deterministic under the real parallel suite, or prove it is already and find the actual culprit. **Scope:** Surfaced by item 97's independent verification (landed 2026-07-26), which is the only reason it was seen — it failed ONCE during a full parallel `cargo test`, then passed in isolation on BOTH base and branch and passed again on a clean full rerun, so it is pre-existing and orthogonal to item 97's diff. The reported failure was a CJK font-family mismatch in the sidecar's per-script font resolution. That shape points at shared global font state rather than at the assertion: CLAUDE.md's standing discipline is one process-wide reentrant guard, `crate::testlock::serial()`, taken by every test and every `cfg(test)` global writer, so the first question is whether this test (or a `cfg(test)` writer it races) is missing that guard. Do NOT fix this by retrying, loosening the assertion, or marking it `ignored`. A flaky test in the suite that GATES the merge train is worse than a failing one — it teaches everyone to re-run until green, which is exactly how item 89's and item 94's real defects would have been waved through. **Done:** The test passes deterministically across repeated full-suite runs, and whatever global it races is either serialized through `testlock::serial()` or no longer global. **Verify:** Run the full suite repeatedly (at least 5×) and under deliberate parallel load, exit status preserved; if the race is in a `cfg(test)` global writer, add a law that the writer takes the lock. **Unclaimed; small but load-bearing — the merge gate's honesty depends on it.**

96. **The sidecar mixes scaled and unscaled geometry and never reports zoom — close the trap at the source.** **Build:** Make the capture sidecar's reported font metrics agree in scale with everything else it reports, so `top + n * font.line_height` is correct at every zoom rather than only at 1.0. **Scope:** Surfaced by item 93's diagnosis (landed 2026-07-26). `Metrics::with_dpi` (`src/render.rs:389`) scales every pixel metric by `s = zoom * dpi`, and the sidecar's `text_origin` and `page.column` ARE scaled — but `src/capture/sidecar.rs:196` emits `lh = render::LINE_HEIGHT` and `font.size` as the raw UNSCALED base constants, and the sidecar has no `zoom` field at all. The failure mode is the dangerous kind: a test computing a row band from `font.line_height` silently aims at the wrong row and reports a plausible-looking pixel diff instead of an error — which is exactly how item 93 presented as a render regression for a day. Item 93 fixed the immediate red by pinning `AWL_CONFIG` and passing `--zoom 1.0`, and documented the hazard in `CAPTURE.md`/`render.rs`; it did NOT close it at the source. Emit the EFFECTIVE `metrics.font_size` / `metrics.line_height`, and/or add an explicit `font.zoom`; bump `capture::SCHEMA_VERSION` with its ledger row and update `CAPTURE.md`'s field table. Audit every existing consumer of `font.line_height` for the same latent bug — CLAUDE.md's standing policy is that a bug's neighborhood gets audited because bugs cluster. **Done:** The sidecar's reported font metrics and its reported origins share one scale at any zoom; a test can derive a row band correctly at zoom 1.5 without knowing about the trap. **Verify:** Capture the same document at two or more zooms and assert the MEASURED row pitch in the PNG equals the reported `line_height` at each — the law item 93 could not write because the value was wrong; schema-ledger test; full native and wasm gates. **Unclaimed; harness-correctness follow-up — the sidecar is the state oracle, so an oracle that lies about scale outranks feature work.**

## Ready — current user-visible wave

89. **Item-86 zigzag-field correctness follow-up — tile Quokka and Gumtree’s page-margin zigzags across the field.** Reopen item 86’s light-world ground as a correctness repair: the current shader repeats teeth along one wandering chevron line but does not tile lines vertically across the margin field, while the previous tests proved only that some pattern pixels existed. Replace that isolated stroke with a genuinely repeated Mario-like zigzag field so every substantial page-margin region visibly contains material rather than large blank areas. Keep the writing column untouched. At an ordinary window height, show roughly three broad visible zigzag rows, with Quokka tighter and more playful and Gumtree broader and quieter. Preserve both worlds’ cards, typography, and palettes, and leave Bombora unchanged; do not open a new taste round unless real captures expose a genuine design fork. Gate Bombora’s WGSL-mirror constants `WAVE_AMP`, `WAVE_FREQ`, `WAVE_PHASE_1`, and `WAVE_PHASE_2` plus `waves_boundaries()` with `cfg(test)`, making the helper private if no cross-module test requires it; preserve runtime `waves_drift_radians()` and the valuable Bombora tests, and do not use `allow(dead_code)`. Add real-pixel occupancy laws that sample multiple vertical and horizontal cells, assert exclusion from the writing column and deterministic rendering, generate representative Quokka and Gumtree captures at multiple sizes, run the standing vision smoke and frame-cost check, confirm the release build has no dead-code warnings from the Bombora test mirrors, then pass the full native and wasm gates. 🟡 IN PROGRESS (REPAIR BUILT, RE-VERIFY RUNNING) — claude, branch `item-89-zigzag-field` @ 184636e, rebased onto main dade6ab. The repair made coverage STRUCTURAL rather than tuned: the first fold used `row_h = period_px`, a square lattice that tiles but does not cover — row k's ribbon only reaches within `amp + t` of `k·row_h`, so whenever `2·amp + 2·t < period_px` a hard blank lane exists that no chevron ever enters (~70px every 250px on Gumtree's dials). Setting `row_h = 2·amp + thickness` makes each row sweep exactly one pitch, so consecutive rows abut for ANY dials, angle, viewport or aspect; it retires the old amplitude-vs-period collision clamp rather than adding a guard. Gumtree's dials moved 250/85 → 170/60 at the same ~0.35 profile ratio item 86 authored; Quokka 100/24 unchanged. Self-reported: 324 cells across a 12-geometry sweep, tightest occupancy 5.48× the floor, zero blanks, both previously-failing bands inked; full suite green including the two tests item 93 fixed. NOT YET INDEPENDENTLY VERIFIED — the first re-verify agent aborted without running, so a fresh one is measuring now, including whether coverage really is structural at dial pairs the authors did not choose.

  **89a. ⛔ QUESTION FOR THE USER — the row-count tradeoff (does not block the correctness fix; blocks only the final look).** The board specified "roughly three broad visible zigzag rows" at an ordinary height. Void-free coverage forces Gumtree to about SIX. The repair argues this is unavoidable rather than a tuning miss: a chevron that leaves no blank lane must have teeth spanning its own pitch, so "rows three times the height of their teeth" and "no blank lane" are mutually exclusive — it reports checking the escape hatch, where even a 57px-thick ribbon at ~250px pitch still left blank cells across the sweep. Broadness now lives in Gumtree's 170px tooth wavelength against Quokka's 100px — a pitch 2.5× Quokka's at a third of its contrast — rather than in tall sparse rows. The re-verify has been asked to independently check whether the tradeoff is genuinely forced. (a) ACCEPT ~6 denser rows and amend the item's three-row line; (b) keep three rows and accept bounded blank lanes, reverting to a coverage claim about typical geometry rather than all of it; (c) something else on seeing the captures. Purely a look call — the harness can measure row counts but not taste.

92. **Per-world pre-rendered macOS app icons — export the `aw` + cursor-backed `l` lockup through a renderer-independent web compositor.** **Build:** Turn the decided Turn-3 icon direction into one minimal, deterministic offline exporter and a complete checked-in icon set. The wordmark is `aw` plus a regular lowercase `l` in the world’s display face, at the same font size and baseline; the cursor is a deliberately fake logo shape drawn behind the `l`, never awl’s live Block/Morph/I-beam renderer. Support exactly three logo-cursor presets — rectangular block, pill, and super-narrow pill — with bounded per-face optical tuning for radius/padding/weight only; ordinary web shaping owns the letterspacing, so there are no hand-positioned glyph coordinates or per-world render branches. Colors come from the real theme data (`base_100` ground, `base_content` `aw`, `primary` cursor, `primary_content` `l`), ambient backgrounds flatten to `base_100`, and shared faces reuse one tuning. Extract only the lockup from the downloaded Claude Design study: do not ship or depend on its generated `support.js`, React runtime, bundled page, remote fonts, or network. Use the repo’s already-bundled/licensed font files and one theme-derived export manifest so icon palette/font facts are not copied into a second hand-maintained table. Render the required macOS icon representations ahead of time with a pinned offline web export step, review them, and commit the resulting assets; ordinary Cargo builds and the shipping app invoke no browser and no awl/wgpu renderer. Package one canonical default `.icns` for Finder and use the pre-rendered active-world image for the live Dock/app-switcher icon after sticky-theme restore and on a committed theme change only — picker hover previews do not churn the Dock. Scope this round to macOS; Linux launcher and web-favicon behavior are follow-ups, not release blockers. **Done:** Every shipped world has a recognizable, correctly colored icon; its normal-size `l` remains baseline-aligned and legible inside the assigned fake cursor at master, Dock, and small app-switcher sizes; switching/committing/relaunching a theme selects the matching embedded image while Finder retains the canonical bundle icon; the release bundle no longer falls back to the generic application icon. **Verify:** Generate a named all-world gallery on dark and light Dock strips at the full representation roster, with the 24/44/56px checks from the study; add no-wildcard coverage proving every `THEMES` member resolves exactly one asset and every asset maps back to a live world; use pixel arithmetic for safe-area bounds, ground/ink/cursor token identity, `l` visibility, and small-size distinction; prove regeneration is byte-deterministic in the pinned exporter; dry-run package and inspect `Contents/Resources/Awl.icns` plus `CFBundleIconFile`; live-smoke initial sticky-theme restore, committed switching, cancel/revert, app switcher, About panel, and relaunch, then pass native and wasm gates. **Release-blocking visual asset round — bounded candidate/gallery generation, Fable judges the generated images only, implementer applies the verdict, then Sonnet performs the pixel/package audit.** DECIDED INPUT: the Claude Design study is `~/Downloads/Awl icon directions.html` (outside the repo — extract only the lockup; never ship or depend on its `support.js`, React runtime, bundled page, or remote fonts). 🟡 IN PROGRESS — claude, branch `item-92-world-app-icons`.

## Ready — shared ownership and performance

94. **Reusable Settings range rows — one keyboard-and-pointer slider grammar, proved first on Zoom.** **Build:** Add a generic `SettingKind::Range` and one typed range-spec owner (minimum, maximum, step, default, display unit, and linear/logarithmic rail mapping), then migrate the existing Zoom value row as its first real customer. A range row always shows its quiet rail, thumb, and current value in the secondary column; when selected, Left/Right changes exactly one authored step, with ordinary key repeat for longer travel. Mouse-only use is equally complete: clicking the rail sets the nearest step and dragging the thumb scrubs live through the same setter. Enter retains the existing exact numeric-entry path. Do not add Shift/Option fine/coarse variants, plus/minus buttons, wheel-to-change, or a bespoke scroll-only widget. Clicking the label selects without changing; clicking or dragging the rail changes immediately, with a generous hit target around the visually small thumb. Pointer drag applies live on every resolved step but persists once on release; keyboard steps use the existing discrete sticky-setting persistence path. Route keyboard, pointer, replay/headless, readout, apply, and persistence through the same range identity/spec owner; keep the caret as the only amber element, so rail/thumb use muted theme-derived ink. **Scope:** This item builds the reusable interaction before the risky scroll migration. Zoom uses its existing 50%–300% clamp and 10-percentage-point step on a linear percentage rail, preserving its current authored increments and exact-entry parsing. Do not migrate page widths yet and do not introduce new settings. **Done:** Zoom can be changed, live-previewed, persisted, restored, and exactly typed with either keyboard or mouse; no input path computes a parallel value; toggles, pickers, submenus, paths, actions, and ordinary numeric entry remain unchanged. **Verify:** Add exhaustive no-wildcard laws for every `SettingKind`/`SettingId`, pure rail↔value round-trip and clamping tests, incremental-vs-batched drag equivalence, click/drag hit-target tests, keyboard/mouse parity through the real apply seam, one-persist-per-drag proof, sidecar/replay coverage, light/dark real-pixel geometry and contrast checks, the standing vision smoke, native + both keymap conventions + wasm gates. **Interaction-system prerequisite for item 90; routine implementation followed by an independent accessibility/input audit.** 🟡 IN PROGRESS (REPAIR ROUND) — claude, branch `item-94-settings-range-rows`. First build (2542a26) FAILED the independent accessibility/input audit and is NOT merged. The RangeSpec architecture, identity map, rowlayout Rail geometry, keyboard/pointer parity, single-owner value math, hit target (~39x the drawn thumb area), honoured exclusions and log-rail generality (25/50/100/200/400 seat at equal intervals) all verified GOOD and stay. DEFECT 1 (correctness, blocks item 90): a PAUSED drag persists a mid-gesture value before release, contradicting the item's own "persist ONCE on release". `range_apply_live` -> `set_zoom` -> `mark_zoom_dirty` arms the same `zoom_persist_at` debounce the Cmd-+/- and Cmd-wheel doors use, and `about_to_wait_impl` fires it after 500ms of quiet with no gate on `range_drag`. The shipped proof never advanced the clock, so it structurally could not catch this. Repair must leave ONE owner of "when does zoom persist" and add the missing clock-driven law (VirtualClock/RecordingScheduler, as in which_key.rs). DEFECT 2 (accessibility regression this item introduced): the Settings footer is fixed per-OverlayKind and still reads "left-right lens", but Left/Right now step and persist the value on a Range row — a keyboard-only user following the on-screen hint silently mutates their zoom. ACCESSIBILITY.md rests on keyboard operability plus a trustworthy hint bar; there is no screen-reader tree to fall back on.

90. **Continuous document scrolling — replace row quantization with one shared semantic scroll position.** **Build:** Replace awl’s row-quantized document scrolling with one continuous scroll position shared by mouse/trackpad scrolling and keyboard caret-follow. **Scope:** The current failure is that trackpad pixels are accumulated into 16px thresholds and then rounded to whole visual-row changes, while caret-follow can also position the viewport only at visual-row boundaries; because one inline-image row may be hundreds of pixels tall, either input can discard the entire image in one jump. Introduce one semantic scroll position consisting of a top visual-row anchor plus a pixel offset within that row; feed real trackpad deltas through item 94’s decided Scroll sensitivity range, translate real-mouse wheel notches into a pixel distance, leave Page Up/Down a caret mover while deriving its screenful from the pixel viewport, and make caret-follow move only enough to reveal the caret row box. Route hit-testing, zoom anchoring, typewriter mode, session/buffer restoration, rendering, and headless capture through that owner. Picker lists remain deliberately row-based; preserve item 82’s intersection-aware image culling. **Done:** Tall images and other variable-height blocks move smoothly and progressively under small mouse deltas; pressing Down past an image scrolls only enough to reveal the new caret line; reversing a scroll gesture restores the same geometry without jumps; ordinary wrapped text, headings, tables, folds, page mode, and buffer switching remain aligned. **Verify:** Extend the capture seam to express and report intra-row scroll; prove that batched and incremental pixel deltas reach the same position, that Down/Up across a viewport-tall image is minimal and monotone in its own direction, that an image remains drawn until its actual bottom exits, and that caret/hit-test geometry agrees throughout. Audit tall images, tall tables, headings, wrapping, zoom/DPI, resize, selection drag, typewriter mode, folds, session restore, native wheel and trackpad paths, and WebGL; measure release scrolling with O(visible) work, then pass the full native and wasm gates. **High-risk scroll-state/ownership migration.** ⏸ WAITING ON ITEM 94 — all three user feel decisions are RESOLVED in 90b below; the qualifying ownership map and adversarial review are DONE (2026-07-26), and S0–S8 are dispatchable once the shared Range interaction lands.

  **90a. DECIDED PLAN (binding; supersedes any re-derivation).** The scroll fact is one `usize` mirrored through three fields — `BufferExtra.scroll_lines` (`app/files/active.rs:38`), `ViewState.scroll_lines` (`render/viewstate_def.rs:35`), `TextPipeline.scroll_lines` (`render.rs:3238`) — becoming pixels at ONE formula written twice (`geometry.rs:1239`, `geometry.rs:1835`). Everything downstream is already pixel-accurate via `RowGeom` (`render/rowgeom.rs`), so this is a TYPE migration with a narrow behavioral tail, not a rewrite. Introduce `render::ScrollPos { row: usize, px_q: i32 }` with `TextPipeline` as the sole `ScrollPos ↔ pixels` resolver. **Store the sub-row offset as fixed-point integer sub-pixels (1/64 px), never `f32`** — it preserves exact `Eq`/`Hash`, keeps `session::BufferPos`'s `Eq` derive (`session.rs:38`) and its integer-TOML round trip, and removes float drift from the invariants. Justify `(row, px)` on its real merit — it reproduces today's semantics exactly, which is what makes the byte-identity gate provable — and do NOT claim row-anchoring survives a re-wrap unharmed (it does not; that is why `zoom_anchor_scroll` exists).

  **Sequence — the representation change and the behaviour change are SEPARATE steps; combining them is a plan failure because a later regression becomes unattributable.** S0 archive baseline benches + a reference capture set outside the repo. S1 value type + resolver, unused on the live path. S2 renderer projection — the wide compile-error step, and it ABSORBS the mechanical `ScrollPos::at_row(old)` shims across `app/`, `capture/`, `benchsuite/` and `tests/` (they are not deferrable to S3/S4; `scroll_lines` has 103 references in `src/`); gated on capture BYTE-IDENTITY since `px` is structurally 0 here. S3 the App's *stored* fact becomes `ScrollPos`. S4 the sidecar *reports* it, and the three independent scroll re-derivations (`capture/modes.rs:506`, `capture/animated.rs:112`, `:240`) collapse into one `settled_scroll` owner. S5 input becomes pixel-native (first step where `px ≠ 0`). S6 follow becomes minimal. S7 page + persistence. S8 audit + gates. S4 ∥ S5; S6 needs both.

  **Corrections that are part of the decision (the adversarial pass caught these; do not re-litigate).** (1) The caret-follow reveal target is the caret's **ROW BOX**, affinity-derived as today — NOT the caret rect. The caret is drawn centred in its row height (`caret.rs:154-155`), so revealing the caret rect would scroll a 520px image row only until its MIDDLE cleared the fold, leaving the bottom half below it: a straight regression on the exact case this item exists to fix. Over-tall rows pin to `row_top`, preserving today's behaviour from both directions. Drop `caret_doc_rect` (its model `caret_doc_xy` is `cfg(not(wasm32))`-gated — a follow-critical helper must not be native-gated). (2) Do NOT delete the notch accumulator; only the *document* remainder moves into `BufferExtra.scroll`. The overlay/picker and ⌘-wheel-zoom arms still need it, and a law must assert a 3px trackpad event advances a picker selection by ZERO rows. (3) `char_screen_top` (`geometry.rs:1351`) is a THIRD copy of the origin formula and `keys.rs:211` a fourth partial one — route both; make `row_top_px` module-private and assert the needle `- self.row_top_px(` occurs exactly once repo-wide, because the naive audit needle would pass forever while the copy survived. (4) The History-diff arm of `ViewState.scroll` constructs `ScrollPos::at_row(ds)` and NEVER touches `px`; it clamps on `max_scroll_rows`, not pixels; add a law that `scroll_px == 0` whenever a diff preview is active. (5) `--wheel` does NOT prove the input path — it never constructs an `App`. Either give `wheel_scroll` a real injectable seam (viewport height + resolver as parameters) or declare that arm LIVE-ONLY behind the `wheel-px` probe; App unit tests have no `gpu`, so `max == 0` and today's `wheel_scroll` clamps to zero, making the "app test seam" laws unreachable as written. (6) Split the DONE claim: *scroll gestures* are reversible; *caret motion* is minimal and monotone-in-direction, NOT reversible — minimal-reveal is legitimately hysteretic, so a naive Down/Up reversibility gate would fail by design. (7) Normalize incrementally (`px += dy` then carry across row boundaries), not by round-tripping through absolute pixels. (8) Report BOTH `scroll_top_px` (the rendered, rounded offset the PNG obeys) and `scroll_px` (the intra-row state), and say which is which in CAPTURE.md's field table. (9) Add the blur-backdrop cache key (`pipeline_prepare.rs:178`) to the hash — it is the only scroll-keyed cache in the renderer, and a stale frosted backdrop is the failure. (10) Name and law-test the neighbouring table-pan unit bug (`mouse.rs:1113-1117` mixes px on one axis with notches on the other, so a diagonal wheel pans a table instead of scrolling) rather than fixing it silently.

  **SCOPE amendments (decided).** Edge-hold selection auto-scroll is OUT — it does not exist; `on_drag` only re-hit-tests on real pointer motion, so drag scrolling is ordinary caret-follow and building a timer would put a clock in a deliberately clock-free path. Item 82's image culling needs NO change (`rects.rs:388-391` is pure box intersection with no scroll input) — verified, keep it that way. Keep `OVERSCROLL_KEEP_ROWS = 1`. Accept the rendered-offset rounding (it is what prevents vertical AA shimmer, and it mirrors `adaptive_column_left`'s existing whole-pixel floor). Persist the intra-row offset across relaunch. `--bench` claims exclude `hit_test`, which walks `layout_runs()` from the document start — pre-existing O(doc), not to be "proven" O(visible) by accident.

  **LIVE-ONLY — flag for human confirmation, never claim verified:** trackpad feel and momentum, wheel-notch feel, and scroll smoothness over real seconds. The harness cannot verify timing or feel.

  **90b. DECIDED FEEL (user, 2026-07-26; binding).** (1) A real-mouse `LineDelta` notch is **3 × the zoomed base line height in pixels**. This preserves today's ordinary-text rhythm while continuous motion prevents a notch from swallowing a tall image; it is neither a fixed physical constant nor a viewport fraction. (2) **Page Up/Down stays a caret mover.** Preserve its boundary recoil, overlay/history arms, and the settled Shift non-selection contract. Item 90 changes only the two divergent screenful estimates to read one `viewport_avail_px` owner; converting Page into viewport-only scrolling is out. (3) Smooth `PixelDelta` input is **1:1 physical movement at the authored 100% default**, multiplied at the input boundary by a new `scroll_sensitivity` preference supplied through item 94's generic Range row. The range is **25%–400%**, displays whole percentages, maps its rail logarithmically so 25/50/100/200/400 occupy equal intervals, and changes in **5-percentage-point steps** by Left/Right, click, or drag; Enter permits an exact percentage. Do not add modifier-dependent steps. Apply the multiplier to both axes of smooth pixel input, but not to discrete mouse notches or row-based picker navigation. Persist the preference; current accidental ~320% remains representable without being the default. Trackpad feel, OS momentum, wheel-notch feel, and smoothness over real seconds remain LIVE-ONLY and must be reported for the user's hands, never claimed verified by the harness.

95. **Adopt the shared Range interaction for the remaining bounded numeric Settings rows.** **Build:** After items 94 and 90 land and prove the generic control with Zoom and Scroll sensitivity, migrate `Page width (prose)` and `Page width (code)` from plain `Value` rows to the same Range grammar. Keep their existing 20–200-column clamp and exact numeric-entry path; use a linear rail and an authored whole-column step that keeps the current/default values reachable. Left/Right, click, drag, Enter-to-type, live apply where the active buffer class matches, and persistence all route through item 94's shared owner—no page-width-specific slider code. **Scope:** This is a consolidation pass, not permission to invent sliders or new preferences for categorical/toggle values. Audit the entire Settings roster and migrate only bounded numeric values for which continuous visual judgment is honest; document why every remaining non-Range kind stays categorical, path-based, action-based, or exact-entry-only. **Done:** Every suitable bounded numeric setting uses one consistent keyboard-and-mouse range interaction; no duplicate range math, hit testing, display formatting, or persistence path remains. **Verify:** Exhaustive SettingId×SettingKind roster law, page-width min/default/max and exact-entry tests, mouse/keyboard parity, prose-vs-code live-apply isolation, persistence/restoration, representative narrow/wide captures, standing vision smoke, native + both keymap conventions + wasm gates. **Depends on 94 + 90; dispatch only after both land so the interaction is consolidated from real use rather than guessed upfront.**

## Timed — not blocked

20. **Pre-tag taste pass.** At the user’s explicit tag/release start, the implementation/release owner generates one current world screenshot export, then Fable judges only those images for per-world bullets, squiggle size/baseline including Bilby, dash padding, and Saltpan font outcomes; Fable never implements or edits. Ordinary pushes do not trigger it.

24. **Release-adjacent user-facing docs refresh.** After the current user-visible wave settles and before release preparation, update GUIDE, welcome/tour, and site guide for the current product, chords, and features. Matter-of-fact voice; facts verified. Site copy may change; deployment remains separately user-gated.

## Parked — explicit gate or future design

- **Export save-dialog scope:** macOS + Linux, one live-only cross-platform seam; capture uses an explicit path. Decided, not scheduled.
- **Per-world living-band choreography:** audition TwoShape/Slam/Soft against Morph; live feel is the oracle. Needs a design session.
- **Per-world copy-pulse differentiation:** possible future motion tweak; needs a design session.
- **Site deployment:** only on the user’s explicit word.

## Monitoring — non-blocking

- **Hands-on checks still useful:** Dawn/Bilby world feel; writer-diff panel/Tab + zoom readout; phantom image resize handle; upward scrolling past images in release; right-click Add-to-dictionary summon; 2px Wagtail stipple taste.
- **Live-only follow-ups from the 76–88 wave (harness can't drive them):** 80 — Find/Replace scroll smoothness over real typing; 81 — heading-chevron mouse-press→toggle wiring (no GPU App in unit tests; geometry+toggle proven headlessly); 85 — theme-picker felt input→present lag (latency-probe ms numbers are live-only); 87 — Bombora drift speed / counter-motion / calmness over real seconds.
- **GPU memory:** no action unless the 6 GB symptom recurs; then probe the live surface with the window foregrounded.

## Release blockers and reminders

- App icon — item 92.
- Dictionary/font/license notices plus code copyright/NOTICE review.
- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user’s explicit word. A dry run may precede them.
