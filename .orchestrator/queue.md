# awl — live build queue

> Live execution state only. Completed and superseded work is in git history
> (`git log -p .orchestrator/queue.md`). Protocol, claiming, worktrees, and
> execution hygiene live in `.orchestrator/README.md`.

## In flight (cooking — 2026-07-25)

Background Workflows off `main`; integrated serially on the merge gate as each verifies SHIP. Landed items sit on LOCAL main (not yet pushed — one batched both-conventions gate + CI push pending once build-contention drops). Workers report shas; the board is orchestrator-only.

**All 13 landed + PUSHED @022bce5** (76 · 77+data-loss-fix · 78 · 79 · 80 · 81 · 82 · 83 · 84+84-fix · 85 · 86 · 87 · 88). Local both-conventions + wasm-*build* gate green (2904/2904).
**CI RED — merge train paused:** run 30129670187 on 022bce5 = mac ✅ · mac live-probe ✅ · linux ✅ · **web ❌**. The web job's L2 (`cargo test --target wasm32`, which the local gate + every worktree SKIPPED — no node runner locally) fails to compile: item 85's native-only latency-probe symbols (LATENCY_PENDING/SAMPLES, FLIGHT_ACTIVE/SINK, arm_flight, recording, note_presented_frame, latency_distribution, mark_movement_input) are referenced ungated on the wasm test path (25× E0425). Not a runtime bug; a missing `cfg(not(wasm32))` gate.
**Fix in flight — MUST land + re-push before this is done:** item-85-wasm-probe-fix (gate the probe refs out of the wasm test compile; repro = `cargo test --target wasm32 --no-run`). **Gate lesson: add `cargo test --target wasm32 --no-run` to the local gate — build-only misses this.**

**Live-only flags to confirm by hand** (harness can't drive them): 80 — Find/Replace scroll smoothness over real typing · 81 — chevron mouse-press→toggle wiring (no GPU App in unit tests; geometry+toggle proven headlessly) · 85 — felt input→present lag (latency probe ms numbers are live-only) · 87 — Bombora drift speed / counter-motion / calmness (harness can't see time; user is the oracle).

## Ready — current user-visible wave

76. **One active folder; ordinary documents; retire the Notes desk.** Replace the notes-specific navigation/document model with one coherent active-folder context. Launch precedence is one law: an explicit file/folder/`--root` (including `awl .` and an OS open-file request) wins; an argument-free terminal or desktop launch restores the last active folder + document; a first launch with nothing remembered opens the configured `default_folder` (`~/notes` initially). Bare `awl` therefore resumes, while terminal “use this directory” is explicitly `awl .`; any successful context change, regardless of entry door, becomes the next remembered context. Persist that context through one owner so sticky project state and session restore cannot disagree. Rename the user/config concept from `notes_root` to `default_folder`; this is pre-release, so retire the old key rather than carrying a compatibility layer. Remove the `Notes`/`NotesFlip` command, two-desk return memory, and lasting note identity. Rename `New note` to **New document**: Cmd-N creates a fresh Markdown document in the active folder, the first meaningful line supplies its filename once on first material save, and later title edits never rename it automatically. Thereafter it is an ordinary file; Rename/Duplicate/Move are generic file verbs. Make `Last file` restore the complete previous location (folder + buffer/view), not a notes-specific return path. Do not add a Library/sidebar, per-folder desk stack, or terminal/desktop mode. **High-risk ownership round: Opus plans the state/launch migration; Sonnet implements; targeted Opus verification attacks explicit-vs-restored launch precedence, daemon handoff, first-run fallback, A→B→A folder/file/view restoration, one-shot naming, generic file verbs, and the absence of Notes rows/state; full native + wasm gates.**

77. **One file-visibility switch keeps project context without opening binary data as prose.** After item 76 establishes the one active folder, replace the separate hidden-file behavior with one sticky **File visibility: Text / All** setting (default Text; exposed through the ordinary Settings/palette surface). Text mode lists normal, non-hidden files awl can actually decode and edit, including recognised prose/code, extensionless files, and unfamiliar extensions whose bytes are valid supported text; it is not an extension allow-list. All mode reveals both hidden entries and unsupported/binary files, so one switch answers “show me the complete project.” Unsupported rows remain visible for context but are styled as unavailable with a concise type label and calmly report “PNG · not editable in awl” (equivalent per type) if invoked; they never feed binary bytes into `Buffer`, change the active document/folder, or invent image-preview/project-tree machinery. Retire the old independent “Show hidden files” toggle/chord/state rather than preserving a four-state matrix. An explicit CLI/OS-open of supported unusual text succeeds regardless of picker visibility; an explicit unsupported/binary file is refused by the same one capability owner and leaves the remembered context intact. Preserve zero-network/sandbox behavior. Audit nested folders, hidden text/binary combinations, ignored files, invalid UTF-8/NUL-bearing files, unknown valid UTF-8, direct launch/daemon handoff, both visibility modes, and picker selection stability; the missing law test exhausts every open door against the same capability verdict. **DISPATCH after item 76; Sonnet implementation + standing Sonnet neighborhood audit; native + wasm gates.**

78. **Empty-list Enter distinguishes an intentional marker from the continuation awl just created.** A manually typed lone Markdown bullet such as `- ` followed by Enter preserves that marker and opens a writable plain line beneath it. By contrast, Enter on the empty continuation produced by the immediately preceding list-continuation Enter removes that generated marker and exits the list, matching the ordinary “Enter twice to leave a list” gesture. Own the short-lived provenance at the edit/action seam; any intervening typing, movement, selection change, undo/redo, buffer switch, reload, or non-list edit clears it, so identical bytes loaded from disk are never guessed to be generated. Apply the same law to supported bullet, numbered, and task-list continuations without changing non-empty continuation, indentation, undo grouping, CRLF restoration, or the prior intentional-empty-marker fix. Verify manual-vs-generated twins, completed and incomplete siblings, nested lists, end/middle of document, undo/redo, buffer A→B→A, save/reload, both keymap conventions, and raw/preview Markdown rows; add the missing no-guess law test. **Sonnet editing-correctness round; full native + wasm gates.**

79. **Insert Date gets a useful default chord.** Bind the existing Insert Date action to `Cmd-Shift-D` in the native slot and `C-c .` in the quiet Emacs slot through `assets/keymap-defaults.toml`, keeping palette access and `[keys] insert_date` rebinding unchanged. Confirm the native chord does not shadow ordinary typing or Duplicate, the prefix chord resolves through the existing sequence machinery, Linux native/emacs conventions advertise and fire the correct effective slots, and the date-format setting still owns the inserted value. Update generated/default config help only where the existing keymap documentation requires it. **Sonnet mechanical round; catalog/keymap agreement, both-convention replay, native + wasm gates.**

80. **Find/Replace text boxes keep a stable exterior width while their contents scroll internally.** Typing or pasting a long query must not widen the Find card, change the page measure, or push the document/controls sideways. Keep the caret visible by horizontally scrolling/clipping the shared `TextBox` contents inside the fixed field; Backspace, selection, Home/End, mouse placement, Find stepping, case toggle, and the Replace row use the same rule. Summoning and dismissing may choose geometry normally, but query edits never resize the surrounding surface. Verify short→long→short queries, wide glyphs/CJK, selection and mouse hit-testing at both clipped edges, Find-only and Find+Replace, narrow/wide windows, page mode on/off, all three world rails, and no lost query state; capture geometry asserts an invariant exterior rect and the missing law test enumerates every `TextField` consumer that promises fixed geometry. **Sonnet implementation + standing Sonnet neighborhood audit; native + wasm gates.**

81. **A heading’s left chevron is the fold target in both directions.** Hovering any foldable Markdown heading—expanded or collapsed—reveals the chevron in the left affordance lane without shifting text or changing row geometry. That chevron’s own hit region and pointer cursor toggle the section in either direction; the post-title tail may remain an additional generous target, but it is no longer the only way to collapse an expanded heading. Non-heading rows and non-Markdown files show no fold affordance, and nested/wrapped headings resolve to the true raw heading line. Extend the real mouse/capture seam enough to verify hover→cursor→click rather than inferring it from state. Audit expanded/collapsed, nested folds, wrapped headings, selection/conceal reveal, page mode, zoom/DPI, three alignment rails, and Outline navigation; pixel arithmetic proves the hover affordance is visible and the missing law test proves both visible states share one fold target. **Sonnet correctness round + standing Sonnet neighborhood/vision smoke; native + wasm gates.**

82. **Inline images scroll like persistent document content at the viewport’s top edge.** A tall image remains laid out and partially visible while its rectangle intersects the viewport; it is clipped progressively at the top and disappears only once its bottom has passed above the viewport. Scrolling upward restores the same pixels/row geometry without a jump, blank collapse, stale placeholder, or cursor/selection displacement. Extend the harness to place the viewport through this live-reported boundary and make the benchmark/capture witness actual image shaping rather than relying on the existing document-wide row-height test. Audit upward and downward traversal, an image taller than the viewport, consecutive images, text before/after, caret above/inside/below, page mode, all rails, zoom/DPI changes, resize, buffer swap/version-zero cache isolation, and native/WebGL rendering; record a release before/after frame sample and add the missing intersection/culling law test. **Sonnet diagnosis + implementation; escalate only if evidence exposes an ownership ambiguity; standing Sonnet neighborhood audit and vision smoke.**

83. **Overlay content uses the room it has, and query text is optically centered.** Fix the shared overlay layout contracts behind two visible failures: Quokka’s query/title/caret sits vertically centered in its strip, and compact Bars cards do not elide labels such as “Compare with version…” or “Clean unused assets…” while usable inset-stage width remains. A right-anchored card still hugs the right inset rail as one content-sized group, but its measured natural width may grow toward the center up to the normal cap; the secondary shortcut column yields before the primary label, and “content-sized” must never be computed from text already truncated by the proposed card width. Keep the established split Pane, inset rails, theme-owned alignment, and Quokka’s card material/silhouette unchanged. Verify query/rename/link text fields, plain and shortcut-bearing pickers, narrow/wide windows, long/CJK labels, all rails, Split Pane, 1×/2× DPI, and click geometry. Generate real captures of the failing specimens plus the matrix; Fable judges only vertical rhythm, breathing room, and visual balance, never implements or edits; the implementer applies the call. Finish with geometry/pixel laws and the standing Sonnet vision smoke. **Sonnet implementation; Fable taste-only capture review; native + wasm gates.**

84. **Selection paint is clipped to the writing surface.** Dragging or extending a selection beyond the page’s left edge clamps hit-testing to the nearest valid document position and never paints the selection wash, revealed markup, caret, or auxiliary selection geometry outside the active writing column/card clip. Preserve selection semantics, auto-scroll, Markdown raw-line reveal, and ordinary full-line selection; this is a visual/hit-test boundary, not a narrower selectable range. Audit leftward and rightward drags, wrapped first/last visual rows, headings/lists/concealed markup, page mode on/off, split surfaces, zoom/DPI, and left/center/right world rails. Pixel arithmetic asserts zero selection-colour spill beyond the resolved content clip, sidecar asserts the correct byte range, and the missing law test makes the clip owner common to every selection quad path. **Sonnet correctness round + standing Sonnet neighborhood audit/vision smoke; native + wasm gates.**

85. **Theme-picker world jumps cannot make a stationary pointer chase rows; measure the live input-to-present path.** Preserve the accepted “you are dropped into different worlds” behavior: previewing a world may change the picker’s rail, size, type, and material immediately. However, layout moving underneath an unmoved pointer must not synthesize another hover selection or cascade across rows. Re-hit-test hover only after real pointer motion; keep the highlighted world stable across its own re-layout, and make a click activate the row actually under the press/release according to one explicit rule. Keyboard navigation still advances exactly one visible row per press. Add a live diagnostic/probe that measures key-or-mouse event → first presented frame for theme-picker movement, alongside the existing offscreen `--bench-theme-burst`; make it witness the actual re-layout/present work and report distributions without introducing a broad shared live/headless interaction driver. Audit worlds with different row counts/heights, all three rails, stationary pointer after world crossing, slow/fast pointer travel, wheel, click, keyboard, resize, focus loss, 1×/2× DPI, and preview dismissal. Keep capture deterministic and record release-mode before/after live metrics; the user remains the oracle for felt lag. **Sonnet live-input diagnosis/implementation; standing Sonnet neighborhood audit; escalate only from evidence.**

86. **Differentiate the light worlds with quieter but more authored background materials.** Make one theme-data-driven static taste round: Bowerbird’s summoned cards return to the existing flat rectangular treatment (remove `JaggedWave` from that world); Quokka keeps its current Sour Gummy, chamfered halftone cards exactly as shipped but replaces its page-margin dot ground with a repeating zigzag field; Gumtree replaces the broad bands with a distinct, broader and quieter eucalyptus zigzag ground; Bilby receives only a small differentiation—retain its composition, type, and identity while making the warm ground/accents a little paler and less peach so it no longer reads as near-Quokka. Quokka and Gumtree must not look like recolours of one asset: vary scale, profile/direction, spacing, and contrast through world data read by one renderer. Patterns stay out of the writing column, preserve figure/ground and the one-amber law, add no per-world code path, and remain deterministic/WebGL2-safe. Generate the real world-gallery specimen with page mode, outline, H2, list, and light Markdown texture at representative widths/DPI, including before/after and close comparison plates. Fable judges only the generated images for distinctness, restraint, legibility, and zigzag dials; Fable never implements or edits, and the implementer applies the verdict. Verify exhaustive background/card-cap rosters, non-target worlds byte-identical, contrast/pixel laws, frame cost, no idle work, full suite/native+wasm, and update `WORLDS.md`/`THEMES.md` in the landing round. **Sonnet implementation; Fable taste-only review on real captures.**

87. **Bombora’s loved three-band waves drift almost imperceptibly, with the middle tier counter-moving.** Keep the current Bombora wave drawing, palette, band count, static settled composition, and text geometry; add only a very slow seamless horizontal phase drift, with the middle band travelling in the opposite direction to the outer bands so the sea feels layered rather than translated as one sheet. Route scheduling through the existing ambient-motion owner: `ambient_motion = false`, Reduce Motion, blur, occlusion, inactive worlds, and deterministic capture all freeze it with zero idle frames/CPU; theme crossing starts from a stable phase rather than a visible pop. Use world/theme data and the shared background renderer, never a Bombora-only render path. Bench the active release build before/after with the animation actually ticking, assert O(visible) work and bounded GPU instances, and verify stopped states schedule nothing. Generate representative settled captures for Fable to guard contrast/figure-ground (taste only; no implementation), but treat a live release build and the user’s eyes as the oracle for speed, counter-motion, looping, and calmness; flag that hands-on check honestly rather than claiming the harness saw time. **DISPATCH after item 86 because the rounds share theme/render ownership; Sonnet implementation + perf audit; Fable static-taste review only.**

88. **Put Mulga’s literal asterism in the star-authored break slot.** Swap only Mulga’s first two section-break ornaments from `⁂ / ⁑ / ❦` to `⁑ / ⁂ / ❦`, so raw `***` conceals to the typographically and semantically matching asterism `⁂`; `---` becomes `⁑`, and the About card follows the established dash-ornament law to use `⁑`. Preserve Mulga’s Junicode ornament face, ornate scale, bullets, palette, typography, and every other world byte-for-byte. Update the `WORLDS.md` roster and any exact theme/Markdown expectations. Verify the raw-on-caret/concealed-off-caret triplet in a real Mulga capture, glyph coverage/no tofu, About end-mark parity with `---`, three-symbol distinctness, and exhaustive non-Mulga identity; no Fable round is needed for this decided semantic swap. **Small Sonnet mechanical round; do not run concurrently with items 86/87 because they share theme/world ownership; native + wasm gates.**

## Ready — shared ownership and performance

## Timed — not blocked

20. **Pre-tag taste pass.** At the user’s explicit tag/release start, the implementation/release owner generates one current world screenshot export, then Fable judges only those images for per-world bullets, squiggle size/baseline including Bilby, dash padding, and Saltpan font outcomes; Fable never implements or edits. Ordinary pushes do not trigger it.

24. **Release-adjacent user-facing docs refresh.** After the current user-visible wave settles and before release preparation, update GUIDE, welcome/tour, and site guide for the current product, chords, and features. Matter-of-fact voice; facts verified. Site copy may change; deployment remains separately user-gated.

## Parked — explicit gate or future design

- **Export save-dialog scope:** macOS + Linux, one live-only cross-platform seam; capture uses an explicit path. Decided, not scheduled.
- **Per-world living-band choreography:** audition TwoShape/Slam/Soft against Morph; live feel is the oracle. Needs a design session.
- **Per-world copy-pulse differentiation:** possible future motion tweak; needs a design session.
- **Site deployment:** only on the user’s explicit word.

## Monitoring — non-blocking

- **Hands-on checks still useful:** Dawn/Bilby world feel; writer-diff panel/Tab + zoom readout; phantom image resize handle; upward scrolling past images in release; right-click Add-to-dictionary summon; 2px Wagtail stipple taste.
- **GPU memory:** no action unless the 6 GB symptom recurs; then probe the live surface with the window foregrounded.

## Release blockers and reminders

- App icon.
- Dictionary/font/license notices plus code copyright/NOTICE review.
- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user’s explicit word. A dry run may precede them.
