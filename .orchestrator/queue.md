# awl — live build queue

> Live execution state only. Completed and superseded work is in git history
> (`git log -p .orchestrator/queue.md`). Protocol, claiming, worktrees, and
> execution hygiene live in `.orchestrator/README.md`.

## CI incidents

CI-30360849276. 🟡 **FIX PUSHED, AWAITING THE REMOTE RUN.** Linux `Rust code health` had been red for at least six consecutive `main` runs, unnoticed, because nothing checked the remote result before pushing. **Cause:** CI resolves `dtolnay/rust-toolchain@stable` to current stable (1.97.1) while the local toolchain sat on 1.94.0, so `clippy::byte_char_slices` — added after 1.94 — fired only remotely on `src/syntax/cpp.rs:43`. Not a Linux problem; a toolchain-version problem that no local gate could see. **Repair:** the one line, plus `rustup update stable` to 1.97.1 locally. Under 1.97 the tree is clean (zero diagnostics), full suite 3129 both conventions, wasm, health, and debug/release captures agree. Two standing rules added to `.orchestrator/README.md`: read the last `main` run before pushing, and keep the local toolchain level with CI's.

## Ready — harness correctness

### Active claims pending stale-line reconciliation

- 155 `src/app.rs` — ✅ `9f34bbc2`; `src/render/rects.rs` — ✅ `e363d948`;
  `src/render/text.rs` — ⏭ no safe move-only seam; `src/main/args.rs` —
  ✅ `18f13ba3`; `src/render/spans.rs` — ✅ `eba84ee2`;
  `src/app/apply.rs` — ⏭ one ordered apply transaction, no move-only seam;
  `src/render/caret.rs` — ✅ `ff350158`; `src/main/run.rs` —
  ⏭ launch/replay/capture dispatcher has no safe move-only seam;
  `src/keymap.rs` — ⏭ action/default-policy ownership has no safe
  move-only seam; `src/render/chrome/mod.rs` — ⏭ already decomposed to
  sibling owners, remaining code is shared geometry/pipeline glue;
  `src/app/input/mouse.rs` — ⏭ one gesture-state transaction, no safe
  move-only seam after item 147; `src/markdown/spans.rs` — ⏭ one
  ordered span-emission rule set, no obvious MOVE seam; `src/actions.rs`
  — ⏭ one ordered apply-core transaction, no obvious MOVE seam;
  `src/render/framebench.rs` — ⏭ one shared benchmark witness harness;
  `src/capture/sidecar.rs` — ⏭ one byte-stable schema serializer;
  `src/buffer.rs` — ⏭ core editing owner, no obvious MOVE seam.
  ✅ ITEM 155 COMPLETE: every ranked file was either safely split or
  explicitly declined at its ownership boundary.
- 107 enum-derived rosters — ✅ LANDED @ `c3f7eb27`.
- 109 icon-ratchet subset erosion — ✅ LANDED @ `d45d34f3`.
- 108 Gumtree chevron visibility — ✅ LANDED @ `f8d384de`.
- 122 Quokka alternating wallpaper bands — ✅ LANDED @ `2edcfdeb`;
  live/dashboard vision smoke remains a display gate.
- 127 fold-chevron optical centring — ✅ LANDED @ `d338a08d`.
- 128 native macOS identity/About panel — ✅ LANDED @ `cb8aca95`;
  live About screenshot remains a display/manual verification.
- 117 Bowerbird organic-cutout ground — ✅ LANDED @ `ddd17260`;
  combined-main roster/health/wasm audit green; live motion/taste remains
  user-gated.
- 121 Firetail Block/icon-ground A/B/C trial — 🔴 BLOCKED: canonical
  pinned-Chromium exporter hangs before its first PNG even for an unchanged
  32px control; clean review-only worktree, no shipping mutation.
- 95 remaining bounded Settings ranges — 🟡 IN PROGRESS,
  `item_108_gumtree` (codex), branch `codex/item-95-settings-ranges`.
- 132 Kite warped-grid world — 🟡 IN PROGRESS, `item_132_kite` (codex),
  branch `codex/item-132-kite`.

## Ready — current user-visible wave

20. **Build the visual review dashboard — one command, one openable HTML index over the real app’s important screens and shipped icons.** **Build:** Add `scripts/review.sh` as the dedicated review-build command. It builds awl once (release by default; `--debug` for iteration), wipes and recreates a gitignored `gallery/review/`, drives the real headless binary through deterministic fixtures/key replay, generates the current shipped icon review sheets through the existing theme-derived offline icon pipeline without packing or mutating committed app assets, and writes `gallery/review/index.html`. The HTML is a review shell around REAL PNG captures, never an HTML mockup of awl. **Scope:** One code-owned scene manifest defines the important static screens: every world’s Room and command-palette Frame; the canonical WYSIWYG document (headings, prose, lists, formatting, code, table, image, folds/outline); command palette; Settings (all facets, including the Zoom rail); theme picker; caret picker; Goto; Find/Replace; spell suggestions/Add-to-dictionary; format popover; version history/diff; and representative narrow/wide layouts. Include the current per-world shipped icon sheets at master, Dock, and app-switcher sizes. Derive the world roster from `awl --list-worlds`, never a copied list. Use a hermetic absent config and zero network. The generated index is a responsive, scrollable contact sheet with section navigation, world/surface/light-dark filters, captions naming the exact fixture/theme/keys, click-to-open full-resolution images, links to JSON sidecars, git commit + dirty-state provenance, and a concise failures/omissions summary. It may use relative local assets beside the HTML—the contract is one entry point that opens directly from disk, not one enormous base64 blob. Do not make ordinary `cargo build` run the expensive matrix; this is an explicit build artifact command. **Done:** Running `scripts/review.sh` from the repo root produces a complete current dashboard that the user can open and review without assembling galleries by hand; a new world enrolls automatically; a missing/failed/stale capture, wrong sidecar theme/state, absent icon sheet, duplicate scene id, or external URL fails the build by name. Item 20’s old one-shot pre-tag Fable pass is superseded: this dashboard is the durable human taste surface and may be run now, before tags, and again during release preparation. **Verify:** Run the release command end-to-end; assert manifest↔DOM card counts and unique ids, every `<img>`/sidecar target exists and is non-empty, every world has its required surfaces/icons, no `http:`/`https:` dependency exists, and two identical runs produce the same scene/image set (provenance timestamp excluded or absent). Open the file locally and perform affordance-locating vision smoke across the sections. Existing capture, icon, native, both-convention, and wasm gates remain green. ✅ LANDED @ `8b444c6` — 18-world Room/Frame matrix + 18 expected-state scenes + two current shipped-icon sheets; 56-card offline dashboard; two release runs byte-identical; native suite 3,052 + integration green; wasm L1/L1.5/L2 green.

20a. **Add a full-screen image lightbox to the visual review dashboard.** **Build:** Clicking any dashboard screenshot opens that real full-resolution asset in an in-page modal/lightbox over a dark backdrop instead of navigating away. Include the card caption and position, previous/next controls, click-outside close, a visible close control, and keyboard navigation (`Escape`, `←`, `→`). Preserve ordinary modified-click/open-in-new-tab behavior and keep the generated file fully offline. **Done:** A reviewer can enter one image, inspect it at the largest size the viewport allows, walk the filtered visible set without returning to the grid, and dismiss back to the same scroll position; focus returns to the invoking image. **Verify:** Generated-markup law for required dialog/accessibility hooks; browser smoke for open, next/previous, Escape, backdrop close, focus return, and filtered-set navigation. ✅ LANDED @ `342d15b` — release dashboard regenerated with 56 lightbox triggers; generated interaction script parses; required dialog, navigation, dismissal, and focus-return hooks are build-validated. Local `file://` browser-control attachment was unavailable, so live clicking remains the user-visible smoke in the reopened dashboard.

20b. **Split the dashboard’s two giant shipped-icon strips into one review card per world.** **Build:** Extend the existing offline icon exporter with a shipped-per-world page: that world’s assigned `Theme::icon_cursor`, the native 256/128/64/44/32/24 size ladder, and both light/dark Dock surfaces in one bounded image. The dashboard icon section consumes the 18 generated per-world sheets rather than the two full-roster vertical strips; each card carries the world/mode filter data and opens normally in the lightbox. Keep the full strips in the icon exporter for dedicated overview use, but stop presenting them as the dashboard’s primary icon review unit. **Done:** Icon review is 18 scannable cards, each world visible without traversing an enormous image; a new world enrolls automatically from the manifest. **Verify:** Capture/icon/dashboard rosters agree exactly; one non-empty shipped-world image per world; manifest↔DOM count moves from 56 to 72; release review build passes offline. ✅ LANDED @ `85c7249` — release review generated 18 non-empty 768×958 per-world icon sheets plus the two retained overview strips; block/pill representatives checked at 256→24 px on both Dock surfaces; dashboard roster and lightbox trigger counts are exactly 72.

113. 🟡 **IN PROGRESS — CODE INTEGRATED LOCALLY @ `1f2e89d`; live release probe awaits an unlocked display.** Debug now retains latest and worst completed theme-switch transactions in a separate trailing five-second wall-clock history, with the worst transaction’s phase breakdown; frame `CostRing` remains independent, eviction uses natural redraws only, Debug-off clears through one App-owned predicate/reset seam, and headless stays numberless. Fake-clock, >`COST_WINDOW`, zero-redraw, capture, mutation-red, full native integration (3,070 passed, 11 ignored), and wasm 16/16 are green. The remaining queue requirement is the live release comparison against `--live-script`/flight-recorder timestamps and onscreen readability; the local display is locked, so no live claim is made. **Routing:** diagnose/build/repairs=`gpt-5.6-terra` medium; independent verification=`gpt-5.6-terra` medium; final integration=root serial native + wasm gates.

114. 🔴 **BLOCKED — clean branch `codex/item-114-workspace-takeover`; no production commit. User decision required:** the acceptance criteria require a real App-owned workspace that can summon/render/dismiss, while Scope permits only Settings and History, forbids migrating either in this item, and forbids a dormant alternate UI path. Choose one: **A (recommended)** authorize a capture/developer summon seam that drives the real production state/render/input path until item 115 supplies the user-facing Settings door; **B** combine the first Settings consumer migration into item 114; or **C** let item 114 land as a non-summonable dependency and move summon/render acceptance to item 115. **Build the shared summoned-workspace takeover — awl’s second transient surface class, not a new collection of permanent screens.** **Build:** Introduce one reusable workspace state/layout/render/input primitive for sustained tasks that relocate attention, alongside the existing contextual overlay primitive. A workspace is still modal, summoned, and transient: it occupies the editor viewport while active, the current document recedes as backdrop rather than competing as active content, and `Esc` returns to the exact editor state. Give the shell a calm title/search region, optional navigation rail, primary content pane, focus ownership, footer hints, and responsive composition: wide windows may split navigation and content; narrow windows stage them as one surface with an explicit back path rather than cramming both columns. **Scope:** This is the shared mechanism for Settings and Version History only. It is not a route stack, tab system, persistent sidebar, separate OS window, or permission to migrate every picker. Theme, Caret, Command, Goto, Find/Replace, Spell, formatting, Rename, Insert Link, and other brief/contextual interactions remain editor-backed overlays. World character stays theme data through the existing surface/value system; depth and focus use value, never amber; nothing animates except the caret. Keep Workspace state distinct from `OverlayKind` while sharing lower-level rowlayout, text shaping, range controls, focus cues, and keyboard/pointer seams wherever behavior is genuinely identical. The primitive may land only as the immediate dependency of item 115—do not grow a generic framework beyond the two decided consumers or leave an alternate disabled UI path. Amend DESIGN/SCOPE with the decided rule: overlays split attention for brief/contextual choices; summoned workspaces relocate attention for sustained inspection/configuration. **Done:** One App-owned workspace can summon, focus, navigate responsively, and dismiss without mutating or losing the underlying buffer/view state; wide and narrow geometry have one owner; overlay behavior is unchanged; the shell is ready for Settings without feature-specific branches. **Verify:** Pure state/focus/back-stack laws; geometry and drawn↔hit-test agreement at narrow/wide, zoom, and DPI; theme roster and 1-bit Wagtail value laws; deterministic capture/sidecar coverage; explicit regression captures proving Theme and Find remain editor-backed overlays; native, both-convention, and wasm gates. **Routing:** plan/adversarial ownership map=`gpt-5.6-sol` high; build=`gpt-5.6-terra` medium; verify=`gpt-5.6-terra` medium. **User design decision 2026-07-26.**

115. **Move Settings into the shared summoned workspace — configuration is a sustained task, not a command-palette card.** **Build:** Make Settings the first production consumer of item 114. The workspace has a searchable settings list, a category rail (`Editor / Appearance / Writing / Files / Keybindings / Advanced`), current values and existing controls in the main pane, and responsive one-column navigation on narrow windows. Preserve every setting’s current live-apply, persistence, range, exact-entry, toggle, and sub-picker semantics. `Settings…` opens the workspace; Settings and Keybindings results found through Cmd-P deep-link into the workspace with the relevant category/row focused instead of editing inside the command-palette card; direct one-shot commands remain one-shot. **Scope:** Keybindings belongs inside this workspace and its direct command deep-links there. Do not redesign or slow the Theme audition: direct Theme picking remains the current fast editor-backed overlay with live whole-document/world preview, immediate keyboard/pointer movement, Enter commit, and Esc exact revert. If Theme is entered from Settings, suspend the workspace, run that same existing editor-backed audition surface, then return to the preserved Settings position—never embed a diminished imitation in the workspace. Apply the same “reuse the real picker” rule to Caret and other existing value-pick children. Remove the old standalone Settings-overlay presentation and its Settings-specific geometry/tests rather than retaining parallel disabled UI. Item 112 continues to own shared overlay rhythm for the overlays that remain; do not spend item 112 effort polishing the Settings card this item deletes. **Done:** Settings reads as a coherent place for several related decisions; categories, search, controls, nested pickers, focus, and back behavior are legible at wide and narrow sizes; Cmd-P remains the discoverability door; Theme remains as quick and distinctive as today; no old Settings-overlay path or dead presentation code survives. **Verify:** Every `SettingId × SettingKind` through direct Settings, Cmd-P deep-link, keyboard, and pointer; category/search state preservation across child pickers; exact Theme preview/commit/revert parity against pre-migration captures and replay; narrow/wide layout and hit-test laws; settings persistence; dashboard captures for every category and Theme round-trip; vision smoke; native, both conventions, and wasm gates. **Depends on 114; unclaimed; user design decision 2026-07-26.**

116. **Move Version History into the shared summoned workspace — timeline and prose diff become one readable task, never three competing layers.** **Build:** Preserve the existing history store, git backend, pruning, facets, descriptions, kept versions, and prose-diff engine, but replace the current History overlay/diff-as-preview composition with item 114’s workspace. On wide windows, show a narrow timeline/navigation pane beside a large read-only comparison pane; moving through versions updates the comparison immediately. On narrow windows, show the timeline first and enter the comparison as a second stage with an explicit return path. The current editor is backdrop/state, not a third readable layer. **Scope:** Keep local loose-file snapshots and git-managed history behind the same UI with only a quiet source label. Preserve independent diff scrolling and a clear focus transfer between timeline and comparison. `Esc` leaves the current buffer byte-for-byte unchanged; restore must be a deliberate, footer-taught action rather than bare `Enter`, and remains undoable. `Version history…` and `Compare with version…` deep-link into the same workspace at the appropriate focus; `Keep version…` retains its brief naming prompt and returns coherently. Remove the old History overlay, document-under-card preview composition, and feature-specific diff-panel dressing only when their last consumer is gone—retain the generic prose-diff machinery and do not strand parallel disabled paths. **Done:** A user can answer “which version, what changed, and do I want it back?” without overlapping titles, hidden prose, or ambiguity about whether the editor is active; the same flow works for local and git history; exiting is a true no-op and restoring is deliberate and undoable. **Verify:** Timeline→live comparison→focus/scroll→back/exit/restore journeys for local snapshots, named/pinned versions, git commits, empty history, renamed files, and pruned ladders; narrow/wide/zoom/DPI captures across representative light/dark, Pane/Bars, and Wagtail worlds; pixel laws proving timeline and comparison never overlap and the original document does not remain a competing readable layer; restore undo law; capture/replay parity; dashboard vision smoke; native, both conventions, and wasm gates. **Depends on 114 then 115, preserving the user-decided implementation sequence; unclaimed; user design decision 2026-07-26.**

117. **Give Bowerbird a slowly drifting organic-cutout ground — raise its idle loudness from 2/5 to 3/5 without making a second Lava world.** **Build:** Replace Bowerbird’s regular `Dots` margin ground with one reusable data-driven biomorphic/cutout background mechanism: flat layered silhouettes in several scales (large masses, medium islands, tiny droplets), including occasional holes and embedded cool-tone pieces so it reads as authored graphic design rather than generic noise. Use three or four cool navy/blue tones inside Bowerbird’s own ground value band. The writing column remains a calm flat plane; the pattern lives only in the Frame. **Decided motion:** movement is almost subliminal—“you should not watch it moving; you should notice that it has moved.” Two or three cut-paper layers drift only slightly at differing directions/rates, with at most very slight silhouette deformation. No bubbling, merging, rhythmic pulsing, spawning, or dissolving: those belong to `Background::Lava`. Reuse the shared ambient clock/gates; pause on blur/move/resize, freeze under Reduce Motion and `ambient_motion = false`, and pin a deterministic settled phase in capture. **One-accent bound:** do not copy the reference image’s coral fragments into the ground. Warm color remains the caret’s alone; all pattern tones stay cool and clear of `primary`. **Scope:** This augments Bowerbird rather than adding a second dark-navy world—the roster already has that palette/voice pole. Keep Bowerbird’s IBM Plex Sans/JetBrains Mono, palette, caret, tags, ornaments, and quiet default chrome unless real-pixel contrast forces a ground-only token adjustment. The new variant is capability data through the shared renderer, never a Bowerbird identity branch, and remains reusable by a future world with independently authored scale/density/motion dials. Target **3/5 idle loudness**: clearly more graphic than today, below Potoroo/Quokka/Mangrove/Cassowary, far below Firetail; the original full-bleed reference would be 5/5 and is not the target. **Done:** In ordinary page-mode widths Bowerbird is immediately recognizable from its organic navy Frame while prose stays dominant, the warm caret remains the sole accent, and a minute of live observation reads as extremely slow drifting collage rather than liquid animation. **Verify:** Use item 20’s real Room dashboard for a bounded density/scale candidate sweep; assert full-viewport field occupancy at short/tall and narrow/wide geometries, no writing-column/Outline/gutter encroachment, every animation phase inside the ground value band and off the caret hue, deterministic/frozen reduced-motion captures, seamless loop/no catch-up leap, zero scheduled frames when gated, background-roster no-wildcard enrollment, vision smoke over Bowerbird plus the empirical nearest worlds (Brolga, Mangrove, Bombora), release perf, native + wasm gates, and final live motion/3-of-5 taste confirmation. **Unclaimed; user design decision 2026-07-26.**

120. **A/B the Lava field with twelve smaller, more varied bodies on Firetail + Mangrove before changing the shipping default.** **Trial only:** Build the experiment in an isolated worktree/branch with a temporary developer-only selector; the default arm remains byte-identical to current shipping Lava, and no trial switch survives the eventual landing. **A — CONTROL:** the exact current eight viewport-relative bodies, current radii/weights, Gaussian silhouette, and sinusoidal motion. **B — CHEAP TUNING PASS:** keep the current viewport-relative field and shared Firetail/Mangrove mechanism, raise capacity/population from 8 to **12**, and author a clearly wider deterministic size hierarchy (large, medium, small/satellite) with the average radius about 10% below A. Reposition the two bodies A mostly hides behind the writing column and distribute the four additional bodies so a large Retina window exposes a healthier population in both margins rather than two enormous fog banks. Slightly firm the existing silhouette through its current field constants and reduce horizontal sway so the unchanged sine motion reads predominantly vertical; do not build buoyancy physics, area-responsive population, stable world-space tiling, or a fluid simulation. Those larger ideas are explicitly **BANKED, NOT BUILT** unless B fails and a later user decision reopens them. **Controlled comparison:** Firetail and Mangrove share B’s geometry; palettes, page width, document, animation phase, ambient cadence/gates, frost treatment, and one-accent/value-band laws remain fixed. Capture A/B at the same frozen phases at 1200×800 logical and the user’s 2560×1378 logical Retina geometry (5120×2756 physical), then run each live in `--release`. Judge by named questions: how many distinct bodies are visible per margin; whether calm ground remains between them; whether large/medium/satellite hierarchy reads; whether movement reads more like contained wax and less like spotlights/fog; whether the Room remains dominant; and whether Mangrove and Firetail both benefit from the one shared field. **Rollback/landing:** rejection deletes the experimental worktree with main untouched. Approval removes the selector and lands only B as one focused commit; a later reversal is one `git revert`, never a destructive reset. **Done:** The user can switch between exact A and tuned-twelve B on the same large Retina window and make a live taste decision with frozen before/after evidence; no responsive-field redesign is smuggled into the trial. **Verify:** fixed-phase real-pixel coverage/population arithmetic across short/tall, narrow/wide, 1×/2× and several page widths; prove all twelve bodies contribute somewhere and neither margin collapses into full coverage or emptiness; existing worst-phase ground-band, hue-clear, column mask, Outline/gutter frost, seamless loop, delayed-wake, Reduce Motion, ambient-off, capture determinism, release perf, native + wasm laws; live-only motion feel explicitly reserved for the user. **Unclaimed; user design decision 2026-07-27.**

121. **Make Firetail’s shipped icon carry the world’s wine-pink identity, and change its fake logo cursor from Pill to Block without falsifying its gold caret.** **Build:** After item 110 lands, change Firetail’s theme-owned `IconCursor` assignment from `Pill` to the existing shared `Block` preset, then audition the icon tile ground at three exact states: **A** current `base_100`; **B** 25% from `base_100` toward Firetail `base_300`; **C** 40% toward `base_300`. Keep the `aw` wordmark in the real `base_content`, the Block cursor in the real gold `primary`, and its knocked-out `l` in the real `primary_content`; Firetail’s actual world palette and live caret remain byte-identical. The problem is that the shipped icon currently samples only Firetail’s darkest oxblood plane, so it reads near-black/cream/gold and omits the pink-wine identity carried by `base_300`, lava `blob_hi`, selection, and muted ink; the current Pill also is no longer the chosen icon silhouette. **Mechanism:** The cursor change uses the existing theme-owned preset assignment, not a Firetail branch. Represent any chosen non-default ground as theme-owned icon data/capability with `base_100` as the inert default for every world; never add a Firetail-name branch and never give the icon cursor a color different from the actual caret. No lava illustration or extra background motif in this round—the Block silhouette plus the large tile plane get the first chance to solve the identity problem cheaply. **Done:** At 256/128/64/44/32/24 px on both light and dark Dock surfaces, Firetail uses the Block logo cursor, immediately reads wine-pink plus ember-gold, remains recognizably the same world as the live Room, and does not collapse toward Potoroo’s rust or Bombora’s violet/rose identity. The user chooses A/B/C from one Block-cursor comparison sheet before any shipping asset changes land. **Verify:** Regenerate only through the canonical exporter/packer; assert Firetail alone changes preset membership from Pill to Block and every other world’s assignment remains unchanged; run the full 18-world icon distance/crowding/stale-entry laws from items 102/109, with Firetail’s empirical nearest pairs named; assert icon cursor/knockout colors still equal Firetail `primary`/`primary_content`; perform `l` containment, small-size legibility, and dark/light Dock vision smoke; rebuild item 20’s dashboard; native + wasm gates. **Depends on item 110 because both touch the exporter and generated icon assets; unclaimed; user design decision updated 2026-07-27.**

108. **Make Gumtree’s broad chevron field visibly present — keep the shipped geometry, reject imperceptibility.** **Build:** Retain Gumtree’s broad/quiet structural identity (`period_px: 170`, `amplitude_px: 60`, shallow `angle: 0.26`, hence roughly six rows at 1200×800) and increase the mark’s actual visibility. The user’s live screenshot settled the old 89a fork: fewer rows are not the need; the current chevrons are effectively invisible. Tune the authored contrast inputs—`density` and, only if needed, the theme-derived tint relationship—through a small measured candidate sweep displayed in item 20’s dashboard. Do not change the shared zigzag tiling/abutment infrastructure or make a Gumtree code path. **Scope:** The current authored `density: 0.20` is multiplied by the shared `PATTERN_MAX_COVERAGE: 0.55`, capping the line near 11% blend before its close eucalyptus tint is considered; that explains the live result. Preserve the gradient, broad tooth/profile/direction, six-row rhythm, text/page geometry, and Quokka’s distinct tight/loud pole. Seek “clearly perceptible at a glance, still background,” not an opaque motif. **Done:** At normal page-mode widths and representative short/tall windows, a human can immediately locate and follow Gumtree’s chevrons without being prompted, while prose remains dominant and Quokka remains materially tighter, steeper, and louder. **Verify:** Dashboard A/B candidates covering the current value and a bounded density/tint sweep; pixel arithmetic over both margins at representative geometries proving a meaningful contrast increase and no new blank lanes; existing zigzag occupancy/abutment/row-rhythm laws; full world/background roster sweep and standing vision smoke; native + wasm gates; final live user pick. **Unclaimed; depends on item 20 only for the review surface, not the shader mechanism.**

105. **ITEM 91 DID NOT FIX THE LIVE BLOCK CARET — Mopoke and Gumtree still show the broken glyph/EOL geometry.** **Build:** Reproduce and repair the user's paired 2026-07-26 release screenshots on current `main`: the same `aaa` line with the Block caret inhabiting the final `a` versus one step later at end-of-line has a visibly discontinuous outer cell—on the glyph it follows the new raster-ink-box arm, at EOL it falls back to the generic line-cell arm. The user first identified Mopoke and immediately confirmed Gumtree has the same bug; treat this as a cross-world mechanism failure, not a theme dial. **Scope:** Item 91 (`07f1b7d`) explicitly introduced the two-arm `caret_cell_vertical`: proportional single-glyph anchors use the raster `InkBox` grown by zoom/DPI-scaled `CARET_INK_PAD`, while whitespace/EOL/empty/ligature/mono anchors use `caret_block_h` around `caret.pos.y`. Its laws proved each arm separately and called the fallback “unchanged”; they did not prove the lived transition between adjacent anchors has one coherent cell. Diagnose with pixel bboxes and baselines from the user's exact `aaa` fixture before choosing policy: distinguish excess scaled pad, a baseline/center mismatch, knockout-mask placement, and an inherently incompatible ink-box↔line-cell switch. Audit the bug's neighborhood across every proportional world, x-height/ascender/descender glyphs, punctuation, space, EOL, empty line, ligatures, wrapped boundaries, Block and Morph (rest plus travel), zoom 50–300%, and 1×/2× DPI; retain item 97's uniform mono-cell contract. **Done:** A one-character move between a glyph and adjacent space/EOL no longer makes the Block caret visibly jump in top, bottom, or overall cell character; the covered glyph remains correctly seated/legible inside it, descenders remain contained, Morph has no settle/travel thickness pop, and the original item-91 failure (a large empty accent cap over x-height letters) does not return. Mopoke and Gumtree agree through the shared owner with no identity branch. **Verify:** Add the missing TRANSITION law using real rendered pixels: capture the same line at each adjacent caret column and compare outer caret bbox, glyph bbox, baseline, pad, and knockout containment; prove it fails on `07f1b7d`/current main and passes after the repair. Sweep the full proportional-world roster × representative glyph class × glyph/space/EOL transition at two zooms and two DPIs; keep the mono complement sweep, motion law, sidecar/pixel agreement, and standing vision smoke; full native, both conventions, wasm, and release live confirmation by the user. **User-reported item-91 repair failure.** 🔨 ROUND 3 IN FLIGHT 2026-07-26 — worktree `105-caret-cell`, branch head `b0ca2cf`, NOT yet merged.

  **105a. DIAGNOSIS (settled, do not re-derive).** The two arms of `caret_cell_vertical` (`render/caret.rs:458`) were expressed in **two different vertical reference frames**. The ink arm centres on the glyph's raster ink box relative to the font BASELINE (`cy = baseline − ink.top + ink.height/2`, reconstructed from the row's `max_ascent`/`max_descent`). The fallback arm centres on `caret.pos.y`, the ROW-BOX GEOMETRIC MIDPOINT (`line_top + row_h/2`), which never reads the ascent/descent split at all. The offset is a closed form — `(max_ascent − max_descent)/2 − (ink.top − ink.height/2)` — font- and glyph-dependent with no structural reason to be zero. Measured on the user's `aaa` fixture at zoom 1: Mopoke Δtop −3 / Δh +2; Gumtree Δtop −6 / Δh +3; Bombora worst at Δtop −7 / Δh +7 of a 22px cell (a 32% jump). Reproduces in 11/11 proportional worlds and 0/7 mono worlds (mono never leaves the fallback arm, so no seam exists to cross — consistent with item 91's `font_is_mono` gate). Scales with zoom, so it is geometry, not rounding. Candidate (a) excess `CARET_INK_PAD` is **ruled out**: the pad is symmetric and the ink cell measures SMALLER than the fallback, whereas excess pad would make it larger. Candidate (c) knockout-mask placement is **ruled out** for this bug (Mopoke/Gumtree both use the `Normal` block style; the mask is not on their path) — though it remains unaudited on the `Filled` + mono/ligature combination and deserves its own follow-up. Candidate (d) inherent incompatibility is **rejected**: the offset is computable, so the two conventions can be reconciled.

  **Why item 91's laws stayed green:** its nine laws in `caret_ink_box.rs` each prove ONE arm in isolation and never construct the transition. `glyphless_fallbacks_keep_the_line_box_cell` asserts `cy == caret.pos.y` — it *enshrines the wrong invariant*, proving the fallback is self-consistent with the row-box-centre convention without ever asking whether that convention agrees with the new ink arm sitting next to it on the same row. The file's own docstring calls the fallback "unchanged". This is the CLAUDE.md trap exactly: each imagined case correct in isolation, the seam between them unswept.

  **105b. ROUND 2 STATE (`b0ca2cf`, green: suite 3023 passed both conventions, wasm L1 clean).** **The user's literal reported bug is fixed and confirmed by direct PNG bbox arithmetic, not by sidecar:** `aaa` last-glyph → EOL is Δtop 0 / Δbottom 0 / Δheight 0 on Mopoke, Gumtree, Bilby and Bombora. Gallery for a look: `caret-105/*.png` in the worktree. **But the general mechanism is NOT closed, and the reason is an architecture choice that diverged from the diagnosis above.** The diagnosis recommended giving the fallback a SYNTHETIC ink box derived from the row's own baseline and ascent/descent metrics, so every glyphless column in a row gets one consistent, font-derived cell. The build instead implemented NEIGHBOUR-BORROW — the glyphless column borrows a real nearby glyph's raster ink (`nearest_row_raster_box`, outward search bounded to the visual row). Borrow is inherently DIRECTIONAL, and that bred a defect chain of one class: round 1's single backward hop failed at column 0 and on runs of 2+ glyphless columns; round 2's outward search fixed both to a bit-exact 0.00px — and introduced a third. **OPEN, HIGH SEVERITY, IN SCOPE, INDEPENDENTLY REPRODUCED TWICE:** a single space with DIFFERENT real glyphs on both sides is always resolved backward by the tie-break, so its FORWARD seam — literally "one glyph-anchored, one not" at adjacency, the item's own scope — is left wide open. Fixture `| 1`: the pipe→space hop is a perfect 0.00px, but space→digit is 6.00 Mopoke, 5.00 Gumtree, 8.00 Bilby, 8.00 Bombora, 8.00 Galah — 10 of 11 worlds over the file's 3.0px tight bound and three over its 7.5px absolute ceiling. The shape is everyday in a markdown editor (table pipe-padding, any tall-glyph/short-glyph pair). No shipped law exercises this fixture class; every existing fixture has a real neighbour on at most one side, or a run flanked by copies of the SAME letter, which cannot expose a tie-break asymmetry. Also disclosed and independently confirmed: an "interior flip" on runs of 4+ glyphless columns flanked by two different letters (6.5px Bilby per the build, 7.0px Bombora per the verifier — the build understated its own worst case), which is out of the item's stated scope but is the same mechanism.

  **105d. ROUND 3 RESULT (`5b2ddb7`, gates green: suite 0, both conventions 0, wasm 0; NOT merged, does not fast-forward — main is 11 commits ahead but none touch `caret.rs` or the item-105 test file).** The bakeoff ran and **the incumbent won on measurement**, which is the outcome a fair test is for. The empirical bar was established: real adjacent glyph-to-glyph steps across the 11-world proportional roster measure **median 6.00px, worst 14.00px** (n≈77–87) — so "no worse than what the product already accepts" is a genuinely loose bar, and both arms cleared it. The decisive numbers: **borrow-blend** keeps the user's `aaa`→EOL case at **exactly 0.00px on all 11 worlds** with a worst glyphless seam of **4.00px** (29% of the bar), while **synthetic-only** never reaches exact zero on the user's case (0.40–2.60px per world) and has a worst seam of **11.85px** (85% of the bar). Importantly, the winning arm is NOT the tie-break flip that was forbidden: `nearest_row_raster_box` now **linearly interpolates both neighbours** weighted by relative distance, so it is non-directional and symmetric by construction, degenerating to the single box when only one side has real ink. The old `| 1` defect is closed (forward seam 6–8px → 0.14–3.85px) and the interior-flip case is now **exactly 0.00px** on every world. Also measured and worth keeping: the rejected synthetic variant "ascent-to-baseline, ratio 1.0" reproduces item 91's original defect in miniature (up to 15.25px on Gumtree) — the per-font mean-of-x-height/cap-height ratio is doing real work.

  **105e. ✅ ROUND 4 SETTLED IT — THE FOURTH FINDING WAS A MEASUREMENT ARTEFACT, and round 4 is the first of the four rounds that found nothing to repair.** Landed `d5059ba`, no product code touched. The reported ~30px jump on an all-blank wrapped row is **84–100% pure row pitch**: the row's own `line_height` is a flat 32.000px on every proportional world at that width, and the caret is *supposed* to drop one visual row and reset toward the left margin when it wraps — the quoted Δx ≈ −88px is exactly that left-margin reset, and is correct. Isolating the genuine row-relative residual at the tier-2/tier-3 boundary (each column's `cy` minus its OWN row's baseline) gives **1.0–2.2px cy and 0.4–5.85px h** across an 11-world × 2-zoom sweep, inside the file's existing 7.5px `TRANSITION_BOUND_WIDE_PX` and well under the 14px empirical bar. Two agents measured this independently with **entirely different fixtures** (`a`×20/` `×30/`b`×20 at 260px versus `c`×15/` `×40/`d`×15 at 300px) and landed on 5.85 vs 5.848px — agreement to the decimal across different inputs is what makes the refutation trustworthy. The lesson is now a permanent law, `glyphless_row_transition_is_bounded_row_relatively_across_a_wrap`, whose body documents WHY an absolute-position delta across a wrap is expected, so this cannot be re-reported a fifth time. **The genuinely weakest measured seam, for the record:** leaving an all-blank row back INTO real ink costs up to 5.85px of height, because tier 3's synthetic "typical letter" estimate has no way to see that a full ascender is coming on the next row. That is comparable in kind to already-accepted real-glyph class-to-class variation (Mopoke's real ligature step measures 6.0px against the same bound), so it is bounded, not broken.

  **105g. HOUSEKEEPING, not this round's job:** `src/render/tests/caret_transition_item105.rs` is 1821 lines, well past CLAUDE.md's ~500-line ceiling. Pre-existing debt, modestly worsened across these four rounds; a future split into a submodule dir.

  **105e-original. ⚠️ THE FINDING AS FIRST REPORTED (kept for the record).** A verifier and the gate independently reported a ~30px caret jump on a **wrapped visual row containing zero real ink** (a space run long enough to fill a whole wrapped row, flanked above and below by real-ink rows of the same logical line): both directions of `nearest_row_raster_box`'s search return `None` because it is bounded to the caret's own visual row by design, so the cell drops to `caret_synthetic_ink_box` anchored to *that row's* baseline. **But both reports measured ABSOLUTE caret position across what is very likely a wrap boundary** — they quote Δy ≈ +30px together with Δx ≈ −88px, which is precisely what a wrap looks like and is correct behaviour, and the cell HEIGHT deltas they actually quote are 0.5px and 5.5px, both inside the existing bounds. So the finding is not yet established. Round 4 must first adjudicate by measuring **row-relative** cell placement, not absolute y, and only then fix. Note the architectural point either way: the winner kept THREE tiers (real ink → borrow-blend → synthetic), so if a defect exists it lives at the tier-2/tier-3 boundary, which is where a seam would be expected to reappear.

  **105f. PROCESS ERROR, MINE, NOW FIXED IN THE README.** Round 3's brief named ONE worktree for two concurrently-running bakeoff arms. The second arm found the first's uncommitted edits appear underneath it mid-task on a branch it had not checked out; it stashed rather than destroyed them (`stash@{0}` on `105-caret-cell`, plus `105-r3-synthetic-iso` at `0139e4d` holding the synthetic implementation) and moved to an isolated worktree. Nothing was lost, and the landed tree at `5b2ddb7` was verified clean. `.orchestrator/README.md` now requires one worktree per concurrent WRITING agent.

  **105c. ROUND 3 DIRECTION (orchestrator, dispatched — outcome in 105d).** **Flipping the tie-break to prefer forward is NOT a fix and must not ship** — it rotates the 8px jump from the forward seam onto the backward seam, which is precisely the item-94 failure pattern of patching within a class instead of leaving it. Round 3 must measure BOTH architectures on the full adjacency axis before choosing: (i) the SYNTHETIC per-row box the diagnosis originally recommended, and (ii) neighbour-borrow retained. The target is measurable and needs no taste call: the product ALREADY accepts that the cell changes between different real letters (the file's own 7.5px real-ink-to-real-ink ceiling), so the bar is that **glyphless seams are no worse than the accepted glyph-to-glyph seams**, not that they are zero. Round 2 is held unmerged pending the outcome.

109. **The icon guard's aggregate check gates on the MEAN, so a coordinated SUBSET of blessed pairs erodes silently — and the per-pair slack is ~200x the measured noise floor.** **Build:** Close item 102's quantified residual. **Scope:** Item 102's round-3 `SYSTEMIC_DRIFT_SLACK` check (`src/app_icon/tests.rs`, `check_pair_axes`) gates on the mean erosion across an axis's ENTIRE blessed list against 1% (half of `RATCHET_SLACK` = 2%). Two independent verifiers reproduced the hole live against the real shipped code, with exact arithmetic: erosion of k pairs at 1.9% each gives mean `k*1.9%/N`, so **differing is silent at 6/12 and caught at 7/12; mean is silent at 5/10, caught at 6/10; ink is silent at 2/5, caught at 3/5.** One construction eroded 11 of 12 differing pairs to 1.99% and still returned `failures: []`. This is not a contrived attack: a font/antialiasing/gamma change, or one exporter code path touching only a preset, plausibly moves the near-black-ground worlds and not the near-cream ones — i.e. exactly a subset. That partial-roster shared cause is what the check's own doc comment names as its reason to exist. **THE DIRECTION WORTH TRYING FIRST, from item 102's own measurements:** do not reach for a cleverer aggregate statistic. The build measured per-pair erosion on today's shipped roster at **~0%** — there is essentially no antialiasing noise to tolerate. `RATCHET_SLACK = 2%` was chosen defensively against noise that does not appear to exist. **Tighten the per-pair ratchet toward the measured noise floor and the subset attack disappears without any aggregate check at all**, because no pair can move 1.9% silently in the first place. Establish the true noise floor first (re-export the icons unchanged, or across machines/OS versions, and measure the spread); set the slack from that measurement plus honest headroom; and only then decide whether any systemic check is still earning its complexity. Deleting the mean check would be a good outcome. **Done:** A coordinated erosion of ANY number of blessed pairs, from one to all, either fails or is deliberately re-blessed; the per-pair slack is justified by a measured noise figure recorded next to it, not by caution. **Verify:** Reproduce both verifier constructions (first-half-of-list at 1.9%, and 11-of-12 at 1.99%) and confirm both now FAIL; confirm an unchanged re-export still PASSES (this is the test that the slack is not too tight, and it is the one that matters); keep item 102's Ibis, ground-clone, roster-growth and stale-entry probes green. **Unclaimed; filed 2026-07-26 from item 102's verification.**

107. **`OverlayKind::ALL` and its siblings are hand-kept arrays beside compile-time-exhaustive matches, so a roster sweep silently covers less than it claims.** **Build:** Make a "sweep every variant" law actually sweep every variant, by deriving the roster from the enum rather than from a parallel array a human maintains. **Scope:** This is now the single most repeated defect class in the codebase's recent history, found FOUR times in two waves by verifiers rather than builds: item 97 (a hardcoded mono-face list), item 104 (a hand-typed facet strip, twice, in consecutive rounds), item 106 (`OverlayKind::ALL`). The shape is always the same and always passes review — a law writes a no-wildcard `match` so it cannot compile when a variant is added, but then ITERATES a separate `::ALL` array, so a variant present in the enum and in the match but missing from `ALL` compiles clean and is silently never exercised. `OverlayKind::ALL`'s own doc already admits it is "kept in lockstep by hand", and roughly 18 other call sites across the tree share the idiom. **Done:** A roster law's runtime coverage is bounded by the enum, not by an array; adding a variant without updating every roster fails to build rather than silently narrowing a sweep. Prefer a derive or a macro that generates `ALL` from the enum over asking humans to keep two lists aligned — same behaviour, same code. **Verify:** Add a variant to an enum, omit it from its `ALL`, and confirm the build FAILS; enumerate every `::ALL`-style array in the tree and either route it through the generated owner or document why it is genuinely different; re-run every existing roster law and confirm its covered-count now equals its enum's variant count (assert that equality, so the two can never diverge again). **Unclaimed; filed 2026-07-26 from item 106's verification. The recursion is the point: this is the law-quality failure that keeps producing the law-quality failures.**

122. **Turn Quokka’s zigzag outline field into horizontal alternating wallpaper bands.** **Build:** Extend the shared data-driven zigzag background with an alternating-band treatment and assign it to Quokka: a tinted chevron band, then an untouched-background band, repeating continuously across the viewport. Rotate the field so the bands travel horizontally rather than as today’s steep diagonal stair columns. The tinted bands are clearly visible at a glance but remain background behind the calm writing Room. **Scope:** This is actual filled-band alternation, not “draw one outline, skip one outline” and not alternating wedges within one tooth. Preserve Quokka’s palette, page, typography, tight tooth scale, and one continuous viewport-space field across both margins; the writing column still occludes the field. The new treatment is reusable capability data, never a Quokka branch. Gumtree keeps its authored outline treatment and current geometry byte-identical; sequence this item with item 108 rather than editing the shared zigzag shader concurrently. **Done:** Quokka reads as horizontal tinted/background chevron wallpaper in both margins, with an unbroken alternating rhythm at narrow/wide and short/tall windows, while prose remains dominant and Gumtree is unchanged. **Verify:** Host/GPU lockstep for band phase and angle; real-pixel classification proving tinted and untouched lanes alternate at representative cells; continuity across both margins behind the page mask; full viewport/zoom/DPI/page-width sweep; no blank lanes, no writing-column ink, Quokka↔Gumtree distinctness, deterministic captures, dashboard vision smoke, native + wasm gates. **Depends on/serializes with 108; unclaimed; user design decision 2026-07-27.**

125. **Present the repaired shipped-icon typography roster for user review after item 110.** **Review:** Rebuild the per-world icon dashboard after the shared cursor/baseline repair, then audit the typography rendered inside the icons themselves across all 18 worlds at 256/128/64/44/32/24 on light and dark Dock surfaces. This is an optical consistency review, not an instruction to match a separate reference or change font families. Include Bilby↔Gumtree and the six worlds from item 99a; show the user the repaired 32px strip and representative larger sizes before making any further taste change. **Done:** The user receives one scannable current roster and can answer whether the wordmarks sit consistently, whether every assigned cursor contains the intended `l`, and whether any remaining small-size loss is acceptable. Findings become separate concrete items; this review itself does not smuggle palette/font changes into item 110. **Verify:** Dashboard assets are current to item 110’s commit and complete for every world/size/surface; measured wordmark/cursor bboxes accompany the visual review; no stale pre-repair image is presented. **Depends on 110; user-review gate 2026-07-27.**

126. 🔴 **BLOCKED — code/oracle candidate `cccf24b` is clean and independently ready; the sole remaining item-specific gate is a foreground WGPU/Metal adapter.** The shared proportional Block/Morph visual-body floor is geometry-driven with no punctuation/world branch; Morph’s supporting body preserves the glyph through `primary_content`. Code health, pinned file sizes, shared ownership, a body-only glyph-mask mutation that runs the real assertion red, and reproducible source provenance are green. The mandatory 270-case real-pixel sweep fails before case one with `no wgpu adapter`, so the full punctuation × mode × DPI/zoom inset/knockout result and release-mode feel cannot yet be claimed. Resume when the display is unlocked, run that sweep, then integrate through the exact-main tier. **Routing:** build/repairs=`gpt-5.6-terra` medium; verify=`gpt-5.6-terra` medium. **User design decision 2026-07-27.**

127. **Optically centre the fold chevron on its heading row across every world.** **Build:** Repair the shared fold-chevron placement exposed by Wave 7’s Wagtail `## wave 7` screenshot: the chevron is attached to the heading’s baseline, but its visible ink sits low/crooked relative to the heading row, and the same defect appears in other world faces. Centre the visible chevron mark against the real shaped heading-row box through one shared ink/geometry rule—or use one stable shared UI mark if measurement proves the per-world glyph shapes themselves are the problem—rather than adding world-specific offsets. **Scope:** Preserve the current left-of-heading placement, caret/hover summon rule, fold direction/state, no-heading-shift guarantee, and generous click target. Paint and hit-test continue to share one geometry owner. Do not retune fold contrast or background chevron patterns in this item. **Done:** The chevron looks vertically balanced beside H1/H2/H3 headings in every world, including Wagtail, without moving the heading text or changing the row height. **Verify:** Exact reported H2 fixture plus all heading levels, full world roster, expanded/collapsed and caret/hover states, narrow/wide, 1×/2×, and zoom sweep; pixel arithmetic compares the chevron ink centre to the shaped row centre, while hit-test laws prove the click box still encloses the painted mark and toggles the same heading. Add a representative vision smoke because optical centring cannot be established by shared-baseline equality alone. **Unclaimed; user-reported Wave 7 polish 2026-07-27.**

128. **Finish Awl’s native macOS identity: capitalise the app name and simplify/centre the About panel.** **Build:** Make the packaged Mac application display `Awl`, not `awl`, in the App menu while preserving the lowercase `awl` CLI executable/command. Keep the standard native About panel, centre its credits text like the reference Mac panels, and reduce its content to the native app icon/name/version plus one centred line: `Frank Lu · GPL-3.0`. Remove `A calm, opinionated plain-text editor for prose and light code.` from the panel; do not add a copyright line. **Scope:** Resolve the displayed App-menu name through the real bundle/process identity seam rather than changing the inert pure-roster title; keep catalog labels `About` and `About Awl`, menu routing, app metadata, `NOTICE`, GPL declaration, Credits, and licensing facts intact. Coordinate with the app-icon/package work so a development placeholder icon is not mistaken for the shipped result, but do not redesign the icon here. Clean up the stale internal About documentation so it agrees that macOS uses the native panel while other platforms retain the in-app card. **Done:** A packaged launch shows `Awl` in the Mac menu and a visually centred, spare native About panel containing no slogan and no fabricated legal claim; terminal use remains `awl`. **Verify:** Bundle metadata/package assertions, pure menu roster/routing laws, real packaged-app menu smoke, and a live About screenshot checked for centred text, correct capitalization, version, author/license line, and current app icon; native + wasm gates prove the platform split remains intact. **Unclaimed; user design decision 2026-07-27.**

131. **Give Mangrove and Magpie mirrored diagonal-line compositions across contextual menus and the real Settings workspace.** **Build:** Add one reusable theme-owned diagonal row composition through the shared rowlayout/surface machinery, then assign its two authored orientations: **Mangrove** draws a continuous descending `\` spine, with row clusters left-aligned on the RIGHT side; **Magpie** draws a continuous ascending `/` spine, with row clusters right-aligned on the LEFT side. The line is mandatory in both—the striking read comes from the drawn division and triangular negative space, not merely staggered text. It may visually bleed toward the surface corners, while row attachment points occupy an inset middle band so the first/last rows retain usable width. **Line treatment:** never amber/primary. Mangrove uses a crisp tidal-teal line derived from its muted ink; Magpie uses a crisp graphite line from its muted ink. Resting weight is clearly visible but subordinate to text; the selected row brightens and thickens only the local spine segment toward `base_content`, extends a short connector to the row, and steps the row outward by a few crisp pixels—no spring, pulse, or full-width selection bar. Existing bottom-left Mangrove stipple and bottom-right Magpie ghost placards occupy the opposite empty triangle rather than colliding with the rows. **Controls are in scope, not a fallback:** model each row as a measured `label + fixed gap + accessory/control` cluster. Reserve consistent label/accessory extents across the visible set so shortcuts, values, toggles, checkmarks, exact-entry fields, and Range sliders trace a stable parallel rail with honest spacing; anchor the whole cluster to the spine instead of independently nudging text. Query/title/category navigation/footer regions remain horizontal and stable—the diagonal owns the candidate/setting rows, not every glyph on the surface. Filtering and scrolling sample a fixed surface-relative line at fixed row y positions, so content changes never make the spine or surviving rows jump horizontally. **Surface reach:** enroll every contextual overlay’s row section that currently consumes theme list-style data, not Commands alone; non-row panels keep their existing geometry. After item 115 removes the old Settings overlay, apply the SAME diagonal owner to the Settings workspace’s main setting list for Mangrove/Magpie, while its category rail, search/title shell, child Theme/Caret auditions, and narrow-stage navigation remain item 115’s workspace behavior. The empty main-pane triangle may carry the existing active category/title typography, but never a duplicate decorative label. **Responsive bound:** size the slope from the real widest visible cluster and available side territory; widen within the existing surface limit first, then reduce horizontal travel while preserving a visibly diagonal direction. Never overlap, clip, shrink type/controls, introduce horizontal scrolling, or silently fall back to Pane/Bars. **Scope:** This is a third data-driven row composition, not Mangrove/Magpie branches and not permission to diagonalize the document, workspace shell, History comparison, native Mac menu, or web/Linux persistent menu bar. Other worlds and their Pane/Bars results remain byte-identical. Item 112 owns the shared overlay rhythm first; items 114/115 own the workspace and Settings migration first. **Done:** Mangrove reads like a tidal instrument panel and Magpie like an editorial spread in both brief menus and sustained Settings, with a visible line, deliberate negative space, readable controls, obvious selection, and no loss of keyboard/pointer usability at any supported width. **Verify:** Full no-wildcard `OverlayKind` row-surface sweep plus every `SettingId × SettingKind`; simple/long labels, chords, values, toggles, text entry, sliders, empty/short/full/filtered/scrolled lists, category changes, child-picker return, and narrow/wide staging; drawn line/row/control ↔ hit-test agreement at zoom and 1×/2× DPI; pixel laws for orientation, line continuity, inset attachment band, fixed label-control gap, local selected segment, placard/row non-overlap, non-primary ink, and no clipping; exact before/after identity for every non-assigned world; dashboard captures and affordance-locating vision smoke over Commands plus every Settings category in both worlds; native, both conventions, and wasm gates. **Depends on 112 + 114 + 115; unclaimed; ambitious user design decision 2026-07-27.**

132. **Add Kite — a loud light world travelling through a peripheral warped-grid tunnel.** **Identity:** Kite is the nineteenth world and the deliberate light `5/5` counterpart to Firetail: cool rather than warm, geometric rather than organic, crisp rather than liquid, and directional rather than drifting. Use a near-white mineral/pale-lavender ground, indigo/graphite grid and deep cool prose ink, with a single hot vermilion caret that reads like the bird's red eye. Its authored font must be bundled, licensed, distinct from the existing roster, and reported through the normal asset-size/licensing audit; do not choose one merely to satisfy the concept. **Background:** Add one data-driven ambient warped-grid capability shared through the existing renderer, not a Kite/world branch or a literal 3D engine. The writing page stays flat, opaque, motionless, and fully legible. The grid lives in the Frame/margins as two intentionally composed slices of one curved tunnel: distort the useful curvature outward so the opaque central page does not hide the vanishing point or all of the top/bottom bend. The left and right margins must communicate one journey—during a left turn the left grid compresses while the right opens; climbing/descending produces coherent opposing vertical flow—rather than looking like two unrelated animations. Fade/quiet the grid near the page edge and progressively simplify it at narrow margins to a gently translating warped field; never squeeze a tiny illegible tunnel behind the page or draw through prose. Use anti-aliased major/minor line hierarchy, with every fifth line stronger and minor lines fading before projected spacing can create Retina/WebGL moiré. **Motion:** The camera travels continuously forward. Roughly once a minute it eases into a new long direction—straight, left/right bends, climbs, and descents—without a cut, jump, hard reversal, obvious one-minute restart, roll, or attention-grabbing acceleration. A much longer deterministic route may eventually repeat invisibly; the visible contract is uninterrupted travel, not a short looping clip. Keep speed calm enough for writing even though the world is visually loud. Losing focus pauses in place and resumes without catch-up; ambient motion off and Reduce Motion freeze to one composed still; headless capture has a deterministic settled pose. **Scope:** This is an explicit statement-world exception, not permission to increase motion elsewhere. Preserve the one-accent law, page geometry, adaptive column, overlay readability, zero-network rule, and theme-as-data architecture. Measure release-mode GPU/frame cost on native Metal, wasm WebGPU, and WebGL fallback; reduce grid density/cadence through the shared capability before compromising text or inventing a platform-specific look. **Done:** On a wide Retina display, Kite unmistakably feels like the stable writing surface is gliding and gradually steering through curved space, with the decisive geometry visible in the margins despite the central page; on a narrow display it remains recognizably Kite without shimmer or clutter. It is light, technical, pale-purple, and `5/5`, balancing Firetail without resembling it. **Verify:** Add roster/no-wildcard enrollment laws and exact non-assigned-world identity; deterministic wide/narrow captures for straight, left/right, climb/descent, paused, ambient-off, and Reduce Motion states; pixel arithmetic for page exclusion, edge fade, cross-margin directional coherence, line hierarchy, contrast, on-canvas bounds, and no high-frequency minor-line aliasing at 1×/2× and zoom; release benches on native/WebGPU/WebGL; dashboard Room/Frame/icon enrollment; affordance-locating vision smoke and a live multi-minute human review for speed, steering legibility, seamless transitions, peripheral comfort, and motion sensitivity. Harness results must not claim the live motion feels calm. **Unclaimed; user design decision 2026-07-27.**

133. **Produce a five-treatment Paperbark background trial in the real app, then stop for the user's choice.** **Build:** Create one deliberately temporary, isolated Paperbark trial capability with five selectable static profiles: **A Broad sheets** (large horizontal peeled layers with soft depth), **B Deckled strata** (nested torn handmade-paper contours gathering around the writing page), **C Loose fibres** (sparse long fibres and translucent veins), **D Relief print** (flatter ochre block forms with dry imperfect edges), and **E Peeling curls** (thin lifted ribbons and shallow cast shadows). These are five treatments of ONE candidate world, not five themes: hold the provisional cream/pale-honey ground, deep bark-brown ink, coral/vermilion caret, typography, document, page width, zoom, selection/caret state, and all chrome identical so the background is the only changed variable. Render the material procedurally through Awl's real GPU/background path at Retina resolution; do not paint the editor or the candidate backgrounds in HTML, use stock texture images, fetch assets, enroll Paperbark in the shipping roster, or build five permanent code paths. Keep every profile fully static in this first decision round—motion and raking light are explicitly deferred until the user reduces the field to two. **Review artifact:** Generate one offline local HTML contact sheet under the existing gitignored review-gallery family. Its primary row is exactly five identically sized wide real-app PNG captures labelled A–E, visible together at a useful comparison scale; each opens the untouched full-resolution PNG through the dashboard's existing lightbox grammar. Include a secondary narrow-window capture for each treatment and concise factual captions naming the profile and capture geometry. No HTML recreation of Awl, external URL, animation, ranking, or agent recommendation may bias the sheet. Reuse the review dashboard's visual language and generation helpers where practical without enrolling these candidates in the ordinary 18/19-world roster counts. **Rollback/review gate:** Do this in the item's worktree as disposable trial machinery. Report the openable absolute HTML path and the five source PNG paths to the user, then STOP. Do not select a winner, add lighting, tune typography/palette independently per profile, commit generated gallery files, pack an icon, or land Paperbark on `main`. After the user's visual choice, delete the rejected profiles and trial selector; the chosen background plus the separately judged slow-light treatment becomes a fresh production queue decision. **Done:** The user can open one page, compare five honest Awl screenshots at normal and narrow widths, inspect each at full Retina resolution, and make the next taste decision without imagining how a material study would sit around the real writing page. **Verify:** Assert five unique profile ids and exactly ten non-empty PNGs, identical sidecar document/theme geometry and palette/type/caret state within each width class, background-only pixel differences outside the opaque page, unchanged writing pixels, correct A–E labels/targets, no external dependencies, and no shipping-world enrollment. Open the page locally and ask only affordance-locating/material-identification questions; visual quality remains the user's call. **Unclaimed; review-gated user design decision 2026-07-27.**

## Ready — shared ownership and performance

143. 🟡 **IN PROGRESS — item-143-test-relocation (codex), branch codex/item-143-test-relocation.** Relocate inline test modules out of the oversized production files. **Evidence:** the files that feel biggest are mostly tests — `src/main/run.rs` 4,397 lines of which 2,993 (68%) are one inline `mod tests`; `src/commands.rs` 2,669 / 1,881 (70%); `src/keymap.rs` 2,807 / 1,691 (60%); `src/settings.rs` 1,674 / 957 (57%); `src/render/geometry.rs` 2,339 / 880 (38%). Production logic in each is a normal-sized module. **Build:** Move each inline `mod tests` into a sibling `tests.rs` (or `<mod>/tests/` where one already exists), following the `render.rs`→`render/` and `buffer.rs`→`buffer/` precedent. **Scope:** Pure relocation. No test renamed, reordered, merged, split, or deleted; no production line changed beyond the `mod tests;` declaration and whatever `use super::*` the move requires. If a test reaches private items the move breaks, prefer `pub(crate)` on the item over rewriting the test. **Done:** The five named files contain production logic and a one-line test-module declaration; the test count is unchanged. **Verify:** Test count identical before and after (exact number, not "all green"); `git diff` shows no assertion text altered; code health, both native conventions, wasm. **Dependency on 142 is CLEARED** — 142 landed at `f70b55e3`. **Precedent to follow:** `02208419` did exactly this move for `render/overrides.rs` and `src/syntax/mod.rs`; copy its shape, including the two traps it hit — a relocated test can break a `println_audit`/`durable` audit table that pins a path, and a Clippy exception pinned to the old file:line needs repointing, not deleting. **Note:** `scripts/code-health.py::production` did not exempt a sibling `tests.rs` until `86503ab1`, so before that fix this relocation would have moved lines from one measured file to another and accomplished nothing; it now works as intended. **User decision 2026-07-28.**

144. 🟡 **IN PROGRESS — item-144-ratchet (codex), branch codex/item-144-size-ratchet.** Make the file-size ceiling a high-water mark that only ratchets down. **The defect, measured 2026-07-28.** `check_structural` (`scripts/code-health.py:181-186`) compares each file against its size at frozen baseline `f12d04a`, not its current size. It correctly blocks a new oversized file (verified: a planted 600-line file fails by path), but **63 files have SHRUNK since that baseline and may silently regrow — 19,492 lines of slack**, 2,083 of it in `render.rs` alone. Verified: appending 5 lines to `theme/worlds.rs` leaves the gate green. This forgives exactly the work the repo has been doing — every consolidation converts its saving into permission to regrow. **Build:** measure against the smaller of (baseline, current). That means storing the marks, which the current design deliberately avoids; preserve its intent — a stored mark may only DECREASE, and the checker must reject a mark larger than the file's current size. Keep the `> FILE_LIMIT` arm for new files unchanged. Also fix a robustness bug found here: `(ROOT / path).read_text()` on a tracked-but-absent file crashes the gate with a raw `FileNotFoundError` instead of a diagnostic. **Verify:** non-vacuity both ways — appending one line to `theme/worlds.rs` must fail where it passes today, and a legitimate shrink must still pass. Prove a hand-raised mark is rejected. Extend `--self-test`. **Must land BEFORE item 155**, or the grind's savings are handed straight back. **The 87-file mass-split originally in this item is DROPPED; item 155 carries the bounded version.**


95. **Adopt the shared Range interaction for the remaining bounded numeric Settings rows.** **Build:** After items 94 and 90 land and prove the generic control with Zoom and Scroll sensitivity, migrate `Page width (prose)` and `Page width (code)` from plain `Value` rows to the same Range grammar. Keep their existing 20–200-column clamp and exact numeric-entry path; use a linear rail and an authored whole-column step that keeps the current/default values reachable. Left/Right, click, drag, Enter-to-type, live apply where the active buffer class matches, and persistence all route through item 94's shared owner—no page-width-specific slider code. **Scope:** This is a consolidation pass, not permission to invent sliders or new preferences for categorical/toggle values. Audit the entire Settings roster and migrate only bounded numeric values for which continuous visual judgment is honest; document why every remaining non-Range kind stays categorical, path-based, action-based, or exact-entry-only. **Done:** Every suitable bounded numeric setting uses one consistent keyboard-and-mouse range interaction; no duplicate range math, hit testing, display formatting, or persistence path remains. **Verify:** Exhaustive SettingId×SettingKind roster law, page-width min/default/max and exact-entry tests, mouse/keyboard parity, prose-vs-code live-apply isolation, persistence/restoration, representative narrow/wide captures, standing vision smoke, native + both keymap conventions + wasm gates. **Depends on 94 + 90; dispatch only after both land so the interaction is consolidated from real use rather than guessed upfront.**

145. 🟡 **IN PROGRESS — item-145-fixture-laws (codex), branch codex/item-145-fixture-laws.** Put the three unreferenced sample fixtures to work, and close the mixed-script gap they expose. **Evidence:** `samples/image-reveal.md`, `samples/mixed-cjk.md`, and `samples/wide-table.md` are tracked but referenced by zero tests. Each was added alongside the feature it demonstrates (`136fdd0b` images v2, `12fc8ee2` the Chinese round, `77071c3a` tables) and each states its own law in its prose — "the row height never changes, so no line dances"; "the grid must allocate column widths without ever breaking a word mid-word". **The real gap:** every i18n capture test loads a SINGLE-script document (`samples/japanese.md`, `chinese.md`, `korean.md`). Nothing loads a document containing two CJK scripts at once. `samples/mixed-cjk.md` declares `lang: zh-Hans` in frontmatter and then mixes Simplified Chinese with Japanese hiragana and katakana — and `src/script.rs:45` records that Han is "ambiguous on its own among ja / zh-Hans / zh-Hant / ko", so per-script face resolution in that document depends on a declared lang that covers only one of the scripts present. Han unification means a wrong resolution renders Chinese glyphs in Japanese forms: instantly visible to a native reader, and invisible to the never-tofu law, which only proves a glyph resolved to something. **Build:** Add the mixed-script case to `src/capture/tests/i18n_fixtures.rs`, asserting the resolved face PER SCRIPT RUN within the one document rather than one face for the buffer. Wire `image-reveal.md` and `wide-table.md` into the existing image and table capture tests as additional realistic corpora — their laws are already covered by inline fixtures (`render/tests/images.rs:116-125`, `render/tests/tables.rs:11`), so this is defence in depth on two committed-direction features, not new law. **Scope:** Do not restate laws that already exist; add the corpus to the sweep the law already performs. If a fixture turns out to prove nothing the inline one doesn't, say so and delete it instead of keeping a decorative test. **Done:** No tracked fixture is unreferenced, and mixed-script face resolution has a named law. **Verify:** Prove the mixed-script law non-vacuously — force both scripts to one face and watch it go red. Existing i18n, image, and table captures stay byte-identical. Code health, both native conventions, wasm. **User decision 2026-07-28.**

147. 🟡 **IN PROGRESS — item-147-hit-test (codex), branch codex/item-147-live-hit-test.** The click/drag-selection suite tests a code path no live click can reach. **Evidence:** `App::hit_test_line_col` (`src/app/input/mouse.rs:11`) forks on GPU presence — `Some(gpu)` runs `gpu.pipeline.hit_test_scroll(...)` (per-glyph advances), `None` runs `render::hit_test` (`render/geometry.rs:60`), a fixed-pitch `col = round(rel_x / metrics.char_width)` over `CHAR_WIDTH = 14.4` (`render.rs:130`). `App::window_event` (`src/app.rs:1745`) has an unconditional `if self.gpu.is_none() { return; }` BEFORE the `match event` that dispatches `CursorMoved`/`MouseInput`, so a live click can only reach `on_press` with a GPU present. **The `None` branch is therefore unreachable in production and exists only for tests** — and all 24 tests in `src/app/input/tests.rs` build via `App::new_hermetic`, so the entire click/drag-selection suite exercises it exclusively. **Two docs assert the opposite and are wrong:** `app/input/tests.rs:19` — "so this drives the exact same math a real click does" — and `render/geometry.rs:57` — "Mirrors EXACTLY the layout math used to place glyphs + the caret, so a click lands on the right glyph." Both hold only for a monospace face at exactly the 0.6-em pitch; awl ships seventeen proportional worlds. Measured on a proportional world, per-character advances run ~6.0px (`i`), ~21.0px (`m`), ~18.4px (`w`) against a uniform 14.4px assumption, misplacing the resolved column by one to two columns. **Consequence:** `hit_test_scroll` — the hit-testing every real mouse click runs — has zero unit coverage, while a green suite reports click behavior verified. **Build:** Decide the fallback's fate first, and state the decision. Either (a) delete `render::hit_test`'s fixed-pitch path as dead production code and move the click tests to a seam where a real pipeline exists — `src/test_gpu.rs`'s shared device and `render/tests`' `headless_pipeline()` now make that cheap, and `hit_test_scroll` is a `TextPipeline` method reachable there — or (b) keep it for a declared reason and correct both doc claims. (a) is preferred: a test-only shadow of production geometry is the exact anti-pattern the Toggle and shared-view-policy consolidations exist to retire. **Done:** The hit-testing a user's click actually runs is covered by tests, and no doc claims equivalence that is not true. **Verify:** Cover `hit_test_scroll` against a proportional world where the two formulas provably disagree — a mono-only test would pass under either implementation and prove nothing. Prove non-vacuity by perturbing the advance arithmetic and watching the new tests go red. Existing selection/drag behavior unchanged; code health, both native conventions, wasm. **Audit finding 2026-07-28, reproduced.**

148. 🟡 **IN PROGRESS — item-148-replay-trace (codex), branch codex/item-148-replay-trace.** A skipped `--keys` effect must leave a trace in the sidecar, not only on stderr. **Evidence, reproduced:** `awl --screenshot out.png --root DIR --keys "s-p m o v e Enter Enter" DIR/note.md` prints to STDERR `--keys replay: skipped unsupported effect \`overlay_accept\` from action Newline — the note move (mkdir + rename) is live-App-only`, and the file is never moved. The sidecar reports `overlay.active: false`, `notice: ""`, and no error field — **byte-identical in shape to a successful accept**. Rename shows the same pattern (`rename_note_commit`), and `src/replay.rs`'s `accept_class`/`effect_class` indicate `Effect::DuplicateNote` and `Effect::SettingPathPick` share it. **Why this matters:** CAPTURE.md teaches the sidecar as the state oracle, so the natural verification of "did Move… work" is to read `overlay.active == false` — which yields a false pass. The two available tells are a stderr line that sidecar-driven verification never reads, and the absence of a change in `buffers.active` — a negative-space check nobody is prompted to make. `--strict-replay` handles this correctly (aborts, exit 1, writes nothing); the gap is specifically that the PERMISSIVE default path's positive artifacts record nothing. **Build:** Record skipped effects in the sidecar — a list naming each skipped effect and its originating action. Keep the permissive path permissive: it exists so a capture can drive past a live-only step, and turning every skip into an abort would duplicate `--strict-replay` and break existing captures. The change is that the artifact stops being silent. Update CAPTURE.md's field table. **Scope:** Do not widen replay's effect support in this item; the point is honest reporting of what was skipped, not making live-only effects work headlessly. **Done:** No capture can silently report a clean state for an operation that did not happen; a reader of the sidecar alone can tell. **Verify:** Assert the new field is populated for each of the four known live-only effects, and empty for a capture that skips nothing — a test only of the populated case would pass against an implementation that always reports a skip. Confirm `--strict-replay` still aborts. Code health, both native conventions, wasm. **Audit finding 2026-07-28, reproduced.**

155. 🟡 **IN PROGRESS — item-155-tranche-a-layers (codex), branch codex/item-155-layers-split.** Grind the oversized production files down in ranked tranches. **User decision 2026-07-28, overriding a recommendation to skip it.** 86 production files exceed 500 lines / 84,180 lines; item 143's test relocation clears 25, leaving **61 files holding 55,209 lines of real logic**. **Sequence: 143, then 144, then this** — no split belongs in a file that is mostly tests, and until 144 lands, shrinking a file earns regrowth slack instead of banking the saving. **EXCLUDED, do not rediscover as candidates:** `render.rs` (the declared GPU floor in CLAUDE.md); `theme/worlds.rs` (measured: **1 function across 1,113 lines** of const data — splitting is churn); `render/geometry.rs` (held while 150/152's work settles). **Tranche A, ranked by logic density:** `render/layers.rs` (2,523 logic/38 fns), `app.rs` (2,131/42), `render/rects.rs` (1,863/58), `render/text.rs` (1,440/38), `main/args.rs` (1,399/23), `render/spans.rs` (1,279/38), `app/apply.rs` (1,259/28), `render/caret.rs` (1,244/48). **Tranche B, after 143 strips their test bulk:** `main/run.rs`, `keymap.rs`, `render/chrome/mod.rs`, `app/input/mouse.rs`, `markdown/spans.rs`, `actions.rs`, `render/framebench.rs`, `capture/sidecar.rs`, `buffer.rs`. **Binding constraint:** `buffer.rs`, `actions.rs` and `markdown/spans.rs` sit on the editing path, where a split is a MOVE — no rule merged, no branch dropped, no edge case tidied. If a seam is not obvious, leave the file long and declare it. **Known traps** (hit at `02208419`): a relocated item breaks the path-keyed `EXPECTED` tables in `println_audit.rs` and `durable.rs`, and a Clippy exception pinned to `file:line` must be repointed, not deleted. **Verify per branch:** exact before/after test counts, no assertion text altered, capture pixel identity where a render owner moved, full gates. One file per branch. Stop and report rather than forcing a seam that is not there.

156. 🟡 **IN PROGRESS — item-156-release-gate (codex), branch codex/item-156-release-profile-gate.** Close the release-profile blind spot, then take the two lint jobs that are actually worth doing. **Why, measured:** every gate builds the DEV profile — 3129 tests, both conventions, wasm, code health — so anything that differs between debug and release is invisible by construction. Four action families dispatched from inside `debug_assert!` and were therefore absent from every release build; the editor shipped with no typing, motion, formatting or overlay commands, and all gates stayed green. A user found it. **(1) The gate — do this first, it is the actual hole.** Confirmed by experiment: a RELEASE headless capture reproduces it (`--keys "h e l l o"` yields `text: ""` with the defect planted, `"hello"` without). No window, no GPU display, deterministic, parallel-safe. Build it as a DIFFERENTIAL law, not hand-written assertions: run the same `--keys` captures under debug and release and require the sidecars to be IDENTICAL. That covers the whole class — `debug_assert!` side effects, overflow-check differences, optimization-sensitive paths — rather than only the failure someone imagined. Size it to one capture per `ActionFamily` (Buffer, Viewport, Format, Overlay, Session, View, Align, Export), which is exactly the blast radius. Cost: a warm release rebuild is ~17s plus ~1s per capture. **Run it at the push train in the main tree, not per-landing** — a worktree's first release build compiles every dependency at `opt-level = 3`, which is minutes, and pre-tag. Do NOT put the live-window probe in the routine gate: it needs a foregrounded window, cannot run in parallel, and inherits the occlusion tripwire; reserve it for pre-tag and live-only reports. **(2) Five zero-hit lints, free to deny now:** `mem_forget`, `dbg_macro`, `todo`, `unimplemented`, `mutable_key_type`. Each has zero hits, so denying costs nothing and only stops one arriving. (`debug_assert_with_mut_call` is already denied and was verified to error on the replanted defect.) **(3) `assertions_on_result_states`, 33 hits, all `assert!(x.is_err())` in test files** — mechanical, zero behaviour risk, better failure messages. Real but small; do not sequence it ahead of (1). **(4) The three PRODUCTION `cognitive_complexity` exceptions** (13 total, 10 of them in tests). Complexity is where defects live. **EXPLICITLY OUT, do not rediscover:** `float_cmp` (373 hits — every sampled site is correct as written: zoom change-detection, `w == adv` monospace detection, `max == r` in RGB→HSV where `max` was computed as one of r/g/b, and the `edge0 == edge1` smoothstep divide-guard; an epsilon breaks all four, and 373 `#[allow]`s buy nothing), and the 99 `too_many_lines` exceptions (40 of them in tests; length is not a defect and item 155 already owns the files worth splitting). **Consider but decide deliberately:** pinning `rust-toolchain.toml` so local and CI cannot drift again, versus keeping both on floating stable and relying on the new `rustup check` rule. **Verify:** prove the differential gate non-vacuous by planting the `debug_assert!` shape and watching it go red; confirm it passes with the fix. Full gates.

## Timed — not blocked

**Final product task before release preparation:** item 24. Write the welcome/tutorial
only after the editor, worlds, Settings, and taught interactions have settled.

24. **Make the file opened on first launch serve as both Awl’s welcome and its tutorial, then refresh the surrounding user-facing docs.** **Build:** Write one real Markdown file that opens as the first-run document and teaches Awl by being edited in Awl. It welcomes the user, establishes that Awl creates ordinary Markdown files, and introduces the essential writing experience through the document itself: WYSIWYG reveal at the caret, native keyboard shortcuts, the command palette, pointer-accessible contextual formatting, changing worlds, and where files are saved. Keep it useful as readable prose rather than turning it into a feature checklist or a sequence of modal lessons. No separate onboarding wizard, coach marks, account step, or second tour surface. Returning users resume their own work rather than seeing the welcome again. Update GUIDE and the site guide to agree with the finished product and this first-run path; matter-of-fact voice, facts verified. **Done:** A new user opens Awl directly into a beautiful editable document, understands what the editor is and how to begin, and can dismiss, replace, or save the file without special state leaking into later sessions. **Verify:** Fresh-profile first launch, second launch/session restore, edit/save/replace/close behavior, every taught chord through the real keymap on supported platforms, links between welcome/GUIDE/site claims and implemented behavior, and representative first-run captures. Site deployment remains separately user-gated. **Timed after the current user-visible wave and before release preparation; user decision 2026-07-29.**

118. **Pre-release world-loudness audit — repeat the 1–5 idle Room/Frame check before release.** **Audit definition:** “idle loudness” is how strongly a world asks for attention while the user is simply writing in page mode: palette, typography, margin pattern, and ambient motion count; summoned overlays do not. `1/5` is the quiet pole (Wagtail), `3/5` is recognizable/alive but comfortable for hours, and `5/5` is a deliberately rare statement world (Firetail). **Baseline from the 2026-07-26 design session, eighteen shipping worlds:** `1/5: 1`, `2/5: 10`, `3/5: 2`, `4/5: 4`, `5/5: 1` (mean 2.67). Bowerbird item 117 intentionally changes that to `1, 9, 3, 4, 1` (mean 2.72). **Direction for a twenty-world roster:** hover around 3 with awl’s calm bias; the provisional healthy shape is `1, 7, 7, 4, 1` (mean 2.85), not a symmetric theme-park bell curve. The current gap is the middle, not more 5s. This is a diagnostic distribution, never permission to turn up a world merely to fill a bin—each world still earns its own identity. Bilby/Brolga/Tawny/Saltpan are valuable quiet anchors; Galah and Mulga are candidates to inspect, not pre-decided promotions. **Run:** Immediately before release preparation, review every shipping world’s current Room in item 20’s dashboard at representative page widths, then observe every ambient world live in `--release`; independently assign 1–5 before looking at the baseline, reconcile the roster together, and record any chosen changes as separate concrete queue items. Include any nineteenth/twentieth worlds that have graduated by then; do not graduate Cassowary Light or any other candidate merely to hit twenty. **Done:** The final roster has a user-confirmed loudness map, its mean/distribution and outliers are explicit, near-duplicate intensity poles are named, and every proposed rebalance is either rejected on purpose or queued with a world-specific reason. **Verify:** Affordance-locating vision smoke over all Room captures; live-only confirmation of speed/calmness for Lava, Currawong stars, Bombora waves, Bowerbird cutouts, and any later ambient world. Pixel/sidecar arithmetic may prove territory and contrast but never claims the taste score. **Timed before release preparation; user design decision 2026-07-26.**

## Parked — explicit gate or future design

- **Export save-dialog scope:** macOS + Linux, one live-only cross-platform seam; capture uses an explicit path. Decided, not scheduled.
- **Per-world living-band choreography:** audition TwoShape/Slam/Soft against Morph; live feel is the oracle. Needs a design session.
- **Per-world copy-pulse differentiation:** possible future motion tweak; needs a design session.
- **Site deployment:** only on the user’s explicit word.

## Monitoring — non-blocking

- **Hands-on checks still useful:** Dawn/Bilby world feel; writer-diff panel/Tab + zoom readout; phantom image resize handle; upward scrolling past images in release; right-click Add-to-dictionary summon; 2px Wagtail stipple taste.
- **Live-only follow-up from item 151 (geometry is law-tested; feel is not):** how clicking actually *feels* over real Thai, Devanagari and Hebrew text with a mouse, and whether the tie-goes-forward choice (a click exactly mid-cluster resolves to its END) reads right in the hand. The harness proves the caret never rests inside a cluster; it cannot prove that landing on the far side is the side a writer wanted.
- **Live-only follow-ups from the 76–88 wave (harness can't drive them):** 80 — Find/Replace scroll smoothness over real typing; 81 — heading-chevron mouse-press→toggle wiring (no GPU App in unit tests; geometry+toggle proven headlessly); 85 — theme-picker felt input→present lag (latency-probe ms numbers are live-only); 87 — Bombora drift speed / counter-motion / calmness over real seconds.
- **GPU memory:** no action unless the 6 GB symptom recurs; then probe the live surface with the window foregrounded.

## Release blockers and reminders

- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user’s explicit word. A dry run may precede them.
