# awl — live build queue

> Live execution state only. Completed and superseded work is in git history
> (`git log -p .orchestrator/queue.md`). Protocol, claiming, worktrees, and
> execution hygiene live in `.orchestrator/README.md`.

## Ready — current user-visible wave

113. **Run the final live release verification for the integrated theme-switch flight recorder (`1f2e89d`).** The implementation and automated gates are complete: latest and worst completed theme-switch transactions remain available for five seconds with phase breakdowns; frame cost history stays independent; Debug-off clears through the App-owned reset seam; headless remains numberless; fake-clock, long-window, zero-redraw, capture, mutation, full native integration (3,070 passed, 11 ignored), and wasm 16/16 are green. **Run:** In an active unlocked macOS session, compare `--live-script` events with flight-recorder timestamps and inspect onscreen readability in `--release`. This is verification only; diagnose or repair only if the live result fails. **Done:** Timing correspondence and readability are recorded, or a concrete defect is returned to the queue. **Routing:** live verification=`gpt-5.6-terra` medium; any repair=`gpt-5.6-terra` medium; final integration=root only if a repair is needed.

114. **Build Settings as the first real consumer of the shared summoned workspace.** **Build:** Replace the standalone Settings overlay with one App-owned workspace opened normally through `Settings…`. It relocates attention while active, keeps the document as a quiet backdrop, and returns with `Esc` to the exact editor state. Give it search, the category rail (`Editor / Appearance / Writing / Files / Keybindings / Advanced`), current values and existing controls, wide two-pane and narrow staged layouts, coherent focus/back behavior, and Cmd-P deep-links to the relevant category or row. Preserve every setting’s live apply, persistence, range, exact-entry, toggle, and sub-picker behavior. Direct Theme and Caret audition remain the existing fast editor-backed overlays: suspend Settings, run the real picker, then return to the preserved Settings position. Remove the old Settings presentation and do not land an empty workspace shell, developer-only entrance, dormant alternate path, generic route stack, tab system, or persistent sidebar. The shared workspace is deliberately scoped to Settings and Version History. Amend DESIGN/SCOPE with the rule that overlays split attention for brief contextual choices while summoned workspaces relocate it for sustained work. **Done:** Settings is one coherent, normally accessible place at wide and narrow sizes; no parallel Settings UI survives; underlying buffer/view state is unchanged on exit; Theme remains as quick and distinctive as before; the shell is ready for item 116 without feature-specific branches. **Verify:** State/focus/back laws; every `SettingId × SettingKind` through direct Settings, Cmd-P, keyboard, and pointer; category/search preservation across child pickers; exact Theme commit/revert parity; narrow/wide, zoom, DPI, drawn↔hit-test, theme-roster and Wagtail laws; deterministic capture/sidecar and dashboard vision smoke; native, both conventions, wasm. **Unclaimed; user decision 2026-07-29.**

116. **Move Version History into the shared summoned workspace — timeline and prose diff become one readable task, never three competing layers.** **Build:** Preserve the existing history store, git backend, pruning, facets, descriptions, kept versions, and prose-diff engine, but replace the current History overlay/diff-as-preview composition with item 114’s workspace. On wide windows, show a narrow timeline/navigation pane beside a large read-only comparison pane; moving through versions updates the comparison immediately. On narrow windows, show the timeline first and enter the comparison as a second stage with an explicit return path. The current editor is backdrop/state, not a third readable layer. **Scope:** Keep local loose-file snapshots and git-managed history behind the same UI with only a quiet source label. Preserve independent diff scrolling and a clear focus transfer between timeline and comparison. `Esc` leaves the current buffer byte-for-byte unchanged; restore must be a deliberate, footer-taught action rather than bare `Enter`, and remains undoable. `Version history…` and `Compare with version…` deep-link into the same workspace at the appropriate focus; `Keep version…` retains its brief naming prompt and returns coherently. Remove the old History overlay, document-under-card preview composition, and feature-specific diff-panel dressing only when their last consumer is gone—retain the generic prose-diff machinery and do not strand parallel disabled paths. **Done:** A user can answer “which version, what changed, and do I want it back?” without overlapping titles, hidden prose, or ambiguity about whether the editor is active; the same flow works for local and git history; exiting is a true no-op and restoring is deliberate and undoable. **Verify:** Timeline→live comparison→focus/scroll→back/exit/restore journeys for local snapshots, named/pinned versions, git commits, empty history, renamed files, and pruned ladders; narrow/wide/zoom/DPI captures across representative light/dark, Pane/Bars, and Wagtail worlds; pixel laws proving timeline and comparison never overlap and the original document does not remain a competing readable layer; restore undo law; capture/replay parity; dashboard vision smoke; native, both conventions, and wasm gates. **Depends on the completed item 114 Settings workspace; unclaimed; user design decision 2026-07-26.**

120. **Land the approved twelve-body Lava field on Firetail + Mangrove.** **Build:** Promote trial arm B as the sole production implementation: keep the shared viewport-relative Firetail/Mangrove field, raise population from eight to twelve, use the approved wider large/medium/satellite hierarchy with average radius about 10% below the old field, expose a healthier population in both margins, firm the silhouette slightly, and reduce horizontal sway so motion reads mainly vertical. Remove the temporary selector, the A control arm, and all disposable review machinery. Do not add buoyancy physics, responsive population, world-space tiling, or fluid simulation. Palettes, page width, ambient cadence/gates, frost, and one-accent/value-band laws remain unchanged. **Done:** Both worlds ship only the user-approved B composition; no further taste decision is required. **Verify:** fixed-phase contribution and coverage across short/tall, narrow/wide, page widths and 1×/2×; all twelve bodies contribute somewhere; neither margin becomes empty or fully covered; existing ground-band, hue-clear, page mask, frost, seamless loop, delayed-wake, Reduce Motion, ambient-off, determinism and release-perf laws; native and wasm. **User choice 2026-07-29.** ✅ **LANDED — `f7d35425`** (`e271dddb`, `4b7ced3e`, `f999fcdd`). Selector and A arm gone, verified by grep across `.rs`/`.wgsl`/`.md`/`.toml`. Gates on the candidate: 3149 passed under both conventions, wasm L1+L2, code health clean, captures at 1200×800 / 700×1200 / 2560×1080 / 2×.

**Surprise worth keeping:** `theme::tests::gutter_frost_pill_keeps_ink_contrast_on_every_lava_world` always read `BACKDROP_BLOBS` directly, so through the whole trial it exercised the eight-body control and never once touched the twelve-body data it was meant to guard — a law green over the exact thing it was named for. Promoting exposed that the approved data left the bottom-left frost pocket dead (raw field peaked 0.39, under the 0.38 flat-ground cutoff; blur+dim then washed it out).

⚠️ **OPEN — needs a live human look, not settled.** The frost repair moved ONE satellite's `cy` from 0.67 to 0.75 (radius, weight and every other body untouched, confirmed against the real `frost_field`/`frost_pixel` path). That is a deliberate divergence from the composition the user approved in the trial captures: the approved arm B rendered with `cy` 0.67. Two live questions, neither harness-provable: whether the reduced horizontal sway genuinely reads as mainly-vertical drift over real seconds, and whether the moved satellite looks natural rather than engineered. If the user rejects the new placement, the frost pocket needs a different repair — the defect is real and reverting `cy` alone reopens it.

121. **Make Firetail’s shipped icon carry the world’s wine-pink identity, and change its fake logo cursor from Pill to Block without falsifying its gold caret.** **Build:** After item 110 lands, change Firetail’s theme-owned `IconCursor` assignment from `Pill` to the existing shared `Block` preset, then audition the icon tile ground at three exact states: **A** current `base_100`; **B** 25% from `base_100` toward Firetail `base_300`; **C** 40% toward `base_300`. Keep the `aw` wordmark in the real `base_content`, the Block cursor in the real gold `primary`, and its knocked-out `l` in the real `primary_content`; Firetail’s actual world palette and live caret remain byte-identical. The problem is that the shipped icon currently samples only Firetail’s darkest oxblood plane, so it reads near-black/cream/gold and omits the pink-wine identity carried by `base_300`, lava `blob_hi`, selection, and muted ink; the current Pill also is no longer the chosen icon silhouette. **Mechanism:** The cursor change uses the existing theme-owned preset assignment, not a Firetail branch. Represent any chosen non-default ground as theme-owned icon data/capability with `base_100` as the inert default for every world; never add a Firetail-name branch and never give the icon cursor a color different from the actual caret. No lava illustration or extra background motif in this round—the Block silhouette plus the large tile plane get the first chance to solve the identity problem cheaply. **Done:** At 256/128/64/44/32/24 px on both light and dark Dock surfaces, Firetail uses the Block logo cursor, immediately reads wine-pink plus ember-gold, remains recognizably the same world as the live Room, and does not collapse toward Potoroo’s rust or Bombora’s violet/rose identity. The user chooses A/B/C from one Block-cursor comparison sheet before any shipping asset changes land. **Verify:** Regenerate only through the canonical exporter/packer; assert Firetail alone changes preset membership from Pill to Block and every other world’s assignment remains unchanged; run the full 18-world icon distance/crowding/stale-entry laws from items 102/109, with Firetail’s empirical nearest pairs named; assert icon cursor/knockout colors still equal Firetail `primary`/`primary_content`; perform `l` containment, small-size legibility, and dark/light Dock vision smoke; rebuild item 20’s dashboard; native + wasm gates. **Depends on item 110 because both touch the exporter and generated icon assets; unclaimed; user design decision updated 2026-07-27.**

131. **Give Mangrove and Magpie mirrored diagonal-line compositions across contextual menus and the real Settings workspace.** **Build:** Add one reusable theme-owned diagonal row composition through the shared rowlayout/surface machinery, then assign its two authored orientations: **Mangrove** draws a continuous descending `\` spine, with row clusters left-aligned on the RIGHT side; **Magpie** draws a continuous ascending `/` spine, with row clusters right-aligned on the LEFT side. The line is mandatory in both—the striking read comes from the drawn division and triangular negative space, not merely staggered text. It may visually bleed toward the surface corners, while row attachment points occupy an inset middle band so the first/last rows retain usable width. **Line treatment:** never amber/primary. Mangrove uses a crisp tidal-teal line derived from its muted ink; Magpie uses a crisp graphite line from its muted ink. Resting weight is clearly visible but subordinate to text; the selected row brightens and thickens only the local spine segment toward `base_content`, extends a short connector to the row, and steps the row outward by a few crisp pixels—no spring, pulse, or full-width selection bar. Existing bottom-left Mangrove stipple and bottom-right Magpie ghost placards occupy the opposite empty triangle rather than colliding with the rows. **Controls are in scope, not a fallback:** model each row as a measured `label + fixed gap + accessory/control` cluster. Reserve consistent label/accessory extents across the visible set so shortcuts, values, toggles, checkmarks, exact-entry fields, and Range sliders trace a stable parallel rail with honest spacing; anchor the whole cluster to the spine instead of independently nudging text. Query/title/category navigation/footer regions remain horizontal and stable—the diagonal owns the candidate/setting rows, not every glyph on the surface. Filtering and scrolling sample a fixed surface-relative line at fixed row y positions, so content changes never make the spine or surviving rows jump horizontally. **Surface reach:** enroll every contextual overlay’s row section that currently consumes theme list-style data, not Commands alone; non-row panels keep their existing geometry. After item 115 removes the old Settings overlay, apply the SAME diagonal owner to the Settings workspace’s main setting list for Mangrove/Magpie, while its category rail, search/title shell, child Theme/Caret auditions, and narrow-stage navigation remain item 115’s workspace behavior. The empty main-pane triangle may carry the existing active category/title typography, but never a duplicate decorative label. **Responsive bound:** size the slope from the real widest visible cluster and available side territory; widen within the existing surface limit first, then reduce horizontal travel while preserving a visibly diagonal direction. Never overlap, clip, shrink type/controls, introduce horizontal scrolling, or silently fall back to Pane/Bars. **Scope:** This is a third data-driven row composition, not Mangrove/Magpie branches and not permission to diagonalize the document, workspace shell, History comparison, native Mac menu, or web/Linux persistent menu bar. Other worlds and their Pane/Bars results remain byte-identical. Item 112 owns the shared overlay rhythm first; items 114/115 own the workspace and Settings migration first. **Done:** Mangrove reads like a tidal instrument panel and Magpie like an editorial spread in both brief menus and sustained Settings, with a visible line, deliberate negative space, readable controls, obvious selection, and no loss of keyboard/pointer usability at any supported width. **Verify:** Full no-wildcard `OverlayKind` row-surface sweep plus every `SettingId × SettingKind`; simple/long labels, chords, values, toggles, text entry, sliders, empty/short/full/filtered/scrolled lists, category changes, child-picker return, and narrow/wide staging; drawn line/row/control ↔ hit-test agreement at zoom and 1×/2× DPI; pixel laws for orientation, line continuity, inset attachment band, fixed label-control gap, local selected segment, placard/row non-overlap, non-primary ink, and no clipping; exact before/after identity for every non-assigned world; dashboard captures and affordance-locating vision smoke over Commands plus every Settings category in both worlds; native, both conventions, and wasm gates. **Depends on 112 + 114 + 115; unclaimed; ambitious user design decision 2026-07-27.**

132. **Add Kite — a loud light world travelling through a peripheral warped-grid tunnel.** **Identity:** Kite is the nineteenth world and the deliberate light `5/5` counterpart to Firetail: cool rather than warm, geometric rather than organic, crisp rather than liquid, and directional rather than drifting. Use a near-white mineral/pale-lavender ground, indigo/graphite grid and deep cool prose ink, with a single hot vermilion caret that reads like the bird's red eye. Its authored font must be bundled, licensed, distinct from the existing roster, and reported through the normal asset-size/licensing audit; do not choose one merely to satisfy the concept. **Background:** Add one data-driven ambient warped-grid capability shared through the existing renderer, not a Kite/world branch or a literal 3D engine. The writing page stays flat, opaque, motionless, and fully legible. The grid lives in the Frame/margins as two intentionally composed slices of one curved tunnel: distort the useful curvature outward so the opaque central page does not hide the vanishing point or all of the top/bottom bend. The left and right margins must communicate one journey—during a left turn the left grid compresses while the right opens; climbing/descending produces coherent opposing vertical flow—rather than looking like two unrelated animations. Fade/quiet the grid near the page edge and progressively simplify it at narrow margins to a gently translating warped field; never squeeze a tiny illegible tunnel behind the page or draw through prose. Use anti-aliased major/minor line hierarchy, with every fifth line stronger and minor lines fading before projected spacing can create Retina/WebGL moiré. **Motion:** The camera travels continuously forward. Roughly once a minute it eases into a new long direction—straight, left/right bends, climbs, and descents—without a cut, jump, hard reversal, obvious one-minute restart, roll, or attention-grabbing acceleration. A much longer deterministic route may eventually repeat invisibly; the visible contract is uninterrupted travel, not a short looping clip. Keep speed calm enough for writing even though the world is visually loud. Losing focus pauses in place and resumes without catch-up; ambient motion off and Reduce Motion freeze to one composed still; headless capture has a deterministic settled pose. **Scope:** This is an explicit statement-world exception, not permission to increase motion elsewhere. Preserve the one-accent law, page geometry, adaptive column, overlay readability, zero-network rule, and theme-as-data architecture. Measure release-mode GPU/frame cost on native Metal, wasm WebGPU, and WebGL fallback; reduce grid density/cadence through the shared capability before compromising text or inventing a platform-specific look. **Done:** On a wide Retina display, Kite unmistakably feels like the stable writing surface is gliding and gradually steering through curved space, with the decisive geometry visible in the margins despite the central page; on a narrow display it remains recognizably Kite without shimmer or clutter. It is light, technical, pale-purple, and `5/5`, balancing Firetail without resembling it. **Verify:** Add roster/no-wildcard enrollment laws and exact non-assigned-world identity; deterministic wide/narrow captures for straight, left/right, climb/descent, paused, ambient-off, and Reduce Motion states; pixel arithmetic for page exclusion, edge fade, cross-margin directional coherence, line hierarchy, contrast, on-canvas bounds, and no high-frequency minor-line aliasing at 1×/2× and zoom; release benches on native/WebGPU/WebGL; dashboard Room/Frame/icon enrollment; affordance-locating vision smoke and a live multi-minute human review for speed, steering legibility, seamless transitions, peripheral comfort, and motion sensitivity. Harness results must not claim the live motion feels calm. **Unclaimed; user design decision 2026-07-27.**

158. **Graduate Paperbark with Deckled Strata, and delete the rejected material trial arms.** **Build:** Turn item 133’s B profile into Paperbark’s one production background: nested deckled handmade-paper contours gather around the writing page in the provisional cream/pale-honey palette, with deep bark-brown prose and the coral/vermilion caret kept as the sole accent. Preserve the quiet static treatment the user approved; do not add raking light, motion, stock texture, or a second material mode in this item. Remove A Broad Sheets, C Loose Fibres, D Relief Print, E Peeling Curls, and the temporary trial selector from the Paperbark branch. C is not discarded: item 159 gives the reusable mechanism a different theme-owned assignment. **Scope:** Enroll Paperbark normally as a candidate world only after font licensing, roster distinctness, icon, dashboard, and zero-network requirements are satisfied; do not ship disposable review machinery. **Done:** Paperbark has one coherent material identity rather than five dormant treatments, reads as deckled paper around a calm opaque writing page, and does not collapse toward Saltpan’s regular pinstripes or Bilby’s quiet gradient. **Verify:** wide/narrow/short/tall and 1×/2× captures; page exclusion, contour continuity, contrast and no-moiré pixel laws; exact rejection-arm absence; world/icon/dashboard roster enrollment; nearest-world vision smoke; native, both conventions, wasm. **User choice 2026-07-29; unclaimed.**

**Orchestrator findings 2026-07-29 — read before starting.** Paperbark is not a world yet. `src/paperbark_trial.rs` on `codex/item-133-paperbark-trial` (`b7c91c41`) deliberately keeps it out of `theme::THEMES`: it is a provisional `PAPERBARK_THEME` value plus a `shaders/background.wgsl` selector behind `AWL_PAPERBARK_TRIAL`, inert without that variable. So this item creates the twentieth world outright — roster entry, icon, dashboard, laws — not merely a background swap. The trial arms never reached main; build from the trial branch rather than deleting around it. **Display face: EB Garamond**, chosen by the orchestrator under the user's 2026-07-29 instruction to pick an already-bundled face and not block. It is already bundled and SIL OFL 1.1, so no new asset weight and no licensing gate; the trial theme already pairs Paperbark with `ORNAMENT_GARAMOND`; and its only current owner, Bombora, is a dark world, so it cannot pull Paperbark toward the light-world neighbours the Done clause names. **Fraunces was rejected despite being what the approved B captures rendered with**, because it is Saltpan's face and Saltpan is the exact world this item must not collapse toward — sharing a face there doubles the collapse risk rather than diluting it. Face-sharing itself is established precedent (Bowerbird/Brolga, Currawong/Cassowary, Potoroo/Firetail). The pick is provisional pending the user's look at a real Room capture; see Release blockers. **This item owns the promotion of the shared background mechanism** from trial code to theme-owned data with an inert default; item 159 follows as its second assignment and proves reusability. **Icon generation depends on item 162** — the canonical exporter currently hangs, so the world's shipped icon and dashboard rebuild cannot be produced until that lands.

159. **Give Galah the Loose Fibres treatment as sparse pink plumage.** **Build:** Reuse item 133’s C mechanism through theme-owned background data and assign it to Galah, recolored entirely through Galah’s existing pink/mauve value ladder. Author the strands finer and sparser than the Paperbark trial so they read as loose feather filaments or plumage in the Frame, not cream handmade-paper fibres copied between worlds. Keep the writing page flat and untouched, the treatment fully static, and Galah’s Figtree/IBM Plex Mono typography, flower ornaments, Ghost placard, Poster Bars, and narrow berry caret intact. **Scope:** This promotes an existing quiet world from a pleasant flat gradient toward the missing 3/5 middle; it is not a new world, animation license, or permission to change Galah’s overlay personality. The reusable mechanism must have an inert default and no `Galah` name branch. **Done:** Galah is recognizable from its Room before an overlay appears, reads pink/plumaged rather than papery, remains comfortable for long writing, and stays distinct from Paperbark’s broad deckled strata, Bilby’s dawn gradient, and Brolga’s cool blue plane. **Verify:** full background-roster no-wildcard enrollment; exact non-assigned-world identity; density/strand-width sweep at wide/narrow/short/tall and 1×/2×; no page/Outline/gutter encroachment or aliasing; dashboard Room/Frame captures and affordance-locating vision smoke against Paperbark, Bilby, Brolga, and Saltpan; native, both conventions, wasm. **User choice 2026-07-29; unclaimed. Sequenced after item 158**, which promotes the shared background mechanism this item reuses; running them concurrently would have two owners writing the same mechanism.

160. **Replace the visually anonymous standard macOS About panel with a deliberate custom native Awl window.** **Build:** Preserve the native macOS window class and App-menu routing from item 128, but replace `orderFrontStandardAboutPanel…` with one focused, non-editor About window whose composition reaches the quality bar of the user’s Ghostty reference without copying its neon treatment. Give the current shipped Awl icon real scale, then `Awl`, one concise product line, version/build/commit facts, and `Frank Lu · GPL-3.0`; include restrained Docs and GitHub actions only as explicit user clicks. **Typography is deliberately only two levels:** normal body ink for the product/title, product line, author/license, and button labels; one shared greyish secondary ink for version/build/commit facts. Do not introduce a third size/weight/color tier, and remove the horizontal `---` divider entirely—the grouping comes from whitespace and rhythm. Centre the composition with generous spacing and make it authored in both light and dark system appearances. **Scope:** This is rare native identity chrome, not another in-app workspace, theme surface, account screen, updater, or runtime fetch. The panel never phones home; links merely open their fixed destinations after a click. Keep the lowercase `awl` CLI name, App-menu capitalization, bundle metadata, licensing facts, and non-macOS in-app About card intact. Do not fabricate copyright or build facts, and do not make the window imitate whichever world happens to be active. **Done:** About Awl looks like a considered product moment rather than AppKit’s generic default while remaining unmistakably macOS and spare; its visible type resolves into body and secondary grey only, with no divider. **Verify:** packaged light/dark screenshots; pixel/color-role assertion that all visible text belongs to exactly those two ink roles and no divider pixels survive; current icon/name/version/build/commit/license assertions; link destination and keyboard-dismissal tests; single-window reuse rather than duplicates; menu routing; native + wasm platform split. **User decision refined from live screenshot 2026-07-29.** 🟡 IN PROGRESS — claude, branch `claude/item-160-about-window`.

161. **Optically raise the shipped icon cursor lockup for Mopoke, Currawong, Bilby, and Saltpan.** **Defect:** In the repaired per-world dashboard, the fake logo cursor containing the `l` still sits visibly low relative to the `aw` wordmark in four worlds. Mopoke needs the clearest upward correction; Currawong needs only a very slight lift; Bilby and Saltpan need similarly restrained lifts. The remaining fourteen icons passed the user’s review. **Build:** Measure the cursor body, contained `l`, wordmark ink, cap/x-height and shared baseline at every native size, then express the optical correction through one data-owned icon alignment mechanism with an inert default—not four renderer branches and not a change to the live editor caret. Preserve each world’s assigned Block/Pill/Narrow silhouette, palette, font, baseline relationship inside the text run, and containment. Tune authored offsets from the large master, with size-aware rounding only where the canonical downsampler proves it necessary. **Done:** The four cursor bodies read vertically balanced with their wordmarks at 256/128/64/44/32/24 on light and dark Dock surfaces; Mopoke receives the stronger correction, the other three remain subtle, and every passing world is byte-identical. **Verify:** geometry report before/after; `l` containment and cursor/wordmark bbox laws; exact fourteen-world identity; full distance/crowding/stale-entry ratchets; canonical export/pack and item-20 dashboard rebuild; small-size affordance-locating vision smoke. **User review 2026-07-29; unclaimed.**

162. **Repair the pinned-Chromium screenshot exporter, then retry Kite and Firetail’s icon trial.** **Defect:** The canonical offline exporter hangs in `Page.captureScreenshot` before its first PNG, including an unchanged 32px control and a 16×16 run outside the sandbox. This blocks item 121’s Firetail comparison and item 132’s required Kite icon/dashboard proof even though Kite’s verified implementation commits (`673d2758`, no-slack repair `fc5337a9`) remain recoverable on isolated branches. **Build:** Reproduce the smallest hanging control, determine whether the failure belongs to the pinned browser/runtime, launch lifecycle, page readiness, GPU/backend, or capture call, and repair the canonical exporter rather than adding a one-off Kite path. Preserve the offline, deterministic, theme-derived pipeline; replacing the pinned browser is allowed only with versioned inputs and byte-stability proof. After the repair, regenerate the unchanged control first, then item 121’s A/B/C sheet and Kite’s full icon/dashboard pack. Rebase Kite’s verified branch onto current main, rerun its native/WebGPU/WebGL and visual gates, and retry integration; do not recover it by cherry-picking around failed proofs. **Done:** The exporter completes twice with identical assets, Firetail is reviewable, and Kite returns to main only with every required artifact and live motion review. **Verify:** named timeout diagnostics, orphan-process cleanup, two-run determinism, existing 18-world identity, full dashboard build, native + wasm. **User retry request 2026-07-29.** ✅ **EXPORTER REPAIRED — `472a8e6e`** (`9aba7832`, `e8cce5b5`, `eb37dfd6`). 121, 161 and 132 are unblocked. The Kite rebase/integration and Firetail's A/B/C sheet were deliberately NOT done in this dispatch and remain open under items 132 and 121.

**Surprise — the premise was wrong: the hang did not reproduce.** The unchanged exporter rendered all 592 PNGs in 35s, including the 32px control. Every candidate layer was eliminated with evidence (pinned browser, launch lifecycle, page readiness, GPU/backend, capture call), so the stalling layer cannot be named — the exporter was structurally incapable of recording it. That incapacity was the repo's, via two defects that each reproduce the reported signature: the browser's stderr was piped to a reader that never existed (the pinned Chromium writes 78 KB/8s when it has something to say, past the macOS pipe buffer — a browser blocked mid-write to stderr answers no CDP request again, `captureScreenshot` included, and the explanation is stuck in the unread pipe), and there was no timeout anywhere with `connect()` outside the `try/finally`, leaving unnamed headless browsers running. Thirteen abandoned `$TMPDIR` profiles corroborated the leak on disk. **The exporter can now no longer stall without naming the stage, and leaves no orphan process or profile.** Determinism: full export tree ×2 → `e6f0ca1b…` both runs; all 109 tracked assets and 18 `.icns` byte-identical after regenerate. Orchestrator verified independently that no Rust/Cargo file is touched (so the scripts-only gate set is legitimate, not a smaller tier hiding a failure) and that a full export left every tracked asset unmodified.

**Follow-ups found, not yet queued:** (a) macOS `ps -o command=` truncates before `--user-data-dir` and gives a false "0 orphans" reading — any orphan check needs `pgrep -f` + `ps -ww`; (b) ~2,594 `awl-test-config-sandbox-*` / `awl-verify101-*` directories in `$TMPDIR` indicate the Rust suite leaks temp dirs at scale — unrelated to this item and worth its own item alongside 165/166.

163. **Make Bowerbird’s slow organic drift perceptible without making it watchable.** **Defect:** Live user review reported “I literally don’t see any drift.” The shipped shader’s entire organic field moves only roughly 4–5px over its long ambient cycle, so the intended outcome—notice later that it moved—does not survive real viewing even though phase-motion laws pass. **Build:** First prove the live ambient scheduler advances Bowerbird under default settings and distinguishes a strength failure from Reduce Motion or `ambient_motion = false`. Then increase authored displacement and, only if necessary, give the layers subtly different trajectories while preserving the cut-paper composition, static page, cool value band, shared ambient clock, pause/freeze gates, no catch-up jump, and zero idle frames when gated. No bubbling, pulsing, spawning, dissolving, or Lava-like deformation. **Done:** A writer does not watch the collage travel, but a before/after glance over roughly a minute makes the change evident; the Room remains about 3/5. **Verify:** timestamped live release before/after, deterministic phase-delta pixel arithmetic, worst-phase value/hue/page bounds, seamless route, reduce-motion/ambient-off/focus gates, release perf, Bowerbird-neighbour vision smoke. **User-reported 2026-07-29.** 🟡 IN PROGRESS — claude, branch `claude/item-163-bowerbird-drift`.

164. **Unify overlay selection timing so pointer switching cannot highlight only a shortcut.** **Defect:** In the user’s live Command palette screenshot, the pointer is on “Switch project…” while the full selected band remains on “Go to file…” and only the second row’s shortcut has switched ink. The secondary column reads the new logical selection immediately while the living selected-row band and primary ink still display the previous animated row, producing two simultaneous answers to “which command is selected?” **Build:** Give the band, primary label, shortcut/value/accessory ink, hit-test acceptance and sidecar one shared visual-selection transaction throughout pointer hover and keyboard/wheel movement. If the band moves between rows, every selected visual either travels with it or waits coherently; never snap one column ahead. Preserve immediate click acceptance of the row under the press and the stationary-pointer/reanchor protections. Audit every overlay kind and both Pane/Bars families because the split owner is shared. **Done:** At every intermediate frame exactly one row reads selected, and the row activated by a click is the one the pointer indicates. **Verify:** reproduce the exact first→second command hover; deterministic intermediate-frame pixel law across label/band/shortcut; pointer, keyboard and wheel sweeps; every overlay/accessory kind; mutation proof by restoring immediate secondary recolor; native, both conventions, wasm. **User-reported screenshot 2026-07-29.** 🟡 IN PROGRESS — claude, branch `claude/item-164-selection-transaction`. Lands before item 114 — both own overlay row presentation.

165. **Share dependency build artifacts across worktrees with `sccache` as `RUSTC_WRAPPER`.** **Defect:** `[profile.dev.package."*"] opt-level = 3` buys the measured 531s→182s suite win but costs one optimized dependency rebuild per target dir, and every worktree pays it separately. On 2026-07-29 the fleet reached 70 GB across 49 worktrees — roughly 3 GB each, nearly all of it identical dependency output. `Cargo.toml` already names `sccache` as the intended escape hatch "if that bites"; it has never been installed or wired. It bit. **Build:** Install `sccache`, set it as `RUSTC_WRAPPER` where every worktree and CI inherits it, and bound the cache size explicitly. **Do not use a shared `CARGO_TARGET_DIR` instead:** cargo takes an exclusive lock on a target dir, which would serialize the concurrent worktree workers the fleet exists to enable, and 19 call sites across `capture.sh`, `smoke-menus.sh`, `package-macos.sh`, `bench.sh`, `capture-worlds.sh`, `release-profile-gate.sh`, and `live-probe.sh` hardcode `$ROOT/target/{debug,release}/awl`. **Done:** A fresh worktree's first build reuses dependency artifacts rather than rebuilding them, per-worktree `target/` shrinks materially, and no script, gate, or capture path changes. **Verify:** Cold-worktree build time and `target/` size before/after, with `sccache --show-stats` proving cache hits rather than inferring them; concurrent worktree builds still run in parallel; both conventions, wasm, and the capture harness still resolve their binaries. **Routing:** production tier. **User request 2026-07-29; unclaimed.**

166. **Make `scripts/sweep.sh` cover the worktrees the fleet actually uses.** **Defect:** `sweep.sh` runs `cargo sweep --recursive` over `$ROOT`, the repo root, and its header assumes worktrees live under `.claude/worktrees/` and `.agents/worktrees/` — which is why `--hidden` is required. But the fleet's worktrees are created in the SIBLING directory `<repo>-worktrees/`, outside `$ROOT` entirely, so the sweep has never once seen them. On 2026-07-29 it was structurally blind to 65 GB there while covering only the 8.9 GB under `.claude/worktrees/`; the disk reached 32 GB free and the fleet had to be reclaimed by hand. `cargo-sweep` is installed and the script exits 0, so the failure is silent — the same shape as the `--hidden` bug its own header warns about, one directory further out. **Build:** Derive the swept roots from `git worktree list` rather than assuming a layout, so every worktree is covered wherever the orchestrator puts it, and report reclaimed size per root. Keep the existing day-threshold and `DRY_RUN` interface. **Done:** One sweep reclaims stale artifacts from every registered worktree, and a worktree outside the repo root can no longer grow unbounded unnoticed. **Verify:** With a worktree deliberately outside `$ROOT`, assert its stale artifacts are swept and its fresh ones survive, and that the run names every root in `git worktree list`; prove non-vacuity by pointing the current script at the same tree and watching it reclaim nothing. **Routing:** production tier. **Found 2026-07-29 during a disk-full incident; unclaimed.**

167. **Make a live macOS Awl process retain product identity outside the packaged app.** **Defect:** A bare development launch is the lowercase `awl` executable rather than `Awl.app`. Although the packaged bundle already sets `CFBundleName`/`CFBundleDisplayName` to `Awl` and carries `Awl.icns`, AppKit has no bundle metadata on the bare path: the top-left application menu therefore says `awl`, and Stage Manager shows the generic application icon. The runtime `setApplicationIconImage` adoption is sufficient for the Dock/app switcher but does not supply a real bundle identity to every macOS surface. **Build:** Diagnose each surface against both bare and packaged `--release` launches, then establish one supported development-launch path that presents the same capitalized product name and canonical/current-world icon wherever macOS can honor them. Prefer launching a locally assembled `Awl.app` through the existing canonical packaging metadata over fragile process-title spoofing; keep the actual CLI command and `Contents/MacOS/awl` executable lowercase. Do not weaken per-world settled icon adoption, signing/release separation, or Finder’s canonical static icon. **Done:** The menu bar reads `Awl`, Stage Manager shows an Awl icon rather than the generic blueprint tile, and packaged release behavior remains unchanged; if macOS fundamentally requires a bundle for one surface, the normal dev/run script uses that bundle and the bare-binary limitation is explicit rather than silently advertised as equivalent. **Verify:** live screenshot matrix for bare diagnostic versus supported development bundle versus packaged build across menu bar, Stage Manager, Dock, and Cmd-Tab; assert bundle name/display name/executable/icon metadata and canonical icon presence; change-world commit still updates only the settled running icon; no hover churn; native packaging gate. **User-reported 2026-07-29.** 🟡 CLAIMED — claude, follow-on to item 160 in worktree `item-160-about`, same owner. Deliberately sequenced rather than parallel: both items touch macOS chrome and the packaging metadata, and the 160 owner already holds the AppKit/bundle context. **Orchestrator note:** the cause is confirmed, so the open question is only which surfaces macOS will honor without a bundle. Expect the answer to be "the menu bar and Stage Manager cannot be fixed on the bare binary" — in that case the deliverable is the supported `Awl.app` dev-launch path plus an explicit statement of the bare-binary limitation, NOT a process-title spoof. Do not weaken per-world settled icon adoption to get there.

## Timed — not blocked

**Final product task before release preparation:** item 24. Write the welcome/tutorial
only after the editor, worlds, Settings, and taught interactions have settled.

24. **Make the file opened on first launch serve as both Awl’s welcome and its tutorial, then refresh the surrounding user-facing docs.** **Build:** Write one real Markdown file that opens as the first-run document and teaches Awl by being edited in Awl. It welcomes the user, establishes that Awl creates ordinary Markdown files, and introduces the essential writing experience through the document itself: WYSIWYG reveal at the caret, native keyboard shortcuts, the command palette, pointer-accessible contextual formatting, changing worlds, and where files are saved. Keep it useful as readable prose rather than turning it into a feature checklist or a sequence of modal lessons. No separate onboarding wizard, coach marks, account step, or second tour surface. Returning users resume their own work rather than seeing the welcome again. Update GUIDE and the site guide to agree with the finished product and this first-run path; matter-of-fact voice, facts verified. **Done:** A new user opens Awl directly into a beautiful editable document, understands what the editor is and how to begin, and can dismiss, replace, or save the file without special state leaking into later sessions. **Verify:** Fresh-profile first launch, second launch/session restore, edit/save/replace/close behavior, every taught chord through the real keymap on supported platforms, links between welcome/GUIDE/site claims and implemented behavior, and representative first-run captures. Site deployment remains separately user-gated. **Timed after the current user-visible wave and before release preparation; user decision 2026-07-29.**

118. **Pre-release world-loudness audit — repeat the 1–5 idle Room/Frame check before release.** **Audit definition:** “idle loudness” is how strongly a world asks for attention while the user is simply writing in page mode: palette, typography, margin pattern, and ambient motion count; summoned overlays do not. `1/5` is the quiet pole (Wagtail), `3/5` is recognizable/alive but comfortable for hours, and `5/5` is a deliberately rare statement world (Firetail). **Baseline from the 2026-07-26 design session, eighteen shipping worlds:** `1/5: 1`, `2/5: 10`, `3/5: 2`, `4/5: 4`, `5/5: 1` (mean 2.67). Bowerbird item 117 intentionally changes that to `1, 9, 3, 4, 1` (mean 2.72). **Direction for a twenty-world roster:** hover around 3 with awl’s calm bias; the provisional healthy shape is `1, 7, 7, 4, 1` (mean 2.85), not a symmetric theme-park bell curve. The current gap is the middle, not more 5s. This is a diagnostic distribution, never permission to turn up a world merely to fill a bin—each world still earns its own identity. Bilby/Brolga/Tawny/Saltpan are valuable quiet anchors; Galah and Mulga are candidates to inspect, not pre-decided promotions. **Run:** Immediately before release preparation, review every shipping world’s current Room in item 20’s dashboard at representative page widths, then observe every ambient world live in `--release`; independently assign 1–5 before looking at the baseline, reconcile the roster together, and record any chosen changes as separate concrete queue items. Include any nineteenth/twentieth worlds that have graduated by then; do not graduate Cassowary Light or any other candidate merely to hit twenty. **Done:** The final roster has a user-confirmed loudness map, its mean/distribution and outliers are explicit, near-duplicate intensity poles are named, and every proposed rebalance is either rejected on purpose or queued with a world-specific reason. **Verify:** Affordance-locating vision smoke over all Room captures; live-only confirmation of speed/calmness for Lava, Currawong stars, Bombora waves, Bowerbird cutouts, and any later ambient world. Pixel/sidecar arithmetic may prove territory and contrast but never claims the taste score. **Timed before release preparation; user design decision 2026-07-26.**

## Parked — explicit gate or future design

- **Export save-dialog scope:** macOS + Linux, one live-only cross-platform seam; capture uses an explicit path. Decided, not scheduled.
- **Per-world living-band choreography:** audition TwoShape/Slam/Soft against Morph; live feel is the oracle. Needs a design session.
- **Per-world copy-pulse differentiation:** possible future motion tweak; needs a design session.
- **Site deployment:** only on the user’s explicit word.

## Monitoring — non-blocking

- **Hands-on checks still useful:** writer-diff panel/Tab + zoom readout; heading-chevron mouse-press→toggle wiring; theme-picker felt input→present lag; Bombora drift speed / counter-motion / calmness over real seconds.
- **GPU memory:** no action unless the 6 GB symptom recurs; then probe the live surface with the window foregrounded.

## Release blockers and reminders

- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user’s explicit word. A dry run may precede them.
- **Confirm Paperbark’s display face before tagging.** The orchestrator picked EB
  Garamond from the already-bundled roster (item 158 records why Fraunces was
  rejected). The user has not yet seen a Paperbark Room rendered in it — the
  approved trial captures used Fraunces. Nothing is blocked on this; the face is
  one constant, and changing it later is cheap. Show a real Room capture and get
  a yes or a swap.

157. **Create a new hero social image for the site.** Design a dedicated sharing
image for awl rather than reusing an editor screenshot. Add it to the social
metadata on the landing page and the supporting site pages. Keep the image
recognizably awl: beautiful, intriguing, and earned rather than a feature collage.
Verify the rendered image at common social-card crops and confirm every metadata
reference resolves. **Queued at the user’s request 2026-07-29.**
