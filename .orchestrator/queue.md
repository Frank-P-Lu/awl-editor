# awl — live build queue

> Live execution state only. Completed and superseded work is in git history
> (`git log -p .orchestrator/queue.md`). Protocol, claiming, worktrees, and
> execution hygiene live in `.orchestrator/README.md`.

## Latest integration receipt

Items 177, 159, and 171 are landed on `main` in that order. The final combined
train is `18b4b7fff638761e6f692061a8a4ebe7d834d9fa`; web/wasm is 16/16 and the
canonical receipt is `conventions=mac,linux scope=all-targets`. Integration
caught and repaired Galah's stale health ratchets plus a parallel spellcheck
state leak in the macOS About command-roster sweep; the latter now has a
process-wide cleanliness law. This note supersedes their older waiting-for-
approval annotations below.

## Ready — current user-visible wave

178. **CI RED — repair the three clippy errors item 171 landed on `main`, then find out why a local gate could not see them.** **Run:** https://github.com/Frank-P-Lu/awl-next/actions/runs/30505177929 (push `de969313`, 2026-07-30T01:16Z). **First known bad commit:** `be941bdb` (item 171's `actions: interpret typed effects explicitly`, merged as `f6bd95e1`); last green CI is `dd4235ce`. **Defect:** the linux job's `bash scripts/code-health.sh` step exits 101 on three clippy errors under `-D warnings` — `clippy::question_mark` at `src/app/apply.rs:254:9` and `:261:9`, and `clippy::needless_borrow` at `src/main/run/effect_interpreter.rs:10:54`. The mac job passed; only the linux job runs code health. **The second half is the point:** item 171 was reported "code health clean" and item 170 exists precisely to stop a gate claiming a tier it did not run, so establish with evidence which arms `code-health.sh`/`native-gate.sh` run locally on macOS versus what linux CI enforces, and whether a class of lint is structurally invisible to a local macOS gate. A gate blind by construction is the same shape as item 166's sweep blindness. Implement the cheapest honest fix, or state the blindness explicitly rather than advertising the local gate as equivalent. **Blocks all further integration until green.** **Routing:** production tier. ✅ **LANDED — merge `4da623cb`** (`be53b914`). Code health exits 0 on the combined candidate.

**This item's own premise was wrong, and that is the finding worth keeping.** The brief assumed a local macOS gate could not see these three lints, because only CI's linux job runs `code-health.sh`. It can see them: `cargo clippy --all-targets --all-features -- -D warnings` reproduces all three on this Mac at exit 101, same lines and same lint names, on the same toolchain that produced the CI failure (`rustup check`: stable 1.97.1, so drift is excluded too). The gate was not blind here — **item 171 reported "code health clean" without the run behind it.** That is item 170's failure in a new costume: not a smaller tier substituted for a larger one, but a tier claimed without being run at all. The prose guard exists; what is missing is anything mechanical that stops a worker's report from asserting a gate outcome it never observed.

**A narrower real blind spot does exist, and is now closed.** Local clippy lints only the host triple, so any `#[cfg(not(target_os = "macos"))]` arm — the non-mac trash can, the GPU backend split, the dock-icon stub, and five other files — is never compiled on a Mac, and a lint living only there is invisible however thorough `--all-targets --all-features` looks. Proven by mutation rather than argued: a `needless_return` planted in `src/assets.rs`'s non-mac arm passed the host pass at exit 0 and failed cross-compiled at exit 101, naming `src/assets.rs:253`. `code-health.sh` now runs a linux-target clippy arm on Darwin hosts; it never installs a target, and when the target is absent it names the gap loudly rather than letting a clean run impersonate parity with CI. Orchestrator verified the arm genuinely executes rather than trusting the log's silence: the combined-candidate run compiled `wayland-sys`, `linux-raw-sys`, `rustix` and `dlib`, which only appear when targeting linux.

**Follow-up routed, not folded in silently:** item 179.

179. **Lint the macOS-only code that no CI job currently lints.** **Defect:** found while repairing item 178. CI's `linux` job is the only job that runs `scripts/code-health.sh`; the `mac` job runs `scripts/native-gate.sh` alone. So `#[cfg(target_os = "macos")]` code — the About window, dock-icon adoption, the menu bar, the packaging and bundle-identity paths — is linted by **no CI job at all**, and is caught only if a developer happens to run `code-health.sh` locally on a Mac. Item 178 closed the mirror-image gap (a mac host now cross-lints the non-mac arms), which leaves this as the remaining half. **Build:** Add a code-health step to CI's existing `mac` job. Two shapes are available and the cheaper one may well be right: the full `code-health.sh`, or a mac-only clippy-only step that avoids duplicating `cargo fmt`, `code-health.py` and `cargo-machete` work the linux job already does. **Expect to find existing debt:** mac-only lint debt has never been measured, so the first run may fail and the item includes triaging whatever it names rather than blanket-allowing it. **Done:** every supported target's code is linted by some CI job, and no `cfg`-gated arm depends on a developer's host OS to be checked. **Verify:** mutation proof — plant a lint inside a `cfg(target_os = "macos")` arm and watch the new step fail by name, and confirm the linux job still catches the non-mac arms. Report the CI-minute cost. **Routing:** production tier. **Found 2026-07-30 during the item 178 CI repair; unclaimed.**

180. **Switch-project leaves the Project picker listing the old workspace's siblings.** **Defect:** found by item 172's ownership map while classifying `ProjectLocation`, and it is a live user-visible bug rather than a code-health observation. `App::set_root` re-derives `project` and the file index but **not** `workspace_root`; `App::reload_config` re-derives `workspace_root` but not the others. Because `resolve_workspace` falls back to `root.parent()`, switching project into a tree with a different parent leaves `C-x p`'s Project picker listing the **previous** workspace's siblings until something happens to call `reload_config`. Two half-owners of one derived value, neither complete. **Build:** Give the derivation one owner so root, project, file index and workspace root cannot disagree — the `ProjectLocation` slice's `set_root` transition is the obvious home, which is why this was deliberately NOT fixed inside item 172's byte-identity round. **Done:** after Switch-project into any tree, every consumer of the project location agrees, with no dependency on a later config reload. **Verify:** drive Switch-project through the real keymap into a tree whose parent differs from the old one and assert the picker's contents from the sidecar; sweep the case where the new root's parent is the same, and the no-parent/filesystem-root edge; mutation proof must make the stale-sibling case fail by name. **Routing:** production tier. **Found 2026-07-30 by item 172's census; unclaimed.**

181. **A tall flat picker overruns the canvas — the flat geometry path has no height clamp.** **Defect:** found by item 174 while migrating the overlay row family. The theme picker reports `card_h: 934` against `canvas_h: 800`. The grouped/sections geometry path clamps itself through `avail_px`/`fit_items`; the flat path has no equivalent, so a flat picker with enough rows simply draws past the bottom of the canvas. This is pre-existing and was deliberately left outside item 174's byte-identity envelope. It matters more than it looks: the theme picker **retired its lens strip** (user decision 2026-07-15) and is therefore flat, so the shipping world-switching surface is the one that overruns. **Build:** Give both families one height-clamp owner rather than adding a second copy of `fit_items` to the flat path — same-behavior-implies-same-code, routed through item 174's planner now that it owns row geometry for both families. **Done:** no picker's card height can exceed its canvas at any supported size, zoom or DPI, and the clamp has one owner. **Verify:** sweep every `OverlayKind` × both list styles × canvas sizes including short/narrow and 1×/2× DPI, asserting `card_h <= canvas_h` from the sidecar and that the last row is fully drawn in pixels — the sidecar is a state oracle, so the "fully drawn" half is pixel arithmetic. Mutation proof removes the clamp and makes the tall-flat case fail by name; prove non-vacuity by confirming the current theme picker actually trips it before the fix. **Routing:** production tier. **Found 2026-07-30 by item 174; unclaimed.**

182. **The code-health ledger's stated rule and its enforced rule disagree, and three lanes read it three different ways in one night.** **Defect:** `scripts/code-health.toml`'s header says a file-size mark "must never be raised." `scripts/code-health.py` enforces something weaker: the real ceiling is `min(frozen_baseline_size, mark)`, so raising a mark is mechanically permitted anywhere *below* the frozen historical baseline and only fails past it. The hard invariant is therefore "a file may never exceed its frozen baseline", not "marks never rise." **Evidence this matters, from 2026-07-30:** item 172 read the prose, said "the manifest forbids raising", and paid for its growth by real decomposition (`viewstate.rs` 576→533 via a genuine `sync_view` simplification); item 174 likewise lowered six marks rather than raise any; item 132 read the mechanism and raised seven, each with an inline reason, and its gate passed. All three were acting in good faith on the same file. **Build:** decide which rule is the real one and make prose and mechanism agree. Note the two are not equally cheap: six of item 132's seven raises were +1/+2/+5/+9 lines at *single-owner seams* — the one exhaustive construction site, the one-owner accessor sequence, the one dispatch call site — where CLAUDE.md's own architecture requires the line to be there and "decompose instead" would break the exhaustiveness property the design depends on. A blanket never-raise rule therefore taxes exactly the seams the codebase most wants to keep singular. A defensible shape: marks may rise only below the frozen baseline, only with a recorded reason, and never for a file whose growth is avoidable by extraction. **Done:** a worker reading either the toml header or the script reaches the same conclusion, and the ledger's intent survives contact with a capability landing across single-owner seams. **Verify:** mutation proof over each rule the new wording states; confirm the script's own self-tests still pin it (`code-health.py` already asserts "a hand-raised file-size mark must fail" for the shrink case — check that assertion still means what its name says). **Routing:** production tier. **Found 2026-07-31 while integrating item 132; unclaimed.**

113. **Run the final live release verification for the integrated theme-switch flight recorder (`1f2e89d`).** The implementation and automated gates are complete: latest and worst completed theme-switch transactions remain available for five seconds with phase breakdowns; frame cost history stays independent; Debug-off clears through the App-owned reset seam; headless remains numberless; fake-clock, long-window, zero-redraw, capture, mutation, full native integration (3,070 passed, 11 ignored), and wasm 16/16 are green. **Run:** In an active unlocked macOS session, compare `--live-script` events with flight-recorder timestamps and inspect onscreen readability in `--release`. This is verification only; diagnose or repair only if the live result fails. **Done:** Timing correspondence and readability are recorded, or a concrete defect is returned to the queue. **Routing:** live verification=`gpt-5.6-terra` medium; any repair=`gpt-5.6-terra` medium; final integration=root only if a repair is needed. 🔵 **BLOCKED ON AN INTERACTIVE UNLOCKED SESSION** — the 2026-07-29 release probe built and registered `Awl.app`, but the app never signalled ready and macOS returned `Connection Invalid` for `com.apple.hiservices-xpcservice`; the recorder contained only its armed header, so no product verdict is claimed. The same session boundary blocks the Lava and Bowerbird real-time checks; release-GPU headless capture remains healthy.

114. **Build Settings as the first real consumer of the shared summoned workspace.** **Build:** Replace the standalone Settings overlay with one App-owned workspace opened normally through `Settings…`. It relocates attention while active, keeps the document as a quiet backdrop, and returns with `Esc` to the exact editor state. Give it search, the category rail (`Editor / Appearance / Writing / Files / Keybindings / Advanced`), current values and existing controls, wide two-pane and narrow staged layouts, coherent focus/back behavior, and Cmd-P deep-links to the relevant category or row. Preserve every setting’s live apply, persistence, range, exact-entry, toggle, and sub-picker behavior. Direct Theme and Caret audition remain the existing fast editor-backed overlays: suspend Settings, run the real picker, then return to the preserved Settings position. Remove the old Settings presentation and do not land an empty workspace shell, developer-only entrance, dormant alternate path, generic route stack, tab system, or persistent sidebar. The shared workspace is deliberately scoped to Settings and Version History. Amend DESIGN/SCOPE with the rule that overlays split attention for brief contextual choices while summoned workspaces relocate it for sustained work. **Done:** Settings is one coherent, normally accessible place at wide and narrow sizes; no parallel Settings UI survives; underlying buffer/view state is unchanged on exit; Theme remains as quick and distinctive as before; the shell is ready for item 116 without feature-specific branches. **Verify:** State/focus/back laws; every `SettingId × SettingKind` through direct Settings, Cmd-P, keyboard, and pointer; category/search preservation across child pickers; exact Theme commit/revert parity; narrow/wide, zoom, DPI, drawn↔hit-test, theme-roster and Wagtail laws; deterministic capture/sidecar and dashboard vision smoke; native, both conventions, wasm. **Depends on item 173; unclaimed; user decision 2026-07-29.**

116. **Move Version History into the shared summoned workspace — timeline and prose diff become one readable task, never three competing layers.** **Build:** Preserve the existing history store, git backend, pruning, facets, descriptions, kept versions, and prose-diff engine, but replace the current History overlay/diff-as-preview composition with item 114’s workspace. On wide windows, show a narrow timeline/navigation pane beside a large read-only comparison pane; moving through versions updates the comparison immediately. On narrow windows, show the timeline first and enter the comparison as a second stage with an explicit return path. The current editor is backdrop/state, not a third readable layer. **Scope:** Keep local loose-file snapshots and git-managed history behind the same UI with only a quiet source label. Preserve independent diff scrolling and a clear focus transfer between timeline and comparison. `Esc` leaves the current buffer byte-for-byte unchanged; restore must be a deliberate, footer-taught action rather than bare `Enter`, and remains undoable. `Version history…` and `Compare with version…` deep-link into the same workspace at the appropriate focus; `Keep version…` retains its brief naming prompt and returns coherently. Remove the old History overlay, document-under-card preview composition, and feature-specific diff-panel dressing only when their last consumer is gone—retain the generic prose-diff machinery and do not strand parallel disabled paths. **Done:** A user can answer “which version, what changed, and do I want it back?” without overlapping titles, hidden prose, or ambiguity about whether the editor is active; the same flow works for local and git history; exiting is a true no-op and restoring is deliberate and undoable. **Verify:** Timeline→live comparison→focus/scroll→back/exit/restore journeys for local snapshots, named/pinned versions, git commits, empty history, renamed files, and pruned ladders; narrow/wide/zoom/DPI captures across representative light/dark, Pane/Bars, and Wagtail worlds; pixel laws proving timeline and comparison never overlap and the original document does not remain a competing readable layer; restore undo law; capture/replay parity; dashboard vision smoke; native, both conventions, and wasm gates. **Depends on the completed item 114 Settings workspace; unclaimed; user design decision 2026-07-26.**

120. **Land the approved twelve-body Lava field on Firetail + Mangrove.** **Build:** Promote trial arm B as the sole production implementation: keep the shared viewport-relative Firetail/Mangrove field, raise population from eight to twelve, use the approved wider large/medium/satellite hierarchy with average radius about 10% below the old field, expose a healthier population in both margins, firm the silhouette slightly, and reduce horizontal sway so motion reads mainly vertical. Remove the temporary selector, the A control arm, and all disposable review machinery. Do not add buoyancy physics, responsive population, world-space tiling, or fluid simulation. Palettes, page width, ambient cadence/gates, frost, and one-accent/value-band laws remain unchanged. **Done:** Both worlds ship only the user-approved B composition; no further taste decision is required. **Verify:** fixed-phase contribution and coverage across short/tall, narrow/wide, page widths and 1×/2×; all twelve bodies contribute somewhere; neither margin becomes empty or fully covered; existing ground-band, hue-clear, page mask, frost, seamless loop, delayed-wake, Reduce Motion, ambient-off, determinism and release-perf laws; native and wasm. **User choice 2026-07-29.** ✅ **LANDED — `f7d35425`** (`e271dddb`, `4b7ced3e`, `f999fcdd`). Selector and A arm gone, verified by grep across `.rs`/`.wgsl`/`.md`/`.toml`. Gates on the candidate: 3149 passed under both conventions, wasm L1+L2, code health clean, captures at 1200×800 / 700×1200 / 2560×1080 / 2×.

**Surprise worth keeping:** `theme::tests::gutter_frost_pill_keeps_ink_contrast_on_every_lava_world` always read `BACKDROP_BLOBS` directly, so through the whole trial it exercised the eight-body control and never once touched the twelve-body data it was meant to guard — a law green over the exact thing it was named for. Promoting exposed that the approved data left the bottom-left frost pocket dead (raw field peaked 0.39, under the 0.38 flat-ground cutoff; blur+dim then washed it out).

⚠️ **OPEN — needs a live human look, not settled.** The frost repair moved ONE satellite's `cy` from 0.67 to 0.75 (radius, weight and every other body untouched, confirmed against the real `frost_field`/`frost_pixel` path). That is a deliberate divergence from the composition the user approved in the trial captures: the approved arm B rendered with `cy` 0.67. Two live questions, neither harness-provable: whether the reduced horizontal sway genuinely reads as mainly-vertical drift over real seconds, and whether the moved satellite looks natural rather than engineered. If the user rejects the new placement, the frost pocket needs a different repair — the defect is real and reverting `cy` alone reopens it.

121. **Make Firetail’s shipped icon carry the world’s wine-pink identity, and change its fake logo cursor from Pill to Block without falsifying its gold caret.** **Build:** After item 110 lands, change Firetail’s theme-owned `IconCursor` assignment from `Pill` to the existing shared `Block` preset, then audition the icon tile ground at three exact states: **A** current `base_100`; **B** 25% from `base_100` toward Firetail `base_300`; **C** 40% toward `base_300`. Keep the `aw` wordmark in the real `base_content`, the Block cursor in the real gold `primary`, and its knocked-out `l` in the real `primary_content`; Firetail’s actual world palette and live caret remain byte-identical. The problem is that the shipped icon currently samples only Firetail’s darkest oxblood plane, so it reads near-black/cream/gold and omits the pink-wine identity carried by `base_300`, lava `blob_hi`, selection, and muted ink; the current Pill also is no longer the chosen icon silhouette. **Mechanism:** The cursor change uses the existing theme-owned preset assignment, not a Firetail branch. Represent any chosen non-default ground as theme-owned icon data/capability with `base_100` as the inert default for every world; never add a Firetail-name branch and never give the icon cursor a color different from the actual caret. No lava illustration or extra background motif in this round—the Block silhouette plus the large tile plane get the first chance to solve the identity problem cheaply. **Done:** At 256/128/64/44/32/24 px on both light and dark Dock surfaces, Firetail uses the Block logo cursor, immediately reads wine-pink plus ember-gold, remains recognizably the same world as the live Room, and does not collapse toward Potoroo’s rust or Bombora’s violet/rose identity. The user chooses A/B/C from one Block-cursor comparison sheet before any shipping asset changes land. **Verify:** Regenerate only through the canonical exporter/packer; assert Firetail alone changes preset membership from Pill to Block and every other world’s assignment remains unchanged; run the full 18-world icon distance/crowding/stale-entry laws from items 102/109, with Firetail’s empirical nearest pairs named; assert icon cursor/knockout colors still equal Firetail `primary`/`primary_content`; perform `l` containment, small-size legibility, and dark/light Dock vision smoke; rebuild item 20’s dashboard; native + wasm gates. ****Both blockers cleared** — item 110 landed (`04ae1da5`, on main) and item 162 repaired the canonical exporter today, so the A/B/C sheet can actually be produced. ✅ **LANDED — `996ca128`. User chose C (`IconGround::Blend40`).** Gates on the rebased tree with exit codes observed directly: health 0, suite 0 (3221 passed, integration targets included), wasm 0.

**The choice was measurably right, not only a taste call.** Pill→Block had pushed Potoroo/Firetail to the roster's worst ink distance — 54.31% before the item, 49.81% with Block on the old ground — so the cursor change made the very crowding this item exists to fix *worse*. Blend40 does not merely restore it: ink 54.31 → 49.81 → **66.42**, mean 32.87 → 25.94 → **44.89**, differing → 79.49 → **88.77**. Firetail now carries the wine identity *and* sits further from Potoroo than before any of this began.

**Tawny/Firetail was REMOVED from `MEAN_BLESSED`, not re-blessed** — it widened past the 70.0 danger threshold entirely (66.60 → 71.49), and a stale entry for a pair that is no longer crowded silently weakens the law. `stale_blessed_entry_for_a_widened_pair_is_flagged` is what catches that.

**Integration hazards handled rather than absorbed:** the preset tally was recomputed by hand to the true combined **(11 block, 7 pill, 1 narrow)** — neither side of the conflict was right, the same shape as the health-ledger line pin that needed correcting three times today — and the packer confirmed **19 worlds**, so Paperbark survived the rebase.

**Superseded note (kept for the record):** Branch `claude/item-121-firetail-icon` (`35ab776d`, `f98e81ac`, `78ed9ee6`, `c11eb790`, `d42103fc`). The Pill→Block change and the `IconGround` capability (`Base100`/`Blend25`/`Blend40`, inert `Base100` default for every world) are built and gated: 3197 under both conventions, wasm, code health. Firetail alone changes preset membership (tally 9/8/1 → 10/7/1); only Firetail's `.icns`, its two shipped sheets and `geometry.txt` differ.

**Sheet:** `gallery/item-121-firetail-ground/gallery/shipped-{dark,light}.png` in that worktree — all three grounds wearing the Block cursor at 256/128/64/44/32/24.

🟢 **USER CHOSE C (`Blend40`) on 2026-07-29.** Resume the existing `claude/item-121-firetail-icon` branch: set Firetail’s sole shipped `icon_ground` value to `Blend40`, update the inert-default law to assert Firetail as the one explicit exception, regenerate the canonical icon assets and review sheets, rerun the affected distance/legibility/determinism laws plus native and wasm gates, then land. This resolves and supersedes the “blocked on one user decision” note above; no further taste choice remains.

**The measured argument, which the brief did not anticipate:** the Pill→Block change *worsened* the very crowding this item exists to fix. Potoroo/Firetail ink distance was 54.31% before (item 161's measurement) and is **49.81%** with Block — the roster's global minimum. Potoroo is dark rust + orange cursor; Firetail-A is dark brown + gold cursor, and at ≤44px they are genuinely hard to tell apart. So the ground change is not decoration: it is what repairs the crowding the cursor change introduced. The worker states plainly that **A does not satisfy "does not collapse toward Potoroo's rust"**. Three crowding entries were re-blessed against the newly measured pixels.

**Not to be re-run:** the worker deliberately stopped at the sheet and picked no winner, per this item's own instruction that no shipping asset adopts a ground until the user chooses.

**Original dispatch scope:** the Pill→Block preset change and the theme-owned ground capability land, the A/B/C comparison sheet is produced, and the worker stops. Choosing a ground is the user's call and no shipping asset adopts one until they pick. User design decision updated 2026-07-27.**

131. **Give Mangrove and Magpie mirrored diagonal-line compositions across contextual menus and the real Settings workspace.** **Build:** Add one reusable theme-owned diagonal row composition through the shared rowlayout/surface machinery, then assign its two authored orientations: **Mangrove** draws a continuous descending `\` spine, with row clusters left-aligned on the RIGHT side; **Magpie** draws a continuous ascending `/` spine, with row clusters right-aligned on the LEFT side. The line is mandatory in both—the striking read comes from the drawn division and triangular negative space, not merely staggered text. It may visually bleed toward the surface corners, while row attachment points occupy an inset middle band so the first/last rows retain usable width. **Line treatment:** never amber/primary. Mangrove uses a crisp tidal-teal line derived from its muted ink; Magpie uses a crisp graphite line from its muted ink. Resting weight is clearly visible but subordinate to text; the selected row brightens and thickens only the local spine segment toward `base_content`, extends a short connector to the row, and steps the row outward by a few crisp pixels—no spring, pulse, or full-width selection bar. Existing bottom-left Mangrove stipple and bottom-right Magpie ghost placards occupy the opposite empty triangle rather than colliding with the rows. **Controls are in scope, not a fallback:** model each row as a measured `label + fixed gap + accessory/control` cluster. Reserve consistent label/accessory extents across the visible set so shortcuts, values, toggles, checkmarks, exact-entry fields, and Range sliders trace a stable parallel rail with honest spacing; anchor the whole cluster to the spine instead of independently nudging text. Query/title/category navigation/footer regions remain horizontal and stable—the diagonal owns the candidate/setting rows, not every glyph on the surface. Filtering and scrolling sample a fixed surface-relative line at fixed row y positions, so content changes never make the spine or surviving rows jump horizontally. **Surface reach:** enroll every contextual overlay’s row section that currently consumes theme list-style data, not Commands alone; non-row panels keep their existing geometry. After item 115 removes the old Settings overlay, apply the SAME diagonal owner to the Settings workspace’s main setting list for Mangrove/Magpie, while its category rail, search/title shell, child Theme/Caret auditions, and narrow-stage navigation remain item 115’s workspace behavior. The empty main-pane triangle may carry the existing active category/title typography, but never a duplicate decorative label. **Responsive bound:** size the slope from the real widest visible cluster and available side territory; widen within the existing surface limit first, then reduce horizontal travel while preserving a visibly diagonal direction. Never overlap, clip, shrink type/controls, introduce horizontal scrolling, or silently fall back to Pane/Bars. **Scope:** This is a third data-driven row composition, not Mangrove/Magpie branches and not permission to diagonalize the document, workspace shell, History comparison, native Mac menu, or web/Linux persistent menu bar. Other worlds and their Pane/Bars results remain byte-identical. Item 112 owns the shared overlay rhythm first; items 114/115 own the workspace and Settings migration first. **Done:** Mangrove reads like a tidal instrument panel and Magpie like an editorial spread in both brief menus and sustained Settings, with a visible line, deliberate negative space, readable controls, obvious selection, and no loss of keyboard/pointer usability at any supported width. **Verify:** Full no-wildcard `OverlayKind` row-surface sweep plus every `SettingId × SettingKind`; simple/long labels, chords, values, toggles, text entry, sliders, empty/short/full/filtered/scrolled lists, category changes, child-picker return, and narrow/wide staging; drawn line/row/control ↔ hit-test agreement at zoom and 1×/2× DPI; pixel laws for orientation, line continuity, inset attachment band, fixed label-control gap, local selected segment, placard/row non-overlap, non-primary ink, and no clipping; exact before/after identity for every non-assigned world; dashboard captures and affordance-locating vision smoke over Commands plus every Settings category in both worlds; native, both conventions, and wasm gates. **Depends on 112 (landed, `fa64a3a4`) + 114 (pending) — **there is no item 115**; it was folded into 114 by `d726c4bb`, so the references to "item 115" in this item's body mean 114. The one real blocker is 114. Unclaimed; ambitious user design decision 2026-07-27.**

132. **Add Kite — a loud light world travelling through a peripheral warped-grid tunnel.** **Identity:** Kite is the nineteenth world and the deliberate light `5/5` counterpart to Firetail: cool rather than warm, geometric rather than organic, crisp rather than liquid, and directional rather than drifting. Use a near-white mineral/pale-lavender ground, indigo/graphite grid and deep cool prose ink, with a single hot vermilion caret that reads like the bird's red eye. Its authored font must be bundled, licensed, distinct from the existing roster, and reported through the normal asset-size/licensing audit; do not choose one merely to satisfy the concept. **Background:** Add one data-driven ambient warped-grid capability shared through the existing renderer, not a Kite/world branch or a literal 3D engine. The writing page stays flat, opaque, motionless, and fully legible. The grid lives in the Frame/margins as two intentionally composed slices of one curved tunnel: distort the useful curvature outward so the opaque central page does not hide the vanishing point or all of the top/bottom bend. The left and right margins must communicate one journey—during a left turn the left grid compresses while the right opens; climbing/descending produces coherent opposing vertical flow—rather than looking like two unrelated animations. Fade/quiet the grid near the page edge and progressively simplify it at narrow margins to a gently translating warped field; never squeeze a tiny illegible tunnel behind the page or draw through prose. Use anti-aliased major/minor line hierarchy, with every fifth line stronger and minor lines fading before projected spacing can create Retina/WebGL moiré. **Motion:** The camera travels continuously forward. Roughly once a minute it eases into a new long direction—straight, left/right bends, climbs, and descents—without a cut, jump, hard reversal, obvious one-minute restart, roll, or attention-grabbing acceleration. A much longer deterministic route may eventually repeat invisibly; the visible contract is uninterrupted travel, not a short looping clip. Keep speed calm enough for writing even though the world is visually loud. Losing focus pauses in place and resumes without catch-up; ambient motion off and Reduce Motion freeze to one composed still; headless capture has a deterministic settled pose. **Scope:** This is an explicit statement-world exception, not permission to increase motion elsewhere. Preserve the one-accent law, page geometry, adaptive column, overlay readability, zero-network rule, and theme-as-data architecture. Measure release-mode GPU/frame cost on native Metal, wasm WebGPU, and WebGL fallback; reduce grid density/cadence through the shared capability before compromising text or inventing a platform-specific look. **Done:** On a wide Retina display, Kite unmistakably feels like the stable writing surface is gliding and gradually steering through curved space, with the decisive geometry visible in the margins despite the central page; on a narrow display it remains recognizably Kite without shimmer or clutter. It is light, technical, pale-purple, and `5/5`, balancing Firetail without resembling it. **Verify:** Add roster/no-wildcard enrollment laws and exact non-assigned-world identity; deterministic wide/narrow captures for straight, left/right, climb/descent, paused, ambient-off, and Reduce Motion states; pixel arithmetic for page exclusion, edge fade, cross-margin directional coherence, line hierarchy, contrast, on-canvas bounds, and no high-frequency minor-line aliasing at 1×/2× and zoom; release benches on native/WebGPU/WebGL; dashboard Room/Frame/icon enrollment; affordance-locating vision smoke and a live multi-minute human review for speed, steering legibility, seamless transitions, peripheral comfort, and motion sensitivity. Harness results must not claim the live motion feels calm. **User design decision 2026-07-27.** 🔵 **BUILT AND GATED — HELD FOR THE LIVE REVIEW AND ONE PALETTE CALL** — branch `claude/item-132-kite-fresh`, tip `afcbc1b9`, 9 commits. Receipt `native-gate-receipt commit=afcbc1b907126305be452715857ae7329a690663 conventions=mac,linux scope=all-targets`, health 0, wasm 16/16. **Needs a rebase onto `d1877d37` at integration.** Deliberately not landed: this item's own Verify requires a live multi-minute human motion review, and the palette question below may change the shipped palette and therefore the icon export — cheaper to answer first than to land twice.

**Kite is the TWENTIETH world, not the nineteenth** — Paperbark took that slot on 2026-07-29. That closes `PHILOSOPHY.md`'s authored target, so "quality remains the gate; a count never justifies a weak addition" now cuts the other way: there is no twenty-first slot to defer a taste fix into.

**Mechanism.** `Background::WarpedGrid { ground, minor, major, tunnel, spacing_px, curvature, density }` in `theme::ground`, shader id 10, with one theme-owned `theme::Tunnel { Outward, Centred }` riding a scalar through the new `BgDesc::tunnel` (the `LavaEdge::mask_mode` precedent), inert `0.0` for every ground without a tunnel. **The route is Rust's alone** — `warpgrid::route_pose` resolves the pose on the host and it reaches the shader through one 16-byte `Globals.pose` row, so there is no WGSL route mirror to drift; a grep-law asserts `leg_seconds`/`loop_seconds`/`warp_route_pose` are *absent* from the shader. Identity is proven the strong way: all **19 other worlds** render byte-identical at the frozen pose *and* at a hard mid-route pose, with `tunnel_mode() == 0.0` exactly. Font is **Fira Sans**, already bundled, SIL OFL 1.1, zero new bytes and zero new audit rows. Mid-motion capture knob: `AWL_WARP_POSE=straight|left|climb|right|descent|wrap|<seconds>`, inert unless set.

**Three laws were green over real defects before they were fixed — the strongest set tonight.** (a) The aliasing law counted isolated single-pixel marks, but `warp_line` draws at constant pixel width so an over-dense lattice *merges* rather than speckling; rewritten to bound the densest 14×14 tile, and its swept axis was also wrong — every geometry kept the vanishing point behind the page. Adding wide-canvas/narrow-measure geometries where a bend swings the VP *into* a margin made the law **fail on the author's own shipped shader** at 70.9% ink. (b) The wrap law compared `phase == LOOP` with `phase == 0`, which `route_pose` wraps to the same call — vacuous, and green over a broken loop length. (c) The coherence law counted lines crossed per margin, but that count falls on *both* sides during a turn; ink density is the reading the eye actually makes.

**A rejected salvage that was a real bug:** the old branch's `FORWARD_CELLS_PER_LOOP: 64`. Travel enters the ring coordinate as a subtraction whose minor lattice has period 1 but whose every-fifth-line hierarchy has period 5, so 64 wrapped the lines back onto themselves while rotating which were major — a visible one-cell jump at the loop endpoint. Ours is 65.

**Perf:** `--release` `--bench-frame` at 1200×800 puts Kite (1.945/1.908/1.944 ms) inside run-to-run noise of the flat gradient and both shipping procedural grounds, and at 3200×2000 Kite 127.1 ms against Wagtail 128.8 / Paperbark 129.5 / Bowerbird 126.5. Still the literal fullscreen triangle: zero new pipelines, instances or CPU work.

🔵 **USER QUESTION 1 — the palette, and it needs answering before the icons are final.** **Kite and Brolga are the roster's tightest palette pair at 38.8 RMS redmean.** No shipping law caught it: the ≥70 floor is Firetail-specific and the ≥60 floor is Tawny↔Mopoke-specific, so there is no roster-wide floor — but 38.8 is below *both* thresholds this repo has previously chosen to defend. This is inherent to the item as written: "cool light world, near-white ground, deep cool prose ink, vermilion caret" **is Brolga's definition**. Holding `base_content`/`muted` fixed (the role laws pin them tightly), the best reachable is **57.8**, still under 60, and it costs the "near-white mineral" character. The axes that do separate them: the ground (a travelling tunnel vs a static gradient), the caret (70.6 apart), and the ink (violet-graphite vs slate-navy). **Recommendation:** if 38.8 reads too close live, push `base_200`/`base_300` to `#E5DEF3`/`#CDC0E7` and the caret to `#FF3B14` — about 52, and it serves "pale-lavender" better. That needs an icon re-export and re-blessing, which is why it was not done blind. **The missing roster-wide distinctness law can only be written after this call, because it would fail today.**

🔵 **USER QUESTION 2 — the live review, which the harness explicitly cannot answer.** `scripts/dev-app.sh`, `--release`, wide Retina, theme Kite, page mode on, a real document. **(1)** Five uninterrupted minutes *writing*, not watching — does the grid stay peripheral or pull your eye off the line? This decides the world. **(2)** The author's own top concern: the route rides the shared ~10 fps ambient tick, so each frame advances well under a pixel and it *should* read as a smooth creep — watch one margin ~20 s and say whether it glides or steps at ~10 Hz. If it steps, the fix is a capability-level cadence dial, not a redesign. **(3)** Watch ~2 minutes for a steering transition: does it *ease* into the bend? **(4)** During a bend, do the two margins read as one journey? **(5)** Motion sensitivity, if you are at all susceptible. **(6)** Drag wide → ~1100px and back: does it simplify gracefully? **(7)** Reduce Motion and `ambient_motion = false` must both freeze to the same still. **(8)** Put Kite and Brolga side by side in the theme picker — that is the 38.8 in the flesh.

⚠️ **Disclosed gaps, not hidden:** no live browser WebGPU/WebGL2 perf probe (offline `naga 29.0.3` GLSL ES 300 validation plus wasm smoke only, so no WebGPU-only construct slipped in, but no real-browser number exists); the Chromium review dashboard was not built end-to-end though all its roster inputs are green; `scripts/bench.sh` is unreadable on this tree because `benches/baseline.json` was last recorded 2026-07-15; and the vision smoke was self-judged rather than by a second pair of eyes. **Seven code-health size marks were raised** — see item 182, which is about the ledger's contract rather than this item.

158. **Graduate Paperbark with Deckled Strata, and delete the rejected material trial arms.** **Build:** Turn item 133’s B profile into Paperbark’s one production background: nested deckled handmade-paper contours gather around the writing page in the provisional cream/pale-honey palette, with deep bark-brown prose and the coral/vermilion caret kept as the sole accent. Preserve the quiet static treatment the user approved; do not add raking light, motion, stock texture, or a second material mode in this item. Remove A Broad Sheets, C Loose Fibres, D Relief Print, E Peeling Curls, and the temporary trial selector from the Paperbark branch. C is not discarded: item 159 gives the reusable mechanism a different theme-owned assignment. **Scope:** Enroll Paperbark normally as a candidate world only after font licensing, roster distinctness, icon, dashboard, and zero-network requirements are satisfied; do not ship disposable review machinery. **Done:** Paperbark has one coherent material identity rather than five dormant treatments, reads as deckled paper around a calm opaque writing page, and does not collapse toward Saltpan’s regular pinstripes or Bilby’s quiet gradient. **Verify:** wide/narrow/short/tall and 1×/2× captures; page exclusion, contour continuity, contrast and no-moiré pixel laws; exact rejection-arm absence; world/icon/dashboard roster enrollment; nearest-world vision smoke; native, both conventions, wasm. **User choice 2026-07-29.** ✅ **LANDED — merge `73d6fd75`; display face confirmed as EB Garamond on 2026-07-29.**

**Kite recovery audit 2026-07-30.** The old core (`673d2758`) and integration
(`fc5337a9`) branches are design/test prototypes, not merge candidates: they are
155/144 commits behind main, main deliberately reverted the implementation, and
the current `theme::ground`/Deckle ownership replaced their background model.
Do not rebase or cherry-pick them. After item 159 leaves the shared theme and
background-test files, start item 132 fresh from current main; salvage only the
route specification, Fira Sans choice/license facts, gallery poses, and test
intent. The exporter blocker is cleared on main. Fresh native Metal,
WebGPU/WebGL release benches and the multi-minute human motion review remain
mandatory.

🟢 **VERIFIED AND LANDED** — branch `claude/item-158-paperbark`, 7 commits, merge `73d6fd75`. Gates: 3211 under both conventions, wasm, code health, canonical icon export, 75-card dashboard on a 19-world roster.

**Paperbark ships as the nineteenth world.** Trial file, scripts and env knob are `git rm`'d — a repo-wide grep for every arm name and the env var returns one hit, this board line. Runtime proof: `AWL_PAPERBARK_TRIAL=B` now renders Gumtree.

**The mechanism, and item 159's seam.** `Background::Deckle { ground, layer, deckle, weave, period_px, wander_px, density }` in a new `theme::ground` module, with the profile carried as `theme::Weave::{Strata, Fibres}` uploaded as a scalar through `BgDesc::weave` — the existing `LavaEdge::mask_mode()` precedent. Inert default `0.0` for every ground without a weave, so no other world's upload changes shape. **Item 159 changes only Galah's theme literal and touches no renderer code.** Because the Fibres arm has no assignee yet, nothing else in the suite would ever draw it, so it is proven directly: `weave_fibres_draws_a_real_field_distinct_from_strata` shows >50% of margin pixels differ from Strata *at identical dials*, so the second profile is real rather than a fallthrough item 159 could not use.

**A mutation initially passed — the item's own lesson repeating.** The collapse law measured spread and band count, both of which the deckled *boundary* satisfied alone, so a field whose lane interiors all drew one seeded tone stayed green. It now counts distinct tones in the lane **interiors** and fails by name. This is the fourth instance today of a law green over the case its author did not imagine.

**Two laws found real defects, neither bookkeeping.** (a) The world shipped `RenderCaps::DEFAULT`; a light ground must carry the summoned card border — fixed to `Elevation::Bordered`. (b) The fleuron bullet pushed text off the row, a defect logged for months as a *Bombora quirk*. It is not: the ornament tier scales against the concealed `"- "` advance in the world's **body** font, and EB Garamond's narrow punctuation advance is the true cause. Bombora's bare `0.35` literal became `BULLET_SCALE_GARAMOND`, both Garamond-body worlds read it, and the law now derives its allowance from the **face** rather than a world-name list. It also wrote the missing drift check for `theme_caps_law`'s banned world-name list — being a *ban* list, a missing name made the grep-law silently permit what it exists to forbid.

**Empirical nearest icon pairs** (Block preset; Pill was packed and measured too, moving the metric 0.1pp, and Narrow is excluded by law because EB Garamond's `l` is footed): differing pixels — Galah 20.21%, Bilby 20.31%, Saltpan 23.44%; mean channel — Bilby 52.79, Galah 64.45; ink axis — crowds nobody. Paperbark is champion on no axis; the roster minimum stays Currawong/Cassowary at 13.1%.

🟢 **Paperbark face gate closed.** The user confirmed EB Garamond after reviewing the real release-GPU Room capture. The small Garamond fleuron bullets remain a known ornament-tier tradeoff rather than a Paperbark blocker; if changed later, the fix belongs to the shared ornament tier used by Bombora and Paperbark.

Also live-only: `period_px` is physical pixels by family convention (Zigzag and Organic do the same), so a 2× user sees a finer sheet than a 1× user — a real perceptual difference no headless law can judge.

**Orchestrator findings 2026-07-29 — read before starting.** Paperbark is not a world yet. `src/paperbark_trial.rs` on `codex/item-133-paperbark-trial` (`b7c91c41`) deliberately keeps it out of `theme::THEMES`: it is a provisional `PAPERBARK_THEME` value plus a `shaders/background.wgsl` selector behind `AWL_PAPERBARK_TRIAL`, inert without that variable. So this item creates the twentieth world outright — roster entry, icon, dashboard, laws — not merely a background swap. The trial arms never reached main; build from the trial branch rather than deleting around it. **Display face: EB Garamond**, chosen by the orchestrator under the user's 2026-07-29 instruction to pick an already-bundled face and not block. It is already bundled and SIL OFL 1.1, so no new asset weight and no licensing gate; the trial theme already pairs Paperbark with `ORNAMENT_GARAMOND`; and its only current owner, Bombora, is a dark world, so it cannot pull Paperbark toward the light-world neighbours the Done clause names. **Fraunces was rejected despite being what the approved B captures rendered with**, because it is Saltpan's face and Saltpan is the exact world this item must not collapse toward — sharing a face there doubles the collapse risk rather than diluting it. Face-sharing itself is established precedent (Bowerbird/Brolga, Currawong/Cassowary, Potoroo/Firetail). The pick is provisional pending the user's look at a real Room capture; see Release blockers. **This item owns the promotion of the shared background mechanism** from trial code to theme-owned data with an inert default; item 159 follows as its second assignment and proves reusability. **Icon generation depends on item 162** — the canonical exporter currently hangs, so the world's shipped icon and dashboard rebuild cannot be produced until that lands.

159. **Give Galah the Loose Fibres treatment as sparse pink plumage.** **Build:** Reuse item 133’s C mechanism through theme-owned background data and assign it to Galah, recolored entirely through Galah’s existing pink/mauve value ladder. Author the strands finer and sparser than the Paperbark trial so they read as loose feather filaments or plumage in the Frame, not cream handmade-paper fibres copied between worlds. Keep the writing page flat and untouched, the treatment fully static, and Galah’s Figtree/IBM Plex Mono typography, flower ornaments, Ghost placard, Poster Bars, and narrow berry caret intact. **Scope:** This promotes an existing quiet world from a pleasant flat gradient toward the missing 3/5 middle; it is not a new world, animation license, or permission to change Galah’s overlay personality. The reusable mechanism must have an inert default and no `Galah` name branch. **Done:** Galah is recognizable from its Room before an overlay appears, reads pink/plumaged rather than papery, remains comfortable for long writing, and stays distinct from Paperbark’s broad deckled strata, Bilby’s dawn gradient, and Brolga’s cool blue plane. **Verify:** full background-roster no-wildcard enrollment; exact non-assigned-world identity; density/strand-width sweep at wide/narrow/short/tall and 1×/2×; no page/Outline/gutter encroachment or aliasing; dashboard Room/Frame captures and affordance-locating vision smoke against Paperbark, Bilby, Brolga, and Saltpan; native, both conventions, wasm. **User choice 2026-07-29. Sequenced after item 158**, which promotes the shared background mechanism this item reuses; running them concurrently would have two owners writing the same mechanism. 🟠 **VERIFIED, WAITING FOR INTEGRATION APPROVAL — `9b66d28b`; canonical native receipt, wasm 16/16, focused GPU/pixel laws, mutation proof, and visual review are green.**

160. **Replace the visually anonymous standard macOS About panel with a deliberate custom native Awl window.** **Build:** Preserve the native macOS window class and App-menu routing from item 128, but replace `orderFrontStandardAboutPanel…` with one focused, non-editor About window whose composition reaches the quality bar of the user’s Ghostty reference without copying its neon treatment. Give the current shipped Awl icon real scale, then `Awl`, one concise product line, version/build/commit facts, and `Frank Lu · GPL-3.0`; include restrained Docs and GitHub actions only as explicit user clicks. **Typography is deliberately only two levels:** normal body ink for the product/title, product line, author/license, and button labels; one shared greyish secondary ink for version/build/commit facts. Do not introduce a third size/weight/color tier, and remove the horizontal `---` divider entirely—the grouping comes from whitespace and rhythm. Centre the composition with generous spacing and make it authored in both light and dark system appearances. **Scope:** This is rare native identity chrome, not another in-app workspace, theme surface, account screen, updater, or runtime fetch. The panel never phones home; links merely open their fixed destinations after a click. Keep the lowercase `awl` CLI name, App-menu capitalization, bundle metadata, licensing facts, and non-macOS in-app About card intact. Do not fabricate copyright or build facts, and do not make the window imitate whichever world happens to be active. **Done:** About Awl looks like a considered product moment rather than AppKit’s generic default while remaining unmistakably macOS and spare; its visible type resolves into body and secondary grey only, with no divider. **Verify:** packaged light/dark screenshots; pixel/color-role assertion that all visible text belongs to exactly those two ink roles and no divider pixels survive; current icon/name/version/build/commit/license assertions; link destination and keyboard-dismissal tests; single-window reuse rather than duplicates; menu routing; native + wasm platform split. **User decision refined from live screenshot 2026-07-29.** ✅ **LANDED — `ea3d2a2c`**, rebased onto `c5b5649c`. 3193 passed, code health and wasm both exit 0 on the integrated tree. The `releasedWhenClosed` hazard is now recorded in CLAUDE.md's platform tripwires. Orchestrator reviewed both packaged captures directly: two ink roles and no divider as refined, and the displayed commit hash resolves to a real commit rather than a placeholder.

**Crash hazard found — the muda-Menu tripwire in a second costume.** `releasedWhenClosed` defaults to **YES** on a programmatically created `NSWindow`, so the close button would free the window out from under the `Retained` handle and the next summon is a use-after-free. Set to NO; a main-thread-only `thread_local` is the sole owner for the process lifetime. Buttons target the panel itself — it outlives its subviews (no dangle) and `NSButton`'s target is unretained (no cycle). Worth adding to the CLAUDE.md platform tripwires when this lands.

**Facts are real, never fabricated:** version from `CFBundleShortVersionString` else `CARGO_PKG_VERSION`; build from `CFBundleVersion`, shown only when it differs; commit from `AWL_GIT_COMMIT`, stamped by a new `build.rs` via `git rev-parse`. An unknown fact loses its line and the window shrinks — no placeholder exists anywhere. `build.rs` emits no timestamp or hostname and touches no network.

**The two-ink law's first attempt was wrong in an instructive way:** histogramming the region reported **seven** inks for a two-ink window, because every rung of the antialiasing ramp counted as one. It now reads each label's own frame per element and detects dividers by *shape* (thin, long, isolated horizontal band), so any way of drawing a rule trips it. Includes a fixture-staleness guard that fails when layout drifts from the committed capture.

**Two taste fixes came from measuring the captures, not from eyeballing:** provenance was **1.9:1** contrast — decorative rather than legible — now 3.9:1 light / 5.5:1 dark; and AppKit had ringed Docs blue as first responder, putting a link one Return from a dismiss.

⚠️ **Live-only, unexercised by the harness:** whether Docs/GitHub actually open (URLs are law-pinned; the `NSWorkspace` handoff is not), whether `Esc`/`⌘W` dismiss (`dismiss_chord` is unit-tested; `performKeyEquivalent:` wiring is not), and reopen-after-close (ownership argument is sound and `about_windows=1` was observed, but the close→reopen cycle was never run). Also: a pure colour change that leaves geometry alone would pass both fixture ends until retaken.

161. **Optically raise the shipped icon cursor lockup for Mopoke, Currawong, Bilby, and Saltpan.** **Defect:** In the repaired per-world dashboard, the fake logo cursor containing the `l` still sits visibly low relative to the `aw` wordmark in four worlds. Mopoke needs the clearest upward correction; Currawong needs only a very slight lift; Bilby and Saltpan need similarly restrained lifts. The remaining fourteen icons passed the user’s review. **Build:** Measure the cursor body, contained `l`, wordmark ink, cap/x-height and shared baseline at every native size, then express the optical correction through one data-owned icon alignment mechanism with an inert default—not four renderer branches and not a change to the live editor caret. Preserve each world’s assigned Block/Pill/Narrow silhouette, palette, font, baseline relationship inside the text run, and containment. Tune authored offsets from the large master, with size-aware rounding only where the canonical downsampler proves it necessary. **Done:** The four cursor bodies read vertically balanced with their wordmarks at 256/128/64/44/32/24 on light and dark Dock surfaces; Mopoke receives the stronger correction, the other three remain subtle, and every passing world is byte-identical. **Verify:** geometry report before/after; `l` containment and cursor/wordmark bbox laws; exact fourteen-world identity; full distance/crowding/stale-entry ratchets; canonical export/pack and item-20 dashboard rebuild; small-size affordance-locating vision smoke. **User review 2026-07-29.** ✅ **LANDED — merge `0fcecdd4`** (`fa7941eb`, `caf0a1cb`, plus `1e0fa8e7`/`30cc1c20` re-pinning the `app_icon/tests.rs` line exception and `ce051e0b` re-exporting the gallery sheets across both landed icon changes). Its rebase renamed the shas the note below records — `eb92434f`, `c3220a9b` and `feee5e11` are not in `main` and should not be looked for. Orchestrator re-verified the byte-identity claim directly at the merge: `assets/macos/world/` changes exactly Bilby, Currawong, Mopoke, Saltpan plus `Awl.icns`, and nothing else.

**Mechanism — a trap the brief did not anticipate.** Two of the four corrected faces are shared with a *passing* world at the *other* preset: Bitter is Mopoke (pill) **and** Magpie (block); Iosevka is Currawong (pill) **and** Cassowary (block). A flat per-face delta would therefore have moved Magpie and Cassowary, breaking this item's load-bearing requirement. `tuning.json` gained a per-`(family, assigned preset)` seat override, so a correction reaches exactly the worlds sharing both. No world name anywhere — the pre-existing `assertNoWorldKeys` law still rejects one. Absent a `presets` key, or an entry for the preset in play, it composes to exactly zero, so the default is genuinely inert. Each correction is a symmetric `insetTop`/`insetBottom` swing: a pure vertical translation preserving the silhouette exactly.

**Geometry, 256px master (top-lead → bottom-lead asymmetry):** Mopoke 19 → 0 (the clear correction); Bilby 28 → 4; Saltpan 8 → −11; Currawong 11 → 8 (very slight, as the user directed). Full per-size ladder committed at `assets/macos/candidates/geometry.txt`.

**Byte-identity verified by the orchestrator, not taken on report:** `git diff --stat main` over `world/*.icns` shows exactly Bilby, Currawong, Mopoke, Saltpan plus `Awl.icns` (which mirrors Saltpan, the default world); the shipped-world dashboards likewise show exactly those four. Magpie and Cassowary are untouched in everything that ships. Their *non-shipped* exploratory pill candidates do move — a pre-existing structural consequence of family-level tuning that predates this item, disclosed rather than hidden.

**Re-blessed crowding margins, argued rather than recorded.** Eight already-blessed near-pair margins eroded past the 0.05% ratchet slack. Empirically re-measured after the fix: the differing-pixel floor (Currawong/Cassowary 13.09%), mean-channel floor (Currawong/Cassowary 20.75) and ink floor (Potoroo/Firetail 54.31%) are all **unmoved** — no re-blessed pair became a new champion on any axis; they shifted 0.1–1.6 points within their existing rank. Same argument item 110 used when Bilby's seat last moved.

**Roster counts checked** ahead of item 158's twentieth world: `THEMES: [Theme; 18]` and the preset-roster literal are array-length-typed *plus* an explicit `assert_eq!` against `THEMES.len()`, so a new world fails loudly rather than silently. Only cosmetic prose captions say "18"; worth a one-line sweep when 158 lands.

⚠️ **Live-only:** on a real Dock at retina density with OS font smoothing, in both appearances — Mopoke reads balanced rather than overcorrected, Bilby/Saltpan read as restrained, Currawong is imperceptible, and none of the four read "too high" at 16–24px, where a live compositor and headless capture antialias differently.

162. **Repair the pinned-Chromium screenshot exporter, then retry Kite and Firetail’s icon trial.** **Defect:** The canonical offline exporter hangs in `Page.captureScreenshot` before its first PNG, including an unchanged 32px control and a 16×16 run outside the sandbox. This blocks item 121’s Firetail comparison and item 132’s required Kite icon/dashboard proof even though Kite’s verified implementation commits (`673d2758`, no-slack repair `fc5337a9`) remain recoverable on isolated branches. **Build:** Reproduce the smallest hanging control, determine whether the failure belongs to the pinned browser/runtime, launch lifecycle, page readiness, GPU/backend, or capture call, and repair the canonical exporter rather than adding a one-off Kite path. Preserve the offline, deterministic, theme-derived pipeline; replacing the pinned browser is allowed only with versioned inputs and byte-stability proof. After the repair, regenerate the unchanged control first, then item 121’s A/B/C sheet and Kite’s full icon/dashboard pack. Rebase Kite’s verified branch onto current main, rerun its native/WebGPU/WebGL and visual gates, and retry integration; do not recover it by cherry-picking around failed proofs. **Done:** The exporter completes twice with identical assets, Firetail is reviewable, and Kite returns to main only with every required artifact and live motion review. **Verify:** named timeout diagnostics, orphan-process cleanup, two-run determinism, existing 18-world identity, full dashboard build, native + wasm. **User retry request 2026-07-29.** ✅ **EXPORTER REPAIRED — `472a8e6e`** (`9aba7832`, `e8cce5b5`, `eb37dfd6`). 121, 161 and 132 are unblocked. The Kite rebase/integration and Firetail's A/B/C sheet were deliberately NOT done in this dispatch and remain open under items 132 and 121.

**Surprise — the premise was wrong: the hang did not reproduce.** The unchanged exporter rendered all 592 PNGs in 35s, including the 32px control. Every candidate layer was eliminated with evidence (pinned browser, launch lifecycle, page readiness, GPU/backend, capture call), so the stalling layer cannot be named — the exporter was structurally incapable of recording it. That incapacity was the repo's, via two defects that each reproduce the reported signature: the browser's stderr was piped to a reader that never existed (the pinned Chromium writes 78 KB/8s when it has something to say, past the macOS pipe buffer — a browser blocked mid-write to stderr answers no CDP request again, `captureScreenshot` included, and the explanation is stuck in the unread pipe), and there was no timeout anywhere with `connect()` outside the `try/finally`, leaving unnamed headless browsers running. Thirteen abandoned `$TMPDIR` profiles corroborated the leak on disk. **The exporter can now no longer stall without naming the stage, and leaves no orphan process or profile.** Determinism: full export tree ×2 → `e6f0ca1b…` both runs; all 109 tracked assets and 18 `.icns` byte-identical after regenerate. Orchestrator verified independently that no Rust/Cargo file is touched (so the scripts-only gate set is legitimate, not a smaller tier hiding a failure) and that a full export left every tracked asset unmodified.

**Follow-ups found, now routed:** the `ps -o command=` truncation is recorded in `.orchestrator/README.md` as operational protocol (it is a technique, not work); the temp-directory leak is **item 168**, measured directly at 2,768 directories / 9.2 GB.

163. **Make Bowerbird’s slow organic drift perceptible without making it watchable.** **Defect:** Live user review reported “I literally don’t see any drift.” The shipped shader’s entire organic field moves only roughly 4–5px over its long ambient cycle, so the intended outcome—notice later that it moved—does not survive real viewing even though phase-motion laws pass. **Build:** First prove the live ambient scheduler advances Bowerbird under default settings and distinguishes a strength failure from Reduce Motion or `ambient_motion = false`. Then increase authored displacement and, only if necessary, give the layers subtly different trajectories while preserving the cut-paper composition, static page, cool value band, shared ambient clock, pause/freeze gates, no catch-up jump, and zero idle frames when gated. No bubbling, pulsing, spawning, dissolving, or Lava-like deformation. **Done:** A writer does not watch the collage travel, but a before/after glance over roughly a minute makes the change evident; the Room remains about 3/5. **Verify:** timestamped live release before/after, deterministic phase-delta pixel arithmetic, worst-phase value/hue/page bounds, seamless route, reduce-motion/ambient-off/focus gates, release perf, Bowerbird-neighbour vision smoke. **User-reported 2026-07-29.** ✅ **LANDED — `e38c029d`** (`e6c67216`, `39baa76d`, `d59c750a`). Gates on the rebased tree: 3154 passed, wasm L1+L2, code health clean, release bench no measurable regression.

**Diagnosis verdict — (b), amplitude, not scheduling.** `has_ambient_tick` already folds in `is_organic` and the pipeline already resolved a nonzero drift from the shared clock; proven with real pixels through the App's own `advance_lava` tick (39,000+ changed pixels after 167 ticks, confined to margins). The bug was `sin(drift)*5.0, cos(drift*0.73)*4.0` against a 156px cell — under 4% of one cell. Drift is now a fraction of cell size, so it tracks the world's own scale.

**Second defect, found only by sweeping the scale axis.** `organic_rgb` clamps `s` to a 32px floor, and the pure fraction yields 4.16px there — *below the original defect's own 5px ceiling*. A single-scale law would have shipped that green: the exact failure CLAUDE.md records at items 89/97/99/94. Fixed with `ORGANIC_DRIFT_MIN_{X,Y}_PX` under the fraction, swept over scale 32/60/156/400 through a literal `Background::Organic` value rather than Bowerbird's own constant, so a future Organic world cannot silently reintroduce it. Bowerbird ships only `scale_px = 156` today, so this guards the future case, not the present one.

**Perceptibility floor = 10px**, against measured sub-pixel noise (correlation reproduces analytic ground truth: 5px vs 5.0, 20px vs 20.3, 4px vs 4.16; two settled frames measure exactly 0px) and strictly above the old 5px ceiling.

**`AWL_WAVES_PHASE` knob added** — mirrors the shipped `AWL_LAVA`/`AWL_STARS_PHASE` idiom, inert unless set, needed because a headless `--screenshot` never ticks the clock so no mid-drift capture was reachable at all. It drives both consumers of the shared phase (Bowerbird *and* Bombora's waves) because there is one ambient clock, not one per world.

⚠️ **OPEN — live human check required, not harness-provable.** Does the new amplitude read as "notice later that it moved" (the target) or cross into watchable? Check: `--release`, Bowerbird, default settings, focused, real window; note 2–3 margin blobs, leave it ~a minute without watching, glance back. Then confirm the gates live — Reduce Motion / `ambient_motion = false` freezes solid, and ⌘-Tab away shows no ongoing redraw.

164. **Unify overlay selection timing so pointer switching cannot highlight only a shortcut.** **Defect:** In the user’s live Command palette screenshot, the pointer is on “Switch project…” while the full selected band remains on “Go to file…” and only the second row’s shortcut has switched ink. The secondary column reads the new logical selection immediately while the living selected-row band and primary ink still display the previous animated row, producing two simultaneous answers to “which command is selected?” **Build:** Give the band, primary label, shortcut/value/accessory ink, hit-test acceptance and sidecar one shared visual-selection transaction throughout pointer hover and keyboard/wheel movement. If the band moves between rows, every selected visual either travels with it or waits coherently; never snap one column ahead. Preserve immediate click acceptance of the row under the press and the stationary-pointer/reanchor protections. Audit every overlay kind and both Pane/Bars families because the split owner is shared. **Done:** At every intermediate frame exactly one row reads selected, and the row activated by a click is the one the pointer indicates. **Verify:** reproduce the exact first→second command hover; deterministic intermediate-frame pixel law across label/band/shortcut; pointer, keyboard and wheel sweeps; every overlay/accessory kind; mutation proof by restoring immediate secondary recolor; native, both conventions, wasm. **User-reported screenshot 2026-07-29.** ✅ **LANDED — `5118a7bc`** (`139dcb7c`, `4b52202f`, `ae9b1669`, `0b02921d`), rebased clean onto `284a0e77`. Verified on the integrated tree: 3161 passed, code health and wasm both exit 0 — the ledger collision item 120 created was checked, not assumed.

**Premise confirmed, and the owner was worse than reported.** `shape_overlay_right` coloured the shortcut column from the *logical* row while band and primary label rode the animated band — and `overlay_selected_display_line`'s own doc claimed the two "can never disagree", an invariant the living band had broken without the doc catching up. Two further consumers had the same split, both unreported by the user: the range-rail thumb ink, and under `Bars` the chord plate (wrong row *and* a static `y` while its bar glided). New owner `render/chrome/overlay_visual_sel.rs::resolve_visual_selection`, resolved once per overlay frame before shaping, now the only site in the crate running a band animator or reading the logical row for a rendering decision; both ends held by no-wildcard source laws.

**Semantics chosen: secondaries WAIT for the band**, not band-leads-instantly. The ink flip exists so a glyph on the fill stays legible, so flipping ink onto a row the fill has not reached is not merely incoherent — on an inverse-fill world it is *invisible*. Click acceptance deliberately stays outside the transaction: `overlay_row_at` remains pure static geometry, law-tested at every frame of a long glide.

⚠️ **Two deliberate deviations from this item's own text — user call, both argued in their commits.**
1. **"Exactly one row reads selected" is not achievable and was not enforced.** Measured across a real glide: `[1, 2, 1, 1, …]` — one frame lights two rows, because the stretching band majority-covers both and both therefore carry fill needing legible ink. Strict cardinality 1 and the authored living band are mutually exclusive. The law pins the *shape* instead (never more than the two rows a one-row stretch can reach; settles on exactly one). **The defect the user actually reported — band on one row, shortcut on another — is closed.** ✅ **RESOLVED by the user 2026-07-29: the straddle frame stays.** Their reasoning, and it is the right one: the straddle *is* the stretch — it is what the Morph effect is made of, not a flaw in it. Remove it and the band either teleports or one row goes unreadable while fill sits under it. Item 164's Done clause is therefore amended in practice: "exactly one row" was the wrong ask, and the shipped `[1, 2, 1, 1, …]` shape is correct. No follow-up item, and no band-choreography audition is owed on this account.
2. **The sidecar arm was built, then withdrawn** (`7180da4b`). On every capture the harness can produce the field is provably the constant `[window.sel_row]`, because the headless path is clockless — it would answer nothing a reader did not already have, while costing a schema bump and de-grandfathering ~1000 columns of long-line debt in a file already 427 lines over ceiling. Consistent with the Wagtail tripwire: the visual answer's oracle is pixels, not JSON. Cheap to reinstate if the field is wanted anyway.

**Coverage gaps stated rather than papered over:** keyboard/wheel sweeps are structurally vacuous at this seam (all three modalities set `overlay_selected`; the render path carries no input identity, so such a law would exercise byte-identical code — `hybrid_band_snap` already covers keyboard cadence into the band); no full `OverlayKind::ALL` sweep — both geometry families and both list families are covered, leaving the spell popup's `header_rows == 0` geometry untested, same shapers, low risk.

⚠️ **Live-only:** whether the ~110ms coherent wait *feels* right under a fast-moving pointer. The harness proves per-frame agreement, not feel.

165. **Share dependency build artifacts across worktrees with `sccache` as `RUSTC_WRAPPER`.** **Defect:** `[profile.dev.package."*"] opt-level = 3` buys the measured 531s→182s suite win but costs one optimized dependency rebuild per target dir, and every worktree pays it separately. On 2026-07-29 the fleet reached 70 GB across 49 worktrees — roughly 3 GB each, nearly all of it identical dependency output. `Cargo.toml` already names `sccache` as the intended escape hatch "if that bites"; it has never been installed or wired. It bit. **Build:** Install `sccache`, set it as `RUSTC_WRAPPER` where every worktree and CI inherits it, and bound the cache size explicitly. **Do not use a shared `CARGO_TARGET_DIR` instead:** cargo takes an exclusive lock on a target dir, which would serialize the concurrent worktree workers the fleet exists to enable, and 19 call sites across `capture.sh`, `smoke-menus.sh`, `package-macos.sh`, `bench.sh`, `capture-worlds.sh`, `release-profile-gate.sh`, and `live-probe.sh` hardcode `$ROOT/target/{debug,release}/awl`. **Done:** A fresh worktree's first build reuses dependency artifacts rather than rebuilding them, per-worktree `target/` shrinks materially, and no script, gate, or capture path changes. **Verify:** Cold-worktree build time and `target/` size before/after, with `sccache --show-stats` proving cache hits rather than inferring them; concurrent worktree builds still run in parallel; both conventions, wasm, and the capture harness still resolve their binaries. **Routing:** production tier. **User request 2026-07-29.** ✅ **LANDED — `d76eaaaa`, merge `d4cfad45`.** The pinned wrapper is reproducibly provisioned, bounded at 5 GiB, inherited by worktrees/CI, and measured at 109 real Rust hits / 183 misses (37.33%). The premise that sccache would shrink each active worktree’s `target/` was false: the measured target grew independently while the separate cache grew, because sccache reuses compiler results but Cargo must still materialize outputs. Item 166 plus prompt removal of merged worktrees remain the disk-space owners; no shared target directory was introduced. Native receipt, code-health, wasm 16/16, and real capture-path checks are green on the combined candidate.

166. **Make `scripts/sweep.sh` cover the worktrees the fleet actually uses.** **Defect:** `sweep.sh` runs `cargo sweep --recursive` over `$ROOT`, the repo root, and its header assumes worktrees live under `.claude/worktrees/` and `.agents/worktrees/` — which is why `--hidden` is required. But the fleet's worktrees are created in the SIBLING directory `<repo>-worktrees/`, outside `$ROOT` entirely, so the sweep has never once seen them. On 2026-07-29 it was structurally blind to 65 GB there while covering only the 8.9 GB under `.claude/worktrees/`; the disk reached 32 GB free and the fleet had to be reclaimed by hand. `cargo-sweep` is installed and the script exits 0, so the failure is silent — the same shape as the `--hidden` bug its own header warns about, one directory further out. **Build:** Derive the swept roots from `git worktree list` rather than assuming a layout, so every worktree is covered wherever the orchestrator puts it, and report reclaimed size per root. Keep the existing day-threshold and `DRY_RUN` interface. **Done:** One sweep reclaims stale artifacts from every registered worktree, and a worktree outside the repo root can no longer grow unbounded unnoticed. **Verify:** With a worktree deliberately outside `$ROOT`, assert its stale artifacts are swept and its fresh ones survive, and that the run names every root in `git worktree list`; prove non-vacuity by pointing the current script at the same tree and watching it reclaim nothing. **Routing:** production tier. **Found 2026-07-29 during a disk-full incident.** ✅ **LANDED — `12ba642d`, merge `a34a63d8`.** The combined-main shell gate named and swept the external registered worktree exactly once, retained fresh artifacts, honored `DRY_RUN`/`DAYS`, and the root-only mutation left the external stale artifact behind.

167. **Make a live macOS Awl process retain product identity outside the packaged app.** **Defect:** A bare development launch is the lowercase `awl` executable rather than `Awl.app`. Although the packaged bundle already sets `CFBundleName`/`CFBundleDisplayName` to `Awl` and carries `Awl.icns`, AppKit has no bundle metadata on the bare path: the top-left application menu therefore says `awl`, and Stage Manager shows the generic application icon. The runtime `setApplicationIconImage` adoption is sufficient for the Dock/app switcher but does not supply a real bundle identity to every macOS surface. **Build:** Diagnose each surface against both bare and packaged `--release` launches, then establish one supported development-launch path that presents the same capitalized product name and canonical/current-world icon wherever macOS can honor them. Prefer launching a locally assembled `Awl.app` through the existing canonical packaging metadata over fragile process-title spoofing; keep the actual CLI command and `Contents/MacOS/awl` executable lowercase. Do not weaken per-world settled icon adoption, signing/release separation, or Finder’s canonical static icon. **Done:** The menu bar reads `Awl`, Stage Manager shows an Awl icon rather than the generic blueprint tile, and packaged release behavior remains unchanged; if macOS fundamentally requires a bundle for one surface, the normal dev/run script uses that bundle and the bare-binary limitation is explicit rather than silently advertised as equivalent. **Verify:** live screenshot matrix for bare diagnostic versus supported development bundle versus packaged build across menu bar, Stage Manager, Dock, and Cmd-Tab; assert bundle name/display name/executable/icon metadata and canonical icon presence; change-world commit still updates only the settled running icon; no hover churn; native packaging gate. **User-reported 2026-07-29.** ✅ **LANDED — `88a9e4bc`** (`dab59d1a`, `afe6bbcf`). 3200 passed, health and wasm clean.

**Measured per-surface answer** (bare `--release` vs unregistered bundle vs `dev-app.sh` bundle): menu bar `awl` → `Awl` → `Awl`; Stage Manager **no icon** → **generic tile** → real icon; ⌘-Tab name follows the menu bar; ⌘-Tab/Dock icon shows the active world in all three, because that comes from the running app's own `setApplicationIconImage`, a different mechanism.

**The expected limitation held, and there was a second finding under it.** The menu bar and Stage Manager genuinely cannot be fixed on a bare binary — neither consults anything a running process can set — and the process-title spoof was not taken. But **assembling the bundle was not sufficient either**: a freshly built `.app` still drew the generic blueprint tile, the exact defect this item names. A locally built bundle in a build directory is not somewhere LaunchServices has looked; `lsregister -f` is what makes the icon real. A release never needs it because dragging out of the DMG registers the app — which is precisely why packaging alone *looked* sufficient, and why stopping at "assemble a bundle" would have shipped believing the surface was fixed.

**Deliverable:** `scripts/dev-app.sh`, assembling through `package-macos.sh` rather than writing its own metadata, so there is no second source of product identity. `verify_bundle_identity` is the one owner, called by assembly and by a new `--verify` gate, and now also asserts the bundled `Awl.icns` is byte-identical to the canonical one. The limitation is stated in CLAUDE.md and docs/platform.md rather than advertised as equivalent. Per-world adoption proved unweakened by breaking it (previewing all 18 worlds must touch the Dock zero times). The **wasm gate caught a real break** — the new laws read shell scripts off a real filesystem — fixed before landing.

⚠️ **Live-only:** `dev-app.sh` on a machine where the bundle was never registered (the author's Mac is now poisoned by its own successful run), Stage Manager after a world change, Dock hover/name (Dock was autohidden, verified via ⌘-Tab instead), and a real signed release dragged to `/Applications`. Deliberately sequenced rather than parallel: both items touch macOS chrome and the packaging metadata, and the 160 owner already holds the AppKit/bundle context. **Orchestrator note:** the cause is confirmed, so the open question is only which surfaces macOS will honor without a bundle. Expect the answer to be "the menu bar and Stage Manager cannot be fixed on the bare binary" — in that case the deliverable is the supported `Awl.app` dev-launch path plus an explicit statement of the bare-binary limitation, NOT a process-title spoof. Do not weaken per-world settled icon adoption to get there.

168. **Make the test suite clean up after itself — it is leaking 9.2 GB of scratch directories.** **Defect:** `$TMPDIR` holds **2,768** `awl-*` directories totalling **9.2 GB**, from at least eight distinct fixtures that create scratch state and never remove it: `awl-test-config-sandbox` (598), `awl-frost-pixels-headed` (471), `awl-frost-pixels-controls` (471), `awl-bullet72-pixels-main` (369), `awl-spawn-config-law-decoy` (304), `awl-sidecar-metric-scale` (214), `awl-item126-pixels` (47), `awl-launch-ctx-dot` (46). Separately, a `.unnamed.awl-tmp` is sitting in the repo working tree: `fs::write_atomic` writes `.<name>.awl-tmp` then renames, and `tests/fault_kill9.rs` deliberately `SIGKILL`s inside that pre-rename window — the killed process cannot clean up by construction, so the harness must, and does not. This is the same disk-pressure story as items 165/166 arriving from a third direction: 166 found the sweep blind to sibling worktrees, 165 found dependency artifacts duplicated per worktree, and this is the suite itself. Every full run makes it worse, and a repo-root artifact also dirties `git status` for the next reader. **Build:** Give scratch state one owner that removes it on drop, so cleanup survives an early `?`, a panic, and a failing assertion — not an explicit remove at the end of a happy path. Enroll every fixture through it with a no-wildcard law, so a new fixture cannot opt out silently. The fault-injection tests are the deliberate exception: their child is killed and cannot clean up, so ownership belongs to the parent harness, and that exception is named rather than implicit. **Done:** A full suite run under both conventions leaves `$TMPDIR` and the working tree as it found them; `git status` is clean after `cargo test`. **Verify:** Count `awl-*` entries in `$TMPDIR` and `git status --porcelain` before and after a full run under both conventions and assert both are unchanged; prove non-vacuity by removing one fixture's cleanup and watching the law name it; confirm a panicking and an early-returning test both still clean up. **Found 2026-07-29 during the item 162 exporter repair, then measured directly.** ✅ **LANDED — `1678d5dd`** (`593a31e1`, `d0861cd4`, `03810e76`, `255e2cd3`, `1686f598`). Gates: full suite under both conventions, wasm, code health.

**Measurement made honest rather than approximate.** `$TMPDIR` held 2,897 `awl-*` entries during the work, but that number is unattributable with other agents running — so each full-suite run used a private empty `TMPDIR`, invisible to concurrent agents. Result **0 → 0** under both conventions, with `git status --porcelain` clean before and after.

**The guard:** `src/testscratch.rs`'s `ScratchDir` removes on `Drop`, so cleanup survives a panic and an early return — both proven by direct self-tests, not argued. Because the crate ships no `[lib]` target, `tests/*.rs` cannot reach `src/`'s `cfg(test)` internals, so `tests/common/mod.rs` carries a documented duplicate plus a `claim` variant that is safe against the cross-process race in `hermetic_canary`.

**The fault-injection exception is named in three places** and does not opt out of either law: the child is SIGKILLed inside `write_atomic`'s pre-rename window and cannot clean up, so the parent's guard removes the orphan when its test returns.

**Mutation proof worth keeping:** reverting one fixture to the old idiom made both no-wildcard laws name that file — while the fixture's own test stayed green. Exactly the "a green test hides a real defect" case the laws exist for.

**Residual, allowlisted with named reasons rather than hidden:** `probe.rs` flight-recorder logs and two fixed-name files still leak loose *files* (never directories). One adjacent file leak was fixed in passing (`main/tests.rs` sidecars). The pre-existing backlog was deliberately not deleted — the deliverable is that a fresh run adds nothing.

169. **Give concurrent worktree builds an explicit shared CPU budget instead of letting every Cargo process seize all ten cores.** **Defect:** Each worktree runs an independent Cargo process, and Cargo’s jobserver coordinates only the rustc children of that one process. The repo sets neither `build.jobs` nor `CARGO_BUILD_JOBS`, so four concurrent workers can each schedule against all ten logical cores, causing severe contention and making the editor and desktop unresponsive. Item 165’s `sccache` reduces duplicate compilation but does not limit parallelism. **Build:** Put concurrency policy in the orchestration-owned worker launch/build seam, not `.cargo/config.toml`: default every concurrently dispatched worker to `CARGO_BUILD_JOBS=2` on this ten-core host, preserve an unrestricted/full-machine build for the root’s isolated final merge-train gate, and leave roughly two cores available for the interactive session. One owner must set and report the effective budget; individual workers and scripts must not invent competing limits. Keep CI hardware-adaptive and do not globally slow a developer’s lone `cargo build`. Start with this deterministic policy rather than a dynamic semaphore; add slot allocation only if measurements show the fixed split wastes material time. **Done:** Four simultaneous worker builds never schedule more than eight Cargo jobs in aggregate, an isolated final gate can use the full machine, and Awl/the desktop remain responsive during the fleet. **Verify:** Instrument four clean-worktree builds and record effective Cargo job counts, aggregate CPU, wall time, and UI responsiveness before/after; prove an isolated build is not capped; confirm native, both conventions, wasm, and scripts inherit the intended worker budget without changing gate truth. **Separate from but compatible with item 165; user decision 2026-07-29.** ✅ **LANDED — `5f7d894c`, merge `a3f54049`.** Four clean worktrees measured 211 s / 756.3% mean CPU / 1000.8% peak uncapped versus 208 s / 540.9% mean / 915.5% peak at two jobs each; the scheduler pulse probe stayed at 0 ms in both arms. The wrapper law proves four workers cap at eight aggregate jobs while the isolated root gate remains unrestricted. Canonical receipt and wasm passed on the combined landing train.

170. **Make “full native suite” a machine-issued gate receipt, not an agent’s interpretation of a Cargo test count.** **Defect:** The orchestrator repeatedly reported roughly 3,200 tests as the “full native suite” after running `cargo test --bin awl`; that command covers the binary’s unit tests but excludes every integration target under `tests/`. A failing integration test exposed the scope mismatch. Workers happened to run bare `cargo test`, but the root’s narrower command was still described as the stronger tier—exactly the prohibited claim inflation the board currently guards only in prose. **Build:** Add one canonical native-gate script used by local workers, the merge train, and CI. It runs unfiltered bare `cargo test` under both supported conventions, rejects/does not expose target-selection or name-filter arguments, and emits a success receipt containing the exact commit, both conventions, and `scope=all-targets` only after both runs complete. First run a named integration-only canary so disappearance of integration discovery fails loudly. Extend the external structural audit—not a Rust integration test that `--bin` can exclude—to pin that the script’s two suite commands remain unfiltered and CI calls the script rather than reconstructing it. Update orchestration language: only a receipt authorizes “full native suite”; `--bin` is “binary unit tests,” and every filtered run is “targeted tests.” Counts never prove scope. **Done:** No agent can obtain or honestly quote the full-suite receipt from `cargo test --bin awl`, a filtered command, one convention, or a stale commit; local and CI gate definitions are identical. **Verify:** Mutation proof for `--bin`, omitted second convention, skipped canary, stale SHA, and CI bypass; each must fail by name before tests are restored. **Prompted by the item 167 landing correction, 2026-07-29.** ✅ **LANDED — `474c1a8`, `69cc04d`, merge `5f6096e2`.** Machine receipt: `commit=5f6096e217fdb06bf5e9065d53bc39ea4bffc066 conventions=mac,linux scope=all-targets`; code-health and wasm L1/L1.5/L2 16/16 green.

171. **Make action handling return typed effects, with separate live and headless interpreters.** **Defect:** `apply_core` is the shared editing seam, but side effects still leak around it: search-query input differs between live and replay, and replaying Save during a capture writes the real file. This makes the verification path both behaviorally incomplete and unexpectedly mutating. **Build:** Define one closed effect vocabulary for persistence, clipboard, buffer switching, daemon notification, notices, and redraw/reshape requests. Core action handling may describe these effects but may not perform platform or filesystem work. Give the live App and headless replay explicit interpreters; the headless interpreter records or rejects external effects and never writes user files unless a test supplies an isolated filesystem owner. Preserve OS-only paging and clipboard constraints without duplicating editing rules. Route every action through the same query/edit transition before interpretation, and remove the documented live/replay gaps rather than adding adapters around them. **Done:** An action has one state transition and one typed effect result; live behavior remains unchanged; captures and ordinary replay are non-mutating by construction. **Verify:** Exhaustive no-wildcard `Action × Effect` routing law; query-input parity; save/open/finish-buffer mutation traps; clipboard and daemon live-only laws; `--keys` sidecar parity; both conventions, full native, wasm. Mutation proof must make the old direct save path fail by name. **Routing:** deep owner (`gpt-5.6-sol` high) because this changes the editor’s central ownership seam; independent ownership audit at the same tier. **User-requested code-health work 2026-07-29.** ✅ **LANDED — `be941bdb`, merge `f6bd95e1`.** The combined train passed web/wasm 16/16 and the canonical macOS+Linux all-targets gate; integration also repaired and law-tested a parallel spellcheck-state leak exposed by the macOS About command-roster sweep.

172. **Decompose the 107-field `App` into owned state domains with narrow transition APIs.** **Defect:** Physical file splits have kept individual implementation areas navigable, but more than twenty modules still extend `impl App` and can reach the whole live application state. Invariants for documents, input, workspaces, rendering, scheduling, and persistence therefore remain coupled by convention. **Build:** After item 171 establishes the effect boundary, migrate fields and their invariants incrementally into explicit owners: `DocumentSession`, `InputState`, `WorkspaceState`, `RenderRuntime`, `FrameScheduler`, and `PersistenceRuntime` (names may change if the ownership map proves a better cut). Each owner exposes domain transitions rather than public fields; cross-domain work travels through typed outcomes/effects, not back-references to `App`. Preserve the active-buffer whole-slot ownership law, fake-clock determinism, wasm gating, GPU recovery, and byte-identical behavior. Do not introduce a service locator, trait-per-method architecture, message bus, or flag-day rewrite; land coherent vertical slices with compile-time removal of the old field access. **Done:** `App` is lifecycle composition rather than the mutable home of every subsystem, and new workspace or persistence behavior has one obvious owner. **Verify:** First commit an ownership map and call-site census; add structural gates against direct cross-domain field access and growth of root `App`; run focused state/cache identity laws after each slice, then both conventions, full native, wasm, and the live scheduling/GPU probes. **Depends on 171 (landed). Routing:** deep owner plus independent ownership audit. **User-requested code-health work 2026-07-29.** 🟢 **TWO SLICES LANDED — merge `4821c63a`** (`73db7850` map/census, `ec94c743` WorkspaceState, `4399ebf0` PersistenceRuntime, `d56e8bb7` ratchets). **The item remains OPEN**: four domains are mapped and gated but not extracted.

**The ownership map is a deliverable in its own right** — `docs/app-domains.md`, with the same table as executable data in `src/app/tests/domains.rs`. 1,310 production and 586 test references classified, exhaustive by construction (no field in two owners, none unassigned). Remaining, with production references: `DocumentSession` 4 fields/363 refs, `RenderRuntime` 25/277 (**held for item 174**), `InputState` 27/164, `FrameScheduler` 12/123, `ProjectLocation` 9/51, and 22 host/lifecycle fields that stay on `App`. `config` (88 refs, 16 files, one writer) is the best remaining single-field slice.

**The map's own argument against the obvious next cut, worth keeping:** `gpu`'s 160 references across 23 files — the most dispersed field in the struct — are dominated by `gpu.window.request_redraw()`, which is a *scheduling verb wearing a render field*. Taking `RenderRuntime` first would pull 22 files into item 174's blast radius for no invariant. Relatedly, the item's `RenderRuntime`/`FrameScheduler` boundary is **not a real boundary**: for `theme_font_at`, `theme_switch_at`, `theme_settle`, `crossing_settle_at` the *stamp* is scheduling and the *effect* is rendering. The map assigns them to `RenderRuntime` and says so rather than pretending the line is clean; item 174 should move them and record the decision in the classification gate.

**Also worth knowing: `InputState` is the item's largest named owner and its lowest-value one.** 14 of its 27 fields are touched by exactly one file and `app/input/` accounts for 135 of 164 references; extracting it satisfies the item's letter and buys a struct rename. Its only genuine cross-domain leak is `cursor_px`, read at two sites in `apply.rs` — cheaper to pass the position than to move 27 fields. The map argues it should probably never be extracted as a 27-field struct.

⚠️ **THE ITEM NAMED TWO DIFFERENT DOMAINS `WorkspaceState`, and they share no fields.** This item lists it beside `DocumentSession`/`PersistenceRuntime` and closes on "new **workspace** or **persistence** behavior", which reads as the project-folder domain; item 173 says "in item 172's `WorkspaceState`, define one closed lifecycle for editor, brief contextual overlay, sustained summoned workspace, and suspended child audition", which is the summoned-UI domain. **The name went to 173's meaning** — 173 is the downstream consumer and the critical path to 114 — and the project-folder domain is now `ProjectLocation`, with `App::workspace` renamed `workspace_root` so the two cannot be misread for each other. **This item's Done clause is therefore satisfied for persistence and NOT YET for project-folder behavior.** Recorded rather than quietly counted as met.

**The one place byte-identity was consciously chosen over consistency** is `sync_cursor_icon`'s raw `popover_summon_bit()` read: documented, single call site, and law-counted so a second consumer fails by name. A byte-identity refactor preserves pre-existing bugs, so if a second pair of eyes is spent anywhere on this branch, that is the spot.

173. **Establish a typed summoned-UI lifecycle before Settings becomes the first workspace.** **Defect:** Item 114 needs editor return-state preservation, wide/narrow stages, focus/back behavior, and suspension into Theme/Caret auditions. Encoding those as more `App` booleans and exceptional `Esc` branches would make Settings and History parallel state machines before their shared boundary exists. **Build:** In item 172’s `WorkspaceState`, define one closed lifecycle for editor, brief contextual overlay, sustained summoned workspace, and suspended child audition. Model entry, focus transfer, narrow-stage push/back, child suspend/return, commit/revert, and exit as typed transitions carrying the exact editor/workspace state that must survive. Keep feature payloads typed: this is shared lifecycle machinery for Settings and Version History, not a generic route stack, tab system, dormant shell, or UI framework. Migrate the existing Theme/Caret audition journey through the lifecycle without changing its fast editor-backed presentation, then make item 114 consume it rather than creating a second owner. **Done:** Invalid layer combinations are unrepresentable; every `Esc`/Back outcome has one transition; Settings and later History can supply content without owning navigation preservation. **Verify:** Exhaustive state × event transition table with no wildcard; enter/exit byte-identity; wide/narrow focus and back laws; child commit/revert and position restoration; action/replay parity; both conventions, full native, wasm. Mutation proof must reintroduce one parallel flag path and make the structural law fail. **Routing:** deep owner, then production-tier implementation/audit for item 114. **User-requested code-health work 2026-07-29.** 🟢 **UNBLOCKED 2026-07-30 — both dependencies are landed** (171 via `f6bd95e1`; the `WorkspaceState` slice via `4821c63a`). **Unclaimed, and it is now the top of the critical path to 114 → 116/131.** Start here: `src/app/workspace.rs` already owns a closed `Layer` ladder (`Overlay > Search > Popover > Editor`) as a wildcard-free enum over all eight cells of its three-fact space, with seven named write doors (`dismiss_pickers`, `close_search`, `summon_popover`, `dismiss_popover`, `overlay_mut`, `search_mut`, `core_slots`). **`Layer` was deliberately made an enum so this item's fourth rung — the sustained summoned workspace — is a compile error in every match until it is placed.** Extend that ladder rather than introducing a second owner, and note that item 172 gave the name `WorkspaceState` to *this* item's meaning: the project-folder domain is `ProjectLocation` and `App::workspace` is now `workspace_root`.

174. **Separate pure render planning from shaping/cache mechanics and GPU execution.** **Defect:** `TextPipeline` and the render directory jointly own scene policy, document geometry, cache invalidation, hit-test inputs, sidecar-visible facts, GPU resources, and feature-specific drawing. Tests often have to infer planned geometry from pixels, while render-touching work can accidentally couple presentation rules to device state. **Build:** Introduce one deterministic scene/layout planner that consumes `ViewState`, measured text inputs, theme capabilities, and viewport data and emits inspectable primitives plus interaction geometry. Shaping and cache ownership remain a distinct measured stage; GPU execution consumes the plan without deciding feature layout. Route drawing, hit-testing, and sidecar geometry through the same planned objects, migrating one coherent surface family at a time. Preserve O(visible) frame work, buffer-identity cache keys, rowlayout ownership, deterministic capture, and exact output for migrated surfaces. Do not build a retained widget tree, general scene framework, duplicate CPU renderer, or allocate an entire document plan each frame. **Done:** presentation decisions are testable without a device; GPU code executes rather than invents layout; drawn and interactive geometry cannot drift through parallel calculations. **Verify:** Plan-level geometry laws, drawn↔hit-test↔sidecar identity, buffer-swap/resize/zoom invalidation, allocation and reshape-count witnesses, exact before/after capture probes across representative worlds and surfaces, release frame benchmarks, both conventions, full native, wasm/WebGL. Every render slice gets the standing vision smoke. **Independent of 171–173; schedule away from item 114’s overlapping render files. Routing:** deep owner (`gpt-5.6-sol` high) with production-tier outcome audit. **User-requested code-health work 2026-07-29.** 🟢 **FIRST FAMILY LANDED — branch `claude/item-174-render-plan`** (`a10944a8`, `5d97e140`, `3ef6d85d`, `15e3eede`, `a1674d1e`, `629eb937`). **The item remains OPEN**: one surface family is migrated, the rest still own their geometry.

**`src/render/plan/` is device-free** — shapes nothing, measures nothing, reads no clock — and `plan_overlay_rows` emits one `PlannedRow` per candidate display line plus the interaction geometry. **The forward `row -> y` arithmetic and its inverse are module-private, and `overlay_row_top`/`overlay_row_of`/`overlay_row_index` are DELETED from `render/chrome`: the bypass is compiler-enforced, not grep-enforced.** Routed through the one plan: hit-test, range rails (draw and hit), the visual-selection band target and coverage grid, Pane band, Bars plates, chord plates, footer plate, slant clip bands, shaped-row item mapping, and `overlay_window_report`. Item 164's arrangement is preserved and re-pointed, not weakened — the planner is now the sole owner of `selected_display()` and `overlay_row_at` deliberately stays outside the transaction.

**Why this family, from the census rather than the brief:** `overlay_row_top` was a free function taking five loose scalars, re-assembled at **eight** call sites, and `overlay_draw.rs` then abandoned it and re-derived `first_top + k*lh` inline. "How many candidate lines does this card have" was written out **five** times, and the fifth added `+ empty.is_some()` in only one arm — a live defect. "Which line is logically selected" had item 164's owner **and a second, differently-clamped copy inside the test y-probe**, i.e. the oracle itself was a parallel calculation.

**Identity: 470 capture probes** (19 worlds × 10 overlay surfaces, plus 8 geometry axes over 7 worlds). **34 PNGs differ and every one is the deliberate fix; zero other PNG or sidecar bytes changed**, re-verified three times, with after-vs-after 0/470.

**The deliberate output change is a real defect the migration exposed.** `content_rows` omitted the empty-state notice line the card height had already paid for, so a `Bars` world's picker filtered to zero matches drew its **footer plate over the "no matches" row**. The 34 differing cells are exactly the two zero-match states on the five shipping Bars worlds plus geometry variants — measured on Galah as 19,295 px in one band `y 242..300`, one row height, nothing else.

**Two things mutation proof found that reading did not.** (a) `livingband::covered_rows` was stepping its own `first_top + k*lh` grid one module over from the plate with the same bug; the first source law's sentinels could not see a consumer *stepping off the band origin*, and adding that sentinel lit it up immediately. (b) **The device law's `sel_row` arm was tautological** — sidecar and plan both read `selected_display()`, so a planner that forgets grouped headers kept them in perfect agreement while pointing at the wrong row, and it was *watched staying green*. An independent oracle (the reported line must carry `overlay_selected`'s item) made it fail by name. This is the strongest instance yet of the repo's own lesson: agreement between two readers of one function proves nothing.

**Witnesses:** `plans_per_frame = 1` and `plan_rows_per_plan = 12` for a **106-item** palette — one `Vec<PlannedRow>`, 384 bytes per overlay frame, with the bench asserting *exactly* one plan (zero means measuring nothing, more than one means a consumer grew its own). O(visible) proven at 200,000 items. Reshape counts unchanged.

⚠️ **Two measurement honesty notes.** Release frames showed palette cells at median +8.1%, but the **untouched** cells moved median −0.2% across a −7.0%…+22.5% range on the same run — the whole S tier moved on every scenario including ones this change cannot touch, with five workers building concurrently. 0.17 ms is implausible for 384 bytes plus a ≤24-row scan, so the honest reading is *no palette-specific signal, confirmation owed on a quiet host*. Relatedly the **bench baseline was deliberately not re-banked**: re-banking tonight would freeze contention noise into every cell.

**Left for later slices, stated rather than implied:** document-content surfaces, search panel, HUD, gutter, outline, popover, whichkey and readout still own their geometry; the spell popup's anchoring is untouched (its rows are planned, its anchor is not); `overlay_secondary_top`/`overlay_split_bounds`/`overlay_strip_band` remain separate owners and folding the strip band plus secondary column in is the natural next slice; **no sidecar schema change** — publishing planned row rects would let a test assert row geometry with no device at all, but that is a schema bump plus a CAPTURE.md edit.

**Two premise corrections worth keeping.** (a) The brief said overlay rows "already have `render/rowlayout` as a partial owner" — `rowlayout` owns *horizontal* cell layout (primary/secondary budget, elision, rails) and had nothing to do with the vertical row-Y duplication where all the drift lived. They are orthogonal owners and the planner correctly did not subsume it. (b) The theme picker **retired its lens strip** (user decision 2026-07-15) and is now flat, so the grouped/sections path is exercised by the **command palette** under a non-All lens. A brief reaching for the theme picker to test grouped behaviour would have tested a flat card.

**Follow-up routed:** item 181.

175. **Make Paperbark’s deckled texture fixed Room wallpaper that the page covers and reveals.** **Defect:** In the current Paperbark candidate, changing the page width makes the deckled strata appear attached to the moving page border. The material therefore behaves like border decoration even though its intended read is static wallpaper behind an opaque sheet. **Build:** Sample Paperbark’s deckled field in stable Room/viewport coordinates, independent of page bounds. The writing page remains an opaque mask above it: dragging either page edge covers existing wallpaper or reveals the already-existing field, without translating, stretching, reseeding, or reflowing the contours. Keep the background entirely static—no clock, drift, typing response, or animation—and preserve the approved palette, density, weave, page exclusion, and Paperbark-only theme assignment. Diagnose whether the shared Deckle coordinate owner can express this as theme-owned anchoring data; do not add a Paperbark-name shader branch or change Galah’s planned Fibres behavior accidentally. **Done:** A fixed screen point in the Room has the same wallpaper pixel before and after page-width dragging whenever that point is exposed in both states; only the page mask changes. Window resizing remains visually coherent and deterministic without making the texture cling to the new page edge. **Verify:** Capture a page-width sweep at fixed viewport size and assert pixel identity over the intersection of exposed Frame regions; mutation proof restores page-relative sampling and makes the law fail by name. Add drag-trajectory captures, 1×/2× and narrow/wide probes, page-exclusion and no-motion laws, Paperbark Room vision smoke, exact non-assigned-world identity, native and wasm gates. **Depends on item 158’s Paperbark branch landing; coordinate with item 159’s shared Deckle use. Routing:** production tier (`gpt-5.6-terra` medium). **User design decision 2026-07-29.** ✅ **LANDED — `276f5a99`, repair `ec1af3d8`, merge `e369c99e`.** `DeckleAnchor` owns viewport versus page sampling without a world-name shader branch; the exhaustive Weave × anchor law caught and repaired a Fibres-mode regression. The fixed-point page-width law, 11 focused GPU tests, deterministic trajectory artifact, release frame time (0.067 ms), wasm 16/16, and combined canonical native receipt are green.

176. **Restore Bowerbird’s three-object collected-treasure grammar with crisp geometry.** **Problem:** The current rounded masses/islands/holes read as hazy camouflage, but the original compositional idea was right: each loose cell assembled a deterministic pseudo-random stack of three related objects—a large anchor, a smaller offset companion, and a tiny cut-out or accent. The replacement must clarify that grammar, not replace it with unrelated wallpaper compositions. **Build:** Keep Bowerbird’s generous navy ground and scattered cluster rhythm. Replace each organic stack with a crisp circle/square/triangle mix whose scale hierarchy, offset, rotation, overlap, and color assignment vary deterministically from cluster to cluster. Every cluster still reads as one deliberately arranged collection of three; avoid a uniform grid, quilt, broad Bauhaus scatter, isolated constellation, camouflage masses, and unstructured tumbling pieces. Preserve the existing slow whole-field drift without morphing, spawning, dissolving, or independently animating objects inside a cluster. Discard the five-direction O/A/B/C/D/E audition and any trial selector or rejected arms already built; produce one focused comparison between the shipped organic control and the crisp three-object revival at wide and narrow page widths, then stop for user confirmation before shipping. **Verify:** all visible clusters have the three-role hierarchy; deterministic variation does not repeat conspicuously; page exclusion, empty-ground balance, worst-phase value/hue, drift/freeze gates, 1×/2× edge quality, release perf, exact non-Bowerbird identity, and vision questions that can identify the clustered found-object composition without being told. **User clarification 2026-07-30.** 🟡 **RECLAIMED 2026-07-30 — item-176-bowerbird-revival (claude), branch `claude/item-176-bowerbird-revival`.**

**Takeover note.** The `codex/item-176-bowerbird-geometry` claim went stale: its last commit is `a5c79f7a` at 2026-07-30 00:05, with no branch or worktree activity in the ~23 hours since, and the user confirmed the tool is not running. **The stale branch is not a foundation and must not be built on.** Its one work commit is the five-direction O/A/B/C/D/E audition (`shaders/background.wgsl` profiles 1..5 behind a `params.z` selector, plus `scripts/capture-bowerbird-geometry-trial.sh`) — which is exactly what this item's own clarified text orders discarded. Read it for reference only; **profile 2, "B: Collected Objects — one anchor plus arranged finds", is the closest existing expression of the three-object grammar** and is the one piece worth salvaging as math, not as a shipped arm. Start fresh from current `main`.

**Sequencing:** item 132 (Kite) concurrently adds a new warped-grid capability to the same `shaders/background.wgsl`. The two are additive in different functions, but the new owner touches only Bowerbird's own cluster/organic paths, restructures no shared shader scaffolding, and should expect to rebase; integrate one at a time.

**This item ends by stopping.** It produces one focused comparison between the shipped organic control and the crisp three-object revival at wide and narrow page widths, and then waits — nothing ships until the user confirms, and no trial selector survives into the product.

177. **Make low disk space trigger one serialized, recoverable build preflight instead of failing mid-gate.** **Defect:** On 2026-07-30 the canonical item-159 native gate failed while writing a temporary PNG with `No space left on device` (117 MiB free). Item 166 correctly discovers every worktree, but its ordinary seven-day age threshold is intentionally conservative; a dry-run at one day found 21.19 GiB reclaimable in registered Cargo targets, and the audited one-day sweep restored 16 GiB. Coverage alone therefore does not keep a busy fleet from crossing the hard floor between scheduled sweeps. **Build:** Add one orchestration-owned, serialized disk preflight used by concurrent worker builds and the root’s canonical native gate. Read available bytes on the repository filesystem before compilation; above a named healthy floor do nothing, while below it acquire one cross-process lock, re-check, and invoke the existing `scripts/sweep.sh 1` owner exactly once. Never remove source, untracked user files, Cargo caches, current worktree registration, or invent a second target traversal. Concurrent callers wait for/reuse the first recovery rather than launching overlapping sweeps. If the audited sweep cannot restore a smaller named minimum, fail early with a receipt naming free bytes and the attempted recovery instead of letting a later test produce an unrelated I/O error. Keep CI and wasm portability explicit. **Done:** A fleet starting near the floor either recovers enough space before Cargo starts or stops with one truthful disk-preflight error; a healthy disk adds negligible work; four simultaneous workers produce one recovery. **Verify:** external shell test with a fake byte oracle and fake sweep covers healthy, low-space recovery, post-sweep insufficiency, lock contention, and one-owner call count; real dry-run names every registered worktree; mutation proof removes the re-check and makes the concurrency law fail; code health, native receipt, wasm. **Routing:** production tier (`gpt-5.6-terra` medium). **Found by the item-159 canonical gate on 2026-07-30.** ✅ **LANDED — `61d0ea7a`, merge `2358b08a`.** The serialized preflight recovered enough space for the final combined train, which passed web/wasm 16/16 and the canonical macOS+Linux all-targets gate.

## Timed — not blocked

**Final product task before release preparation:** item 24. Write the welcome/tutorial
only after the editor, worlds, Settings, and taught interactions have settled.

24. **Make the file opened on first launch serve as both Awl’s welcome and its tutorial, then refresh the surrounding user-facing docs.** **Build:** Write one real Markdown file that opens as the first-run document and teaches Awl by being edited in Awl. It welcomes the user, establishes that Awl creates ordinary Markdown files, and introduces the essential writing experience through the document itself: WYSIWYG reveal at the caret, native keyboard shortcuts, the command palette, pointer-accessible contextual formatting, changing worlds, and where files are saved. Keep it useful as readable prose rather than turning it into a feature checklist or a sequence of modal lessons. No separate onboarding wizard, coach marks, account step, or second tour surface. Returning users resume their own work rather than seeing the welcome again. Update GUIDE and the site guide to agree with the finished product and this first-run path; matter-of-fact voice, facts verified. **Done:** A new user opens Awl directly into a beautiful editable document, understands what the editor is and how to begin, and can dismiss, replace, or save the file without special state leaking into later sessions. **Verify:** Fresh-profile first launch, second launch/session restore, edit/save/replace/close behavior, every taught chord through the real keymap on supported platforms, links between welcome/GUIDE/site claims and implemented behavior, and representative first-run captures. Site deployment remains separately user-gated. **Timed after the current user-visible wave and before release preparation; user decision 2026-07-29.**

118. **Pre-release world-loudness audit — repeat the 1–5 idle Room/Frame check before release.** **Audit definition:** “idle loudness” is how strongly a world asks for attention while the user is simply writing in page mode: palette, typography, margin pattern, and ambient motion count; summoned overlays do not. `1/5` is the quiet pole (Wagtail), `3/5` is recognizable/alive but comfortable for hours, and `5/5` is a deliberately rare statement world (Firetail). **Baseline from the 2026-07-26 design session, eighteen shipping worlds:** `1/5: 1`, `2/5: 10`, `3/5: 2`, `4/5: 4`, `5/5: 1` (mean 2.67). Bowerbird item 117 intentionally changes that to `1, 9, 3, 4, 1` (mean 2.72). **Direction for a twenty-world roster:** hover around 3 with awl’s calm bias; the provisional healthy shape is `1, 7, 7, 4, 1` (mean 2.85), not a symmetric theme-park bell curve. The current gap is the middle, not more 5s. This is a diagnostic distribution, never permission to turn up a world merely to fill a bin—each world still earns its own identity. Bilby/Brolga/Tawny/Saltpan are valuable quiet anchors; Galah and Mulga are candidates to inspect, not pre-decided promotions. **Run:** Immediately before release preparation, review every shipping world’s current Room in item 20’s dashboard at representative page widths, then observe every ambient world live in `--release`; independently assign 1–5 before looking at the baseline, reconcile the roster together, and record any chosen changes as separate concrete queue items. Include any nineteenth/twentieth worlds that have graduated by then; do not graduate Cassowary Light or any other candidate merely to hit twenty. **Done:** The final roster has a user-confirmed loudness map, its mean/distribution and outliers are explicit, near-duplicate intensity poles are named, and every proposed rebalance is either rejected on purpose or queued with a world-specific reason. **Verify:** Affordance-locating vision smoke over all Room captures; live-only confirmation of speed/calmness for Lava, Currawong stars, Bombora waves, Bowerbird cutouts, and any later ambient world. Pixel/sidecar arithmetic may prove territory and contrast but never claims the taste score. **Timed before release preparation; user design decision 2026-07-26.**

## Parked — explicit gate or future design

- **Export save-dialog scope:** macOS + Linux, one live-only cross-platform seam; capture uses an explicit path. Decided, not scheduled.
- **Per-world living-band choreography:** audition TwoShape/Slam/Soft against Morph; live feel is the oracle. Needs a design session.
- **Per-world copy-pulse differentiation:** possible future motion tweak; needs a design session.
- **Site deployment:** only on the user’s explicit word.

## Monitoring — non-blocking

- **Hands-on checks still useful:** writer-diff panel/Tab + zoom readout; heading-chevron mouse-press→toggle wiring; theme-picker felt input→present lag; Bombora drift speed / counter-motion / calmness over real seconds.
- **GPU memory:** no action unless the 6 GB symptom recurs; then probe the live surface with the window foregrounded.

## Release blockers and reminders

- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user’s explicit word. A dry run may precede them.
- **Paperbark display face confirmed.** The user approved the real release-GPU
  Room capture in EB Garamond on 2026-07-29. No face gate remains before tagging.

157. **Create a new hero social image for the site.** Design a dedicated sharing
image for awl rather than reusing an editor screenshot. Add it to the social
metadata on the landing page and the supporting site pages. Keep the image
recognizably awl: beautiful, intriguing, and earned rather than a feature collage.
Verify the rendered image at common social-card crops and confirm every metadata
reference resolves. **Queued at the user’s request 2026-07-29.** 🔵 **BUILT AND VERIFIED — BLOCKED ON ONE USER TASTE PICK** — branch `claude/item-157-social-image` (`86c806e4`, `56661469`, `adf5659c`). Do not re-run the work; it needs only the pick, then integration.

**The mechanism is done:** `scripts/hero-image.sh` + `scripts/hero-specimen.md` render the card through the real product (no HTML mockup, no image editor, no network), `--install <World>` swaps the shipped world in one command, two-run byte-identical determinism passes on every candidate, and `scripts/hero-verify.py` asserts the result.

**Round 1 shipped an editor screenshot, which the item's first sentence forbids, and it was rejected.** The page-mode orientation gutter drew the fixture's own filename (`hero-specimen.md` over `scripts`) into a public marketing asset, the persistent margin outline drew a second `awl` label, and the outline's rail demand pushed the column off-centre. Round 2 removed both honestly: there is **no `gutter` config key** (only `outline`), so the lever is the gutter's own documented hide floor (`GUTTER_MIN_NAME_CHARS`) — widening the measure to 61 drops the margin under it and the gutter hides itself rather than drawing a stub; `outline = false` is a real sticky pref, and turning it off recentres the column for free by removing its rail push. Orchestrator verified the no-config-key claim directly rather than accepting it.

**Round 1 also shipped relative `og:image`/`twitter:image`, which no scraper resolves** — Facebook, X, LinkedIn and Slack drop the card outright, so the "every metadata reference resolves" check was green over a card that did not work. Repaired in `56661469`: the tags are absolute, the canonical origin has one owner (`site/fly.toml`'s app name → `https://awl-editor.fly.dev`, derived from committed config rather than invented), and `scripts/site-links.sh` now fails if any page disagrees with the deploy config. Mutation-proven five ways. **Worth knowing: that law's first run reported "all 0 URL-valued social tags" and passed over twelve real ones** — BSD `sed` has no `\|` alternation, so the name extraction was silently empty. A vacuous green is indistinguishable from a correct one; the count is the check.

🔵 **THE USER DECISION, exactly as it stands.** *Which world ships as the hero card?* Sheet: `gallery/hero/comparison.png` in that worktree (gitignored; `scripts/hero-image.sh` regenerates). Thumbnail body-text contrast at ~360px, the size a real unfurl renders:
- **Firetail — 4.85:1, passes WCAG AA.** Chunky mono reads best small; oxblood ground is distinctive; the deliberate 5/5 statement world. **Orchestrator recommendation**, on the grounds that legibility at feed size is the medium's actual constraint and it is the only candidate that both passes AA and looks intriguing.
- **Saltpan — 4.43:1, just under AA.** Currently installed. The app's literal default world, so the strongest "earned, not cherry-picked" claim, but its serif body is the weakest of the light options at thumbnail size.
- **Wagtail — 9.06:1, safest by far**, and 1-bit is structurally immune to muddying. Risk: may read as a terminal app rather than a calm writing environment.
- **Bombora — 3.73:1, weakest** on the exact axis this round existed to fix. Included because `site/style.css` already carries a "the Bombora hero is dark" comment, not as a recommendation.

⚠️ **Disclosed cost of round 2, not hidden:** widening the column to hide the gutter pushed square-crop 90%-safe-area ink overflow from 0.00 to ~0.18, so a platform that crops to square may clip an edge. OG 1.91:1 is unaffected and clean. Orchestrator's read: accept it — the leaked filename was a real defect, square cropping is a secondary behaviour on a minority of surfaces. Reject it and the fix is a shorter thesis line or a taller composition, one more lane cycle. **Saltpan stays installed until the user picks, so the branch is landable as-is and a different pick costs one command plus one commit.**
